---
title: Wurden die Modellbindung
author: rick-anderson
description: 
keywords: ASP.NET Core
ms.author: riande
manager: wpickett
ms.date: 10/14/2016
ms.topic: article
ms.assetid: b355a48e-a15c-4d58-b69c-899763613a97
ms.technology: aspnet
ms.prod: asp.net-core
uid: mvc/models/model-binding
ms.openlocfilehash: 597d4058a410e0b5991b1d5a74c9fc7bfe8171b8
ms.sourcegitcommit: 9cdbfd0d670d70b9c354216aabee260c52dad5ee
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 09/12/2017
---
# <a name="model-binding"></a><span data-ttu-id="eaabd-103">Wurden die Modellbindung</span><span class="sxs-lookup"><span data-stu-id="eaabd-103">Model Binding</span></span>

<span data-ttu-id="eaabd-104">Durch [Rachel Appel](https://github.com/rachelappel)</span><span class="sxs-lookup"><span data-stu-id="eaabd-104">By [Rachel Appel](https://github.com/rachelappel)</span></span>

## <a name="introduction-to-model-binding"></a><span data-ttu-id="eaabd-105">Einführung in die Bindung zu modellieren.</span><span class="sxs-lookup"><span data-stu-id="eaabd-105">Introduction to model binding</span></span>

<span data-ttu-id="eaabd-106">Wurden die modellbindung in ASP.NET Core MVC ordnet Daten von HTTP-Anforderungen an die Aktionsmethodenparameter.</span><span class="sxs-lookup"><span data-stu-id="eaabd-106">Model binding in ASP.NET Core MVC maps data from HTTP requests to action method parameters.</span></span> <span data-ttu-id="eaabd-107">Die Parameter werden einfache Typen wie Zeichenfolgen, ganze Zahlen und Gleitkommazahlen, oder es handelt sich um komplexe Typen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-107">The parameters may be simple types such as strings, integers, or floats, or they may be complex types.</span></span> <span data-ttu-id="eaabd-108">Dies ist eine großartige Funktion von MVC, da Zuordnen von eingehenden Daten zu einem Gegenstück ein Szenario häufig wiederholte unabhängig von der Größe und Komplexität der Daten ist.</span><span class="sxs-lookup"><span data-stu-id="eaabd-108">This is a great feature of MVC because mapping incoming data to a counterpart is an often repeated scenario, regardless of size or complexity of the data.</span></span> <span data-ttu-id="eaabd-109">MVC löst dieses Problem, indem ohne Bindung Entwickler müssen also keine etwas abweichende Version des gleichen Codes in jeder app umschreiben zu halten.</span><span class="sxs-lookup"><span data-stu-id="eaabd-109">MVC solves this problem by abstracting binding away so developers don't have to keep rewriting a slightly different version of that same code in every app.</span></span> <span data-ttu-id="eaabd-110">Schreiben Ihren eigenen Text Konvertercode Typ ist mühsam, und fehleranfällig.</span><span class="sxs-lookup"><span data-stu-id="eaabd-110">Writing your own text to type converter code is tedious, and error prone.</span></span>

## <a name="how-model-binding-works"></a><span data-ttu-id="eaabd-111">Funktionsweise der modellbindung</span><span class="sxs-lookup"><span data-stu-id="eaabd-111">How model binding works</span></span>

<span data-ttu-id="eaabd-112">Wenn MVC eine HTTP-Anforderung empfängt, leitet sie es an eine bestimmte Aktionsmethode eines Controllers weiter.</span><span class="sxs-lookup"><span data-stu-id="eaabd-112">When MVC receives an HTTP request, it routes it to a specific action method of a controller.</span></span> <span data-ttu-id="eaabd-113">Bestimmt die Aktionsmethode ausgeführt basierend auf die in der Routendaten, und klicken Sie dann Werte aus der HTTP-Anforderung an diese Aktionsmethode Parameter gebunden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-113">It determines which action method to run based on what is in the route data, then it binds values from the HTTP request to that action method's parameters.</span></span> <span data-ttu-id="eaabd-114">Betrachten Sie beispielsweise die folgende URL:</span><span class="sxs-lookup"><span data-stu-id="eaabd-114">For example, consider the following URL:</span></span>

`http://contoso.com/movies/edit/2`

<span data-ttu-id="eaabd-115">Da die routenvorlage, aussieht `{controller=Home}/{action=Index}/{id?}`, `movies/edit/2` leitet an die `Movies` Controller, und die zugehörige `Edit` Aktionsmethode.</span><span class="sxs-lookup"><span data-stu-id="eaabd-115">Since the route template looks like this, `{controller=Home}/{action=Index}/{id?}`, `movies/edit/2` routes to the `Movies` controller, and its `Edit` action method.</span></span> <span data-ttu-id="eaabd-116">Außerdem akzeptiert einen optionalen Parameter namens `id`.</span><span class="sxs-lookup"><span data-stu-id="eaabd-116">It also accepts an optional parameter called `id`.</span></span> <span data-ttu-id="eaabd-117">Der Code für die Aktionsmethode sollte etwa wie folgt aussehen:</span><span class="sxs-lookup"><span data-stu-id="eaabd-117">The code for the action method should look something like this:</span></span>

<!-- literal_block {"ids": [], "linenos": true, "xml:space": "preserve", "language": "csharp"} -->

```csharp
public IActionResult Edit(int? id)
   ```

<span data-ttu-id="eaabd-118">Hinweis: Die Zeichenfolgen in der URL-Route sind nicht Groß-/Kleinschreibung beachtet.</span><span class="sxs-lookup"><span data-stu-id="eaabd-118">Note: The strings in the URL route are not case sensitive.</span></span>

<span data-ttu-id="eaabd-119">MVC versucht, die Anforderungsdaten an die Aktionsparameter anhand des Namens zu binden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-119">MVC will try to bind request data to the action parameters by name.</span></span> <span data-ttu-id="eaabd-120">MVC sucht nach Werten für jeden Parameter mit dem Parameternamen und die Namen der öffentlichen festlegbaren Eigenschaften.</span><span class="sxs-lookup"><span data-stu-id="eaabd-120">MVC will look for values for each parameter using the parameter name and the names of its public settable properties.</span></span> <span data-ttu-id="eaabd-121">Im obigen Beispiel ist die einzige Aktionsparameter heißt `id`, MVC auf den Wert mit dem gleichen Namen in die Routenwerte gebunden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-121">In the above example, the only action parameter is named `id`, which MVC binds to the value with the same name in the route values.</span></span> <span data-ttu-id="eaabd-122">Zusätzlich zu den Routenwerte MVC wird Binden von Daten aus verschiedenen Teilen der Anforderung und wird in eine festgelegte Reihenfolge.</span><span class="sxs-lookup"><span data-stu-id="eaabd-122">In addition to route values MVC will bind data from various parts of the request and it does so in a set order.</span></span> <span data-ttu-id="eaabd-123">Im folgenden finden Sie eine Liste der Datenquellen in der Reihenfolge, die über diese wurden die modellbindung aussieht:</span><span class="sxs-lookup"><span data-stu-id="eaabd-123">Below is a list of the data sources in the order that model binding looks through them:</span></span>

1. <span data-ttu-id="eaabd-124">`Form values`: Hierbei handelt es sich um Formularwerte, die in der HTTP-Anforderung POST-Methode verwenden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-124">`Form values`: These are form values that go in the HTTP request using the POST method.</span></span> <span data-ttu-id="eaabd-125">(z. B. jQuery-POST-Anforderungen).</span><span class="sxs-lookup"><span data-stu-id="eaabd-125">(including jQuery POST requests).</span></span>

2. <span data-ttu-id="eaabd-126">`Route values`: Der Satz von Routenwerte gebotenen [Routing](../../fundamentals/routing.md)</span><span class="sxs-lookup"><span data-stu-id="eaabd-126">`Route values`: The set of route values provided by [Routing](../../fundamentals/routing.md)</span></span>

3. <span data-ttu-id="eaabd-127">`Query strings`: Die Zeichenfolge Abfrageteil des URIS.</span><span class="sxs-lookup"><span data-stu-id="eaabd-127">`Query strings`: The query string part of the URI.</span></span>

<!-- DocFX BUG
The link works but generates an error when building with DocFX
@fundamentals/routing
[Routing](xref:fundamentals/routing)
-->

<span data-ttu-id="eaabd-128">Hinweis: Bilden Sie Werte Routendaten und Abfrage-Zeichenfolgen als Name / Wert-Paare gespeichert sind.</span><span class="sxs-lookup"><span data-stu-id="eaabd-128">Note: Form values, route data, and query strings are all stored as name-value pairs.</span></span>

<span data-ttu-id="eaabd-129">Da wurden die modellbindung, einen Schlüssel Namens aufgefordert `id` nichts muss mit dem Namen `id` in die Formularwerte es in verschoben, die Routenwerte für diesen Schlüssel zu suchen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-129">Since model binding asked for a key named `id` and there is nothing named `id` in the form values, it moved on to the route values looking for that key.</span></span> <span data-ttu-id="eaabd-130">In unserem Beispiel ist es eine Übereinstimmung.</span><span class="sxs-lookup"><span data-stu-id="eaabd-130">In our example, it's a match.</span></span> <span data-ttu-id="eaabd-131">Bindung erfolgt, und der Wert wird auf die ganze Zahl 2 konvertiert.</span><span class="sxs-lookup"><span data-stu-id="eaabd-131">Binding happens, and the value is converted to the integer 2.</span></span> <span data-ttu-id="eaabd-132">Bearbeiten (Zeichenfolgen-Id) mit derselben Anforderung würde in die Zeichenfolge "2" konvertiert werden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-132">The same request using Edit(string id) would convert to the string "2".</span></span>

<span data-ttu-id="eaabd-133">Bisher wird die einfache Typen verwendet.</span><span class="sxs-lookup"><span data-stu-id="eaabd-133">So far the example uses simple types.</span></span> <span data-ttu-id="eaabd-134">Simple-Typen sind in MVC eine beliebige .NET primitiven Typ oder ein Typ mit einen Typkonverter für die Zeichenfolge an.</span><span class="sxs-lookup"><span data-stu-id="eaabd-134">In MVC simple types are any .NET primitive type or type with a string type converter.</span></span> <span data-ttu-id="eaabd-135">Wenn eine Klasse von der Aktionsmethode Parameter wie z. B. wurden der `Movie` -Typ, der einfache und komplexe Typen enthält, wie die Eigenschaften MVCs-Modell Bindung wird weiterhin ordentlich behandeln.</span><span class="sxs-lookup"><span data-stu-id="eaabd-135">If the action method's parameter were a class such as the `Movie` type, which contains both simple and complex types as properties, MVC's model binding will still handle it nicely.</span></span> <span data-ttu-id="eaabd-136">Er verwendet Reflektion und Rekursion, um die Eigenschaften der komplexen Typen, die Suche nach Übereinstimmungen zu durchlaufen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-136">It uses reflection and recursion to traverse the properties of complex types looking for matches.</span></span> <span data-ttu-id="eaabd-137">Wurden die modellbindung sucht nach dem Muster *parameter_name.property_name* an Eigenschaften Werte zu binden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-137">Model binding looks for the pattern *parameter_name.property_name* to bind values to properties.</span></span> <span data-ttu-id="eaabd-138">Wenn es nicht übereinstimmende Werte dieses Formulars findet, versucht zu binden, indem einfach den Namen der Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eaabd-138">If it doesn't find matching values of this form, it will attempt to bind using just the property name.</span></span> <span data-ttu-id="eaabd-139">Für diese Typen wie z. B. `Collection` Typen, wurden die modellbindung sucht nach Übereinstimmungen mit *Parameter_name [Index]* oder einfach *[Index]*.</span><span class="sxs-lookup"><span data-stu-id="eaabd-139">For those types such as `Collection` types, model binding looks for matches to *parameter_name[index]* or just *[index]*.</span></span> <span data-ttu-id="eaabd-140">Modell Bindung behandelt `Dictionary` Typen auf ähnliche Weise, in der die *Parameter_name [Key]* oder einfach *[Key]*, solange die Schlüssel einfache Typen sind.</span><span class="sxs-lookup"><span data-stu-id="eaabd-140">Model binding treats  `Dictionary` types similarly, asking for *parameter_name[key]* or just *[key]*, as long as the keys are simple types.</span></span> <span data-ttu-id="eaabd-141">Schlüssel, die unterstützt werden entsprechen den Feldnamen HTML und den Tag-Hilfsprogramme, die für den Typ des gleichen Modells generiert.</span><span class="sxs-lookup"><span data-stu-id="eaabd-141">Keys that are supported match the field names HTML and tag helpers generated for the same model type.</span></span> <span data-ttu-id="eaabd-142">Dies ermöglicht Round-Tripping-Werte, die Felder mit der Eingabe des Benutzers, für deren Vereinfachung gefüllt beispielsweise bleiben, wenn gebundene Daten aus einer erstellen oder bearbeiten die Validierung nicht bestanden haben.</span><span class="sxs-lookup"><span data-stu-id="eaabd-142">This enables round-tripping values so that the form fields remain filled with the user's input for their convenience, for example, when bound data from a create or edit did not pass validation.</span></span>

<span data-ttu-id="eaabd-143">Damit Bindung aufweisen muss die Klasse einen öffentlichen Standardkonstruktor verfügen und Member zu bindenden muss auf öffentlichen beschreibbare Eigenschaften.</span><span class="sxs-lookup"><span data-stu-id="eaabd-143">In order for binding to happen the class must have a public default constructor and member to be bound must be public writable properties.</span></span> <span data-ttu-id="eaabd-144">Wenn die modellbindung erfolgt, dass die Klasse mit dem öffentlichen Standardkonstruktor nur instanziiert werden, können die Eigenschaften festgelegt werden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-144">When model binding happens the class will only be instantiated using the public default constructor, then the properties can be set.</span></span>

<span data-ttu-id="eaabd-145">Wenn ein Parameter gebunden ist, wurden die modellbindung beendet die Suche nach Werten, die mit diesem Namen verschoben und auf den nächsten Parameter binden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-145">When a parameter is bound, model binding stops looking for values with that name and it moves on to bind the next parameter.</span></span> <span data-ttu-id="eaabd-146">Wenn die Bindung ein Fehler auftritt, löst MVC keine Fehler.</span><span class="sxs-lookup"><span data-stu-id="eaabd-146">If binding fails, MVC does not throw an error.</span></span> <span data-ttu-id="eaabd-147">Sie können für modellzustandsfehler Abfragen, indem Sie überprüfen die `ModelState.IsValid` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eaabd-147">You can query for model state errors by checking the `ModelState.IsValid` property.</span></span>

<span data-ttu-id="eaabd-148">Hinweis: Jeder Eintrag in des Controllers `ModelState` Eigenschaft ist ein `ModelStateEntry` , enthält eine `Errors property`.</span><span class="sxs-lookup"><span data-stu-id="eaabd-148">Note: Each entry in the controller's `ModelState` property is a `ModelStateEntry` containing an `Errors property`.</span></span> <span data-ttu-id="eaabd-149">Es ist nur selten notwendig, diese Sammlung selbst abzufragen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-149">It's rarely necessary to query this collection yourself.</span></span> <span data-ttu-id="eaabd-150">Verwenden Sie stattdessen `ModelState.IsValid` .</span><span class="sxs-lookup"><span data-stu-id="eaabd-150">Use `ModelState.IsValid` instead.</span></span>

<span data-ttu-id="eaabd-151">Darüber hinaus sind einige spezielle Datentypen, die beim Ausführen der modellbindung MVC berücksichtigen:</span><span class="sxs-lookup"><span data-stu-id="eaabd-151">Additionally, there are some special data types that MVC must consider when performing model binding:</span></span>

* <span data-ttu-id="eaabd-152">`IFormFile`, `IEnumerable<IFormFile>`: Eine oder mehrere hochgeladene Dateien, die Teil der HTTP-Anforderung sind.</span><span class="sxs-lookup"><span data-stu-id="eaabd-152">`IFormFile`, `IEnumerable<IFormFile>`: One or more uploaded files that are part of the HTTP request.</span></span>

* <span data-ttu-id="eaabd-153">`CancelationToken`: Wird verwendet, um die Aktivität in asynchrone Controller "Abbrechen".</span><span class="sxs-lookup"><span data-stu-id="eaabd-153">`CancelationToken`: Used to cancel activity in asynchronous controllers.</span></span>

<span data-ttu-id="eaabd-154">Diese Typen können Action-Parameter oder Eigenschaften eines Klassentyps gebunden werden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-154">These types can be bound to action parameters or to properties on a class type.</span></span>

<span data-ttu-id="eaabd-155">Nach Abschluss der modellbindung [Überprüfung](validation.md) auftritt.</span><span class="sxs-lookup"><span data-stu-id="eaabd-155">Once model binding is complete, [Validation](validation.md) occurs.</span></span> <span data-ttu-id="eaabd-156">Standard-modellbindung funktioniert hervorragend für die meisten Entwicklungsszenarien.</span><span class="sxs-lookup"><span data-stu-id="eaabd-156">Default model binding works great for the vast majority of development scenarios.</span></span> <span data-ttu-id="eaabd-157">Es ist außerdem erweiterbar, sodass, wenn Sie spezielle Anforderungen haben Sie die integrierten Verhalten anpassen können.</span><span class="sxs-lookup"><span data-stu-id="eaabd-157">It is also extensible so if you have unique needs you can customize the built-in behavior.</span></span>

## <a name="customize-model-binding-behavior-with-attributes"></a><span data-ttu-id="eaabd-158">Modell Bindungsverhalten mit Attributen anpassen</span><span class="sxs-lookup"><span data-stu-id="eaabd-158">Customize model binding behavior with attributes</span></span>

<span data-ttu-id="eaabd-159">MVC enthält mehrere Attribute, die Sie zum Weiterleiten von sein Standardverhalten für die Bindung von Modell mit einer anderen Datenquelle verwenden können.</span><span class="sxs-lookup"><span data-stu-id="eaabd-159">MVC contains several attributes that you can use to direct its default model binding behavior to a different source.</span></span> <span data-ttu-id="eaabd-160">Sie können beispielsweise angeben, ob die Bindung für eine Eigenschaft erforderlich ist, oder wenn er überhaupt niemals mit geschehen soll die `[BindRequired]` oder `[BindNever]` Attribute.</span><span class="sxs-lookup"><span data-stu-id="eaabd-160">For example, you can specify whether binding is required for a property, or if it should never happen at all by using the `[BindRequired]` or `[BindNever]` attributes.</span></span> <span data-ttu-id="eaabd-161">Alternativ können Sie die Standarddatenquelle überschreiben, und geben Sie den Modellbinder-Datenquelle.</span><span class="sxs-lookup"><span data-stu-id="eaabd-161">Alternatively, you can override the default data source, and specify the model binder's data source.</span></span> <span data-ttu-id="eaabd-162">Es folgt eine Liste von Modell Binden von Attributen:</span><span class="sxs-lookup"><span data-stu-id="eaabd-162">Below is a list of model binding attributes:</span></span>

* <span data-ttu-id="eaabd-163">`[BindRequired]`: Dieses Attribut fügt einen Modellfehler für Status an, wenn Bindung erfolgen kann.</span><span class="sxs-lookup"><span data-stu-id="eaabd-163">`[BindRequired]`: This attribute adds a model state error if binding cannot occur.</span></span>

* <span data-ttu-id="eaabd-164">`[BindNever]`: Weist den Modellbinder nie an diesen Parameter zu binden.</span><span class="sxs-lookup"><span data-stu-id="eaabd-164">`[BindNever]`: Tells the model binder to never bind to this parameter.</span></span>

* <span data-ttu-id="eaabd-165">`[FromHeader]`, `[FromQuery]`, `[FromRoute]`, `[FromForm]`: Verwenden Sie diese an die genaue Bindungsquelle, anwenden möchten.</span><span class="sxs-lookup"><span data-stu-id="eaabd-165">`[FromHeader]`, `[FromQuery]`, `[FromRoute]`, `[FromForm]`: Use these to specify the exact binding source you want to apply.</span></span>

* <span data-ttu-id="eaabd-166">`[FromServices]`: Dieses Attribut verwendet [Abhängigkeitsinjektion](../../fundamentals/dependency-injection.md) zum Binden von Parametern von Diensten.</span><span class="sxs-lookup"><span data-stu-id="eaabd-166">`[FromServices]`: This attribute uses [dependency injection](../../fundamentals/dependency-injection.md) to bind parameters from services.</span></span>

* <span data-ttu-id="eaabd-167">`[FromBody]`: Verwenden Sie die konfigurierten Formatierer zum Binden von Daten aus dem Anforderungstext ein.</span><span class="sxs-lookup"><span data-stu-id="eaabd-167">`[FromBody]`: Use the configured formatters to bind data from the request body.</span></span> <span data-ttu-id="eaabd-168">Der Formatierer ausgewählt ist, basierend auf den Inhaltstyp der Anforderung.</span><span class="sxs-lookup"><span data-stu-id="eaabd-168">The formatter is selected based on content type of the request.</span></span>

* <span data-ttu-id="eaabd-169">`[ModelBinder]`: Wird verwendet, um den Standardmodellbinder, Bindungsquelle und den Namen zu überschreiben.</span><span class="sxs-lookup"><span data-stu-id="eaabd-169">`[ModelBinder]`: Used to override the default model binder, binding source and name.</span></span>

<span data-ttu-id="eaabd-170">Attribute sind sehr hilfreiche Tools aus, wenn Sie das Standardverhalten der modellbindung überschreiben müssen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-170">Attributes are very helpful tools when you need to override the default behavior of model binding.</span></span>

## <a name="binding-formatted-data-from-the-request-body"></a><span data-ttu-id="eaabd-171">Bindung von formatierten Daten aus dem Anforderungstext</span><span class="sxs-lookup"><span data-stu-id="eaabd-171">Binding formatted data from the request body</span></span>

<span data-ttu-id="eaabd-172">Anforderungsdaten können in einer Vielzahl von Formaten, einschließlich JSON, XML und viele andere stammen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-172">Request data can come in a variety of formats including JSON, XML and many others.</span></span> <span data-ttu-id="eaabd-173">Wenn Sie das Attribut [FromBody] verwenden, um anzugeben, dass Sie einen Parameter an Daten im Anforderungstext binden möchten, verwendet MVC eine konfigurierte Sammlung der Formatierer, um die Daten basierend auf den Inhaltstyp zu behandeln.</span><span class="sxs-lookup"><span data-stu-id="eaabd-173">When you use the [FromBody] attribute to indicate that you want to bind a parameter to data in the request body, MVC uses a configured set of formatters to handle the request data based on its content type.</span></span> <span data-ttu-id="eaabd-174">Standardmäßig MVC umfasst eine `JsonInputFormatter` -Klasse für die Behandlung von JSON-Daten, aber Sie die zusätzliche Formatierungsprogramme hinzufügen kann, für die Behandlung von XML- und anderen benutzerdefinierten Formaten.</span><span class="sxs-lookup"><span data-stu-id="eaabd-174">By default MVC includes a `JsonInputFormatter` class for handling JSON data, but you can add additional formatters for handling XML and other custom formats.</span></span>

> [!NOTE]
> <span data-ttu-id="eaabd-175">Es kann höchstens einen Parameter pro Aktion mit ergänzt `[FromBody]`.</span><span class="sxs-lookup"><span data-stu-id="eaabd-175">There can be at most one parameter per action decorated with `[FromBody]`.</span></span> <span data-ttu-id="eaabd-176">Die ASP.NET-MVC-Kern-Laufzeit delegiert die Verantwortung der Anforderungsdatenstrom an dem Formatierer zu lesen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-176">The ASP.NET Core MVC run-time delegates the responsibility of reading the request stream to the formatter.</span></span> <span data-ttu-id="eaabd-177">Sobald der Anforderungsdatenstrom für einen Parameter gelesen wird, kann im Allgemeinen nicht zum Lesen des Anforderungsstreams erneut zum Binden von anderen `[FromBody]` Parameter.</span><span class="sxs-lookup"><span data-stu-id="eaabd-177">Once the request stream is read for a parameter, it's generally not possible to read the request stream again for binding other `[FromBody]` parameters.</span></span>

> [!NOTE]
> <span data-ttu-id="eaabd-178">Die `JsonInputFormatter` ist der Standardformatierer und basiert auf [Json.NET](https://www.newtonsoft.com/json).</span><span class="sxs-lookup"><span data-stu-id="eaabd-178">The `JsonInputFormatter` is the default formatter and is based on [Json.NET](https://www.newtonsoft.com/json).</span></span>

<span data-ttu-id="eaabd-179">ASP.NET wählt Eingabe Formatierungsprogramme basierend auf der [Content-Type](https://www.w3.org/Protocols/rfc1341/4_Content-Type.html) Header und den Typ des Parameters, es sei denn, es ein Attribut angewendet wird ist, andernfalls angibt.</span><span class="sxs-lookup"><span data-stu-id="eaabd-179">ASP.NET selects input formatters based on the [Content-Type](https://www.w3.org/Protocols/rfc1341/4_Content-Type.html) header and the type of the parameter, unless there is an attribute applied to it specifying otherwise.</span></span> <span data-ttu-id="eaabd-180">Wenn Sie XML-Code verwenden möchten, oder ein anderes Format Sie sie in konfigurieren müssen der *Startup.cs* -Datei, aber Sie ggf. zuerst müssen Sie einen Verweis auf erhalten `Microsoft.AspNetCore.Mvc.Formatters.Xml` mithilfe von NuGet.</span><span class="sxs-lookup"><span data-stu-id="eaabd-180">If you'd like to use XML or another format you must configure it in the *Startup.cs* file, but you may first have to obtain a reference to `Microsoft.AspNetCore.Mvc.Formatters.Xml` using NuGet.</span></span> <span data-ttu-id="eaabd-181">Der Startcode sollte etwa wie folgt aussehen:</span><span class="sxs-lookup"><span data-stu-id="eaabd-181">Your startup code should look something like this:</span></span>

<!-- literal_block {"ids": [], "linenos": true, "xml:space": "preserve", "language": "csharp"} -->

```csharp
public void ConfigureServices(IServiceCollection services)
   {
       services.AddMvc()
          .AddXmlSerializerFormatters();
   }
   ```

<span data-ttu-id="eaabd-182">Im Code der *Startup.cs* -Datei enthält eine `ConfigureServices` Methode mit einer `services` Argument Sie Dienste für Ihre ASP.NET-Anwendung zu erstellen können.</span><span class="sxs-lookup"><span data-stu-id="eaabd-182">Code in the *Startup.cs* file contains a `ConfigureServices` method with a `services` argument you can use to build up services for your ASP.NET app.</span></span> <span data-ttu-id="eaabd-183">In diesem Beispiel werden wir eine XML-Formatierer als Dienst hinzugefügt, die für diese app MVC bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="eaabd-183">In the sample, we are adding an XML formatter as a service that MVC will provide for this app.</span></span> <span data-ttu-id="eaabd-184">Die `options` übergebenen Argument den `AddMvc` Methode ermöglicht das Hinzufügen und Verwalten von Filtern, Formatierungsprogramme und andere von Systemoptionen von MVC beim Start der app.</span><span class="sxs-lookup"><span data-stu-id="eaabd-184">The `options` argument passed into the `AddMvc` method allows you to add and manage filters, formatters, and other system options from MVC upon app startup.</span></span> <span data-ttu-id="eaabd-185">Wenden Sie dann die `Consumes` -Attribut auf Controllerklassen oder Aktionsmethoden, die mit dem Format verwendet werden sollen.</span><span class="sxs-lookup"><span data-stu-id="eaabd-185">Then apply the `Consumes` attribute to controller classes or action methods to work with the format you want.</span></span>

### <a name="custom-model-binding"></a><span data-ttu-id="eaabd-186">Benutzerdefinierte wurden die Modellbindung</span><span class="sxs-lookup"><span data-stu-id="eaabd-186">Custom Model Binding</span></span>

<span data-ttu-id="eaabd-187">Sie können wurden die modellbindung erweitern, indem Sie Ihre eigenen benutzerdefinierten Modellbinder schreiben.</span><span class="sxs-lookup"><span data-stu-id="eaabd-187">You can extend model binding by writing your own custom model binders.</span></span> <span data-ttu-id="eaabd-188">Erfahren Sie mehr über [benutzerdefinierte wurden die modellbindung](../advanced/custom-model-binding.md).</span><span class="sxs-lookup"><span data-stu-id="eaabd-188">Learn more about [custom model binding](../advanced/custom-model-binding.md).</span></span>
