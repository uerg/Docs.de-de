---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: 'Einführung in ASP.NET Web Pages: Aktualisieren von DatenbankenDaten | Microsoft-Dokumentation'
author: tfitzmac
description: In diesem Tutorial erfahren Sie, wie (ändern) einer vorhandenen Datenbank-Eintrag aktualisiert, bei der Verwendung von ASP.NET Web Pages (Razor). Es wird vorausgesetzt, Sie haben die Reihe te...
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 259034a795b9dff7001165a182bc0e84bb690491
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 07/03/2018
ms.locfileid: "37377050"
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="7f30b-104">Einführung in ASP.NET Web Pages: Aktualisieren von DatenbankenDaten</span><span class="sxs-lookup"><span data-stu-id="7f30b-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="7f30b-105">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="7f30b-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="7f30b-106">In diesem Tutorial erfahren Sie, wie (ändern) einer vorhandenen Datenbank-Eintrag aktualisiert, bei der Verwendung von ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="7f30b-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="7f30b-107">Es wird vorausgesetzt, Sie haben die Reihe über [eingeben von Daten von mithilfe von Formularen mithilfe von ASP.NET Web Pages](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="7f30b-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="7f30b-108">Sie lernen Folgendes:</span><span class="sxs-lookup"><span data-stu-id="7f30b-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="7f30b-109">Gewusst wie: Wählen Sie einen einzelnen Datensatz in die `WebGrid` Helper.</span><span class="sxs-lookup"><span data-stu-id="7f30b-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="7f30b-110">So lesen Sie einen einzelnen Datensatz aus einer Datenbank.</span><span class="sxs-lookup"><span data-stu-id="7f30b-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="7f30b-111">Wie Sie ein Formular mit Werten aus den Datenbank-Datensatz vorab zu laden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="7f30b-112">Wie Sie einen vorhandenen Datensatz in einer Datenbank zu aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="7f30b-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="7f30b-113">Informationen zum Speichern von Informationen auf der Seite, ohne es anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="7f30b-114">So verwenden Sie ein ausgeblendetes Feld zum Speichern von Informationen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="7f30b-115">Features/Technologien erläutert:</span><span class="sxs-lookup"><span data-stu-id="7f30b-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="7f30b-116">Die `WebGrid` Helper.</span><span class="sxs-lookup"><span data-stu-id="7f30b-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="7f30b-117">Die SQL-Anweisung `Update` Befehl.</span><span class="sxs-lookup"><span data-stu-id="7f30b-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="7f30b-118">Die `Database.Execute`-Methode.</span><span class="sxs-lookup"><span data-stu-id="7f30b-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="7f30b-119">Ausgeblendete Felder (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="7f30b-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="7f30b-120">Sie lernen Folgendes</span><span class="sxs-lookup"><span data-stu-id="7f30b-120">What You'll Build</span></span>

<span data-ttu-id="7f30b-121">Im vorherigen Tutorial haben Sie gelernt, wie eine Datenbank einen Datensatz hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="7f30b-122">Hier erfahren Sie, wie einen Datensatz für die Bearbeitung angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="7f30b-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="7f30b-123">In der *Filme* Seite aktualisieren Sie die `WebGrid` Helper, sodass, dass die It anzeigt eine **bearbeiten** neben jeder Film:</span><span class="sxs-lookup"><span data-stu-id="7f30b-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid anzuzeigen, einschließlich einen Link "Bearbeiten" für jeden Film](updating-data/_static/image1.png)

<span data-ttu-id="7f30b-125">Beim Klicken auf die **bearbeiten** Link gelangen Sie zu einer anderen Seite befindet, in dem die Filminformationen bereits in einem Formular:</span><span class="sxs-lookup"><span data-stu-id="7f30b-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Bearbeiten von Film-Seite, die mit der Film bearbeitet werden](updating-data/_static/image2.png)

<span data-ttu-id="7f30b-127">Sie können einen der Werte ändern.</span><span class="sxs-lookup"><span data-stu-id="7f30b-127">You can change any of the values.</span></span> <span data-ttu-id="7f30b-128">Wenn Sie die Änderungen übermitteln, wird der Code auf der Seite aktualisiert die Datenbank, und gelangen Sie zurück auf die Movie-Auflistung.</span><span class="sxs-lookup"><span data-stu-id="7f30b-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="7f30b-129">Dieser Teil des Prozesses funktioniert fast genau so wie die *AddMovie.cshtml* Seite, die Sie im vorherigen Tutorial erstellt haben, damit viele der in diesem Tutorial vertraut werden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="7f30b-130">Es gibt mehrere Möglichkeiten, die Sie eine Möglichkeit zum Bearbeiten eines einzelnen Films implementieren könnten.</span><span class="sxs-lookup"><span data-stu-id="7f30b-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="7f30b-131">Der Ansatz gezeigt wurde gewählt, da es sich um einfach zu implementieren und leicht verständlich ist.</span><span class="sxs-lookup"><span data-stu-id="7f30b-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="7f30b-132">Die Movie-Auflistung hinzugefügt einen Link "Bearbeiten"</span><span class="sxs-lookup"><span data-stu-id="7f30b-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="7f30b-133">Um zu beginnen, führen Sie die Aktualisierung der *Filme* Seite, damit enthält jeder Film wird ebenfalls eine **bearbeiten** Link.</span><span class="sxs-lookup"><span data-stu-id="7f30b-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="7f30b-134">Öffnen der *Movies.cshtml* Datei.</span><span class="sxs-lookup"><span data-stu-id="7f30b-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="7f30b-135">Ändern Sie in den Hauptteil der Seite, die `WebGrid` Markup durch Hinzufügen einer Spalte.</span><span class="sxs-lookup"><span data-stu-id="7f30b-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="7f30b-136">Dies ist die geänderte Markup:</span><span class="sxs-lookup"><span data-stu-id="7f30b-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="7f30b-137">Die neue Spalte, ist diese:</span><span class="sxs-lookup"><span data-stu-id="7f30b-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="7f30b-138">Diese Spalte ist einen Link angezeigt (`<a>` Element), dessen Text lautet "Bearbeiten".</span><span class="sxs-lookup"><span data-stu-id="7f30b-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="7f30b-139">Was wir wollen Sie eine Verknüpfung zu erstellen, die wie folgt aussieht, wenn die Seite ausgeführt wird, werden mit der `id` Wert für jeden Film:</span><span class="sxs-lookup"><span data-stu-id="7f30b-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="7f30b-140">Dieser Link wird aufgerufen, eine Seite namens *EditMovie*, und es wird die Abfragezeichenfolge übergeben `?id=7` zu dieser Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="7f30b-141">Die Syntax für die neue Spalte sieht ein wenig komplexer; Dies ist nur, weil es zusammengefügt mehrere Elemente sind.</span><span class="sxs-lookup"><span data-stu-id="7f30b-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="7f30b-142">Jedes einzelnes Element ist einfach.</span><span class="sxs-lookup"><span data-stu-id="7f30b-142">Each individual element is straightforward.</span></span> <span data-ttu-id="7f30b-143">Wenn Sie sich auf konzentrieren lediglich die `<a>` Element dieses Markup angezeigt:</span><span class="sxs-lookup"><span data-stu-id="7f30b-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="7f30b-144">Einige Hintergrundinformationen zur Funktionsweise des Rasters: das Raster zeigt Zeilen an, eine für jeden Datensatz der Datenbank, und Spalten für jedes Feld in der Datenbankdatensatz zeigt.</span><span class="sxs-lookup"><span data-stu-id="7f30b-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="7f30b-145">Während jede Zeile erstellt wird, die `item` Objekt enthält die Datenbank-Datensatz (Item) für diese Zeile.</span><span class="sxs-lookup"><span data-stu-id="7f30b-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="7f30b-146">Diese Anordnung bietet Ihnen eine Möglichkeit, im Code, um die Daten für diese Zeile abzurufen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="7f30b-147">Sie sehen hier ist: der Ausdruck `item.ID` ist die ID-Wert, der das aktuelle Datenbankelement abrufen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="7f30b-148">Sie erhalten möglicherweise eines der Datenbankwerte (Title, "Genre" oder Jahr) die gleiche Weise mit `item.Title`, `item.Genre`, oder `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="7f30b-149">Der Ausdruck `"~/EditMovie?id=@item.ID` kombiniert den fest programmierte Teil der Ziel-URL (`~/EditMovie?id=`) dynamisch abgeleiteten ID</span><span class="sxs-lookup"><span data-stu-id="7f30b-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="7f30b-150">(Sie haben gesehen, die `~` Operator im vorherigen Tutorial; es ist ein ASP.NET-Operator, der das Stammverzeichnis der aktuellen Website darstellt.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="7f30b-151">Das Ergebnis ist, dass dieser Teil des Markups in der Spalte etwa wie das folgende Markup einfach zur Laufzeit generiert:</span><span class="sxs-lookup"><span data-stu-id="7f30b-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="7f30b-152">Natürlich ist der tatsächliche Wert der `id` wird für jede Zeile unterschiedlich sein.</span><span class="sxs-lookup"><span data-stu-id="7f30b-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="7f30b-153">Erstellen eine benutzerdefinierte Anzeige für eine Rasterspalte</span><span class="sxs-lookup"><span data-stu-id="7f30b-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="7f30b-154">Doch nun zurück zu der Spalte.</span><span class="sxs-lookup"><span data-stu-id="7f30b-154">Now back to the grid column.</span></span> <span data-ttu-id="7f30b-155">Die drei Spalten mussten Sie ursprünglich der angezeigten Raster nur Datenwerte (Title, "Genre" und Jahr).</span><span class="sxs-lookup"><span data-stu-id="7f30b-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="7f30b-156">Diese Anzeige durch Übergeben des Namens der Datenbankspalte angegebene &mdash; z. B. `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="7f30b-157">Diese neue **bearbeiten** Link Spalte unterscheidet.</span><span class="sxs-lookup"><span data-stu-id="7f30b-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="7f30b-158">Übergeben Sie einen Spaltennamen an, anstatt eine `format` Parameter.</span><span class="sxs-lookup"><span data-stu-id="7f30b-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="7f30b-159">Mit diesem Parameter können Sie das Markup zu definieren, die die `WebGrid` Helper wird gerendert, zusammen mit den `item` Werts, der die Spaltendaten fett oder grün angezeigt oder in den Inhalt, den Sie formatieren möchten.</span><span class="sxs-lookup"><span data-stu-id="7f30b-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="7f30b-160">Wenn den Titel gewünscht in fett angezeigt werden, konnte Sie z. B. eine Spalte, wie im folgenden Beispiel erstellen:</span><span class="sxs-lookup"><span data-stu-id="7f30b-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="7f30b-161">(Die verschiedenen `@` Zeichen, die in angezeigt werden, die `format` Eigenschaft markieren den Übergang zwischen Markup und ein Code-Wert.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="7f30b-162">Sobald Sie kennen die `format` -Eigenschaft, es ist einfacher zu verstehen wie das neue **bearbeiten** Spalte Link zusammengestellt werden:</span><span class="sxs-lookup"><span data-stu-id="7f30b-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="7f30b-163">Die Spalte besteht aus *nur* der das Markup, das den Link gerendert wird, sowie einige Informationen (ID), extrahiert aus der Datenbank-Datensatz für die Zeile.</span><span class="sxs-lookup"><span data-stu-id="7f30b-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP]
> 
> <span data-ttu-id="7f30b-164">**Benannte Parameter und Positionsparameter für eine Methode**</span><span class="sxs-lookup"><span data-stu-id="7f30b-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="7f30b-165">Oft Wenn Sie eine Methode aufgerufen wird und die entsprechenden Parameter übergeben haben, haben Sie einfach die Parameterwerte, die durch Kommas getrennt aufgeführt.</span><span class="sxs-lookup"><span data-stu-id="7f30b-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="7f30b-166">Im Folgenden sind einige Beispiele angegeben:</span><span class="sxs-lookup"><span data-stu-id="7f30b-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="7f30b-167">Wir können das Problem nicht erwähnt, wenn Sie diesen Code zuerst gesehen haben, aber in jedem Fall Sie Parameter an die Methoden in einer bestimmten Reihenfolge übergeben &mdash; , nämlich die Reihenfolge, in der die Parameter in dieser Methode definiert sind.</span><span class="sxs-lookup"><span data-stu-id="7f30b-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="7f30b-168">Für `db.Execute` und `Validation.RequireFields`, wenn Sie sich die Reihenfolge der Werte Sie übergeben kombiniert, erhalten Sie eine Fehlermeldung angezeigt, wenn die Seite ausgeführt wird, oder zumindest eine gewisse seltsame Ergebnisse.</span><span class="sxs-lookup"><span data-stu-id="7f30b-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="7f30b-169">Natürlich müssen Sie die Reihenfolge, übergeben die Parameter in kennen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="7f30b-170">(In WebMatrix IntelliSense Ermitteln der Namen, Typ und Reihenfolge der Parameter Informationen helfen Ihnen.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="7f30b-171">Als Alternative zum Übergeben von Werten in der Reihenfolge, können Sie *benannte Parameter*.</span><span class="sxs-lookup"><span data-stu-id="7f30b-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="7f30b-172">(Übergeben von Parametern in der Reihenfolge genannt mit *Positionsparameter*.) Für benannte Parameter schließen Sie explizit den Namen des Parameters, wenn den Wert zu übergeben.</span><span class="sxs-lookup"><span data-stu-id="7f30b-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="7f30b-173">Sie haben benannte Parameter bereits eine Anzahl von Malen in diesen Tutorials verwendet.</span><span class="sxs-lookup"><span data-stu-id="7f30b-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="7f30b-174">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="7f30b-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="7f30b-175">und</span><span class="sxs-lookup"><span data-stu-id="7f30b-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="7f30b-176">Benannte Parameter sind für eine Reihe von Situationen nützlich, besonders, wenn eine Methode für viele Parameter.</span><span class="sxs-lookup"><span data-stu-id="7f30b-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="7f30b-177">Eines ist, wenn nur ein oder zwei Parameter übergeben werden sollen, aber die Werte, die Sie übergeben möchten, nicht für die erste Position in der Parameterliste sind.</span><span class="sxs-lookup"><span data-stu-id="7f30b-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="7f30b-178">Eine andere Situation ist, wenn Sie möchten Ihren Code besser lesbar machen, indem Sie die Parameter zu übergeben, in der Reihenfolge, die für Sie am sinnvollsten ist.</span><span class="sxs-lookup"><span data-stu-id="7f30b-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="7f30b-179">Wenn benannte Parameter verwenden möchten, müssen Sie natürlich die Namen der Parameter zu kennen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="7f30b-180">WebMatrix IntelliSense können *anzeigen* Sie die Namen, aber es kann nicht aktuell Geben sie für Sie.</span><span class="sxs-lookup"><span data-stu-id="7f30b-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="7f30b-181">Erstellen die Seite "Bearbeiten"</span><span class="sxs-lookup"><span data-stu-id="7f30b-181">Creating the Edit Page</span></span>

<span data-ttu-id="7f30b-182">Nun können Sie erstellen die *EditMovie* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="7f30b-183">Wenn der Benutzer klicken auf die **bearbeiten** Link sie zum Schluss werden auf dieser Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="7f30b-184">Erstellen Sie eine Seite mit dem Namen *EditMovie.cshtml* , und Ersetzen Sie die neuerungen in der Datei mit folgendem Markup:</span><span class="sxs-lookup"><span data-stu-id="7f30b-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="7f30b-185">Dieses Markup und Code entspricht, was Sie haben der *AddMovie* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="7f30b-186">Es gibt ein kleinen Unterschied in den Text für die Schaltfläche "Senden" aus.</span><span class="sxs-lookup"><span data-stu-id="7f30b-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="7f30b-187">Wie bei der *AddMovie* Seite, gibt es eine `Html.ValidationSummary` Aufruf, der Validierungsfehler angezeigt wird, sofern vorhanden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="7f30b-188">Diesmal haben wir die Aufrufe von auslassen `Validation.Message`, da Fehler in der Zusammenfassung der Validierung angezeigt werden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="7f30b-189">Wie im vorherigen Tutorial erwähnt, können Sie die Zusammenfassung der Validierung und die einzelne Fehlermeldungen in verschiedenen Kombinationen verwenden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="7f30b-190">Beachten Sie erneut, dass der `method` Attribut der `<form>` Element nastaven NA hodnotu `post`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="7f30b-191">Wie bei der *AddMovie.cshtml* Seite dieser Seite werden Änderungen an der Datenbank.</span><span class="sxs-lookup"><span data-stu-id="7f30b-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="7f30b-192">Daher sollten diese Form Ausführen einer `POST` Vorgang.</span><span class="sxs-lookup"><span data-stu-id="7f30b-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="7f30b-193">(Weitere Informationen zu den Unterschieden zwischen `GET` und `POST` Vorgänge, finden Sie unter den [GET, POST und HTTP-Verb Sicherheit](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) Randleiste in diesem Tutorial auf HTML-Formularen.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="7f30b-194">In einem früheren Tutorial haben Sie gesehen die `value` Attribute der Textfelder werden mit Razor-Code festgelegt wird, um sie vorab zu laden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="7f30b-195">Dieses Mal jedoch, Sie verwenden Variablen wie `title` und `genre` für diese Aufgabe anstelle von `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="7f30b-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="7f30b-196">Wie wird vor, dieses Markup Werte des Text mit den Werten der Film vorab zu laden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="7f30b-197">Sehen Sie in einen Moment, warum es nützlich ist, die Variablen dieses Mal verwenden, anstatt, die `Request` Objekt.</span><span class="sxs-lookup"><span data-stu-id="7f30b-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="7f30b-198">Es gibt auch eine `<input type="hidden">` Element auf dieser Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="7f30b-199">Dieses Element werden die Film-ID gespeichert, ohne dass sie auf der Seite angezeigt.</span><span class="sxs-lookup"><span data-stu-id="7f30b-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="7f30b-200">Die ID wird anfangs auf der Seite mit übergeben Wert einer Abfragezeichenfolge (`?id=7` oder in der URL ähnlich).</span><span class="sxs-lookup"><span data-stu-id="7f30b-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="7f30b-201">Indem Sie den ID-Wert in ein verborgenes Feld einfügen, Sie können sicherstellen, dass es verfügbar ist. wenn das Formular übermittelt wird, auch wenn Sie nicht mehr auf die ursprüngliche URL zugreifen, die mit die Seite aufgerufen wurde.</span><span class="sxs-lookup"><span data-stu-id="7f30b-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="7f30b-202">Im Gegensatz zu den *AddMovie* Seite, den Code für die *EditMovie* Seite verfügt über zwei verschiedene Funktionen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="7f30b-203">Die erste Funktion ist, die, wenn die Seite zum ersten Mal angezeigt wird (und *nur* dann), der Code Ruft die Film-ID aus der Abfragezeichenfolge ab.</span><span class="sxs-lookup"><span data-stu-id="7f30b-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="7f30b-204">Der Code nutzt dann die ID den entsprechenden Film aus der Datenbank lesen und anzeigen (vorab zu laden) es in die Textfelder ein.</span><span class="sxs-lookup"><span data-stu-id="7f30b-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="7f30b-205">Die zweite Funktion ist, die klickt der Benutzer die **Änderungen Absenden** Schaltfläche, der Code verfügt lesen die Werte der Textfelder, und überprüfen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="7f30b-206">Der Code hat auch den Datenbank-Artikel mit den neuen Werten aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="7f30b-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="7f30b-207">Diese Technik ist vergleichbar mit dem Hinzufügen eines Eintrags, wie in *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="7f30b-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="7f30b-208">Hinzufügen von Code zum Lesen eines einzelnen Films</span><span class="sxs-lookup"><span data-stu-id="7f30b-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="7f30b-209">Um die erste Funktion auszuführen, fügen Sie folgenden Code am Anfang der Seite:</span><span class="sxs-lookup"><span data-stu-id="7f30b-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="7f30b-210">Der größte Teil dieses Codes wird in einem Block, der beginnt `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="7f30b-211">Die `!` Operator "not", damit der Ausdruck bedeutet, dass bedeutet *ist diese Anforderung keiner Post-Übermittlung*, d.h. ein indirektes Verfahren zu sagen *Wenn die Anforderung beim ersten ist, die auf dieser Seite ausgeführt wurde*.</span><span class="sxs-lookup"><span data-stu-id="7f30b-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="7f30b-212">Wie bereits erwähnt, sollte dieser Code ausgeführt *nur* ersten Mal die Seite ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="7f30b-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="7f30b-213">Wenn Sie nicht, dass den Code in einschließen `if(!IsPost)`, sie würden jedes Mal, wenn die Seite, gibt an, ob das erste Mal aufgerufen wird oder als Reaktion auf eine Schaltfläche auf.</span><span class="sxs-lookup"><span data-stu-id="7f30b-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="7f30b-214">Beachten Sie, die der Code enthält ein `else` diesmal blockieren.</span><span class="sxs-lookup"><span data-stu-id="7f30b-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="7f30b-215">Wie bereits erwähnt, wenn wir eingeführt `if` Blöcke, manchmal alternativen Code ausgeführt wird, wenn die Bedingung, die Sie testen nicht "true" ist werden soll.</span><span class="sxs-lookup"><span data-stu-id="7f30b-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="7f30b-216">Dies ist hier der Fall.</span><span class="sxs-lookup"><span data-stu-id="7f30b-216">That's the case here.</span></span> <span data-ttu-id="7f30b-217">Wenn die Bedingung erfüllt (d.h., wenn die ID, die an die Seite in Ordnung ist), können Sie eine Zeile aus der Datenbank gelesen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="7f30b-218">Jedoch, wenn die Bedingung nicht erfolgreich abgeschlossen, die `else` Block ausgeführt wird und im Code wird eine Fehlermeldung angezeigt.</span><span class="sxs-lookup"><span data-stu-id="7f30b-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="7f30b-219">Überprüfen einen Wert an die Seite übergeben</span><span class="sxs-lookup"><span data-stu-id="7f30b-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="7f30b-220">Der Code verwendet `Request.QueryString["id"]` zum Abrufen der ID, die die Seite übergeben werden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="7f30b-221">Der Code wird sichergestellt, der tatsächlich übergeben eines Werts für die ID.</span><span class="sxs-lookup"><span data-stu-id="7f30b-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="7f30b-222">Wenn kein Wert übergeben wurde, legt der Code einen Validierungsfehler.</span><span class="sxs-lookup"><span data-stu-id="7f30b-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="7f30b-223">Dieser Code zeigt eine andere Möglichkeit, Informationen zu überprüfen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="7f30b-224">Im vorherigen Tutorial, mit denen Sie gearbeitet der `Validation` Helper.</span><span class="sxs-lookup"><span data-stu-id="7f30b-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="7f30b-225">Sie registriert die zu überprüfenden Felder ASP.NET automatisch und die Überprüfung wurde mit Fehlern angezeigt `Html.ValidationMessage` und `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="7f30b-226">In diesem Fall sind jedoch Sie nicht wirklich Benutzereingaben werden überprüft.</span><span class="sxs-lookup"><span data-stu-id="7f30b-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="7f30b-227">Stattdessen können Sie einen Wert überprüfen, der von einer anderen Stelle auf der Seite übergeben wurde.</span><span class="sxs-lookup"><span data-stu-id="7f30b-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="7f30b-228">Die `Validation` Hilfsprogramm nicht, die für Sie.</span><span class="sxs-lookup"><span data-stu-id="7f30b-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="7f30b-229">Aus diesem Grund Sie den Wert selbst überprüfen, testen Sie es mit `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="7f30b-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="7f30b-230">Wenn ein Problem vorliegt, können Sie den Fehler anzeigen, mit `Html.ValidationSummary`, wie Sie mit der `Validation` Helper.</span><span class="sxs-lookup"><span data-stu-id="7f30b-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="7f30b-231">Zu diesem Zweck rufen Sie `Validation.AddFormError` , und übergeben sie eine anzuzeigende Meldung.</span><span class="sxs-lookup"><span data-stu-id="7f30b-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="7f30b-232">`Validation.AddFormError` ist eine integrierte Methode, mit dem Sie benutzerdefinierte Meldungen zu definieren, die sich das Überprüfungssystem zu verknüpfen, die Sie bereits kennen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="7f30b-233">(Später in diesem Lernprogramm befassen wir uns dazu, wie Sie diesen Validierungsprozess etwas stabiler machen mit.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="7f30b-234">Stellen Sie sicher, dass eine für den Film-ID vorhanden ist, liest der Code für die Datenbank ab und sucht für nur eine einzeldatenbank-Element.</span><span class="sxs-lookup"><span data-stu-id="7f30b-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="7f30b-235">(Das allgemeine Muster für Datenbankvorgänge haben wahrscheinlich bemerkt: Öffnen Sie die Datenbank, eine SQL­Anweisung definieren, und führen Sie die Anweisung.) Dieses Mal SQL `Select` -Anweisung schließt `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="7f30b-236">Da die ID eindeutig ist, kann nur ein Datensatz zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="7f30b-237">Die Abfrage erfolgt über `db.QuerySingle` (nicht `db.Query`, da Sie für die Movie-Auflistung verwendet), und der Code legt das Ergebnis in der `row` Variable.</span><span class="sxs-lookup"><span data-stu-id="7f30b-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="7f30b-238">Der Name `row` ist ein beliebiger; Sie können die Variablen name ist beliebig.</span><span class="sxs-lookup"><span data-stu-id="7f30b-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="7f30b-239">Die Variablen initialisiert, die am Anfang werden dann mit den Filmdetails gefüllt, damit diese Werte in den Textfeldern angezeigt werden können.</span><span class="sxs-lookup"><span data-stu-id="7f30b-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="7f30b-240">Testen die Seite "Bearbeiten" (bisher)</span><span class="sxs-lookup"><span data-stu-id="7f30b-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="7f30b-241">Wenn Sie die Seite testen möchten, führen Sie die *Filme* Seite jetzt, und klicken Sie auf eine **bearbeiten** neben jedem Film.</span><span class="sxs-lookup"><span data-stu-id="7f30b-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="7f30b-242">Sehen Sie die *EditMovie* Seite mit den Details für den ausgewählten Film ausgefüllt:</span><span class="sxs-lookup"><span data-stu-id="7f30b-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Bearbeiten von Film-Seite, die mit der Film bearbeitet werden](updating-data/_static/image3.png)

<span data-ttu-id="7f30b-244">Beachten Sie, dass die URL der Seite etwa enthält `?id=10` (oder eine andere Nummer).</span><span class="sxs-lookup"><span data-stu-id="7f30b-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="7f30b-245">Bisher haben Sie getestet, die **bearbeiten** verknüpft die *Film* Seite arbeiten, die Seite wird die ID aus der Abfragezeichenfolge gelesen, und, dass die Datenbank eine zum Abrufen eines einzelnen Films Datensatzes Abfrage funktioniert.</span><span class="sxs-lookup"><span data-stu-id="7f30b-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="7f30b-246">Sie können die Filminformationen ändern, aber die nichts geschieht, wenn Sie auf **Änderungen Absenden**.</span><span class="sxs-lookup"><span data-stu-id="7f30b-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="7f30b-247">Hinzufügen von Code zum Aktualisieren des Films mit die Änderungen des Benutzers</span><span class="sxs-lookup"><span data-stu-id="7f30b-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="7f30b-248">In der *EditMovie.cshtml* Datei, um die zweite Funktion (Änderungen speichern) zu implementieren, fügen Sie folgenden Code innerhalb der schließenden Klammer der der `@` Block.</span><span class="sxs-lookup"><span data-stu-id="7f30b-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="7f30b-249">(Wenn Sie nicht sicher, genau, wo sich der Code platziert sind, sehen Sie sich die [vollständige codeauflistung für die Seite bearbeiten Film](#Complete_Page_Listing_for_EditMovie) , das am Ende dieses Tutorials angezeigt wird.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="7f30b-250">In diesem Fall dieses Markup und Code ähnelt dem Code in *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="7f30b-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="7f30b-251">Der Code befindet sich in einem `if(IsPost)` blockiert werden, da dieser Code ausgeführt wird, nur, wenn der Benutzer klickt der **Änderungen Absenden** Schaltfläche &mdash; , also wenn (und nur wenn) das Formular gesendet wurde.</span><span class="sxs-lookup"><span data-stu-id="7f30b-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="7f30b-252">In diesem Fall verwenden Sie keinen Test wie `if(IsPost && Validation.IsValid())`– d. h., Sie sind nicht kombinieren von beide Tests unter Verwendung and.</span><span class="sxs-lookup"><span data-stu-id="7f30b-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="7f30b-253">Auf dieser Seite Sie zuerst ermitteln, ob es eine formularübertragung (`if(IsPost)`), und nur die Felder für die Überprüfung registrieren.</span><span class="sxs-lookup"><span data-stu-id="7f30b-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="7f30b-254">Und dann die Ergebnisse, die Sie testen (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="7f30b-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="7f30b-255">Der Flow ist etwas anders als in der *AddMovie.cshtml* Seite, jedoch der Effekt ist identisch.</span><span class="sxs-lookup"><span data-stu-id="7f30b-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="7f30b-256">Erhalten Sie die Werte der Textfelder mit `Request.Form["title"]` und ähnlichen Code für die anderen `<input>` Elemente.</span><span class="sxs-lookup"><span data-stu-id="7f30b-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="7f30b-257">Beachten Sie, dass dieses Mal mit dem Code die Film-ID aus dem ausgeblendeten Feld abgerufen (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="7f30b-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="7f30b-258">Wenn die Seite erstmalig ausgeführt haben, hier der Code die ID aus der Abfragezeichenfolge an.</span><span class="sxs-lookup"><span data-stu-id="7f30b-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="7f30b-259">Sie erhalten den Wert aus dem ausgeblendeten Feld, um sicherzustellen, dass Sie die ID des Films, die ursprünglich angezeigt wurde, optimal nutzen, für den Fall, dass die Abfragezeichenfolge irgendwie seither geändert wurde.</span><span class="sxs-lookup"><span data-stu-id="7f30b-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="7f30b-260">Der wirklich wichtige Unterschied zwischen der *AddMovie* und dieser Code ist, dass in diesem Code die Verwendung von SQL `Update` -Anweisung anstelle der `Insert Into` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="7f30b-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="7f30b-261">Das folgende Beispiel zeigt die Syntax des SQL- `Update` Anweisung:</span><span class="sxs-lookup"><span data-stu-id="7f30b-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="7f30b-262">Sie können alle Spalten in einer beliebigen Reihenfolge angeben und Sie müssen nicht unbedingt jede Spalte beim Aktualisieren einer `Update` Vorgang.</span><span class="sxs-lookup"><span data-stu-id="7f30b-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="7f30b-263">(Die ID selbst kann nicht aktualisiert werden, da, die den Datensatz in Kraft, als einen neuen Datensatz speichern würde und, nicht für zulässig ist einen `Update` Vorgang.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="7f30b-264">**Wichtige** der `Where` -Klausel mit der ID ist sehr wichtig, denn das ist wie die Datenbank bekannt ist, welche Datenbank Datensätze aktualisiert werden soll.</span><span class="sxs-lookup"><span data-stu-id="7f30b-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="7f30b-265">Wenn Sie aufgehört haben vorher die `Where` -Klausel, die Datenbank wird aktualisiert *jeder* Datensatz in der Datenbank.</span><span class="sxs-lookup"><span data-stu-id="7f30b-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="7f30b-266">In den meisten Fällen wäre, die einem Notfall.</span><span class="sxs-lookup"><span data-stu-id="7f30b-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="7f30b-267">Im Code werden die Werte aktualisieren für die SQL-Anweisung übergeben, mit Platzhaltern.</span><span class="sxs-lookup"><span data-stu-id="7f30b-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="7f30b-268">Wiederholen, was wir erwähnt habe: aus Sicherheitsgründen *nur* Platzhalter verwenden, um Werte an eine SQL-Anweisung übergeben.</span><span class="sxs-lookup"><span data-stu-id="7f30b-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="7f30b-269">Nachdem der Code verwendet `db.Execute` zum Ausführen der `Update` -Anweisung, die es wieder auf die Angebotsseite, hier Sie die Änderungen sehen leitet.</span><span class="sxs-lookup"><span data-stu-id="7f30b-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="7f30b-270">**Andere SQL-Anweisungen, verschiedene Methoden**</span><span class="sxs-lookup"><span data-stu-id="7f30b-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="7f30b-271">Möglicherweise haben Sie bemerkt, dass Sie andere Methoden verwenden, um verschiedene SQL-Anweisungen auszuführen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="7f30b-272">Zum Ausführen einer `Select` Abfragen gibt, die ein potenzielles mehrere Datensätze, die Sie verwenden die `Query` Methode.</span><span class="sxs-lookup"><span data-stu-id="7f30b-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="7f30b-273">Zum Ausführen einer `Select` Abfrage, die Sie kennen nur ein Datenbankelement zurück, die Sie verwenden die `QuerySingle` Methode.</span><span class="sxs-lookup"><span data-stu-id="7f30b-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="7f30b-274">Führen Sie Befehle, die Änderungen vornehmen, jedoch nicht, die Datenbankelemente zurückgeben, die Sie verwenden die `Execute` Methode.</span><span class="sxs-lookup"><span data-stu-id="7f30b-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="7f30b-275">Sie müssen verschiedene Methoden haben, da jeweils unterschiedliche Ergebnisse zurückgibt, wie Sie bereits gesehen, den Unterschied zwischen haben `Query` und `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="7f30b-276">(Die `Execute` Methode eigentlich gibt einen Wert auch &mdash; , nämlich die Anzahl von Datenbankzeilen, die mit dem Befehl betroffen waren &mdash; , aber Sie haben wurde ignoriert, die bisher.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="7f30b-277">Natürlich die `Query` Methode kann nur eine Datenbankzeile zurück.</span><span class="sxs-lookup"><span data-stu-id="7f30b-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="7f30b-278">ASP.NET behandelt, jedoch immer die Ergebnisse der `Query` Methode als eine Auflistung.</span><span class="sxs-lookup"><span data-stu-id="7f30b-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="7f30b-279">Auch wenn die Methode nur eine Zeile zurückgibt, müssen Sie in dieser Zeile aus der Auflistung zu extrahieren.</span><span class="sxs-lookup"><span data-stu-id="7f30b-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="7f30b-280">Aus diesem Grund in Situationen, in dem Sie *wissen* erhalten Sie einige nur eine Zeile ist dies ein angenehmer zu wenig `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="7f30b-281">Es gibt einigen andere Methoden, die bestimmte Typen von Datenbankvorgänge ausführen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="7f30b-282">Sie finden eine Liste der datenbankmethoden in der [ASP.NET Web Pages-API-Kurzübersicht](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="7f30b-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="7f30b-283">Überprüfung für die ID mehr machen robuster</span><span class="sxs-lookup"><span data-stu-id="7f30b-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="7f30b-284">Beim ersten, die die Seite ausgeführt wird, erhalten Sie die Film-ID aus der Abfragezeichenfolge, damit Sie zugreifen können, Film aus der Datenbank abzurufen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="7f30b-285">Sie haben sichergestellt, dass es sich bei gab es ein Wert zu suchen, die Verwendung dieser Code tatsächlich:</span><span class="sxs-lookup"><span data-stu-id="7f30b-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="7f30b-286">Sie diesen Code verwendet, um sicherzustellen, dass wenn ein Benutzer auf die *EditMovies* Seite, ohne zuvor einen Film in den *Filme* Seite die Seite würde eine benutzerfreundliche Fehlermeldung angezeigt.</span><span class="sxs-lookup"><span data-stu-id="7f30b-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="7f30b-287">(Andernfalls würde Benutzer eine Fehlermeldung, die wahrscheinlich nur diese verwechselt wird angezeigt.)</span><span class="sxs-lookup"><span data-stu-id="7f30b-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="7f30b-288">Diese Überprüfung ist jedoch nicht sehr zuverlässig.</span><span class="sxs-lookup"><span data-stu-id="7f30b-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="7f30b-289">Die Seite kann auch mit diesen Fehlern aufgerufen werden:</span><span class="sxs-lookup"><span data-stu-id="7f30b-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="7f30b-290">Die ID ist keine Zahl.</span><span class="sxs-lookup"><span data-stu-id="7f30b-290">The ID isn't a number.</span></span> <span data-ttu-id="7f30b-291">Beispielsweise könnte die Seite aufgerufen werden, mit einer URL wie `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="7f30b-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="7f30b-292">Die ID ist eine Zahl, aber sie verweist, auf einen Film, die nicht vorhanden (z. B. `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="7f30b-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="7f30b-293">Wenn Sie wissen möchten, die Fehler angezeigt, die aus diesen URLs, führen Sie die *Filme* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="7f30b-294">Wählen Sie einen Film zu bearbeiten, und ändern Sie die URL der *EditMovie* Seite zu einer URL, die ein alphabetisches enthält, ID oder die ID eines Films nicht vorhanden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="7f30b-295">Daher sollten Sie vorgehen?</span><span class="sxs-lookup"><span data-stu-id="7f30b-295">So what should you do?</span></span> <span data-ttu-id="7f30b-296">Die erste Lösung besteht darin sicherzustellen, dass, dass nicht nur eine ID auf der Seite übergeben wird, aber die ID, eine ganze Zahl ist.</span><span class="sxs-lookup"><span data-stu-id="7f30b-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="7f30b-297">Ändern Sie den Code für die `!IsPost` Test aus, um wie im folgenden Beispiel aussehen:</span><span class="sxs-lookup"><span data-stu-id="7f30b-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="7f30b-298">Sie hinzugefügt haben, eine zweite Bedingung, die `IsEmpty` Test ausgeführt wird, verknüpft mit `&&` (logisches AND):</span><span class="sxs-lookup"><span data-stu-id="7f30b-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="7f30b-299">Erinnern Sie sich über die [Einführung in die Programmierung von ASP.NET Web Pages](../introducing-razor-syntax-c.md) Tutorial, in dem Methoden wie `AsBool` ein `AsInt` konvertieren eine Zeichenfolge in einen anderen Datentyp.</span><span class="sxs-lookup"><span data-stu-id="7f30b-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="7f30b-300">Die `IsInt` Methode (und andere wie `IsBool` und `IsDateTime`) sind ähnlich.</span><span class="sxs-lookup"><span data-stu-id="7f30b-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="7f30b-301">Sie testen allerdings nur, ob Sie *können* konvertieren die Zeichenfolge ohne die Konvertierung ausführen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="7f30b-302">Hier können Sie im Wesentlichen besagt *Wenn Abfragezeichenfolgen-Werts in eine ganze Zahl konvertiert werden kann...* .</span><span class="sxs-lookup"><span data-stu-id="7f30b-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="7f30b-303">Das andere mögliche Problem aufgetreten ist nach einem Film suchen, die nicht vorhanden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="7f30b-304">Der Code zum Abrufen eines Films sieht wie im folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="7f30b-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="7f30b-305">Übergeben einer `movieId` Wert der `QuerySingle` -Methode, die keinen tatsächlichen Film entsprechen nicht, wird nichts zurückgegeben und die Anweisungen folgen (z. B. `title=row.Title`) zu Fehlern führen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="7f30b-306">Es ist leicht zu beheben.</span><span class="sxs-lookup"><span data-stu-id="7f30b-306">Again there's an easy fix.</span></span> <span data-ttu-id="7f30b-307">Wenn die `db.QuerySingle` Methode keine Ergebnisse zurückgibt, die `row` Variable wird null sein.</span><span class="sxs-lookup"><span data-stu-id="7f30b-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="7f30b-308">Damit Sie überprüfen können, ob die `row` Variable null ist, bevor Sie versuchen, die von ihr Werte zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="7f30b-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="7f30b-309">Der folgende Code Fügt eine `if` -Block um die Anweisungen, die die Werte von Abrufen der `row` Objekt:</span><span class="sxs-lookup"><span data-stu-id="7f30b-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="7f30b-310">Zwei zusätzliche Validierung ausführen wird die Seite mehr hundertprozentig.</span><span class="sxs-lookup"><span data-stu-id="7f30b-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="7f30b-311">Der vollständige Code für die `!IsPost` Branch sieht nun wie im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="7f30b-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="7f30b-312">Wir werden noch einmal Beachten Sie, dass diese Aufgabe eine gute Verwendung für ist ein `else` Block.</span><span class="sxs-lookup"><span data-stu-id="7f30b-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="7f30b-313">Wenn die Tests übergeben, die `else` Standardresultset Fehlermeldungen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="7f30b-314">Hinzufügen eines Links auf der Seite "Movies" zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="7f30b-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="7f30b-315">Ein Detail endgültig und hilfreich ist, fügen einen Link zurück an die *Filme* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="7f30b-316">In der normale Fluss von Ereignissen, Benutzer beginnt mit der *Filme* Seite, und klicken Sie auf eine **bearbeiten** Link.</span><span class="sxs-lookup"><span data-stu-id="7f30b-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="7f30b-317">Somit kommen sie zu der *EditMovie* Seite, in dem Bearbeiten des Films und klicken Sie auf die Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="7f30b-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="7f30b-318">Nachdem der Code die Änderung verarbeitet hat, leitet Sie an der *Filme* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="7f30b-319">Dabei gilt jedoch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="7f30b-319">However:</span></span>

- <span data-ttu-id="7f30b-320">Benutzer können keine Änderungen vornehmen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="7f30b-321">Der Benutzer kann zu dieser Seite erhalten haben, ohne Klicken auf eine **bearbeiten** -link in der *Filme* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="7f30b-322">In beiden Fällen möchten Sie sie auf die Auflistung der wichtigsten zurückzugebenden erleichtern.</span><span class="sxs-lookup"><span data-stu-id="7f30b-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="7f30b-323">Es ist eine einfache Lösung &mdash; fügen Sie das folgende Markup direkt nach dem schließenden `</form>` Tags im Markup:</span><span class="sxs-lookup"><span data-stu-id="7f30b-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="7f30b-324">Dieses Markup verwendet die gleiche Syntax wie für eine `<a>` -Element, das Sie an anderer Stelle gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="7f30b-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="7f30b-325">Die URL enthält `~` bedeutet "Root der Website".</span><span class="sxs-lookup"><span data-stu-id="7f30b-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="7f30b-326">Testen des Prozesses der Movie-Update</span><span class="sxs-lookup"><span data-stu-id="7f30b-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="7f30b-327">Jetzt können Sie testen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-327">Now you can test.</span></span> <span data-ttu-id="7f30b-328">Führen Sie die *Filme* Seite, und klicken Sie auf **bearbeiten** neben einen Film.</span><span class="sxs-lookup"><span data-stu-id="7f30b-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="7f30b-329">Wenn die *EditMovie* Seite angezeigt wird, nehmen Sie Änderungen an den Film und auf **Änderungen Absenden**.</span><span class="sxs-lookup"><span data-stu-id="7f30b-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="7f30b-330">Wenn die Movie-Liste angezeigt wird, stellen Sie sicher, dass Ihre Änderungen angezeigt werden.</span><span class="sxs-lookup"><span data-stu-id="7f30b-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="7f30b-331">Um sicherzustellen, dass die Validierung funktioniert, klicken Sie auf **bearbeiten** für einen anderen Film.</span><span class="sxs-lookup"><span data-stu-id="7f30b-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="7f30b-332">Wenn man an die *EditMovie* Deaktivieren der **"Genre"** Feld (oder **Jahr** Feld oder beides), und versuchen, Ihre Änderungen zu übernehmen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="7f30b-333">Einen Fehler angezeigt, wenn Sie erwarten:</span><span class="sxs-lookup"><span data-stu-id="7f30b-333">You'll see an error, as you'd expect:</span></span>

![Bearbeiten Sie (Seite) Film Validierungsfehler anzeigt](updating-data/_static/image4.png)

<span data-ttu-id="7f30b-335">Klicken Sie auf die **zurück zum Movie-Angebot** Link, um Ihre Änderungen verwerfen und zurück zu den *Filme* Seite.</span><span class="sxs-lookup"><span data-stu-id="7f30b-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="7f30b-336">Als Nächstes kommen</span><span class="sxs-lookup"><span data-stu-id="7f30b-336">Coming Up Next</span></span>

<span data-ttu-id="7f30b-337">Im nächsten Tutorial sehen Sie, wie Sie einen Film-Datensatz zu löschen.</span><span class="sxs-lookup"><span data-stu-id="7f30b-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="7f30b-338">Vollständige Liste für Movie-Seite (mit Bearbeitungslinks aktualisiert)</span><span class="sxs-lookup"><span data-stu-id="7f30b-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="7f30b-339">Führen Sie die Seite Listeneintrag Movie-Seite "Bearbeiten"</span><span class="sxs-lookup"><span data-stu-id="7f30b-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="7f30b-340">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="7f30b-340">Additional Resources</span></span>

- [<span data-ttu-id="7f30b-341">Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax</span><span class="sxs-lookup"><span data-stu-id="7f30b-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../../getting-started/introducing-razor-syntax-c.md)
- <span data-ttu-id="7f30b-342">[SQL-UPDATE-Anweisung](http://www.w3schools.com/sql/sql_update.asp) auf der Website W3Schools</span><span class="sxs-lookup"><span data-stu-id="7f30b-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="7f30b-343">[Zurück](entering-data.md)
> [Weiter](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="7f30b-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
