---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Einführung in ASP.NET Web Pages - HTML-Formular-Grundlagen | Microsoft Docs
author: tfitzmac
description: In diesem Lernprogramm wird gezeigt, die Grundlagen der wie ein input-Formular erstellen und die Eingaben des Benutzers bei der Verwendung von ASP.NET Web Pages (Razor) zu behandeln. Und jetzt, die Sie...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 6f44f74774c2fa6338524987779e15f3940d1830
ms.sourcegitcommit: 9a35906446af7ffd4ccfc18daec38874b5abbef7
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 06/20/2018
ms.locfileid: "30898920"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="3e820-104">Einführung in ASP.NET Web Pages - HTML-Formular-Grundlagen</span><span class="sxs-lookup"><span data-stu-id="3e820-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="3e820-105">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="3e820-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="3e820-106">In diesem Lernprogramm wird gezeigt, die Grundlagen der wie ein input-Formular erstellen und die Eingaben des Benutzers bei der Verwendung von ASP.NET Web Pages (Razor) zu behandeln.</span><span class="sxs-lookup"><span data-stu-id="3e820-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="3e820-107">Und nun, da Sie eine Datenbank haben, müssen Sie Ihr Formular wissen verwenden, können Benutzer bestimmte Filme in der Datenbank gefunden.</span><span class="sxs-lookup"><span data-stu-id="3e820-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="3e820-108">Es wird vorausgesetzt, Sie haben die Reihe über abgeschlossen [Einführung zum Anzeigen von Daten mithilfe von ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="3e820-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="3e820-109">Lernen Sie:</span><span class="sxs-lookup"><span data-stu-id="3e820-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="3e820-110">Vorgehensweise: Erstellen Sie ein Formular mit standard-HTML-Elemente.</span><span class="sxs-lookup"><span data-stu-id="3e820-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="3e820-111">Gewusst wie: Lesen Sie den Benutzer Benutzereingabe in einem Formular.</span><span class="sxs-lookup"><span data-stu-id="3e820-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="3e820-112">Vorgehensweise: eine SQL-Abfrage erstellen, selektiv Daten mithilfe einer Suche abgerufen, die der Benutzer Begriff bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="3e820-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="3e820-113">Wie Sie die Felder auf der Seite "merken" was der Benutzer eingegeben haben.</span><span class="sxs-lookup"><span data-stu-id="3e820-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="3e820-114">Features/Technologien erläutert:</span><span class="sxs-lookup"><span data-stu-id="3e820-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="3e820-115">Das `Request`-Objekt.</span><span class="sxs-lookup"><span data-stu-id="3e820-115">The `Request` object.</span></span>
> - <span data-ttu-id="3e820-116">Die SQL `Where` Klausel.</span><span class="sxs-lookup"><span data-stu-id="3e820-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="3e820-117">Was müssen Sie erstellen</span><span class="sxs-lookup"><span data-stu-id="3e820-117">What You'll Build</span></span>

<span data-ttu-id="3e820-118">Im vorherigen Lernprogramm Sie eine Datenbank erstellt, Daten hinzugefügt, und dann verwendet der `WebGrid` Hilfsmethode zum Anzeigen der Daten.</span><span class="sxs-lookup"><span data-stu-id="3e820-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="3e820-119">In diesem Lernprogramm fügen Sie ein Suchfeld, mit der Sie die von einer bestimmten "Genre" finden, oder, deren Titel enthält den Wort, das Sie eingeben.</span><span class="sxs-lookup"><span data-stu-id="3e820-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="3e820-120">(Z. B., müssen Sie, können alle Filme, deren "Genre" ist "Aktion", oder zu ermitteln, deren Titel enthält "Harry" oder "Adventure".)</span><span class="sxs-lookup"><span data-stu-id="3e820-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="3e820-121">Wenn Sie mit diesem Lernprogramm fertig sind, müssen Sie eine Seite, wie diese:</span><span class="sxs-lookup"><span data-stu-id="3e820-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Seite "Filme" mit "Genre" "und" Title-Suche](form-basics/_static/image1.png)

<span data-ttu-id="3e820-123">Die Auflistung Teil der Seite ist dasselbe wie bei der letzten Lernprogramm &mdash; ein Raster an.</span><span class="sxs-lookup"><span data-stu-id="3e820-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="3e820-124">Der Unterschied werden, dass im Raster nur die Filme anzeigt, dass Sie nach dem gesucht werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="3e820-125">Informationen zu HTML-Formularen</span><span class="sxs-lookup"><span data-stu-id="3e820-125">About HTML Forms</span></span>

<span data-ttu-id="3e820-126">(Wenn Sie Erfahrung mit HTML-Formulare erstellen und mit den Unterschied zwischen haben `GET` und `POST`, Sie können diesen Abschnitt überspringen.)</span><span class="sxs-lookup"><span data-stu-id="3e820-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="3e820-127">Ein Formular weist benutzereingabeelemente &mdash; Textfelder, Schaltflächen, Optionsfelder, Kontrollkästchen, Dropdownlisten und So weiter.</span><span class="sxs-lookup"><span data-stu-id="3e820-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="3e820-128">Benutzer in diesen Steuerelementen füllen oder treffen und anschließend Absenden des Formulars durch Klicken auf eine Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="3e820-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="3e820-129">Die grundlegende HTML-Syntax eines Formulars wird in diesem Beispiel dargestellt:</span><span class="sxs-lookup"><span data-stu-id="3e820-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="3e820-130">Wenn dieses Markup auf einer Seite ausgeführt wird, erstellt es ein einfaches Formular aus, das in dieser Abbildung ähnelt:</span><span class="sxs-lookup"><span data-stu-id="3e820-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Grundlegende als im Browser gerenderte HTML-Formular](form-basics/_static/image2.png)

<span data-ttu-id="3e820-132">Die `<form>` Element umschließt HTML-Elementen zur übermittelt werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="3e820-133">(Eine einfache Fehler vornehmen wird auf der Seite Elemente hinzufügen, aber vergessen Sie Einsatzort innerhalb einer `<form>` Element.</span><span class="sxs-lookup"><span data-stu-id="3e820-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="3e820-134">In diesem Fall wird keine Aktion gesendet.) Die `method` Attribut teilt dem Browser, wie die Benutzereingabe zu übermitteln.</span><span class="sxs-lookup"><span data-stu-id="3e820-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="3e820-135">Sie festlegen, um `post` , wenn Sie ein Update auf dem Server oder auf durchführen, `get` , wenn Sie nur Daten vom Server abgerufen werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="3e820-136">**GET, POST und HTTP-Verb für Sicherheit**</span><span class="sxs-lookup"><span data-stu-id="3e820-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="3e820-137">HTTP, das Protokoll, Browsern und Servern verwenden, für den Austausch von Informationen, ist erstaunlich einfach in seiner grundlegenden Vorgänge.</span><span class="sxs-lookup"><span data-stu-id="3e820-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="3e820-138">Browsern werden nur einige Verben verwenden, damit Anforderungen an den Server senden.</span><span class="sxs-lookup"><span data-stu-id="3e820-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="3e820-139">Beim Schreiben von Code für das Web ist es hilfreich zu verstehen, diese Verben und dem Browser und dem Server wie verwenden.</span><span class="sxs-lookup"><span data-stu-id="3e820-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="3e820-140">Bei weitem sich dabei um die am häufigsten verwendeten Verben:</span><span class="sxs-lookup"><span data-stu-id="3e820-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="3e820-141">`GET`</span><span class="sxs-lookup"><span data-stu-id="3e820-141">`GET`.</span></span> <span data-ttu-id="3e820-142">Der Browser verwendet das Verb etwas vom Server abgerufen.</span><span class="sxs-lookup"><span data-stu-id="3e820-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="3e820-143">Z. B. Wenn Sie eine URL in Ihren Browser eingeben, die der Browser führt eine `GET` Vorgang die Seite angefordert werden sollen.</span><span class="sxs-lookup"><span data-stu-id="3e820-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="3e820-144">Wenn die Seite Grafiken enthält, führt der Browser zusätzliche `GET` Operations um die Images abzurufen.</span><span class="sxs-lookup"><span data-stu-id="3e820-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="3e820-145">Wenn die `GET` -Vorgang bleibt, Informationen an den Server zu übergeben, die Informationen als Teil der URL in der Abfragezeichenfolge übergeben wird.</span><span class="sxs-lookup"><span data-stu-id="3e820-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="3e820-146">`POST`</span><span class="sxs-lookup"><span data-stu-id="3e820-146">`POST`.</span></span> <span data-ttu-id="3e820-147">Der Browser sendet eine `POST` Anforderung an, um das Senden von Daten, die hinzugefügt oder auf dem Server geändert werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="3e820-148">Z. B. die `POST` Verb wird verwendet, um Datensätze in einer Datenbank erstellen oder vorhandene ändern.</span><span class="sxs-lookup"><span data-stu-id="3e820-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="3e820-149">In den meisten Fällen, wenn Sie ein Formular auszufüllen, und klicken Sie auf die Schaltfläche "Absenden", um der Browser führt eine `POST` Vorgang.</span><span class="sxs-lookup"><span data-stu-id="3e820-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="3e820-150">In einem `POST` -Operation, die Daten an den Server weitergegeben werden im Text der Seite ist.</span><span class="sxs-lookup"><span data-stu-id="3e820-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="3e820-151">Ein wichtiger Unterschied zwischen diesen Verben ist, die eine `GET` Vorgang sollte nicht ändert nichts auf dem Server – oder auf eine etwas geringere abstrakte Weise ausgedrückt ein `GET` Vorgang führt nicht zu einer Änderung auf dem Server.</span><span class="sxs-lookup"><span data-stu-id="3e820-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="3e820-152">Können Sie Ausführen einer `GET` Vorgang auf die gleichen Ressourcen wie oft und solange Ihnen gefällt, und diese Ressourcen nicht ändern.</span><span class="sxs-lookup"><span data-stu-id="3e820-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="3e820-153">(Ein `GET` Operation wird häufig als "sicheren" sein, oder um einen technischen Begriff zu verwenden ist *Idempotent*.) Im Gegensatz dazu sind natürlich eine `POST` Anforderung ändert sich etwas auf dem Server jedes Mal, führen Sie den Vorgang.</span><span class="sxs-lookup"><span data-stu-id="3e820-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="3e820-154">Zwei Beispiele für lassen sich diese Unterscheidung zu veranschaulichen.</span><span class="sxs-lookup"><span data-stu-id="3e820-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="3e820-155">Beim Ausführen einer Suche mit einem Modul wie Bing oder Google, füllen Sie ein Formular, das von einem Textfeld besteht aus, und klicken Sie dann Sie auf die Schaltfläche "Suchen".</span><span class="sxs-lookup"><span data-stu-id="3e820-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="3e820-156">Der Browser führt eine `GET` Vorgang, mit dem Wert, die Sie eingegeben, in das Feld, das als Teil der URL übergeben haben.</span><span class="sxs-lookup"><span data-stu-id="3e820-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="3e820-157">Mit einem `GET` Vorgang für diese Art von Formular Ordnung, ist, da es sich bei ein Suchvorgang keine Ressourcen auf dem Server ändern, es nur Informationen abruft.</span><span class="sxs-lookup"><span data-stu-id="3e820-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="3e820-158">Nun sehen wir uns mit des Prozess etwas online bestellen.</span><span class="sxs-lookup"><span data-stu-id="3e820-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="3e820-159">Sie füllen die Auftragsdetails enthält, und klicken Sie dann auf die Schaltfläche "Absenden".</span><span class="sxs-lookup"><span data-stu-id="3e820-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="3e820-160">Dieser Vorgang wird eine `POST` anfordern, da der Vorgang Änderungen auf dem Server, z. B. einen neuen Auftragsdatensatz, eine Änderung in Ihre Kontoinformationen und möglicherweise viele weitere Änderungen führt.</span><span class="sxs-lookup"><span data-stu-id="3e820-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="3e820-161">Im Gegensatz zu den `GET` -Vorgang können nicht Sie wiederholt Ihre `POST` Anforderung –, wenn Sie Sie, jedes Mal haben Sie erneut die Anforderung übermittelt würden Sie einen neuen Auftrag auf dem Server generieren.</span><span class="sxs-lookup"><span data-stu-id="3e820-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="3e820-162">(In solchen Fällen Websites häufig gewarnt. Sie nicht auf eine Schaltfläche "Absenden" mehr als einmal klicken, oder wird die Schaltfläche "Absenden" deaktivieren, sodass Sie das Formular versehentlich erneut nicht.)</span><span class="sxs-lookup"><span data-stu-id="3e820-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="3e820-163">Im Verlauf dieses Lernprogramms verwenden Sie sowohl eine `GET` Vorgang und ein `POST` Vorgang zur Bearbeitung von HTML-Formularen.</span><span class="sxs-lookup"><span data-stu-id="3e820-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="3e820-164">Wir erläutern in jeder Groß-/Kleinschreibung verdeutlichen, warum das Verb an, das Sie verwenden die entsprechenden handelt.</span><span class="sxs-lookup"><span data-stu-id="3e820-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="3e820-165">(Weitere Informationen zu HTTP-Verben finden Sie unter der [Methodendefinitionen](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) Artikel auf der W3C-Website.)</span><span class="sxs-lookup"><span data-stu-id="3e820-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="3e820-166">Die meisten benutzereingabeelemente sind HTML `<input>` Elemente.</span><span class="sxs-lookup"><span data-stu-id="3e820-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="3e820-167">Sie sehen, wie `<input type="type" name="name">,` , in denen *Typ* gibt die Art der Eingabe Benutzersteuerelement werden sollen.</span><span class="sxs-lookup"><span data-stu-id="3e820-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="3e820-168">Diese Elemente sind die gängigsten Einträge:</span><span class="sxs-lookup"><span data-stu-id="3e820-168">These elements are the common ones:</span></span>

- <span data-ttu-id="3e820-169">Textfeld: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="3e820-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="3e820-170">Kontrollkästchen: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="3e820-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="3e820-171">Optionsfeld: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="3e820-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="3e820-172">Schaltfläche: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="3e820-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="3e820-173">Schaltfläche zum Absenden: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="3e820-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="3e820-174">Sie können auch die `<textarea>` Element, um ein mehrzeiliges Textfeld erstellen und die `<select>` Element, um eine Dropdown-Liste oder einer bildlauffähigen Liste erstellen.</span><span class="sxs-lookup"><span data-stu-id="3e820-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="3e820-175">(Weitere Informationen zu HTML Elemente bilden, finden Sie unter [HTML-Formularen und Eingabe](http://www.w3schools.com/html/html_forms.asp) auf der Website W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="3e820-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="3e820-176">Die `name` Attribut ist sehr wichtig, da der Name wie den Wert des Elements höher erhalten, ist wie Sie in Kürze sehen.</span><span class="sxs-lookup"><span data-stu-id="3e820-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="3e820-177">Ist das interessante daran, was Ihnen als Seitenentwickler tun, mit der Eingabe des Benutzers.</span><span class="sxs-lookup"><span data-stu-id="3e820-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="3e820-178">Es ist kein integriertes Verhalten, die diesen Elementen zugeordnet.</span><span class="sxs-lookup"><span data-stu-id="3e820-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="3e820-179">Stattdessen müssen Sie rufen Sie die Werte, die der Benutzer ausgewählt oder wurde eingegeben und etwas mit ihnen.</span><span class="sxs-lookup"><span data-stu-id="3e820-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="3e820-180">Dies ist in diesem Lernprogramm lernen Sie.</span><span class="sxs-lookup"><span data-stu-id="3e820-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="3e820-181">**HTML5- und als Eingabeformulare**</span><span class="sxs-lookup"><span data-stu-id="3e820-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="3e820-182">Wie Sie wissen vielleicht, HTML befindet sich im Übergangsstadium, und die neueste Version (HTML5) bietet Unterstützung für intuitiver Möglichkeiten für Benutzer Informationen eingeben.</span><span class="sxs-lookup"><span data-stu-id="3e820-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="3e820-183">Beispielsweise können in HTML5, (der Entwickler der Seite ") der Seite Aufschluss darüber, dass der Benutzer ein Datum eingeben soll.</span><span class="sxs-lookup"><span data-stu-id="3e820-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="3e820-184">Browser können Sie automatisch einen Kalender, anstatt dass der Benutzer ein Datum manuell eingeben anzeigen.</span><span class="sxs-lookup"><span data-stu-id="3e820-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="3e820-185">Allerdings HTML5 ist neu und wird nicht in allen Browsern noch unterstützt.</span><span class="sxs-lookup"><span data-stu-id="3e820-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="3e820-186">ASP.NET Web Pages unterstützt HTML5 Benutzereingaben, wenn der Browser des Benutzers ist.</span><span class="sxs-lookup"><span data-stu-id="3e820-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="3e820-187">Für einen Überblick über die neuen Attribute für die `<input>` Element in HTML5, finden Sie unter [HTML &lt;input&gt; -Attributtyp](http://www.w3schools.com/html/html_form_input_types.asp) auf der Website W3Schools.</span><span class="sxs-lookup"><span data-stu-id="3e820-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="3e820-188">Erstellen des Formulars</span><span class="sxs-lookup"><span data-stu-id="3e820-188">Creating the Form</span></span>

<span data-ttu-id="3e820-189">In WebMatrix in der **Dateien** Arbeitsbereich, öffnen die *Movies.cshtml* Seite.</span><span class="sxs-lookup"><span data-stu-id="3e820-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="3e820-190">Nach dem schließenden `</h1>` Tag und vor dem Öffnen `<div>` Tag die `grid.GetHtml` aufrufen, fügen Sie das folgende Markup hinzu:</span><span class="sxs-lookup"><span data-stu-id="3e820-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="3e820-191">Dieses Markup erstellt ein Formular, das ein mit dem Namen Textfeld `searchGenre` und eine Schaltfläche "Absenden".</span><span class="sxs-lookup"><span data-stu-id="3e820-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="3e820-192">Den Text ein, und senden Sie die Schaltfläche in eingeschlossen sind ein `<form>` Element, dessen `method` -Attributsatz zur `get`.</span><span class="sxs-lookup"><span data-stu-id="3e820-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="3e820-193">(Beachten Sie, dass, wenn Sie das Textfeld einfügen, und übermitteln die Schaltfläche ein `<form>` -Element, wird nichts übermittelt werden, wenn Sie die Schaltfläche klicken.) Sie verwenden die `GET` Verb hier, da Sie ein Formular erstellen, die Sie nimmt keine Änderungen auf dem Server, führt dies nur bei einer Suche.</span><span class="sxs-lookup"><span data-stu-id="3e820-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="3e820-194">(Im vorherigen Lernprogramm Sie verwendet eine `post` Methode, die ist, wie Sie Änderungen an den Server übermitteln.</span><span class="sxs-lookup"><span data-stu-id="3e820-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="3e820-195">Die in den nächsten Lernprogrammen erneut angezeigt.)</span><span class="sxs-lookup"><span data-stu-id="3e820-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="3e820-196">Führen Sie die Seite.</span><span class="sxs-lookup"><span data-stu-id="3e820-196">Run the page.</span></span> <span data-ttu-id="3e820-197">Obwohl Sie jedes Verhalten für das Formular definiert haben, können Sie sehen, wie es aussieht:</span><span class="sxs-lookup"><span data-stu-id="3e820-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Suchfeld für "Genre" Filme Seite](form-basics/_static/image3.png)

<span data-ttu-id="3e820-199">Geben Sie einen Wert in das Textfeld ein, z. B. "Comedy."</span><span class="sxs-lookup"><span data-stu-id="3e820-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="3e820-200">Klicken Sie dann auf **Suche "Genre"**.</span><span class="sxs-lookup"><span data-stu-id="3e820-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="3e820-201">Notieren Sie die URL der Seite.</span><span class="sxs-lookup"><span data-stu-id="3e820-201">Take note of the URL of the page.</span></span> <span data-ttu-id="3e820-202">Da Sie festlegen, die `<form>` des Elements `method` -Attribut auf `get`, der eingegebene Wert ist jetzt Teil der Abfragezeichenfolge in der URL wie folgt:</span><span class="sxs-lookup"><span data-stu-id="3e820-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="3e820-203">Formularwerte lesen</span><span class="sxs-lookup"><span data-stu-id="3e820-203">Reading Form Values</span></span>

<span data-ttu-id="3e820-204">Diese Seite enthält bereits Code, der die Datenbankdaten abruft und die Ergebnisse in einem Raster angezeigt.</span><span class="sxs-lookup"><span data-stu-id="3e820-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="3e820-205">Jetzt müssen Sie Code hinzufügen, der den Wert des Textfelds liest, damit Sie eine SQL-Abfrage ausführen können, die den Suchbegriff enthält.</span><span class="sxs-lookup"><span data-stu-id="3e820-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="3e820-206">Da die-Methode des Formulars festgelegt werden, um `get`, Sie können den Wert, der in das Textfeld eingegeben wurde, mithilfe von Code wie folgt lesen:</span><span class="sxs-lookup"><span data-stu-id="3e820-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="3e820-207">Die `Request.QueryString` Objekt (die `QueryString` Eigenschaft von der `Request` Objekt) enthält die Werte von Elementen, die im Rahmen des gesendet wurden die `GET` Vorgang.</span><span class="sxs-lookup"><span data-stu-id="3e820-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="3e820-208">Die `Request.QueryString` Eigenschaft enthält eine *Auflistung* (eine Liste) von den Werten, die in das Formular übermittelt werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="3e820-209">Um einen einzelnen Wert zu erhalten, geben Sie den Namen des Elements, das Sie möchten.</span><span class="sxs-lookup"><span data-stu-id="3e820-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="3e820-210">Daher verfügen eine `name` -Attribut auf die `<input>` Element (`searchTerm`), erstellt das Textfeld.</span><span class="sxs-lookup"><span data-stu-id="3e820-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="3e820-211">(Weitere Informationen zu den `Request` Objekt, finden Sie unter der [Randleiste](#BKMK_TheRequestObject) höher.)</span><span class="sxs-lookup"><span data-stu-id="3e820-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="3e820-212">Es ist einfach genug, um den Wert des Textfelds zu lesen.</span><span class="sxs-lookup"><span data-stu-id="3e820-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="3e820-213">Aber wenn der Benutzer nichts überhaupt in das Textfeld eingeben hat nicht jedoch geklickt **Suche** trotzdem, können Sie dass klicken ignorieren, da es nichts zum Suchen ist.</span><span class="sxs-lookup"><span data-stu-id="3e820-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="3e820-214">Der folgende Code ist ein Beispiel, das zeigt, wie Sie diese Bedingungen zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="3e820-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="3e820-215">(Müssen Sie diesen Code hinzufügen, die noch nicht; Sie erledigen, die Sie in wenigen Augenblicken.)</span><span class="sxs-lookup"><span data-stu-id="3e820-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="3e820-216">Der Test wird auf diese Weise unterteilt:</span><span class="sxs-lookup"><span data-stu-id="3e820-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="3e820-217">Rufen Sie den Wert der `Request.QueryString["searchGenre"]`, d. h. der Wert, der eingegeben wurde die `<input>` Element mit dem Namen `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="3e820-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="3e820-218">Herausfinden, ob sie mithilfe von "leer" ist die `IsEmpty` Methode.</span><span class="sxs-lookup"><span data-stu-id="3e820-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="3e820-219">Diese Methode ist das Standardverfahren, um festzustellen, ob ein Element (z. B. ein Formularelement) einen Wert enthält.</span><span class="sxs-lookup"><span data-stu-id="3e820-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="3e820-220">Aber wirklich, können Sie nur dann, wenn es wichtig *nicht* leer, daher...</span><span class="sxs-lookup"><span data-stu-id="3e820-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="3e820-221">Hinzufügen der `!` Operator vor dem `IsEmpty` testen.</span><span class="sxs-lookup"><span data-stu-id="3e820-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="3e820-222">(Die `!` Operator bedeutet Logisches NOT).</span><span class="sxs-lookup"><span data-stu-id="3e820-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="3e820-223">Im Klartext, die gesamte `if` Bedingung übersetzt werden, in der folgenden: *Wenn das Formular SearchGenre Element nicht leer ist, klicken Sie dann...*</span><span class="sxs-lookup"><span data-stu-id="3e820-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="3e820-224">Dieser Block wird die Phase zum Erstellen einer Abfrage, die den Suchbegriff verwendet.</span><span class="sxs-lookup"><span data-stu-id="3e820-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="3e820-225">Sie erledigen, die Sie im nächsten Abschnitt.</span><span class="sxs-lookup"><span data-stu-id="3e820-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="3e820-226">**Das Request-Objekt**</span><span class="sxs-lookup"><span data-stu-id="3e820-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="3e820-227">Die `Request` Objekt enthält alle Informationen, die der Browser für Ihre Anwendung sendet, wenn eine Seite angefordert oder gesendet wird.</span><span class="sxs-lookup"><span data-stu-id="3e820-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="3e820-228">Dieses Objekt enthält alle Informationen, die der Benutzer eingibt, wie z. B. Textfeldwerte oder eine Datei zum Hochladen.</span><span class="sxs-lookup"><span data-stu-id="3e820-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="3e820-229">Es enthält auch alle möglichen zusätzliche Informationen, wie Cookies, Werte in der URL-Abfragezeichenfolge (sofern vorhanden), den Dateipfad der Seite, die, den Typ des Browsers, die der Benutzer verwendet wird, die Liste der Sprachen ausgeführt wird, die im Browser festgelegt werden, , und vieles mehr.</span><span class="sxs-lookup"><span data-stu-id="3e820-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="3e820-230">Die `Request` Objekt ist ein *Auflistung* (Liste) von Werten.</span><span class="sxs-lookup"><span data-stu-id="3e820-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="3e820-231">Sie erhalten einen einzelnen Wert aus der Auflistung durch Angabe ihres Namens:</span><span class="sxs-lookup"><span data-stu-id="3e820-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="3e820-232">Die `Request` Objekt macht tatsächlich verschiedene Teilmengen.</span><span class="sxs-lookup"><span data-stu-id="3e820-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="3e820-233">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="3e820-233">For example:</span></span>
> 
> - <span data-ttu-id="3e820-234">`Request.Form` Gibt Werte von Elementen in der übermittelten `<form>` Element, wenn die Anforderung ist eine `POST` Anforderung.</span><span class="sxs-lookup"><span data-stu-id="3e820-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="3e820-235">`Request.QueryString` bietet Ihnen nur die Werte in der URL-Abfragezeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="3e820-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="3e820-236">(In einer URL wie `http://mysite/myapp/page?searchGenre=action&page=2`die `?searchGenre=action&page=2` Abschnitt der URL ist die Abfragezeichenfolge.)</span><span class="sxs-lookup"><span data-stu-id="3e820-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="3e820-237">`Request.Cookies` Sammlung erhalten Sie Zugriff auf Cookies, die der Browser gesendet wurde.</span><span class="sxs-lookup"><span data-stu-id="3e820-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="3e820-238">Einen Wert abgerufen, von denen Sie wissen in der übermittelten Form ist, können Sie `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="3e820-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="3e820-239">Alternativ können Sie spezifischeren Versionen `Request.Form["name"]` (für `POST` Anfragen) oder `Request.QueryString["name"]` (für `GET` Anforderungen).</span><span class="sxs-lookup"><span data-stu-id="3e820-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="3e820-240">Natürlich *Namen* ist der Name des abzurufenden Elements.</span><span class="sxs-lookup"><span data-stu-id="3e820-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="3e820-241">Der Name des Elements, die Sie abrufen möchten, muss innerhalb der Sammlung eindeutig sein, das Sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="3e820-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="3e820-242">Deshalb ist die `Request` Objekt bietet die Teilmengen wie `Request.Form` und `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="3e820-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="3e820-243">Nehmen wir an, dass Ihre Seite ein Formularelement mit dem Namen enthält `userName` und *auch* enthält ein Cookie mit dem Namen `userName`.</span><span class="sxs-lookup"><span data-stu-id="3e820-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="3e820-244">Wenn Sie erhalten `Request["userName"]`, er ist mehrdeutig, ob der Formularwert oder das Cookie soll.</span><span class="sxs-lookup"><span data-stu-id="3e820-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="3e820-245">Jedoch, wenn Sie erhalten `Request.Form["userName"]` oder `Request.Cookie["userName"]`, Sie sind explizit der Wert abgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="3e820-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="3e820-246">Es wird empfohlen, spezifisch und verwenden die Teilmenge der `Request` , dass Sie interessiert, z. B. sind `Request.Form` oder `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="3e820-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="3e820-247">Für die einfache Seiten, die Sie in diesem Lernprogramm erstellen, wird nicht wahrscheinlich wirklich jeglicher Unterschied erleichtern.</span><span class="sxs-lookup"><span data-stu-id="3e820-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="3e820-248">Allerdings wie komplexere Seiten erstellen, unter Verwendung der expliziten Version `Request.Form` oder `Request.QueryString` können Sie die Probleme zu vermeiden, die auftreten können, wenn die Seite ein Formular (oder mehrere Formen) enthält Cookies, Abfragezeichenfolgenwerten und So weiter.</span><span class="sxs-lookup"><span data-stu-id="3e820-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="3e820-249">Erstellen einer Abfrage mithilfe eines Suchbegriffs</span><span class="sxs-lookup"><span data-stu-id="3e820-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="3e820-250">Nun, dass Sie wissen, wie den Suchbegriff abgerufen, den der Benutzer eingegeben haben, können Sie eine Abfrage erstellen, die verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="3e820-251">Beachten Sie, um die Film-Elemente aus der Datenbank zu erhalten Sie eine SQL-Abfrage verwenden, die diese Anweisung ähnelt:</span><span class="sxs-lookup"><span data-stu-id="3e820-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="3e820-252">Um nur bestimmte Filme zu erhalten, müssen Sie eine Abfrage verwenden, enthält eine `Where` Klausel.</span><span class="sxs-lookup"><span data-stu-id="3e820-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="3e820-253">Diese Klausel können Sie eine Bedingung für die Zeilen von der Abfrage zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="3e820-254">Im Folgenden ein Beispiel:</span><span class="sxs-lookup"><span data-stu-id="3e820-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="3e820-255">Das Grundformat lautet `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="3e820-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="3e820-256">Verschiedene Operatoren können neben gerade `=`, z. B. `>` (größer als), `<` (kleiner als), `<>` (ungleich), `<=` (kleiner als oder gleich), usw., je nachdem, was Sie suchen.</span><span class="sxs-lookup"><span data-stu-id="3e820-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="3e820-257">Für den Fall, dass Sie sich wundern, sind SQL-Anweisungen nicht Groß-/Kleinschreibung beachtet &mdash; `SELECT` ist identisch mit `Select` (oder sogar `select`).</span><span class="sxs-lookup"><span data-stu-id="3e820-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="3e820-258">Allerdings Personen häufig Großbuchstaben Schlüsselwörter in einer SQL-Anweisung, wie z. B. `SELECT` und `WHERE`, um das Lesen zu erleichtern.</span><span class="sxs-lookup"><span data-stu-id="3e820-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="3e820-259">Den Suchbegriff wird als Parameter übergeben.</span><span class="sxs-lookup"><span data-stu-id="3e820-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="3e820-260">Suchen nach einer bestimmten "Genre" einfach ist (`WHERE Genre = 'Action'`), aber Sie möchten in der Lage, alle "Genre" gesucht, die der Benutzer eingibt.</span><span class="sxs-lookup"><span data-stu-id="3e820-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="3e820-261">Zu diesem Zweck erstellen Sie als SQL-Abfrage, die einen Platzhalter für den Wert zu suchen.</span><span class="sxs-lookup"><span data-stu-id="3e820-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="3e820-262">Es wird mit diesem Befehl formuliert werden:</span><span class="sxs-lookup"><span data-stu-id="3e820-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="3e820-263">Der Platzhalter ist der `@` Zeichen, gefolgt von 0 (null).</span><span class="sxs-lookup"><span data-stu-id="3e820-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="3e820-264">Wie Sie denken möglicherweise, eine Abfrage kann mehrere Platzhalter enthalten, und sie den Namen `@0`, `@1`, `@2`usw.</span><span class="sxs-lookup"><span data-stu-id="3e820-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="3e820-265">Um richten Sie die Abfrage, und der Wert tatsächlich übergeben, verwenden Sie den Code wie folgt:</span><span class="sxs-lookup"><span data-stu-id="3e820-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="3e820-266">Dieser Code ist ähnlich, was Sie bereits durchgeführt haben, um Daten im Raster anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="3e820-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="3e820-267">Die einzigen Unterschiede sind:</span><span class="sxs-lookup"><span data-stu-id="3e820-267">The only differences are:</span></span>

- <span data-ttu-id="3e820-268">Die Abfrage enthält einen Platzhalter (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="3e820-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="3e820-269">Die Abfrage wird in einer Variablen eingefügt (`selectCommand`), bevor Sie die Abfrage direkt zu übergeben der `db.Query` Methode.</span><span class="sxs-lookup"><span data-stu-id="3e820-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="3e820-270">Beim Aufrufen der `db.Query` -Methode, übergeben Sie die Abfrage und der Wert, der für den Platzhalter verwenden.</span><span class="sxs-lookup"><span data-stu-id="3e820-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="3e820-271">(Wenn die Abfrage mehrere Platzhalter hatten, übergeben Sie sie als unterschiedliche Werte für die Methode.)</span><span class="sxs-lookup"><span data-stu-id="3e820-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="3e820-272">Wenn Sie alle diese Elemente zusammengestellt, erhalten Sie den folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="3e820-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="3e820-273">**Wichtig**</span><span class="sxs-lookup"><span data-stu-id="3e820-273">**Important!**</span></span> <span data-ttu-id="3e820-274">Mithilfe von Platzhaltern (z. B. `@0`) werden zum Übergeben von Werten an einen SQL-Befehl *äußerst wichtig* für die Sicherheit.</span><span class="sxs-lookup"><span data-stu-id="3e820-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="3e820-275">Die hier mit Platzhaltern für Variablendaten, sehen besteht die einzige Möglichkeit, die SQL-Befehlen erstellt werden soll.</span><span class="sxs-lookup"><span data-stu-id="3e820-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="3e820-276">Erstellen Sie nie eine SQL-Anweisung, indem zusammenstellen (verketten) Literaltext und Werte, die Sie vom Benutzer erhalten.</span><span class="sxs-lookup"><span data-stu-id="3e820-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="3e820-277">Verketten von Benutzereingaben in einer SQL-Anweisung Öffnet Ihre Website in einem *SQL-Injection-Angriff* ein böswilliger Benutzer übermittelt, in denen Werte auf die Seite, die Ihre Datenbank hack.</span><span class="sxs-lookup"><span data-stu-id="3e820-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="3e820-278">(Erfahren Sie mehr im Artikel [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) der MSDN-Website.)</span><span class="sxs-lookup"><span data-stu-id="3e820-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="3e820-279">Aktualisieren die Seite "Filme" mit Code suchen</span><span class="sxs-lookup"><span data-stu-id="3e820-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="3e820-280">Jetzt kann das Aktualisieren des Codes in der *Movies.cshtml* Datei.</span><span class="sxs-lookup"><span data-stu-id="3e820-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="3e820-281">Um zu beginnen, ersetzen Sie den Code in den Codeblock am oberen Rand der Seite mit dem folgenden Code ein:</span><span class="sxs-lookup"><span data-stu-id="3e820-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="3e820-282">Der Unterschied besteht darin, dass Sie die Abfrage in gesteckt haben die `selectCommand` Variablen, die Sie übergeben müssen `db.Query` später erneut.</span><span class="sxs-lookup"><span data-stu-id="3e820-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="3e820-283">Platzieren die SQL-Anweisung in einer Variablen können Sie die Anweisung ändern, die was Sie tun müssen, um die Suche auszuführen ist.</span><span class="sxs-lookup"><span data-stu-id="3e820-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="3e820-284">Sie haben auch diese beiden Zeilen entfernt, die Sie später in zurückgestellt werden:</span><span class="sxs-lookup"><span data-stu-id="3e820-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="3e820-285">Nicht noch die Abfrage ausgeführt werden soll (d. h. Aufrufen `db.Query`) und nicht initialisiert werden soll die `WebGrid` Helper noch entweder.</span><span class="sxs-lookup"><span data-stu-id="3e820-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="3e820-286">Nachdem Sie ermittelt haben, welche SQL-Anweisung ausgeführt wurde, müssen Sie diese Schritte ausführen.</span><span class="sxs-lookup"><span data-stu-id="3e820-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="3e820-287">Nach dieser umgeschriebene Block können Sie die neue Logik zum Behandeln von die Suche hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="3e820-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="3e820-288">Der vollständige Code sieht wie folgt.</span><span class="sxs-lookup"><span data-stu-id="3e820-288">The completed code will look like the following.</span></span> <span data-ttu-id="3e820-289">Aktualisieren Sie den Code auf der Seite, damit sie in diesem Beispiel entspricht:</span><span class="sxs-lookup"><span data-stu-id="3e820-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="3e820-290">Die Seite funktioniert jetzt wie folgt.</span><span class="sxs-lookup"><span data-stu-id="3e820-290">The page now works like this.</span></span> <span data-ttu-id="3e820-291">Jedes Mal, wenn die Seite ausgeführt wird, wird der Code öffnet die Datenbank und die `selectCommand` Variable wird festgelegt, auf die SQL-Anweisung, die alle Datensätze von abruft, die `Movies` Tabelle.</span><span class="sxs-lookup"><span data-stu-id="3e820-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="3e820-292">Auch im Code initialisiert das `searchTerm` Variable.</span><span class="sxs-lookup"><span data-stu-id="3e820-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="3e820-293">Allerdings enthält die aktuelle Anforderung einen Wert für die `searchGenre` -Element, legt der Code `selectCommand` auf eine andere Abfrage – nämlich, enthält die `Where` -Klausel, um eine "Genre" suchen.</span><span class="sxs-lookup"><span data-stu-id="3e820-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="3e820-294">Außerdem wird `searchTerm` nach Belieben für das Suchfeld übergeben wurde (die ' Nothing ' sein kann).</span><span class="sxs-lookup"><span data-stu-id="3e820-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="3e820-295">Unabhängig davon, welche SQL-Anweisung liegt im `selectCommand`, der Code ruft dann `db.Query` um die Abfrage auszuführen, und übergeben sie den befindet sich in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="3e820-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="3e820-296">Wenn es nichts `searchTerm`, es spielt keine Rolle, da in diesem Fall kein Parameter besteht um den Wert zu übergeben `selectCommand` trotzdem.</span><span class="sxs-lookup"><span data-stu-id="3e820-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="3e820-297">Schließlich im Code initialisiert das `WebGrid` Helper mit den Abfrageergebnissen, genauso wie zuvor.</span><span class="sxs-lookup"><span data-stu-id="3e820-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="3e820-298">Sie können sehen, indem Sie die SQL-Anweisung einfügen und den Suchbegriff in Variablen, haben Sie Flexibilität zum Code hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="3e820-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="3e820-299">Wie Sie weiter unten in diesem Lernprogramm sehen werden, Sie können dieses grundlegende Framework und Logik für die verschiedenen Typen von Suchvorgängen hinzufügen zu halten.</span><span class="sxs-lookup"><span data-stu-id="3e820-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="3e820-300">Testen die Suche nach "Genre"-Funktion</span><span class="sxs-lookup"><span data-stu-id="3e820-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="3e820-301">Führen Sie in WebMatrix die *Movies.cshtml* Seite.</span><span class="sxs-lookup"><span data-stu-id="3e820-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="3e820-302">Die Seite mit dem Textfeld für "Genre" wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="3e820-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="3e820-303">Geben Sie eine "Genre", die Sie für einen der Datensätze Test eingegeben haben, und klicken Sie auf **Suche**.</span><span class="sxs-lookup"><span data-stu-id="3e820-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="3e820-304">Dieses Mal sehen Sie eine Liste der soeben Filme, die dieses Genres entsprechen:</span><span class="sxs-lookup"><span data-stu-id="3e820-304">This time you see a listing of just the movies that match that genre:</span></span>

![Filme-Seite, die nach der Suche nach "Genre""Comedies" auflisten](form-basics/_static/image4.png)

<span data-ttu-id="3e820-306">Geben Sie einen anderen "Genre" und die Suche wiederholen.</span><span class="sxs-lookup"><span data-stu-id="3e820-306">Enter a different genre and search again.</span></span> <span data-ttu-id="3e820-307">Versuchen Sie, die "Genre" mithilfe von klein- oder Großbuchstaben Großbuchstaben, damit Sie sehen, dass es sich bei der Suche nicht berücksichtigt Groß-/Kleinschreibung beachtet wird.</span><span class="sxs-lookup"><span data-stu-id="3e820-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="3e820-308">"Merken", was der Benutzer eingegeben hat.</span><span class="sxs-lookup"><span data-stu-id="3e820-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="3e820-309">Möglicherweise haben Sie bemerkt, die nach einem "Genre" eingegeben und geklickt **Suche "Genre"**, einen Eintrag für dieses Genres gelernt.</span><span class="sxs-lookup"><span data-stu-id="3e820-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="3e820-310">Das Suchfeld ein war jedoch leer &mdash; also nicht merken eingegebene hatte.</span><span class="sxs-lookup"><span data-stu-id="3e820-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="3e820-311">Es ist wichtig zu verstehen, warum dieses Verhalten tritt auf.</span><span class="sxs-lookup"><span data-stu-id="3e820-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="3e820-312">Wenn Sie eine Seite senden, sendet der Browser eine Anforderung an den Webserver.</span><span class="sxs-lookup"><span data-stu-id="3e820-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="3e820-313">Wenn ASP.NET die Anforderung weitergeleitet wird, erstellt eine völlig neue Instanz der Seite, führt den Code im sie und rendert die Seite an den Browser erneut.</span><span class="sxs-lookup"><span data-stu-id="3e820-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="3e820-314">Aktiviert ist, aber weiß die Seite nicht, dass Sie nur mit einer früheren Version von sich selbst arbeiteten.</span><span class="sxs-lookup"><span data-stu-id="3e820-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="3e820-315">Alle bekannt ist, dass die It eine Anforderung erhalten wird, die einige mussten Formulardaten darin.</span><span class="sxs-lookup"><span data-stu-id="3e820-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="3e820-316">Jedes Mal, wenn Sie eine Seite anfordern &mdash; angibt, ob zum ersten Mal oder Senden des Updategrams &mdash; können Sie eine neue Seite abrufen.</span><span class="sxs-lookup"><span data-stu-id="3e820-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="3e820-317">Der Webserver verfügt über kein Speicher Ihrer letzten Anforderung.</span><span class="sxs-lookup"><span data-stu-id="3e820-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="3e820-318">Wird keins ASP.NET und ist weder den Browser.</span><span class="sxs-lookup"><span data-stu-id="3e820-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="3e820-319">Die einzige Verbindung zwischen diesen separaten Instanzen von der Seite "ist keine Daten, mit denen Sie zwischen ihnen zu übertragen.</span><span class="sxs-lookup"><span data-stu-id="3e820-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="3e820-320">Wenn Sie auf eine Seite übermitteln, kann die neue Seiteninstanz z. B. die Formulardaten abrufen, die von der früheren Instanz gesendet wurde.</span><span class="sxs-lookup"><span data-stu-id="3e820-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="3e820-321">(Eine weitere Möglichkeit zum Übergeben von Daten zwischen Seiten werden Cookies verwendet.)</span><span class="sxs-lookup"><span data-stu-id="3e820-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="3e820-322">Eine formale zur Beschreibung dieser Situation besteht darin, sagen, dass Webseiten sind *zustandslose*.</span><span class="sxs-lookup"><span data-stu-id="3e820-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="3e820-323">Webserver und den Seiten selbst und die Elemente auf der Seite beibehalten Informationen zu den vorherigen Zustand einer Seite nicht.</span><span class="sxs-lookup"><span data-stu-id="3e820-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="3e820-324">Im Web wurde so entwickelt werden, da Zustandsverwaltung Informationen zu einzelnen Anforderungen schnell ausgeschöpft würde die Ressourcen von Webservern, die häufig Tausende, vielleicht behandeln sogar Hunderte von Tausenden von Anforderungen pro Sekunde.</span><span class="sxs-lookup"><span data-stu-id="3e820-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="3e820-325">Daher das Textfeld leer war.</span><span class="sxs-lookup"><span data-stu-id="3e820-325">So that's why the text box was empty.</span></span> <span data-ttu-id="3e820-326">Nachdem Sie die Seite übermittelt, ASP.NET erstellt eine neue Instanz der Seite, und Sie werden über den Code und Markup ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="3e820-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="3e820-327">Es wurde nichts in diesen Code, die ASP.NET für den einen Wert in das Textfeld eingefügt mitgeteilt.</span><span class="sxs-lookup"><span data-stu-id="3e820-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="3e820-328">Daher ASP.NET haben nicht keinerlei, und das Textfeld wurde ohne einen Wert darin gerendert.</span><span class="sxs-lookup"><span data-stu-id="3e820-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="3e820-329">Es ist eigentlich eine einfache Möglichkeit, dieses Problem zu umgehen.</span><span class="sxs-lookup"><span data-stu-id="3e820-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="3e820-330">Die "Genre", die Sie in das Textfeld eingegeben *ist* im Code für Sie verfügbaren &mdash; befindet sich im `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="3e820-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="3e820-331">Das Markup für das Textfeld zu aktualisieren, damit die `value` Attribut Ruft ab, deren Werte von `searchTerm`, wie im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="3e820-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="3e820-332">Auf dieser Seite, Sie haben auch Festlegen der `value` -Attribut auf die `searchTerm` eingegebene Variable, da die Variable auch die "Genre" enthält.</span><span class="sxs-lookup"><span data-stu-id="3e820-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="3e820-333">Während mit der `Request` Objekt, das Festlegen der `value` -Attribut an, wie die hier gezeigte der Standardmethode zur Ausführung dieser Aufgabe.</span><span class="sxs-lookup"><span data-stu-id="3e820-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="3e820-334">(Vorausgesetzt, Sie möchten auch dazu &mdash; in einigen Situationen sollten Sie zum Rendern der Seite *ohne* Werte in den Feldern.</span><span class="sxs-lookup"><span data-stu-id="3e820-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="3e820-335">Es hängt alles mit der app was.)</span><span class="sxs-lookup"><span data-stu-id="3e820-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="3e820-336">Sie können keine "den Wert eines Textfelds für die Kennwörter verwendeten merken".</span><span class="sxs-lookup"><span data-stu-id="3e820-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="3e820-337">Es wäre eine Sicherheitslücke Personen mithilfe von Code ein Kennwortfeld ausfüllen können.</span><span class="sxs-lookup"><span data-stu-id="3e820-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="3e820-338">Führen Sie die Seite erneut aus, geben Sie eine "Genre", und klicken Sie auf **Suche "Genre"**.</span><span class="sxs-lookup"><span data-stu-id="3e820-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="3e820-339">Dieses Mal nicht nur sehen Sie die Ergebnisse der Suche, aber das Textfeld speichert Sie zuletzt eingegeben haben:</span><span class="sxs-lookup"><span data-stu-id="3e820-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Seite "zeigt, dass das Textfeld 'den vorherigen Eintrag gespeichert ist"](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="3e820-341">Suchen nach einem beliebigen Wort im Titel</span><span class="sxs-lookup"><span data-stu-id="3e820-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="3e820-342">Sie können jetzt für alle "Genre" suchen, aber Sie können auch einen Titel suchen möchten.</span><span class="sxs-lookup"><span data-stu-id="3e820-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="3e820-343">Es ist schwierig, einen Titel genau richtig machen, bei der Suche stattdessen Sie ein Wort suchen können, die an einer beliebigen Stelle innerhalb eines Titels angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="3e820-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="3e820-344">Verwenden Sie hierzu in SQL die `LIKE` Operator und die Syntax wie folgt:</span><span class="sxs-lookup"><span data-stu-id="3e820-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="3e820-345">Dieser Befehl ruft alle Filme, deren Namen "Adventure" enthalten.</span><span class="sxs-lookup"><span data-stu-id="3e820-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="3e820-346">Bei Verwendung der `LIKE` -Operator, schließen Sie das Platzhalterzeichen `%` als Teil der Suchbegriff.</span><span class="sxs-lookup"><span data-stu-id="3e820-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="3e820-347">Die Suche `LIKE 'adventure%'` bedeutet "beginnend mit 'Adventure'".</span><span class="sxs-lookup"><span data-stu-id="3e820-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="3e820-348">(Technisch gesehen bedeutet dies "Die Zeichenfolge 'Adventure' gefolgt von nichts") Auf ähnliche Weise den Suchbegriff `LIKE '%adventure'` bedeutet, dass "nichts gefolgt von der Zeichenfolge 'Adventure'", die eine andere Möglichkeit zum Ansagen "endet mit 'Adventure'".</span><span class="sxs-lookup"><span data-stu-id="3e820-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="3e820-349">Der Suchbegriff `LIKE '%adventure%'` aus diesem Grund bedeutet "mit"Adventure' an einer beliebigen Stelle in der Titelleiste."</span><span class="sxs-lookup"><span data-stu-id="3e820-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="3e820-350">(Technisch gesehen "Alles, was im Titel, gefolgt von 'Adventure', gefolgt von etwas.")</span><span class="sxs-lookup"><span data-stu-id="3e820-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="3e820-351">Innerhalb der `<form>` Element, fügen Sie das folgende Markup direkt unter dem schließenden `</div>` Tag für die Suche "Genre" (unmittelbar vor dem schließenden `</form>` Element):</span><span class="sxs-lookup"><span data-stu-id="3e820-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="3e820-352">Der Code zum behandeln diese Suche ist ähnlich wie der Code für die Suche "Genre", mit dem Unterschied, dass zusammenstellen stehen Ihnen die `LIKE` suchen.</span><span class="sxs-lookup"><span data-stu-id="3e820-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="3e820-353">In den Codeblock am oberen Rand der Seite, fügen Sie Folgendes `if` blockieren direkt nach der `if` Block für die Suche "Genre":</span><span class="sxs-lookup"><span data-stu-id="3e820-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="3e820-354">Dieser Code verwendet die gleiche Logik, die Sie zuvor gesehen haben, mit dem Unterschied, dass die Suche verwendet einen `LIKE` Operator und der Code setzt "`%`" vor und nach dem Suchbegriff.</span><span class="sxs-lookup"><span data-stu-id="3e820-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="3e820-355">Beachten Sie, wie sie können ganz einfach eine andere Suche auf der Seite hinzugefügt wurde.</span><span class="sxs-lookup"><span data-stu-id="3e820-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="3e820-356">Alles, was Sie musste wurde:</span><span class="sxs-lookup"><span data-stu-id="3e820-356">All you had to do was:</span></span>

- <span data-ttu-id="3e820-357">Erstellen einer `if` Block, der getestet werden, um festzustellen, ob die relevanten Suchfeld einen Wert hat.</span><span class="sxs-lookup"><span data-stu-id="3e820-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="3e820-358">Legen Sie die `selectCommand` -Variablen an eine neue SQL-Anweisung.</span><span class="sxs-lookup"><span data-stu-id="3e820-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="3e820-359">Legen Sie die `searchTerm` Variable auf den Wert an die Abfrage übergeben.</span><span class="sxs-lookup"><span data-stu-id="3e820-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="3e820-360">Hier wird der vollständige Codeblock, der Logik die neue für eine Suche nach Softwaretiteln enthält:</span><span class="sxs-lookup"><span data-stu-id="3e820-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="3e820-361">Hier wird ein Überblick darüber, was bewirkt, dass dieser Code ein:</span><span class="sxs-lookup"><span data-stu-id="3e820-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="3e820-362">Die Variablen `searchTerm` und `selectCommand` oben initialisiert werden.</span><span class="sxs-lookup"><span data-stu-id="3e820-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="3e820-363">Sie sind im Begriff, diese Variablen auf den entsprechenden Suchbegriff (sofern vorhanden) festgelegt und die entsprechenden SQL-Befehl basierend auf der Wirkungsweise des Benutzers auf der Seite.</span><span class="sxs-lookup"><span data-stu-id="3e820-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="3e820-364">Die Standardsuche ist der einfache Groß-/Kleinschreibung aller Filme aus der Datenbank abrufen.</span><span class="sxs-lookup"><span data-stu-id="3e820-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="3e820-365">In den Tests für `searchGenre` und `searchTitle`, legt der Code `searchTerm` auf den Wert, der Sie suchen möchten.</span><span class="sxs-lookup"><span data-stu-id="3e820-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="3e820-366">Auch festlegen, die Codeblöcke `selectCommand` auf einen entsprechenden SQL‑Befehl für die Suche.</span><span class="sxs-lookup"><span data-stu-id="3e820-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="3e820-367">Die `db.Query` Methode ist nur einmal aufgerufen, mit der SQL-Befehl wird `selectedCommand` und der Wert in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="3e820-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="3e820-368">Es ist kein Suchbegriff (keine "Genre" und kein Titel Wort) den Wert des `searchTerm` ist eine leere Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="3e820-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="3e820-369">Allerdings ist, die unerheblich, da in diesem Fall die Abfrage keine Parameter erfordert.</span><span class="sxs-lookup"><span data-stu-id="3e820-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="3e820-370">Testen die Suchfunktion für den Titel</span><span class="sxs-lookup"><span data-stu-id="3e820-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="3e820-371">Jetzt können Sie Ihre abgeschlossenen Suchseite testen.</span><span class="sxs-lookup"><span data-stu-id="3e820-371">Now you can test your completed search page.</span></span> <span data-ttu-id="3e820-372">Führen Sie *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="3e820-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="3e820-373">Geben Sie eine "Genre", und klicken Sie auf **Suche "Genre"**.</span><span class="sxs-lookup"><span data-stu-id="3e820-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="3e820-374">Das Raster zeigt dieses Genres Filme wie vor.</span><span class="sxs-lookup"><span data-stu-id="3e820-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="3e820-375">Geben Sie ein Wort Titel, und klicken Sie auf **Titelsuche**.</span><span class="sxs-lookup"><span data-stu-id="3e820-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="3e820-376">Das Raster zeigt Filme, die das Wort im Titel haben.</span><span class="sxs-lookup"><span data-stu-id="3e820-376">The grid displays movies that have that word in the title.</span></span>

![Filme-Seite, die nach der Suche für die"im Titel auflisten](form-basics/_static/image6.png)

<span data-ttu-id="3e820-378">Lassen Sie beide Textfelder leer, und klicken Sie auf eine Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="3e820-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="3e820-379">Das Raster zeigt alle Filme.</span><span class="sxs-lookup"><span data-stu-id="3e820-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="3e820-380">Kombinieren von Abfragen</span><span class="sxs-lookup"><span data-stu-id="3e820-380">Combining the Queries</span></span>

<span data-ttu-id="3e820-381">Sie können feststellen, die suchen, die Sie ausführen können exklusiv sind.</span><span class="sxs-lookup"><span data-stu-id="3e820-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="3e820-382">Sie können nicht am Titel und an die "Genre" zur gleichen Zeit suchen, selbst wenn beide Suchfelder Werte enthalten.</span><span class="sxs-lookup"><span data-stu-id="3e820-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="3e820-383">Sie können z. B. können nicht für alle Aktion Filme suchen, deren Titel "Adventure" enthält.</span><span class="sxs-lookup"><span data-stu-id="3e820-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="3e820-384">(Wie die Seite nun codiert ist, wenn Sie Werte für "Genre" und Titel eingeben, ruft die Suche nach Softwaretiteln Vorrang.) Um eine Suche zu erstellen, die die Bedingungen kombiniert, müssten Sie eine SQL-Abfrage erstellen, die wie die folgende Syntax verfügt:</span><span class="sxs-lookup"><span data-stu-id="3e820-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="3e820-385">Sie müssten zum Ausführen der Abfrage mit einer Anweisung wie folgt (ungefähr sprechen):</span><span class="sxs-lookup"><span data-stu-id="3e820-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="3e820-386">Erstellen Logik, um viele Variationen von Suchkriterien zulassen kann etwas beteiligt sind, abrufen, wie Sie sehen können.</span><span class="sxs-lookup"><span data-stu-id="3e820-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="3e820-387">Aus diesem Grund müssen wir hier beenden.</span><span class="sxs-lookup"><span data-stu-id="3e820-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="3e820-388">Als Nächstes kommen</span><span class="sxs-lookup"><span data-stu-id="3e820-388">Coming Up Next</span></span>

<span data-ttu-id="3e820-389">In den nächsten Lernprogrammen erstellen Sie eine Seite, die einem Formular verwendet wird, können Benutzer der Datenbank Filme hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="3e820-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="3e820-390">Vollständige Auflistung für Filmdetailseite (inklusive Suche)</span><span class="sxs-lookup"><span data-stu-id="3e820-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="3e820-391">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="3e820-391">Additional Resources</span></span>

- [<span data-ttu-id="3e820-392">Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax</span><span class="sxs-lookup"><span data-stu-id="3e820-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="3e820-393">[SQL-WHERE-Klausel](http://www.w3schools.com/sql/sql_where.asp) auf der Website W3Schools</span><span class="sxs-lookup"><span data-stu-id="3e820-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="3e820-394">[Methodendefinitionen](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) Artikel auf der W3C-Website</span><span class="sxs-lookup"><span data-stu-id="3e820-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="3e820-395">[Zurück](displaying-data.md)
> [Weiter](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="3e820-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
