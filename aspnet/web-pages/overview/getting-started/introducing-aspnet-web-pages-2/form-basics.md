---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Einführung in ASP.NET Web Pages - HTML-Formular-Grundlagen | Microsoft-Dokumentation
author: tfitzmac
description: 'In diesem Tutorial erfahren Sie, die Grundlagen der Vorgehensweise: Erstellen Sie ein Eingabeformular an und wie die Eingabe des Benutzers verarbeitet, bei der Verwendung von ASP.NET Web Pages (Razor). Und nachdem Sie...'
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 5cf628b63e868e3f76aa19879d4af554241979c0
ms.sourcegitcommit: 45ac74e400f9f2b7dbded66297730f6f14a4eb25
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 08/16/2018
ms.locfileid: "41836765"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="29d6a-104">Einführung in ASP.NET Web Pages - HTML-Formular-Grundlagen</span><span class="sxs-lookup"><span data-stu-id="29d6a-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="29d6a-105">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="29d6a-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="29d6a-106">In diesem Tutorial erfahren Sie, die Grundlagen der Vorgehensweise: Erstellen Sie ein Eingabeformular an und wie die Eingabe des Benutzers verarbeitet, bei der Verwendung von ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="29d6a-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="29d6a-107">Und nun, da Sie eine Datenbank haben, verwenden Sie Ihre Kenntnisse Formular können Benutzer bestimmte Filme in der Datenbank gefunden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="29d6a-108">Es wird vorausgesetzt, Sie haben die Reihe über [Einführung zum Anzeigen von Daten mithilfe von ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="29d6a-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="29d6a-109">Sie lernen Folgendes:</span><span class="sxs-lookup"><span data-stu-id="29d6a-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="29d6a-110">Vorgehensweise: Erstellen Sie ein Formular mit standard-HTML-Elemente.</span><span class="sxs-lookup"><span data-stu-id="29d6a-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="29d6a-111">Gewusst wie: Lesen Sie den Benutzer Benutzereingabe in einem Formular.</span><span class="sxs-lookup"><span data-stu-id="29d6a-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="29d6a-112">Wie Sie eine SQL-Abfrage erstellen, selektiv Daten mithilfe einer Suche abgerufen, die der Benutzer Begriff bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="29d6a-113">So, dass Felder, die auf der Seite "erinnert" Eingabe des Benutzers.</span><span class="sxs-lookup"><span data-stu-id="29d6a-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="29d6a-114">Features/Technologien erläutert:</span><span class="sxs-lookup"><span data-stu-id="29d6a-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="29d6a-115">Das `Request`-Objekt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-115">The `Request` object.</span></span>
> - <span data-ttu-id="29d6a-116">Die SQL-Anweisung `Where` Klausel.</span><span class="sxs-lookup"><span data-stu-id="29d6a-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="29d6a-117">Sie lernen Folgendes</span><span class="sxs-lookup"><span data-stu-id="29d6a-117">What You'll Build</span></span>

<span data-ttu-id="29d6a-118">Im vorherigen Tutorial haben Sie eine Datenbank erstellt, Daten hinzugefügt, und dann verwendet der `WebGrid` Hilfsmethode zum Anzeigen der Daten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="29d6a-119">In diesem Tutorial fügen Sie ein Suchfeld, mit der Sie Filme, die von einer bestimmten "Genre" suchen oder, deren Titel enthält den eingegebenen Wort.</span><span class="sxs-lookup"><span data-stu-id="29d6a-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="29d6a-120">(Z. B. Sie Lage finden alle Filme, deren "Genre" "Action" ist oder mit dem Titel enthält "Paul" oder "Adventure".)</span><span class="sxs-lookup"><span data-stu-id="29d6a-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="29d6a-121">Wenn Sie mit diesem Lernprogramm fertig sind, müssen Sie eine Seite wie diese:</span><span class="sxs-lookup"><span data-stu-id="29d6a-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Seite "Movies", mit der Suche nach Genre und Titel](form-basics/_static/image1.png)

<span data-ttu-id="29d6a-123">Der Angebot Teil der Seite wird der gleiche wie in das letzte Tutorial &mdash; ein Raster.</span><span class="sxs-lookup"><span data-stu-id="29d6a-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="29d6a-124">Die Unterschiede werden im Raster nur die Filme anzeigt, dass Sie gesucht.</span><span class="sxs-lookup"><span data-stu-id="29d6a-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="29d6a-125">Informationen zu HTML-Formularen</span><span class="sxs-lookup"><span data-stu-id="29d6a-125">About HTML Forms</span></span>

<span data-ttu-id="29d6a-126">(Wenn Sie Erfahrung mit HTML-Formulare erstellen und mit den Unterschied zwischen haben `GET` und `POST`, können Sie diesen Abschnitt überspringen.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="29d6a-127">Ein Formular hat die benutzereingabeelemente &mdash; Textfelder, Schaltflächen, Optionsfelder, Kontrollkästchen, Dropdownlisten und So weiter.</span><span class="sxs-lookup"><span data-stu-id="29d6a-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="29d6a-128">Benutzer geben Sie in diesen Steuerelementen oder Optionen und klicken Sie dann das Formular durch Klicken auf eine Schaltfläche zu übermitteln.</span><span class="sxs-lookup"><span data-stu-id="29d6a-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="29d6a-129">In diesem Beispiel wird ein Formular grundlegende HTML-Syntax veranschaulicht:</span><span class="sxs-lookup"><span data-stu-id="29d6a-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="29d6a-130">Wenn dieses Markup auf einer Seite ausgeführt wird, wird ein einfaches Formular, das wie die folgende Abbildung aussieht:</span><span class="sxs-lookup"><span data-stu-id="29d6a-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Grundlegende als im Browser gerenderte HTML-Formular](form-basics/_static/image2.png)

<span data-ttu-id="29d6a-132">Die `<form>` Element einschließt, HTML-Elemente übermittelt werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="29d6a-133">(Eine einfache Fehler vornehmen besteht darin, die Elemente auf der Seite hinzufügen, aber vergessen Sie Einsatzort innerhalb einer `<form>` Element.</span><span class="sxs-lookup"><span data-stu-id="29d6a-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="29d6a-134">In diesem Fall ist "nothing" übermittelt.) Die `method` Attribut teilt dem Browser mit, wie Sie die Benutzereingabe zu übermitteln.</span><span class="sxs-lookup"><span data-stu-id="29d6a-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="29d6a-135">Sie legen `post` Wenn Sie ein Update auf dem Server oder auf durchführen `get` , wenn Sie nur Daten vom Server abgerufen werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="29d6a-136">**GET, POST und HTTP-Verb-Sicherheit**</span><span class="sxs-lookup"><span data-stu-id="29d6a-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="29d6a-137">HTTP, das Protokoll, Browser und Server zu verwenden, um Informationen auszutauschen, ist die grundlegende Vorgänge überraschend einfach.</span><span class="sxs-lookup"><span data-stu-id="29d6a-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="29d6a-138">Browser verwenden nur ein paar Verben, um Anforderungen an Server senden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="29d6a-139">Beim Schreiben von Code für das Web, ist es hilfreich zu verstehen, diese Verben und wie Browser und Server sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="29d6a-140">Bei weitem sind die am häufigsten verwendeten Verben dies die:</span><span class="sxs-lookup"><span data-stu-id="29d6a-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="29d6a-141">`GET`</span><span class="sxs-lookup"><span data-stu-id="29d6a-141">`GET`.</span></span> <span data-ttu-id="29d6a-142">Der Browser verwendet dieses Verb etwas vom Server abgerufen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="29d6a-143">Z. B. Wenn Sie eine URL in Ihren Browser eingeben, die der Browser führt eine `GET` Vorgang auf die Seite "anfordern, werden sollen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="29d6a-144">Wenn die Seite Grafiken enthält, führt der Browser zusätzliche `GET` Vorgänge, um die Bilder zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="29d6a-145">Wenn die `GET` -Vorgang bleibt, Informationen an den Server übergeben, die Informationen als Teil der URL in der Abfragezeichenfolge übergeben wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="29d6a-146">`POST`</span><span class="sxs-lookup"><span data-stu-id="29d6a-146">`POST`.</span></span> <span data-ttu-id="29d6a-147">Der Browser sendet eine `POST` Anforderung zum Senden von Daten hinzugefügt oder auf dem Server geändert werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="29d6a-148">Z. B. die `POST` Verb wird verwendet, um Datensätze in einer Datenbank erstellen oder vorhandene ändern.</span><span class="sxs-lookup"><span data-stu-id="29d6a-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="29d6a-149">In den meisten Fällen, wenn Sie ein Formular auszufüllen, und klicken Sie auf die Schaltfläche "Senden", die der Browser führt eine `POST` Vorgang.</span><span class="sxs-lookup"><span data-stu-id="29d6a-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="29d6a-150">In einem `POST` -Vorgang an den Server übergebenen Daten werden im Text der Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="29d6a-151">Ein wichtiger Unterschied zwischen dieser Verben ist, eine `GET` Vorgang darf nicht mehr ändern, Elemente auf dem Server – oder auf etwas abstraktere Weise ausgedrückt eine `GET` Vorgang führt nicht zu einer Änderung in den Zustand auf dem Server.</span><span class="sxs-lookup"><span data-stu-id="29d6a-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="29d6a-152">Durchführen können eine `GET` -Vorgang für die gleichen Ressourcen wie oft und solange aus, und ändern Sie diese Ressourcen nicht.</span><span class="sxs-lookup"><span data-stu-id="29d6a-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="29d6a-153">(Ein `GET` Vorgang wird oft betont, "sicher", oder um einen technischen Begriff, zu verwenden ist *Idempotent*.) Im Gegensatz dazu sind natürlich eine `POST` Anforderung ändert sich etwas auf dem Server jedes Mal, die Sie den Vorgang ausführen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="29d6a-154">Zwei Beispiele hilft diese Unterscheidung veranschaulichen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="29d6a-155">Beim Durchführen einer Suche verwenden ein Modul wie Bing oder Google, füllen Sie ein Formular, das aus einem Textfeld besteht, und klicken Sie dann Sie auf die Schaltfläche "Suchen".</span><span class="sxs-lookup"><span data-stu-id="29d6a-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="29d6a-156">Der Browser führt eine `GET` Vorgang, mit dem Wert, der Sie eingegeben, in das Feld, das als Teil der URL übergeben haben.</span><span class="sxs-lookup"><span data-stu-id="29d6a-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="29d6a-157">Mit einem `GET` Vorgang für diese Art von Form in Ordnung ist, ist, da es sich bei ein Suchvorgang alle Ressourcen auf dem Server nicht ändert, es nur Informationen abruft.</span><span class="sxs-lookup"><span data-stu-id="29d6a-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="29d6a-158">Betrachten Sie nun den Prozess etwas online zu bestellen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="29d6a-159">Sie geben Sie in den Details der Bestellung, und klicken Sie dann auf die Schaltfläche "Senden".</span><span class="sxs-lookup"><span data-stu-id="29d6a-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="29d6a-160">Dieser Vorgang wird eine `POST` anfordern, da Änderungen auf dem Server, z. B. einen neuen Datensatz für die Reihenfolge, eine Änderung der Kontoinformationen und möglicherweise viele weitere Änderungen der Vorgang führt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="29d6a-161">Im Gegensatz zu den `GET` -Operation, Sie können nicht wiederholt werden Ihre `POST` Anforderung – Wenn, jedes Mal haben Sie Sie die Anforderung erneut, würden Sie einen neuen Auftrag auf dem Server generieren.</span><span class="sxs-lookup"><span data-stu-id="29d6a-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="29d6a-162">(In solchen Fällen Websites häufig warnt Sie nicht mehr als einmal auf eine Sendeschaltfläche klicken, oder die Schaltfläche "Senden" wird deaktiviert, so, dass Sie versehentlich nicht erneut das Formular übermitteln.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="29d6a-163">Bei diesem Tutorial verwenden Sie sowohl eine `GET` Vorgang und ein `POST` HTML-Formularen funktioniert.</span><span class="sxs-lookup"><span data-stu-id="29d6a-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="29d6a-164">Erläutert in jeden Fall daher das Verb an, die, das Sie verwenden, das geeignet ist.</span><span class="sxs-lookup"><span data-stu-id="29d6a-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="29d6a-165">(Weitere Informationen zu HTTP-Verben finden Sie unter den [Methodendefinitionen](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) Artikel auf der W3C-Website.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="29d6a-166">Die meisten benutzereingabeelemente sind HTML- `<input>` Elemente.</span><span class="sxs-lookup"><span data-stu-id="29d6a-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="29d6a-167">Sie sehen, wie `<input type="type" name="name">,` , in denen *Typ* gibt die Art von Steuerelement mit Benutzereingabe werden sollen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="29d6a-168">Diese Elemente sind die gängigsten Einträge:</span><span class="sxs-lookup"><span data-stu-id="29d6a-168">These elements are the common ones:</span></span>

- <span data-ttu-id="29d6a-169">Textfeld: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="29d6a-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="29d6a-170">Aktivieren Sie das Kontrollkästchen: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="29d6a-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="29d6a-171">Optionsfeld ":". `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="29d6a-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="29d6a-172">Schaltfläche: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="29d6a-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="29d6a-173">Senden Sie Schaltfläche "": `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="29d6a-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="29d6a-174">Können Sie auch die `<textarea>` Element um ein mehrzeiliges Textfeld zu erstellen und die `<select>` Element zum Erstellen einer Dropdown-Liste oder einer bildlauffähigen Liste.</span><span class="sxs-lookup"><span data-stu-id="29d6a-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="29d6a-175">(Weitere Informationen zu HTML Elemente bilden, finden Sie unter [HTML-Formularen und Eingabe](http://www.w3schools.com/html/html_forms.asp) auf der Website W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="29d6a-176">Die `name` Attribut ist sehr wichtig, da der Name ist wie den Wert der Elements höher erhalten, wie Sie bald sehen werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="29d6a-177">Der interessante Teil befindet, wie Sie, den Seitenentwickler, die Eingabe des Benutzers verwenden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="29d6a-178">Es gibt keine integrierten Verhalten dieser Elemente.</span><span class="sxs-lookup"><span data-stu-id="29d6a-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="29d6a-179">Stattdessen müssen Sie zum Abrufen der Werte, die der Benutzer eingegeben oder ausgewählt haben und etwas mit ihnen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="29d6a-180">Das ist in diesem Tutorial lernen Sie.</span><span class="sxs-lookup"><span data-stu-id="29d6a-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="29d6a-181">**HTML5 und Eingabeformulare**</span><span class="sxs-lookup"><span data-stu-id="29d6a-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="29d6a-182">Wie Sie wissen vielleicht, wird HTML befindet sich im Übergangsstadium, und die neueste Version (HTML5) bietet Unterstützung für intuitiver Möglichkeiten für Benutzer Informationen eingeben.</span><span class="sxs-lookup"><span data-stu-id="29d6a-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="29d6a-183">HTML5 können Sie (der Seitenentwickler) z. B. der Seite erkennen, dass der Benutzer ein Datum eingeben soll.</span><span class="sxs-lookup"><span data-stu-id="29d6a-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="29d6a-184">Der Browser können Sie automatisch einen Kalender, statt den Benutzer manuell eingeben, ein Datum anzeigen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="29d6a-185">Allerdings wird HTML5 ist neu und wird noch nicht unterstützt in allen Browsern.</span><span class="sxs-lookup"><span data-stu-id="29d6a-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="29d6a-186">ASP.NET Web Pages unterstützt HTML5 eingeben, wenn der Browser des Benutzers ist.</span><span class="sxs-lookup"><span data-stu-id="29d6a-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="29d6a-187">Für einen Überblick über die neuen Attribute für die `<input>` Element HTML5 finden Sie unter [HTML &lt;Eingabe&gt; type-Attribut](http://www.w3schools.com/html/html_form_input_types.asp) auf der Website W3Schools.</span><span class="sxs-lookup"><span data-stu-id="29d6a-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="29d6a-188">Erstellen des Formulars</span><span class="sxs-lookup"><span data-stu-id="29d6a-188">Creating the Form</span></span>

<span data-ttu-id="29d6a-189">In WebMatrix in die **Dateien** öffnen Sie im Arbeitsbereich, den *Movies.cshtml* Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="29d6a-190">Nach dem schließenden `</h1>` Tag und vor dem öffnenden `<div>` Tag die `grid.GetHtml` aufzurufen, fügen Sie das folgende Markup hinzu:</span><span class="sxs-lookup"><span data-stu-id="29d6a-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="29d6a-191">Dieses Markup erstellt ein Formular, das ein mit dem Namen Textfeld `searchGenre` sowie eine Senden-Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="29d6a-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="29d6a-192">Der Text ein, und übermitteln Sie die Schaltfläche in eingeschlossen sind ein `<form>` Element, dessen `method` -Attributsatz auf `get`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="29d6a-193">(Beachten Sie, dass, wenn Sie nicht im Textfeld platzieren und Schaltfläche in "Senden" eine `<form>` Element nichts wird gesendet werden, wenn Sie die Schaltfläche klicken.) Sie verwenden die `GET` Verb hier, da Sie ein Formular erstellen, das macht keine Änderungen auf dem Server – er einfach eine Suche ergibt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="29d6a-194">(Im vorherigen Tutorial haben Sie verwendet eine `post` -Methode, die ist, wie Sie Änderungen an den Server übermitteln.</span><span class="sxs-lookup"><span data-stu-id="29d6a-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="29d6a-195">Sie müssen, die im nächsten Tutorial erneut sehen.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="29d6a-196">Führen Sie die Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-196">Run the page.</span></span> <span data-ttu-id="29d6a-197">Obwohl Sie jedes Verhalten für das Formular definiert haben, können Sie sehen, wie es aussieht:</span><span class="sxs-lookup"><span data-stu-id="29d6a-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Seite "Movies", mit dem Suchfeld für "Genre"](form-basics/_static/image3.png)

<span data-ttu-id="29d6a-199">Geben Sie einen Wert in das Textfeld ein, z. B. "Komödie."</span><span class="sxs-lookup"><span data-stu-id="29d6a-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="29d6a-200">Klicken Sie dann auf **Search "Genre"**.</span><span class="sxs-lookup"><span data-stu-id="29d6a-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="29d6a-201">Notieren Sie sich die URL der Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-201">Take note of the URL of the page.</span></span> <span data-ttu-id="29d6a-202">Da Sie festlegen, die `<form>` des Elements `method` Attribut `get`, eingegebene Wert ist jetzt Teil der Abfragezeichenfolge in der URL wie folgt:</span><span class="sxs-lookup"><span data-stu-id="29d6a-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="29d6a-203">Formularwerte lesen</span><span class="sxs-lookup"><span data-stu-id="29d6a-203">Reading Form Values</span></span>

<span data-ttu-id="29d6a-204">Diese Seite enthält bereits Code, der die Datenbankdaten abruft und die Ergebnisse in einem Raster angezeigt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="29d6a-205">Sie haben jetzt fügen Sie Code hinzu, der den Wert des Textfelds liest, sodass Sie eine SQL-Abfrage ausführen können, die den gesuchten Begriff enthält.</span><span class="sxs-lookup"><span data-stu-id="29d6a-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="29d6a-206">Da Sie die-Methode des Formulars, um festlegen `get`, Sie können den Wert, der in das Textfeld eingegeben wurde, mithilfe von Code wie folgt lesen:</span><span class="sxs-lookup"><span data-stu-id="29d6a-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="29d6a-207">Die `Request.QueryString` Objekt (das `QueryString` Eigenschaft der `Request` Objekt) enthält die Werte der Elemente, die als Teil des gesendet wurden die `GET` Vorgang.</span><span class="sxs-lookup"><span data-stu-id="29d6a-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="29d6a-208">Die `Request.QueryString` Eigenschaft enthält eine *Auflistung* (Liste) der Werte, die in das Formular übermittelt werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="29d6a-209">Um einen einzelnen Wert zu erhalten, geben Sie den Namen des Elements, das Sie möchten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="29d6a-210">Daher enthalten sind ein `name` -Attribut für die `<input>` Element (`searchTerm`), erstellt das Textfeld ein.</span><span class="sxs-lookup"><span data-stu-id="29d6a-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="29d6a-211">(Weitere Informationen zu den `Request` Objekt, finden Sie unter der [Randleiste](#BKMK_TheRequestObject) weiter unten.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="29d6a-212">Es ist einfach genug, um den Wert des Textfelds zu lesen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="29d6a-213">Aber wenn der Benutzer nicht eingeben, was überhaupt in das Textfeld geklickt **Suche** jedenfalls können ignoriert werden, klicken Sie auf, da es nichts gibt zu suchen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="29d6a-214">Der folgende Code ist ein Beispiel, das zeigt, wie Sie diese Bedingungen zu implementieren.</span><span class="sxs-lookup"><span data-stu-id="29d6a-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="29d6a-215">(Sie müssen diesen Code hinzufügen, die noch keine; Sie hierfür das weiter unten.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="29d6a-216">Auf diese Weise wird der Test:</span><span class="sxs-lookup"><span data-stu-id="29d6a-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="29d6a-217">Ruft den Wert der `Request.QueryString["searchGenre"]`, d. h. der Wert, der in eingegeben wurde die `<input>` Element mit dem Namen `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="29d6a-218">Erfahren Sie, wenn sie mithilfe von leer ist die `IsEmpty` Methode.</span><span class="sxs-lookup"><span data-stu-id="29d6a-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="29d6a-219">Diese Methode ist die Standardmethode zum bestimmen, ob ein (z. B. ein Formularelement) einen Wert enthält.</span><span class="sxs-lookup"><span data-stu-id="29d6a-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="29d6a-220">Aber wirklich nur, wenn sie gewünschte *nicht* leer, daher...</span><span class="sxs-lookup"><span data-stu-id="29d6a-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="29d6a-221">Hinzufügen der `!` Operator vor dem `IsEmpty` testen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="29d6a-222">(Die `!` Operator bedeutet Logisches NOT).</span><span class="sxs-lookup"><span data-stu-id="29d6a-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="29d6a-223">In einfachen Worten, die gesamte `if` Bedingung in den folgenden übersetzt: *ist SearchGenre-Element des Formulars nicht leer ist, klicken Sie dann...*</span><span class="sxs-lookup"><span data-stu-id="29d6a-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="29d6a-224">Dieser Block legt fest, die als Grundlage für das Erstellen einer Abfrage, die den gesuchten Begriff verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="29d6a-225">Haben Sie Gelegenheit, die im nächsten Abschnitt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="29d6a-226">**Das Request-Objekt**</span><span class="sxs-lookup"><span data-stu-id="29d6a-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="29d6a-227">Die `Request` Objekt enthält alle Informationen, die der Browser zu Ihrer Anwendung sendet, wenn eine Seite angefordert oder abgesendet wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="29d6a-228">Dieses Objekt enthält alle Informationen, die der Benutzer gibt, wie z. B. Textfeldwerte oder eine Datei zum Hochladen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="29d6a-229">Es enthält auch alle möglichen zusätzlichen Informationen, wie Cookies, Werte in der URL-Abfragezeichenfolge (sofern vorhanden), den Dateipfad der Seite, die den Typ des Browsers, die der Benutzer verwendet wird, die Liste der Sprachen ausgeführt wird, die im Browser festgelegt werden , und vieles mehr.</span><span class="sxs-lookup"><span data-stu-id="29d6a-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="29d6a-230">Die `Request` Objekt ist ein *Auflistung* (Liste) von Werten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="29d6a-231">Sie erhalten einen einzelnen Wert aus der Auflistung durch Angabe seines Namens:</span><span class="sxs-lookup"><span data-stu-id="29d6a-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="29d6a-232">Die `Request` Objekt tatsächlich stellt verschiedene Teilmengen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="29d6a-233">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="29d6a-233">For example:</span></span>
> 
> - <span data-ttu-id="29d6a-234">`Request.Form` enthält Werte von Elementen innerhalb der übermittelten `<form>` Element, wenn die Anforderung ist ein `POST` Anforderung.</span><span class="sxs-lookup"><span data-stu-id="29d6a-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="29d6a-235">`Request.QueryString` erhalten Sie nur die Werte in der URL-Abfragezeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="29d6a-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="29d6a-236">(In einer URL wie `http://mysite/myapp/page?searchGenre=action&page=2`, `?searchGenre=action&page=2` Teil der URL ist die Abfragezeichenfolge.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="29d6a-237">`Request.Cookies` Sammlung erhalten Sie Zugriff auf Cookies, die der Browser gesendet hat.</span><span class="sxs-lookup"><span data-stu-id="29d6a-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="29d6a-238">Einen Wert abgerufen, die Sie kennen das übermittelte Formular wird, können Sie mit `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="29d6a-239">Alternativ können Sie die spezifischeren Versionen `Request.Form["name"]` (für `POST` Anforderungen) oder `Request.QueryString["name"]` (für `GET` Anforderungen).</span><span class="sxs-lookup"><span data-stu-id="29d6a-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="29d6a-240">Natürlich *Namen* ist der Name des abzurufenden Elements.</span><span class="sxs-lookup"><span data-stu-id="29d6a-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="29d6a-241">Der Name des Elements, die Sie abrufen möchten muss innerhalb der Auflistung eindeutig sein, die Sie verwenden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="29d6a-242">Deshalb die `Request` Objekt enthält die Teilmengen wie `Request.Form` und `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="29d6a-243">Nehmen wir an, dass Ihre Seite ein Formularelement mit dem Namen enthält `userName` und *auch* enthält ein Cookie mit dem Namen `userName`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="29d6a-244">Wenn man `Request["userName"]`, es ist nicht eindeutig, ob der Formularwert oder das Cookie soll.</span><span class="sxs-lookup"><span data-stu-id="29d6a-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="29d6a-245">Aber wenn man `Request.Form["userName"]` oder `Request.Cookie["userName"]`, Sie sind explizit der Wert abgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="29d6a-246">Es hat sich bewährt, machen Sie genaue Angaben, und verwenden die Teilmenge der `Request` , dass Sie, wie interessieren `Request.Form` oder `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="29d6a-247">Für die einfache Seiten, die Sie in diesem Tutorial erstellen, erleichtern nicht wahrscheinlich wirklich einen Unterschied.</span><span class="sxs-lookup"><span data-stu-id="29d6a-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="29d6a-248">Beim Erstellen komplexere Seiten verwenden jedoch die explizite Version `Request.Form` oder `Request.QueryString` ermöglicht Ihnen, Probleme zu vermeiden, die auftreten können, wenn die Seite enthält, ein Formular (oder mehrere Formulare), Cookies, Abfragezeichenfolgen-Werte und So weiter.</span><span class="sxs-lookup"><span data-stu-id="29d6a-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="29d6a-249">Erstellen einer Abfrage mithilfe eines Suchbegriffs</span><span class="sxs-lookup"><span data-stu-id="29d6a-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="29d6a-250">Nun, dass Sie wissen, wie den Suchbegriff abgerufen, den vom Benutzer eingegebene, können Sie eine Abfrage erstellen, die verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="29d6a-251">Denken Sie daran, dass die um alle Elemente der Film aus der Datenbank zu erhalten, Sie eine SQL-Abfrage verwenden, die diese Anweisung ähnelt:</span><span class="sxs-lookup"><span data-stu-id="29d6a-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="29d6a-252">Um nur bestimmte Filme zu erhalten, müssen Sie eine Abfrage zu verwenden, enthält eine `Where` Klausel.</span><span class="sxs-lookup"><span data-stu-id="29d6a-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="29d6a-253">Diese Klausel können Sie eine Bedingung festlegen, auf der von der Abfrage Zeilen zurückgegeben werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="29d6a-254">Im Folgenden ein Beispiel:</span><span class="sxs-lookup"><span data-stu-id="29d6a-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="29d6a-255">Das Standardformat lautet `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="29d6a-256">Andere Operatoren können neben nur `=`, z. B. `>` (größer als), `<` (kleiner als), `<>` (ungleich), `<=` (kleiner als oder gleich), usw., je nachdem, was Sie suchen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="29d6a-257">Falls Sie sich wundern, SQL-Anweisungen sind keine Groß-/ Kleinschreibung &mdash; `SELECT` ist identisch mit `Select` (oder sogar `select`).</span><span class="sxs-lookup"><span data-stu-id="29d6a-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="29d6a-258">Allerdings Personen häufig profitieren Schlüsselwörter in einer SQL-Anweisung, wie z. B. `SELECT` und `WHERE`, um ihn leichter lesbar zu machen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="29d6a-259">Den Suchbegriff übergeben als Parameter.</span><span class="sxs-lookup"><span data-stu-id="29d6a-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="29d6a-260">Suchen nach einem bestimmten Genre recht einfach ist (`WHERE Genre = 'Action'`), aber Sie möchten in der Lage, die alle Genres suchen, die der Benutzer eingibt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="29d6a-261">Zu diesem Zweck erstellen Sie als SQL-Abfrage, die enthält einen Platzhalter für den Wert zu suchen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="29d6a-262">Es sieht so aus wie mit diesem Befehl:</span><span class="sxs-lookup"><span data-stu-id="29d6a-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="29d6a-263">Der Platzhalter die `@` Zeichens, gefolgt von 0 (null).</span><span class="sxs-lookup"><span data-stu-id="29d6a-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="29d6a-264">Wie Sie sich denken können, eine Abfrage kann mehrere Platzhalter enthalten, und sie den Namen `@0`, `@1`, `@2`usw.</span><span class="sxs-lookup"><span data-stu-id="29d6a-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="29d6a-265">Informationen zum Einrichten von der Abfrage ein, und übergeben Sie sie tatsächlich den Wert, verwenden Sie den Code wie folgt aus:</span><span class="sxs-lookup"><span data-stu-id="29d6a-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="29d6a-266">Dieser Code ist ähnlich, was Sie bereits durchgeführt haben, um Daten im Raster anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="29d6a-267">Der einzige Unterschied besteht:</span><span class="sxs-lookup"><span data-stu-id="29d6a-267">The only differences are:</span></span>

- <span data-ttu-id="29d6a-268">Die Abfrage enthält einen Platzhalter (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="29d6a-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="29d6a-269">Die Abfrage wird in einer Variablen eingefügt (`selectCommand`), bevor Sie die Abfrage direkt zu übergeben, die `db.Query` Methode.</span><span class="sxs-lookup"><span data-stu-id="29d6a-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="29d6a-270">Beim Aufrufen der `db.Query` -Methode, übergeben Sie die Abfrage und der Wert, der für den Platzhalter verwenden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="29d6a-271">(Wenn die Abfrage mehrere Platzhalter enthält, übergeben Sie sie als unterschiedliche Werte für die Methode.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="29d6a-272">Wenn Sie all diese Elemente zusammengestellt, erhalten Sie den folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="29d6a-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="29d6a-273">**Wichtig**</span><span class="sxs-lookup"><span data-stu-id="29d6a-273">**Important!**</span></span> <span data-ttu-id="29d6a-274">Mithilfe von Platzhaltern (z. B. `@0`) Werte an einen SQL-Befehl übergeben wird *äußerst wichtig* für die Sicherheit.</span><span class="sxs-lookup"><span data-stu-id="29d6a-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="29d6a-275">Die Möglichkeit, die Sie es hier mit Platzhaltern für Variable Daten sehen, ist die einzige Möglichkeit, die Sie die SQL-Befehlen erstellen soll.</span><span class="sxs-lookup"><span data-stu-id="29d6a-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="29d6a-276">Erstellen Sie eine SQL-Anweisung nie durch das Erstellen (verketten) Literaltext und Werte, die Sie vom Benutzer zu erhalten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="29d6a-277">Verkettung von Benutzereingaben in einer SQL-Anweisung öffnet die Website eine *SQL Injection-Angriff* , in denen ein böswilliger Benutzer Werte auf der Seite, die Ihre Datenbank hack übermittelt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="29d6a-278">(Erfahren Sie mehr in diesem Artikel [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) der MSDN-Website.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="29d6a-279">Aktualisieren die Seite "Movies" mit Code durchsuchen</span><span class="sxs-lookup"><span data-stu-id="29d6a-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="29d6a-280">Nachdem Sie den Code aktualisieren, können die *Movies.cshtml* Datei.</span><span class="sxs-lookup"><span data-stu-id="29d6a-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="29d6a-281">Ersetzen Sie zunächst den Code im Codeblock am oberen Rand der Seite durch den folgenden Code ein:</span><span class="sxs-lookup"><span data-stu-id="29d6a-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="29d6a-282">Der Unterschied besteht darin, dass Sie die Abfrage in erstellt haben die `selectCommand` Variable, die Sie übergeben `db.Query` später noch mal.</span><span class="sxs-lookup"><span data-stu-id="29d6a-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="29d6a-283">Platzieren die SQL-Anweisung in einer Variablen können Sie die Anweisung ändern, was Sie tun müssen, um die Suche durchgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="29d6a-284">Sie haben auch diese beiden Zeilen und entfernt, die Sie später wieder ein werden:</span><span class="sxs-lookup"><span data-stu-id="29d6a-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="29d6a-285">Sie nicht möchten, führen Sie die Abfrage noch (d. h. Aufrufen `db.Query`) und nicht initialisiert werden soll die `WebGrid` Helper noch eine.</span><span class="sxs-lookup"><span data-stu-id="29d6a-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="29d6a-286">Sie müssen diese Dinge tun, nachdem Sie ermittelt haben, das die SQL-Anweisung werden ausgeführt muss.</span><span class="sxs-lookup"><span data-stu-id="29d6a-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="29d6a-287">Nachdem dieses Blocks umgeschriebene können Sie die neue Logik zur Behandlung von der Suche hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="29d6a-288">Der fertige Code sieht wie folgt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-288">The completed code will look like the following.</span></span> <span data-ttu-id="29d6a-289">Aktualisieren Sie den Code auf der Seite, damit sie in diesem Beispiel entspricht:</span><span class="sxs-lookup"><span data-stu-id="29d6a-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="29d6a-290">Die Seite funktioniert jetzt wie folgt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-290">The page now works like this.</span></span> <span data-ttu-id="29d6a-291">Jedes Mal, wenn die Seite ausgeführt wird, wird der Code öffnet die Datenbank und die `selectCommand` Variable wird festgelegt, auf die SQL-Anweisung, die alle Datensätze aus erhält die `Movies` Tabelle.</span><span class="sxs-lookup"><span data-stu-id="29d6a-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="29d6a-292">Der Code initialisiert, außerdem die `searchTerm` Variable.</span><span class="sxs-lookup"><span data-stu-id="29d6a-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="29d6a-293">Jedoch die aktuelle Anforderung enthält einen Wert für die `searchGenre` -Element, legt der Code `selectCommand` auf eine andere Abfrage – nämlich, beinhaltet die `Where` -Klausel, um ein Genre gesucht.</span><span class="sxs-lookup"><span data-stu-id="29d6a-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="29d6a-294">Außerdem wird `searchTerm` , was für das Suchfeld übergeben wurde (die "nothing" sein kann).</span><span class="sxs-lookup"><span data-stu-id="29d6a-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="29d6a-295">Unabhängig davon, welche SQL-Anweisung liegt im `selectCommand`, der Code ruft dann `db.Query` zum Ausführen der Abfrage, und übergeben sie den befindet sich in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="29d6a-296">Wenn es keine Werte `searchTerm`, es spielt keine Rolle, da in diesem Fall gibt es keinen Parameter, der den Wert zu übergeben, ist `selectCommand` trotzdem.</span><span class="sxs-lookup"><span data-stu-id="29d6a-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="29d6a-297">Der Code schließlich initialisiert der `WebGrid` Hilfe mit den Abfrageergebnissen wie.</span><span class="sxs-lookup"><span data-stu-id="29d6a-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="29d6a-298">Sie können sehen, indem Sie die SQL-Anweisung einfügen und den Suchbegriff in Variablen, haben Sie Flexibilität auf den Code hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="29d6a-299">Wie Sie später in diesem Lernprogramm sehen werden, können Sie dieses grundlegende Framework und behalten Sie die Logik für die verschiedenen Typen von Suchvorgängen hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="29d6a-300">Testen die Suche von "Genre"-Funktion</span><span class="sxs-lookup"><span data-stu-id="29d6a-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="29d6a-301">Führen Sie in WebMatrix die *Movies.cshtml* Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="29d6a-302">Die Seite mit dem Textfeld für "Genre" angezeigt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="29d6a-303">Geben Sie ein Genre, die für eine Ihrer Test-Datensätze eingegeben haben, und klicken Sie dann **Suche**.</span><span class="sxs-lookup"><span data-stu-id="29d6a-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="29d6a-304">Dieses Mal sehen Sie eine Liste der einfach Filme, die dieses Genres entsprechen:</span><span class="sxs-lookup"><span data-stu-id="29d6a-304">This time you see a listing of just the movies that match that genre:</span></span>

![Seite "Movies" nach der Suche nach Genre "Comedies" auflisten](form-basics/_static/image4.png)

<span data-ttu-id="29d6a-306">Geben Sie einen anderen "Genre" und die Suche wiederholen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-306">Enter a different genre and search again.</span></span> <span data-ttu-id="29d6a-307">Versuchen Sie, das Genre mithilfe von allen klein- oder Großbuchstaben Buchstaben, damit Sie sehen, dass es sich bei der Suche nicht Groß-/Kleinschreibung beachtet werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="29d6a-308">"Speichern" Eingabe des Benutzers</span><span class="sxs-lookup"><span data-stu-id="29d6a-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="29d6a-309">Sie haben vielleicht bemerkt, die nach dem Sie ein Genre eingegeben, und klicken auf **Search "Genre"**, Sie haben gesehen, eine Liste für dieses Genres.</span><span class="sxs-lookup"><span data-stu-id="29d6a-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="29d6a-310">Das Suchfeld ein war jedoch leer &mdash; also nicht merken, was Sie eingegeben haben.</span><span class="sxs-lookup"><span data-stu-id="29d6a-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="29d6a-311">Es ist wichtig zu verstehen, warum dieses Verhalten tritt auf.</span><span class="sxs-lookup"><span data-stu-id="29d6a-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="29d6a-312">Wenn Sie eine Seite senden, sendet der Browser eine Anforderung an den Webserver.</span><span class="sxs-lookup"><span data-stu-id="29d6a-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="29d6a-313">Wenn ASP.NET die Anforderung erhält, erstellt eine völlig neue Instanz der Seite, führt den Code in der sie und rendert dann die Seite an den Browser erneut.</span><span class="sxs-lookup"><span data-stu-id="29d6a-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="29d6a-314">Aktiviert ist, wissen nicht jedoch die Seite, dass Sie nur mit einer früheren Version von sich selbst arbeiteten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="29d6a-315">Alle bekannt ist, dass die It eine Anforderung erhalten wird, die einige Formulardaten darin.</span><span class="sxs-lookup"><span data-stu-id="29d6a-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="29d6a-316">Jedes Mal, wenn Sie eine Seite anfordern &mdash; angibt, ob zum ersten Mal, oder Senden des Updategrams &mdash; bekommen Sie eine neue Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="29d6a-317">Der Webserver verfügt über kein Speicher Ihrer letzten Anforderung.</span><span class="sxs-lookup"><span data-stu-id="29d6a-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="29d6a-318">Wird weder auf ASP.NET und ist weder des Browsers.</span><span class="sxs-lookup"><span data-stu-id="29d6a-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="29d6a-319">Die einzige Verbindung zwischen diesen separaten Instanzen der Seite ist, alle Daten, die Sie zwischen diesen übertragen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="29d6a-320">Wenn Sie eine Seite senden, kann die neue Seiteninstanz z. B. Daten aus dem Formular abrufen, die von der früheren Instanz gesendet wurde.</span><span class="sxs-lookup"><span data-stu-id="29d6a-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="29d6a-321">(Eine weitere Möglichkeit zur Datenübergabe zwischen Seiten ist Cookies verwendet.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="29d6a-322">Eine formale zum Beschreiben dieser Situation besteht darin, sagen, dass Webseiten sind *zustandslosen*.</span><span class="sxs-lookup"><span data-stu-id="29d6a-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="29d6a-323">Webserver und den Seiten selbst und die Elemente auf der Seite beibehalten keine Informationen zu den vorherigen Zustand einer Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="29d6a-324">Das Web wurde so entwickelt, da das Beibehalten des Zustands für einzelne Anforderungen schnell erschöpfen würde die Ressourcen der Webserver, die häufig Tausende, vielleicht verarbeiten sogar Hunderte von Tausenden von Anforderungen pro Sekunde.</span><span class="sxs-lookup"><span data-stu-id="29d6a-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="29d6a-325">Warum ist das Textfeld leer war.</span><span class="sxs-lookup"><span data-stu-id="29d6a-325">So that's why the text box was empty.</span></span> <span data-ttu-id="29d6a-326">Nachdem Sie die Seite übermittelt, wird von ASP.NET erstellt eine neue Instanz der Seite und über Code und Markup ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="29d6a-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="29d6a-327">Gab es keine anderen in diesem Code, der sagte ASP.NET, einen Wert in das Textfeld platzieren.</span><span class="sxs-lookup"><span data-stu-id="29d6a-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="29d6a-328">Also ASP.NET nicht nichts ein, und das Textfeld wurde ohne einen Wert darin gerendert.</span><span class="sxs-lookup"><span data-stu-id="29d6a-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="29d6a-329">Es ist tatsächlich eine einfache Möglichkeit zum Umgehen dieses Problems.</span><span class="sxs-lookup"><span data-stu-id="29d6a-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="29d6a-330">Das Genre, die Sie in das Textfeld eingegeben *ist* im Code für Sie verfügbaren &mdash; befindet sich im `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="29d6a-331">Aktualisieren Sie das Markup für das Textfeld ein, damit die `value` Attribut Ruft den Wert von `searchTerm`, wie im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="29d6a-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="29d6a-332">Auf dieser Seite, Sie haben auch Festlegen der `value` -Attribut auf die `searchTerm` Variable, da diese Variable auch das Genre enthält Sie eingegeben haben.</span><span class="sxs-lookup"><span data-stu-id="29d6a-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="29d6a-333">Während mit den `Request` Objekt zum Festlegen der `value` Attribut wie gezeigt, hier ist das Standardverfahren zum Ausführen dieser Aufgabe.</span><span class="sxs-lookup"><span data-stu-id="29d6a-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="29d6a-334">(Vorausgesetzt, Sie möchten auch dazu &mdash; in einigen Fällen sollten Sie zum Rendern der Seite *ohne* Werte in den Feldern.</span><span class="sxs-lookup"><span data-stu-id="29d6a-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="29d6a-335">Es hängt alles was mit Ihrer Anwendung vor sich geht.)</span><span class="sxs-lookup"><span data-stu-id="29d6a-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="29d6a-336">Sie können nicht "den Wert eines Textfelds, die verwendet wird, für die Kennwörter merken".</span><span class="sxs-lookup"><span data-stu-id="29d6a-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="29d6a-337">Es wäre eine Sicherheitslücke Personen mithilfe von Code in ein Kennwortfeld zu füllen können.</span><span class="sxs-lookup"><span data-stu-id="29d6a-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="29d6a-338">Führen Sie die Seite erneut aus, geben Sie ein Genre, und klicken Sie auf **Search "Genre"**.</span><span class="sxs-lookup"><span data-stu-id="29d6a-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="29d6a-339">Dieses Mal nicht nur sehen Sie die Ergebnisse der Suche, aber das Textfeld gespeichert, was letzten Besuch eingegeben haben:</span><span class="sxs-lookup"><span data-stu-id="29d6a-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Seite mit, dass das Textfeld "des vorherigen Eintrags gespeichert hat"](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="29d6a-341">Suchen nach einem beliebigen Wort im Titel</span><span class="sxs-lookup"><span data-stu-id="29d6a-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="29d6a-342">Sie können jetzt für alle "Genre" suchen, aber Sie können auch einen Titel suchen möchten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="29d6a-343">Es ist schwierig, einen Titel, ganz genau zu erhalten, bei der Suche stattdessen Sie nach einem Wort suchen können, die einen Titel an einer beliebigen Stelle angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="29d6a-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="29d6a-344">Zu diesem Zweck in SQL, die Sie verwenden die `LIKE` Operator und die Syntax wie folgt aus:</span><span class="sxs-lookup"><span data-stu-id="29d6a-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="29d6a-345">Dieser Befehl ruft alle Filme, deren Namen "Adventure" enthalten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="29d6a-346">Bei Verwendung der `LIKE` -Operator, enthalten Sie das Platzhalterzeichen `%` als Teil des Suchbegriffs.</span><span class="sxs-lookup"><span data-stu-id="29d6a-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="29d6a-347">Die Suche `LIKE 'adventure%'` bedeutet "beginnend mit 'Adventure'".</span><span class="sxs-lookup"><span data-stu-id="29d6a-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="29d6a-348">(Technisch gesehen bedeutet dies "Die Zeichenfolge 'Adventure' gefolgt von etwas".) Auf ähnliche Weise den gesuchten Begriff `LIKE '%adventure'` bedeutet "Alles gefolgt von der Zeichenfolge 'Adventure'", d.h., dass eine andere Möglichkeit, sagen Sie "bis hin zu 'Adventure'".</span><span class="sxs-lookup"><span data-stu-id="29d6a-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="29d6a-349">Der Suchbegriff `LIKE '%adventure%'` bedeutet daher "mit"Adventure' eine beliebige Stelle im Titel."</span><span class="sxs-lookup"><span data-stu-id="29d6a-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="29d6a-350">(Technisch gesehen "alles im Titel, gefolgt von 'Adventure' ist, gefolgt von der nichts.")</span><span class="sxs-lookup"><span data-stu-id="29d6a-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="29d6a-351">In der `<form>` -Element, fügen Sie das folgende Markup direkt unter dem schließenden `</div>` Tag für die Suche für "Genre" (direkt vor dem abschließenden `</form>` Element):</span><span class="sxs-lookup"><span data-stu-id="29d6a-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="29d6a-352">Der Code zum Behandeln dieser Suche ist ähnelt dem Code für die Suche für "Genre", mit dem Unterschied, dass Sie Zusammenstellen der `LIKE` suchen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="29d6a-353">Fügen Sie den Codeblock am oberen Rand der Seite, diese `if` blockieren direkt nach der `if` Block für die Suche für "Genre":</span><span class="sxs-lookup"><span data-stu-id="29d6a-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="29d6a-354">Dieser Code verwendet die gleiche Logik, die Sie zuvor gesehen haben, mit dem Unterschied, dass die Suche verwendet einen `LIKE` Operator und der Code setzt "`%`" vor und nach dem Suchbegriff.</span><span class="sxs-lookup"><span data-stu-id="29d6a-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="29d6a-355">Beachten Sie, wie sie mühelos eine neue Suche auf der Seite hinzugefügt wurde.</span><span class="sxs-lookup"><span data-stu-id="29d6a-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="29d6a-356">Alles, was Sie tun musste wurde:</span><span class="sxs-lookup"><span data-stu-id="29d6a-356">All you had to do was:</span></span>

- <span data-ttu-id="29d6a-357">Erstellen Sie eine `if` blockieren, die getestet werden, um festzustellen, ob das relevante Suchfeld einen Wert verfügte.</span><span class="sxs-lookup"><span data-stu-id="29d6a-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="29d6a-358">Legen Sie die `selectCommand` -Variable an eine neue SQL-Anweisung.</span><span class="sxs-lookup"><span data-stu-id="29d6a-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="29d6a-359">Legen Sie die `searchTerm` Variable auf den Wert für die Abfrage übergeben.</span><span class="sxs-lookup"><span data-stu-id="29d6a-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="29d6a-360">Hier ist der vollständige Code-Block, enthält die Logik die neue für eine Suche nach Softwaretiteln:</span><span class="sxs-lookup"><span data-stu-id="29d6a-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="29d6a-361">Hier ist eine Zusammenfassung der Funktionsweise dieses Codes:</span><span class="sxs-lookup"><span data-stu-id="29d6a-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="29d6a-362">Die Variablen `searchTerm` und `selectCommand` oben initialisiert werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="29d6a-363">Diese Variablen mit dem entsprechenden Suchbegriff (sofern vorhanden) festgelegt werden soll, und entsprechende SQL-Befehl basierend auf der Funktionsweise des Benutzers auf der Seite.</span><span class="sxs-lookup"><span data-stu-id="29d6a-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="29d6a-364">Die Standardsuche ist ein einfacher Vorgang alle Filme aus der Datenbank abrufen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="29d6a-365">In den Tests für `searchGenre` und `searchTitle`, legt der Code `searchTerm` auf den Wert, der Sie suchen möchten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="29d6a-366">Außerdem legen Sie diese Codeblöcke `selectCommand` auf einen entsprechenden SQL‑Befehl für die Suche.</span><span class="sxs-lookup"><span data-stu-id="29d6a-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="29d6a-367">Die `db.Query` Methode wird aufgerufen, nur einmal verwendet den SQL-Befehl in `selectedCommand` und einen beliebigen Wert in `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="29d6a-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="29d6a-368">Es ist kein Suchbegriff (keine "Genre" und kein Titel Wort) den Wert der `searchTerm` ist eine leere Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="29d6a-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="29d6a-369">Allerdings ist, die unerheblich, da in diesem Fall die Abfrage keine Parameter erfordert.</span><span class="sxs-lookup"><span data-stu-id="29d6a-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="29d6a-370">Testen die Suchfunktion für den Titel</span><span class="sxs-lookup"><span data-stu-id="29d6a-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="29d6a-371">Jetzt können Sie Ihre abgeschlossenen Suchseite testen.</span><span class="sxs-lookup"><span data-stu-id="29d6a-371">Now you can test your completed search page.</span></span> <span data-ttu-id="29d6a-372">Führen Sie *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="29d6a-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="29d6a-373">Geben Sie ein Genre, und klicken Sie auf **Search "Genre"**.</span><span class="sxs-lookup"><span data-stu-id="29d6a-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="29d6a-374">Das Raster zeigt Filme, die von diesem "Genre", wie vor.</span><span class="sxs-lookup"><span data-stu-id="29d6a-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="29d6a-375">Geben Sie ein Wort Titel, und klicken Sie auf **Suchtitel**.</span><span class="sxs-lookup"><span data-stu-id="29d6a-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="29d6a-376">Das Raster zeigt Filme, die das Wort im Titel haben.</span><span class="sxs-lookup"><span data-stu-id="29d6a-376">The grid displays movies that have that word in the title.</span></span>

![Seite "Movies" nach der Suche für "Der" im Titel auflisten](form-basics/_static/image6.png)

<span data-ttu-id="29d6a-378">Lassen Sie beide Felder leer, und klicken Sie auf eine Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="29d6a-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="29d6a-379">Das Raster zeigt alle Filme.</span><span class="sxs-lookup"><span data-stu-id="29d6a-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="29d6a-380">Kombinieren von Abfragen</span><span class="sxs-lookup"><span data-stu-id="29d6a-380">Combining the Queries</span></span>

<span data-ttu-id="29d6a-381">Sie werden feststellen, dass die suchen, die Sie ausführen können ausgeschlossen werden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="29d6a-382">Sie können nicht den Titel und das Genre zur gleichen Zeit, suchen, selbst wenn beide Suchfelder Werte enthalten.</span><span class="sxs-lookup"><span data-stu-id="29d6a-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="29d6a-383">Sie können z. B., können nicht für alle Aktion Filme suchen, deren Titel "Adventure" enthält.</span><span class="sxs-lookup"><span data-stu-id="29d6a-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="29d6a-384">(Wie die Seite jetzt codiert ist, wenn Sie Werte für "Genre" und Titel eingeben, ruft der Suche nach Softwaretiteln Vorrang.) Um eine Suche zu erstellen, die die Bedingungen kombiniert, müssten Sie eine SQL-Abfrage zu erstellen, die Syntax folgendermaßen:</span><span class="sxs-lookup"><span data-stu-id="29d6a-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="29d6a-385">Und Sie müssten zum Ausführen der Abfrage mit einer Anweisung wie folgt (etwa "Sprache"):</span><span class="sxs-lookup"><span data-stu-id="29d6a-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="29d6a-386">Erstellen Logik, um die Anzahl der möglichen Permutationen von Suchkriterien zulassen kann etwas beteiligt sind, abrufen, wie Sie sehen können.</span><span class="sxs-lookup"><span data-stu-id="29d6a-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="29d6a-387">Aus diesem Grund werden wir hier beenden.</span><span class="sxs-lookup"><span data-stu-id="29d6a-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="29d6a-388">Als Nächstes kommen</span><span class="sxs-lookup"><span data-stu-id="29d6a-388">Coming Up Next</span></span>

<span data-ttu-id="29d6a-389">Im nächsten Tutorial erstellen Sie eine Seite, die einem Formular verwendet wird, um Benutzer der Datenbank Filme hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="29d6a-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="29d6a-390">Vollständige Liste für Movie-Seite (mit der Suche aktualisiert)</span><span class="sxs-lookup"><span data-stu-id="29d6a-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="29d6a-391">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="29d6a-391">Additional Resources</span></span>

- [<span data-ttu-id="29d6a-392">Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax</span><span class="sxs-lookup"><span data-stu-id="29d6a-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="29d6a-393">[SQL-WHERE-Klausel](http://www.w3schools.com/sql/sql_where.asp) auf der Website W3Schools</span><span class="sxs-lookup"><span data-stu-id="29d6a-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="29d6a-394">[Methodendefinitionen](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) Artikel auf der W3C-Website</span><span class="sxs-lookup"><span data-stu-id="29d6a-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="29d6a-395">[Zurück](displaying-data.md)
> [Weiter](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="29d6a-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
