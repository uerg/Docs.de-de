---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: 'Einführung in ASP.NET Web Pages: Anzeigen von Daten | Microsoft-Dokumentation'
author: tfitzmac
description: In diesem Tutorial erfahren Sie, wie Sie eine Datenbank in WebMatrix erstellen "und" Vorgehensweise beim Anzeigen von Daten auf einer Seite, bei der Verwendung von ASP.NET Web Pages (Razor). Es wird davon ausgegangen, y...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
ms.technology: dotnet-webpages
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: de4ed9df2c65a1aaa4548b035c619cfa9bae7a8e
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 07/03/2018
ms.locfileid: "37384968"
---
<a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="890dd-104">Einführung in ASP.NET Web Pages - Anzeige von Daten</span><span class="sxs-lookup"><span data-stu-id="890dd-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>
====================
<span data-ttu-id="890dd-105">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="890dd-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="890dd-106">In diesem Tutorial erfahren Sie, wie Sie eine Datenbank in WebMatrix erstellen "und" Vorgehensweise beim Anzeigen von Daten auf einer Seite, bei der Verwendung von ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="890dd-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="890dd-107">Es wird vorausgesetzt, Sie haben die Reihe über [Einführung in die Programmierung von ASP.NET Web Pages](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="890dd-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="890dd-108">Sie lernen Folgendes:</span><span class="sxs-lookup"><span data-stu-id="890dd-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="890dd-109">So verwenden Sie WebMatrix-Tools, um Datenbanken und Tabellen zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="890dd-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="890dd-110">So verwenden Sie WebMatrix-Tools zum Hinzufügen von Daten in einer Datenbank.</span><span class="sxs-lookup"><span data-stu-id="890dd-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="890dd-111">Die Vorgehensweise beim Anzeigen von Daten aus der Datenbank auf einer Seite.</span><span class="sxs-lookup"><span data-stu-id="890dd-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="890dd-112">Informationen zum Ausführen von SQL-Befehlen in ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="890dd-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="890dd-113">Gewusst wie: Anpassen der `WebGrid` Hilfsmethode, die die Datenanzeige ändern und Hinzufügen von Paginierung und Sortierung.</span><span class="sxs-lookup"><span data-stu-id="890dd-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="890dd-114">Features/Technologien erläutert:</span><span class="sxs-lookup"><span data-stu-id="890dd-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="890dd-115">WebMatrix-Datenbanktools.</span><span class="sxs-lookup"><span data-stu-id="890dd-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="890dd-116">`WebGrid` -Hilfsprogramm.</span><span class="sxs-lookup"><span data-stu-id="890dd-116">`WebGrid` helper.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="890dd-117">Sie lernen Folgendes</span><span class="sxs-lookup"><span data-stu-id="890dd-117">What You'll Build</span></span>

<span data-ttu-id="890dd-118">Im vorherigen Tutorial haben Sie für ASP.NET Web Pages eingeführt wurden (*.cshtml* Dateien), auf die Grundlagen der Razor-Syntax und -Hilfsprogramme.</span><span class="sxs-lookup"><span data-stu-id="890dd-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="890dd-119">In diesem Tutorial werden Sie beginnen, erstellen die eigentliche Webanwendung, die Sie für den Rest der Reihe verwenden.</span><span class="sxs-lookup"><span data-stu-id="890dd-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="890dd-120">Die app ist eine einfache Movie-Anwendung, mit dem Sie die anzeigen, hinzufügen, ändern und Löschen von Informationen über Filme.</span><span class="sxs-lookup"><span data-stu-id="890dd-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="890dd-121">Wenn Sie mit diesem Lernprogramm fertig sind, werden Sie möglicherweise eine Movie-Liste anzeigen, die diese Seite ähnelt:</span><span class="sxs-lookup"><span data-stu-id="890dd-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![WebGrid-Anzeige, die CSS-Klassennamen festgelegten Parametern enthält.](displaying-data/_static/image1.png)

<span data-ttu-id="890dd-123">Aber damit beginnen, müssen Sie eine Datenbank zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="890dd-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="890dd-124">Eine sehr kurze Einführung in Datenbanken</span><span class="sxs-lookup"><span data-stu-id="890dd-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="890dd-125">In diesem Tutorial wird nur die briefest Einführung in Datenbanken bereit.</span><span class="sxs-lookup"><span data-stu-id="890dd-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="890dd-126">Wenn Sie Datenbank-Erfahrung haben, können Sie in diesem kurzen Abschnitt überspringen.</span><span class="sxs-lookup"><span data-stu-id="890dd-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="890dd-127">Eine Datenbank enthält eine oder mehrere Tabellen, die Informationen enthalten &mdash; z. B. Tabellen, für Kunden, Bestellungen und Anbietern oder für Schüler/Studenten, Lehrer, Klassen und Noten.</span><span class="sxs-lookup"><span data-stu-id="890dd-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="890dd-128">Strukturell gesehen ähnelt eine Datenbanktabelle ein Arbeitsblatt ein.</span><span class="sxs-lookup"><span data-stu-id="890dd-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="890dd-129">Angenommen Sie, ein typisches Adressbuch.</span><span class="sxs-lookup"><span data-stu-id="890dd-129">Imagine a typical address book.</span></span> <span data-ttu-id="890dd-130">Für jeden Eintrag im Adressbuch (d. h. für jede Person) Sie haben verschiedene Angaben wie z. B. Vorname, Nachname, Adresse, e-Mail-Adresse und Telefonnummer.</span><span class="sxs-lookup"><span data-stu-id="890dd-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Beispiel-Datenbanktabelle als ein einfaches Raster](displaying-data/_static/image2.png)

<span data-ttu-id="890dd-132">(Zeilen werden manchmal als *Datensätze*, und Spalten werden manchmal als *Felder*.)</span><span class="sxs-lookup"><span data-stu-id="890dd-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="890dd-133">Für die meisten-Datenbanktabellen enthalten sind muss die Tabelle eine Spalte verwenden, die einen eindeutigen Wert ein, z. B. eine Kundennummer, Kontonummer und So weiter enthält.</span><span class="sxs-lookup"><span data-stu-id="890dd-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="890dd-134">Dieser Wert wird als der Tabelle bezeichnet *Primärschlüssel*, und verwenden Sie zum Identifizieren jeder Zeile in der Tabelle.</span><span class="sxs-lookup"><span data-stu-id="890dd-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="890dd-135">Im Beispiel ist die ID-Spalte der Primärschlüssel für das Adressbuch, die im vorherigen Beispiel gezeigt.</span><span class="sxs-lookup"><span data-stu-id="890dd-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="890dd-136">Ein Großteil der Arbeit in Webanwendungen besteht aus Lesen von Informationen aus der Datenbank und auf einer Seite anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="890dd-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="890dd-137">Sie oft auch Informationen von Benutzern erfassen und in einer Datenbank hinzufügen oder ändern Sie Datensätze, die bereits in der Datenbank vorhanden sind.</span><span class="sxs-lookup"><span data-stu-id="890dd-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="890dd-138">(Wir alle diese Vorgänge im Verlauf dieser tutorialreihe behandelt).</span><span class="sxs-lookup"><span data-stu-id="890dd-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="890dd-139">Für Datenbanken kann überaus komplex sein und kann spezielle Kenntnisse erforderlich.</span><span class="sxs-lookup"><span data-stu-id="890dd-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="890dd-140">Für diese tutorialreihe müssen Sie jedoch nur grundlegende Konzepte, zu verstehen, die alle erläutert wird, wie Sie fortfahren.</span><span class="sxs-lookup"><span data-stu-id="890dd-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="890dd-141">Erstellen einer Datenbank</span><span class="sxs-lookup"><span data-stu-id="890dd-141">Creating a Database</span></span>

<span data-ttu-id="890dd-142">WebMatrix enthält Tools, die zum Erstellen einer Datenbank und zum Erstellen von Tabellen in der Datenbank zu erleichtern.</span><span class="sxs-lookup"><span data-stu-id="890dd-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="890dd-143">(Die Struktur einer Datenbank wird als der Datenbank bezeichnet *Schema*.) Für diese tutorialreihe, erstellen Sie eine Datenbank, die nur eine Tabelle enthält &mdash; Filme.</span><span class="sxs-lookup"><span data-stu-id="890dd-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="890dd-144">Öffnen Sie WebMatrix aus, wenn Sie dies nicht bereits getan haben, und öffnen Sie die WebPagesMovies-Website, die Sie im vorherigen Tutorial erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="890dd-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="890dd-145">Klicken Sie im linken Bereich auf die **Datenbank** Arbeitsbereich.</span><span class="sxs-lookup"><span data-stu-id="890dd-145">In the left pane, click the **Database** workspace.</span></span>

![Registerkarte "WebMatrix-Datenbank-Arbeitsbereich"](displaying-data/_static/image3.png)

<span data-ttu-id="890dd-147">Die Menüband-Änderungen Aufgaben im Zusammenhang mit der Datenbank angezeigt werden soll.</span><span class="sxs-lookup"><span data-stu-id="890dd-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="890dd-148">Klicken Sie im Menüband auf **neue Datenbank**.</span><span class="sxs-lookup"><span data-stu-id="890dd-148">In the ribbon, click **New Database**.</span></span>

![Schaltfläche "Neue Datenbank" WebMatrix-Menüband](displaying-data/_static/image4.png)

<span data-ttu-id="890dd-150">WebMatrix erstellt eine SQL Server CE-Datenbank (eine *.sdf* Datei), die den gleichen Namen hat, als Standort für die &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="890dd-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="890dd-151">(Sie wird nicht dies hier tun, jedoch können Sie die Datei auf beliebigen, umbenennen, solange sie verfügt über eine *.sdf* Erweiterung.)</span><span class="sxs-lookup"><span data-stu-id="890dd-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="890dd-152">Erstellen einer Tabelle</span><span class="sxs-lookup"><span data-stu-id="890dd-152">Creating a Table</span></span>

<span data-ttu-id="890dd-153">Klicken Sie im Menüband auf **neue Tabelle**.</span><span class="sxs-lookup"><span data-stu-id="890dd-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="890dd-154">WebMatrix wird den Tabellen-Designer in einer neuen Registerkarte geöffnet. (Wenn die neue Tabellenoption nicht verfügbar ist, stellen Sie sicher, dass die neue Datenbank in der Strukturansicht auf der linken Seite ausgewählt ist.)</span><span class="sxs-lookup"><span data-stu-id="890dd-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

![Schaltfläche "Neue Tabelle" WebMatrix-Menüband](displaying-data/_static/image5.png)

<span data-ttu-id="890dd-156">Geben Sie in das Textfeld am oberen (, in dem das Wasserzeichen für "Name der Tabelle eingeben" heißt), "Movies" ein.</span><span class="sxs-lookup"><span data-stu-id="890dd-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Einen Tabellennamen einzugeben, in der Datenbank-Designer von WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="890dd-158">Der Bereich unterhalb der Tabellenname ist, in dem Sie die einzelnen Spalten definieren.</span><span class="sxs-lookup"><span data-stu-id="890dd-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="890dd-159">Für die *Filme* Tabelle in diesem Tutorial erstellen Sie nur wenige Spalten: *ID*, *Titel*, *"Genre"*, und *Jahr*.</span><span class="sxs-lookup"><span data-stu-id="890dd-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="890dd-160">In der **Namen** Geben Sie "ID".</span><span class="sxs-lookup"><span data-stu-id="890dd-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="890dd-161">Alle Steuerelemente für die neue Spalte wird aktiviert, wenn Sie hier einen Wert eingeben.</span><span class="sxs-lookup"><span data-stu-id="890dd-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="890dd-162">Die Registerkarte der **Datentyp** aus, und wählen Sie **Int**. Dieser Wert gibt an, dass die ID-Spalte Daten für ganze Zahlen (Anzahl enthält).</span><span class="sxs-lookup"><span data-stu-id="890dd-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="890dd-163">Wird nicht nennen wir jegliche hier (wesentlich), jedoch können Sie standardmäßige Windows-tastaturgesten wechseln in diesem Raster.</span><span class="sxs-lookup"><span data-stu-id="890dd-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="890dd-164">Beispielsweise können Sie mit der tab zwischen den Feldern, können Sie einfach beginnen mit der Eingabe, um ein Element in einer Liste auswählen und so weiter.</span><span class="sxs-lookup"><span data-stu-id="890dd-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>


<span data-ttu-id="890dd-165">Letzte Registerkarte die **Standardwert** Feld (d. h. es leer lassen).</span><span class="sxs-lookup"><span data-stu-id="890dd-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="890dd-166">TAB-Taste zu der **Primärschlüssel wird** Kontrollkästchen, und wählen Sie ihn.</span><span class="sxs-lookup"><span data-stu-id="890dd-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="890dd-167">Diese Option wird die Datenbank angewiesen, die die *ID* Spalte enthält die Daten, die einzelne Zeilen zu identifizieren.</span><span class="sxs-lookup"><span data-stu-id="890dd-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="890dd-168">(D. h. jede Zeile einen eindeutigen Wert in der ID-Spalte, die Sie verwenden können, um diese Zeile zu suchen müssen.)</span><span class="sxs-lookup"><span data-stu-id="890dd-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="890dd-169">Wählen Sie die **ist Identity** Option.</span><span class="sxs-lookup"><span data-stu-id="890dd-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="890dd-170">Diese Option weist der Datenbank, dass die nächste laufende Nummer für jede neue Zeile automatisch generiert werden sollen.</span><span class="sxs-lookup"><span data-stu-id="890dd-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="890dd-171">(Die **ist Identity** Wirkung der option nur, wenn Sie auch ausgewählt haben die **Primärschlüssel wird** Option.)</span><span class="sxs-lookup"><span data-stu-id="890dd-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="890dd-172">Klicken Sie in der nächsten Zeile des Datenblatts, oder drücken Sie die Tabulatortaste zweimal, um die aktuelle Zeile fertig zu stellen.</span><span class="sxs-lookup"><span data-stu-id="890dd-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="890dd-173">Entweder Geste speichert die aktuelle Zeile und den darauffolgenden beginnt.</span><span class="sxs-lookup"><span data-stu-id="890dd-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="890dd-174">Beachten Sie, dass die **Standardwert** Spalte sagt jetzt **Null**.</span><span class="sxs-lookup"><span data-stu-id="890dd-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="890dd-175">(Null ist der Standardwert für den Standardwert, sozusagen.)</span><span class="sxs-lookup"><span data-stu-id="890dd-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="890dd-176">Wenn Sie die Definition der neuen abgeschlossen haben **ID** Spalte der Designer sieht dieser Abbildung:</span><span class="sxs-lookup"><span data-stu-id="890dd-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![WebMatrix-Datenbank-Designer nach dem Definieren der ID-Spalte, für die Tabelle Filme](displaying-data/_static/image7.png)

<span data-ttu-id="890dd-178">Klicken Sie zum Erstellen der nächsten Spalte in das Feld in der **Namen** Spalte.</span><span class="sxs-lookup"><span data-stu-id="890dd-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="890dd-179">Geben Sie für die Spalte "Title", und wählen Sie dann **Nvarchar** für die **Datentyp** Wert.</span><span class="sxs-lookup"><span data-stu-id="890dd-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="890dd-180">Der Teil "Var" **Nvarchar** weist der Datenbank an, dass es sich bei die Daten für diese Spalte eine Zeichenfolge handelt, dessen Größe von Datensatz zu Datensatz variieren.</span><span class="sxs-lookup"><span data-stu-id="890dd-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="890dd-181">(Das Präfix "n" stellt "national", was bedeutet, dass das Feld für alle Alphabet oder ein Schreibsystem-Zeichendaten enthalten kann: das Feld enthält, wird die Unicode-Daten.)</span><span class="sxs-lookup"><span data-stu-id="890dd-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="890dd-182">Bei der Auswahl **Nvarchar**, einem anderen Feld wird angezeigt, in dem Sie die maximale Länge für das Feld eingeben können.</span><span class="sxs-lookup"><span data-stu-id="890dd-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="890dd-183">Geben Sie 50 ein, unter der Annahme, dass keine Filmtitel, die Sie in diesem Tutorial verwenden werden mehr als 50 Zeichen ist.</span><span class="sxs-lookup"><span data-stu-id="890dd-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="890dd-184">Skip **Standardwert** und deaktivieren Sie die **NULL-Werte zulassen** Option.</span><span class="sxs-lookup"><span data-stu-id="890dd-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="890dd-185">Sie möchten nicht die Datenbank Filme, die in der Datenbank eingegeben werden können, die einen Titel besitzen.</span><span class="sxs-lookup"><span data-stu-id="890dd-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="890dd-186">Wenn Sie fertig sind und auf die nächste Zeile verschieben, sieht der Designer die folgende Abbildung aus:</span><span class="sxs-lookup"><span data-stu-id="890dd-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![WebMatrix-Datenbank-Designer nach dem Definieren der Titelspalte für die Tabelle Filme](displaying-data/_static/image8.png)

<span data-ttu-id="890dd-188">Wiederholen Sie diese Schritte zum Erstellen einer Spalte mit dem Namen "Genre", mit Ausnahme von der Länge, es zu lediglich 30.</span><span class="sxs-lookup"><span data-stu-id="890dd-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="890dd-189">Erstellen Sie eine andere Spalte, die mit dem Namen "Year".</span><span class="sxs-lookup"><span data-stu-id="890dd-189">Create another column named "Year."</span></span> <span data-ttu-id="890dd-190">Wählen Sie für den Datentyp **Nchar** (nicht **Nvarchar**) und die Länge auf 4 festgelegt.</span><span class="sxs-lookup"><span data-stu-id="890dd-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="890dd-191">Für das Jahr machen Sie 4 Ziffern wie "1995" oder "2010" verwenden, damit Sie eine Spalte mit variabler Größe nicht benötigen.</span><span class="sxs-lookup"><span data-stu-id="890dd-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="890dd-192">Hier ist der fertige Entwurf aussieht:</span><span class="sxs-lookup"><span data-stu-id="890dd-192">Here's what the finished design looks like:</span></span>

![WebMatrix-Datenbank-Designer, nachdem alle Felder für die Tabelle Filme definiert sind](displaying-data/_static/image9.png)

<span data-ttu-id="890dd-194">Drücken Sie STRG + S, oder klicken Sie auf die **speichern** die Schnellzugriff-Symbolleiste die Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="890dd-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="890dd-195">Schließen Sie den Datenbankdesigner, schließen Sie die Registerkarte.</span><span class="sxs-lookup"><span data-stu-id="890dd-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="890dd-196">Einige Beispieldaten hinzufügen</span><span class="sxs-lookup"><span data-stu-id="890dd-196">Adding Some Example Data</span></span>

<span data-ttu-id="890dd-197">Weiter unten in diesem Tutorial erstellen Sie eine Seite, in dem Sie neue Filme in einem Formular eingeben können.</span><span class="sxs-lookup"><span data-stu-id="890dd-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="890dd-198">Jetzt können Sie jedoch einige Beispiele für Daten hinzufügen, die Sie anschließend auf einer Seite anzeigen können.</span><span class="sxs-lookup"><span data-stu-id="890dd-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="890dd-199">In der **Datenbank** Arbeitsbereich in WebMatrix, beachten Sie, dass es eine Struktur, die Aufschluss über die *.sdf* Datei, die Sie zuvor erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="890dd-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="890dd-200">Öffnen Sie den Knoten für Ihre neue *.sdf* Datei, und öffnen Sie dann die **Tabellen** Knoten.</span><span class="sxs-lookup"><span data-stu-id="890dd-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Arbeitsbereich "WebMatrix-Datenbank" Struktur Filme Tabelle öffnen](displaying-data/_static/image10.png)

<span data-ttu-id="890dd-202">Mit der rechten Maustaste die **Filme** Knoten und wählen Sie dann **Daten**.</span><span class="sxs-lookup"><span data-stu-id="890dd-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="890dd-203">WebMatrix öffnet ein Raster, in dem Sie Daten eingeben können, die *Filme* Tabelle:</span><span class="sxs-lookup"><span data-stu-id="890dd-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Datenbank-Eintrag-Raster in WebMatrix (leer)](displaying-data/_static/image11.png)

<span data-ttu-id="890dd-205">Klicken Sie auf die **Titel** Spalte, und geben Sie "Bei der Harry erfüllt Sally".</span><span class="sxs-lookup"><span data-stu-id="890dd-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="890dd-206">Verschieben Sie in der **"Genre"** Spalte (Sie können die Tab-Taste verwenden), und geben Sie "Romantisch Komödie".</span><span class="sxs-lookup"><span data-stu-id="890dd-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="890dd-207">Verschieben Sie in der **Jahr** Spalte, und geben Sie "1989":</span><span class="sxs-lookup"><span data-stu-id="890dd-207">Move to the **Year** column and enter "1989":</span></span>

![Datenbank-Eintrag-Raster in WebMatrix mit einem Datensatz](displaying-data/_static/image12.png)

<span data-ttu-id="890dd-209">Drücken Sie die EINGABETASTE, und WebMatrix wird den neuen Film gespeichert.</span><span class="sxs-lookup"><span data-stu-id="890dd-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="890dd-210">Beachten Sie, dass die **ID** Spalte ausgefüllt wurde.</span><span class="sxs-lookup"><span data-stu-id="890dd-210">Notice that the **ID** column has been filled in.</span></span>

![Datenbank-Eintrag-Raster in WebMatrix mit einem Datensatz und automatisch generierte ID](displaying-data/_static/image13.png)

<span data-ttu-id="890dd-212">Geben Sie einen anderen Film (z. B. "nicht mehr mit der Wind", "Drama", "1939").</span><span class="sxs-lookup"><span data-stu-id="890dd-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="890dd-213">Die ID-Spalte wird erneut ausgefüllt:</span><span class="sxs-lookup"><span data-stu-id="890dd-213">The ID column is filled in again:</span></span>

![Datenbank-Eintrag-Raster in WebMatrix mit zwei Datensätze und automatisch generierten IDs](displaying-data/_static/image14.png)

<span data-ttu-id="890dd-215">Geben Sie einen dritten Film (z. B. "Ghostbusters", "Komödie").</span><span class="sxs-lookup"><span data-stu-id="890dd-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="890dd-216">Lassen Sie zum Experimentieren die **Jahr** Spalte leer, und drücken Sie dann die EINGABETASTE.</span><span class="sxs-lookup"><span data-stu-id="890dd-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="890dd-217">Da Sie nicht ausgewählt. die **NULL-Werte zulassen** option, die Datenbank wird ein Fehler angezeigt:</span><span class="sxs-lookup"><span data-stu-id="890dd-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

!["Ungültige Daten"-Fehler angezeigt, wenn ein erforderliche Spalte kein Wert angegeben ist](displaying-data/_static/image15.png)

<span data-ttu-id="890dd-219">Klicken Sie auf **OK** zurückgehen und beheben Sie den Eintrag (das Jahr für "Ghostbusters" ist 1984) und dann die EINGABETASTE drücken.</span><span class="sxs-lookup"><span data-stu-id="890dd-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="890dd-220">Geben Sie mehrere Filme bis 8 sind, oder damit.</span><span class="sxs-lookup"><span data-stu-id="890dd-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="890dd-221">(8 eingeben erleichtert es funktioniert mit paging später noch mal.</span><span class="sxs-lookup"><span data-stu-id="890dd-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="890dd-222">Aber wenn dies auf zu viele ist, geben Sie ein paar vorerst.) Die tatsächlichen Daten spielt keine Rolle.</span><span class="sxs-lookup"><span data-stu-id="890dd-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Datenbank-Eintrag-Raster in WebMatrix mit beiden Einträgen](displaying-data/_static/image16.png)

<span data-ttu-id="890dd-224">Wenn Sie alle Filme fehlerfrei eingegeben haben, sind die ID-Werte sequenziell.</span><span class="sxs-lookup"><span data-stu-id="890dd-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="890dd-225">Wenn Sie versucht, einen unvollständige Movie-Datensatz zu speichern, können die ID-Nummern nicht sequenziell sein.</span><span class="sxs-lookup"><span data-stu-id="890dd-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="890dd-226">Wenn dies der Fall ist, ist das kein Problem.</span><span class="sxs-lookup"><span data-stu-id="890dd-226">If so, that's okay.</span></span> <span data-ttu-id="890dd-227">Die Zahlen müssen keine Bedeutungen behaftet, und das einzige, die wichtig ist, dass sie in eindeutig sind die *Filme* Tabelle.</span><span class="sxs-lookup"><span data-stu-id="890dd-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="890dd-228">Schließen Sie die Registerkarte, die den Datenbank-Designer enthält.</span><span class="sxs-lookup"><span data-stu-id="890dd-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="890dd-229">Jetzt können Sie zum Anzeigen der Daten auf einer Webseite aktivieren.</span><span class="sxs-lookup"><span data-stu-id="890dd-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="890dd-230">Anzeigen von Daten auf einer Seite mit der WebGrid-Hilfsprogramm</span><span class="sxs-lookup"><span data-stu-id="890dd-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="890dd-231">Um Daten auf einer Seite anzuzeigen, also verwenden Sie die `WebGrid` Helper.</span><span class="sxs-lookup"><span data-stu-id="890dd-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="890dd-232">Dieses Hilfsprogramm erzeugt eine Anzeige in einem Raster oder einer Tabelle (Zeilen und Spalten).</span><span class="sxs-lookup"><span data-stu-id="890dd-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="890dd-233">Wie Sie sehen werden, werden Sie können kooperiert das Raster mit Formatierung und andere Features sein.</span><span class="sxs-lookup"><span data-stu-id="890dd-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="890dd-234">Um das Raster auszuführen, müssen Sie ein paar Zeilen Code schreiben.</span><span class="sxs-lookup"><span data-stu-id="890dd-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="890dd-235">Diese Zeilen werden als eine Art von Muster für fast alle den Datenzugriff dienen, die Sie in diesem Tutorial.</span><span class="sxs-lookup"><span data-stu-id="890dd-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="890dd-236">Sie müssen den tatsächlich viele Optionen zum Anzeigen von Daten auf einer Seite; die `WebGrid` Helper ist nur eine.</span><span class="sxs-lookup"><span data-stu-id="890dd-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="890dd-237">Wir gewählt, der für dieses Tutorial, da es die einfachste Möglichkeit zum Anzeigen von Daten ist und es ausreichend flexibel ist.</span><span class="sxs-lookup"><span data-stu-id="890dd-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="890dd-238">In den nächsten Tutorial sehen Sie, wie Sie eine weitere "manuell" Möglichkeit zur Verwendung von Daten auf der Seite, wodurch Sie die bessere Kontrolle über die Vorgehensweise beim Anzeigen von Daten verwenden.</span><span class="sxs-lookup"><span data-stu-id="890dd-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>


<span data-ttu-id="890dd-239">Klicken Sie im linken Bereich in WebMatrix auf die **Dateien** Arbeitsbereich.</span><span class="sxs-lookup"><span data-stu-id="890dd-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="890dd-240">Die neue Datenbank, die Sie erstellt haben, ist in der *App\_Daten* Ordner.</span><span class="sxs-lookup"><span data-stu-id="890dd-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="890dd-241">Wenn der Ordner nicht vorhanden, von WebMatrix für Ihre neue Datenbank erstellt.</span><span class="sxs-lookup"><span data-stu-id="890dd-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="890dd-242">(Der Ordner möglicherweise vorhanden waren, wenn Sie die Hilfsprogramme zuvor installiert hatten.)</span><span class="sxs-lookup"><span data-stu-id="890dd-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="890dd-243">Wählen Sie in der Strukturansicht den Stamm der Website ein.</span><span class="sxs-lookup"><span data-stu-id="890dd-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="890dd-244">Sie müssen den Stamm der Website auswählen; Andernfalls kann die neue Datei hinzugefügt werden, für die App\_Datenordner.</span><span class="sxs-lookup"><span data-stu-id="890dd-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="890dd-245">Klicken Sie im Menüband auf **neu**.</span><span class="sxs-lookup"><span data-stu-id="890dd-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="890dd-246">In der **wählen Sie einen Dateityp** wählen **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="890dd-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="890dd-247">In der **Namen** benennen Sie die neue Seite "Movies.cshtml":</span><span class="sxs-lookup"><span data-stu-id="890dd-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

!["Wählen Sie einen Dateityp" Dialogfeld Anzeigen der Seite "Movies"](displaying-data/_static/image17.png)

<span data-ttu-id="890dd-249">Klicken Sie auf die **OK** Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="890dd-249">Click the **OK** button.</span></span> <span data-ttu-id="890dd-250">WebMatrix öffnet eine neue Datei mit einigen Skelett Elementen darin.</span><span class="sxs-lookup"><span data-stu-id="890dd-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="890dd-251">Zunächst schreiben Sie Code zum wechseln, die Daten aus der Datenbank abzurufen.</span><span class="sxs-lookup"><span data-stu-id="890dd-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="890dd-252">Anschließend fügen Sie Markup zum Anzeigen der Daten auf der Seite.</span><span class="sxs-lookup"><span data-stu-id="890dd-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="890dd-253">Der Code der Abfrage Daten schreiben</span><span class="sxs-lookup"><span data-stu-id="890dd-253">Writing the Data Query Code</span></span>

<span data-ttu-id="890dd-254">Am oberen Rand der Seite zwischen dem `@{` und `}` Zeichen, geben Sie den folgenden Code.</span><span class="sxs-lookup"><span data-stu-id="890dd-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="890dd-255">(Stellen Sie sicher, dass Sie diesen Code zwischen den öffnenden und schließenden geschweiften Klammern eingeben.)</span><span class="sxs-lookup"><span data-stu-id="890dd-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="890dd-256">Die erste Zeile wird die Datenbank, die Sie zuvor erstellt haben. das ist immer der erste Schritt vor dem nachgehen mit der Datenbank geöffnet.</span><span class="sxs-lookup"><span data-stu-id="890dd-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="890dd-257">Sie erkennen die `Database.Open` Methodennamen der Datenbank zu öffnen.</span><span class="sxs-lookup"><span data-stu-id="890dd-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="890dd-258">Beachten Sie, die Sie nicht einschließen *.sdf* im Namen.</span><span class="sxs-lookup"><span data-stu-id="890dd-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="890dd-259">Die `Open` Methode wird davon ausgegangen, dass es gesuchten ein *.sdf* Datei (, also *WebPagesMovies.sdf*) und der *.sdf* Datei befindet sich in der *App\_ Daten* Ordner.</span><span class="sxs-lookup"><span data-stu-id="890dd-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="890dd-260">(Wir erwähnt, die die *App\_Daten* Ordner ist reserviert, dieses Szenario ist eine der Quellen, in dem ASP.NET zu diesem Namen Annahmen.)</span><span class="sxs-lookup"><span data-stu-id="890dd-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="890dd-261">Wenn die Datenbank geöffnet wird, wird ein Verweis darauf in der benannten Variablen eingefügt `db`.</span><span class="sxs-lookup"><span data-stu-id="890dd-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="890dd-262">(Das könnte etwas benannt werden.) Die `db` Variable ist, wie Sie ansonsten mit der Datenbank interagieren.</span><span class="sxs-lookup"><span data-stu-id="890dd-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="890dd-263">Die zweite Zeile ruft tatsächlich Daten in der Datenbank mithilfe der `Query` Methode.</span><span class="sxs-lookup"><span data-stu-id="890dd-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="890dd-264">Beachten Sie, wie dieser Code funktioniert: die `db` Variable enthält einen Verweis auf die Datenbank geöffnet, und rufen Sie die `Query` Methode mithilfe der `db` Variable (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="890dd-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="890dd-265">Die Abfrage selbst ist eine SQL `Select` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="890dd-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="890dd-266">(Ein paar Hintergrundinformationen zur SQL, finden Sie auf die Erläuterung weiter unten). In der Anweisung `Movies` identifiziert die Tabelle auf Abfrage.</span><span class="sxs-lookup"><span data-stu-id="890dd-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="890dd-267">Die `*` Zeichen gibt an, dass die Abfrage alle Spalten aus der Tabelle zurückgeben soll.</span><span class="sxs-lookup"><span data-stu-id="890dd-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="890dd-268">(Sie können auch Spalten einzeln auflisten durch Kommas getrennt.)</span><span class="sxs-lookup"><span data-stu-id="890dd-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="890dd-269">Die Ergebnisse der Abfrage, sofern vorhanden, werden zurückgegeben und in zur Verfügung gestellt der `selectedData` Variable.</span><span class="sxs-lookup"><span data-stu-id="890dd-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="890dd-270">In diesem Fall kann die Variable Namen.</span><span class="sxs-lookup"><span data-stu-id="890dd-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="890dd-271">Die dritte Zeile weist ASP.NET schließlich, dass Sie eine Instanz der verwenden möchten die `WebGrid` Helper.</span><span class="sxs-lookup"><span data-stu-id="890dd-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="890dd-272">Sie erstellen (*instanziieren*) das Hilfsobjekt mithilfe der `new` Schlüsselwort, und übergeben sie die Ergebnisse der Abfrage über die `selectedData` Variable.</span><span class="sxs-lookup"><span data-stu-id="890dd-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="890dd-273">Die neue `WebGrid` Objekt zusammen mit den Ergebnissen der Datenbankabfrage, stehen in der `grid` Variable.</span><span class="sxs-lookup"><span data-stu-id="890dd-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="890dd-274">Sie benötigen dieses Ergebnis in einen Moment Zeit, um die Daten tatsächlich auf der Seite anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="890dd-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="890dd-275">In dieser Phase wird die Datenbank geöffnet worden ist, haben Sie die Daten abgerufen werden sollen, und Sie vorbereitet haben die `WebGrid` Helper mit diesen Daten.</span><span class="sxs-lookup"><span data-stu-id="890dd-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="890dd-276">Als Nächstes wird das Markup auf der Seite zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="890dd-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="890dd-277">**Strukturierte Abfragesprache (SQL)**</span><span class="sxs-lookup"><span data-stu-id="890dd-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="890dd-278">SQL ist eine Sprache, die in den meisten relationalen Datenbanken für die Verwaltung von Daten in einer Datenbank verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="890dd-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="890dd-279">Es enthält Befehle, mit denen Sie Daten abrufen und aktualisieren und, mit denen Sie erstellen, ändern und Verwalten von Daten in Datenbanktabellen.</span><span class="sxs-lookup"><span data-stu-id="890dd-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="890dd-280">SQL ist eine Programmiersprache (z. B. C#)) unterscheidet.</span><span class="sxs-lookup"><span data-stu-id="890dd-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="890dd-281">Mit SQL Sie legen fest, die Datenbank Was soll, und es ist Aufgabe der Datenbank, um zu ermitteln, wie Sie die Daten abzurufen, oder führen Sie die Aufgabe.</span><span class="sxs-lookup"><span data-stu-id="890dd-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="890dd-282">Hier sind Beispiele für einige SQL-Befehle und was sie tun:</span><span class="sxs-lookup"><span data-stu-id="890dd-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="890dd-283">Die erste `Select` Anweisung werden alle Spalten (angegeben durch `*`) aus der *Filme* Tabelle.</span><span class="sxs-lookup"><span data-stu-id="890dd-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="890dd-284">Die zweite `Select` Anweisung ruft die Spalten-ID, Name und Preis von Datensätzen in der *Produkt* Tabelle, deren Spaltenwerte Preis mehr als 10 ist.</span><span class="sxs-lookup"><span data-stu-id="890dd-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="890dd-285">Die Ergebnisse zurückgegeben in alphabetischer Reihenfolge anhand der Werte der Spalte "Name".</span><span class="sxs-lookup"><span data-stu-id="890dd-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="890dd-286">Wenn keine Datensätze auf die Preis Kriterien entsprechen, gibt der Befehl eine leere Menge zurück.</span><span class="sxs-lookup"><span data-stu-id="890dd-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="890dd-287">Dieser Befehl fügt einen neuen Datensatz in die *Produkt* Tabelle die Spalte "Name", "Croissant", die Spalte "Beschreibung", "A-unzuverlässigen begeistern" und der Preis auf 1,99 festlegen.</span><span class="sxs-lookup"><span data-stu-id="890dd-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="890dd-288">Beachten Sie, dass bei der Angabe eines nicht numerischen Wert, der Wert in einfache Anführungszeichen (keine doppelten Anführungszeichen, wie in c#) eingeschlossen ist.</span><span class="sxs-lookup"><span data-stu-id="890dd-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="890dd-289">Sie verwenden diese Anführungszeichen ein, um Text oder Datumswerte, aber nicht um Zahlen.</span><span class="sxs-lookup"><span data-stu-id="890dd-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="890dd-290">Dieser Befehl löscht Datensätze in der *Produkt* Tabelle, deren Ablauf Datumsspalte älter als der 1. Januar 2008 ist.</span><span class="sxs-lookup"><span data-stu-id="890dd-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="890dd-291">(Der Befehl setzt voraus, dass die *Produkt* Tabelle besitzt eine solche Spalte natürlich.) Das Datum wird hier im Format MM/TT/JJJJ eingegeben, aber es muss angegeben werden, in dem Format, das für Ihr Gebietsschema verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="890dd-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="890dd-292">Die `Insert` und `Delete` Befehle keine Resultsets zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="890dd-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="890dd-293">Stattdessen geben sie eine Zahl, die Aufschluss darüber gibt, wie viele Datensätze betroffen sind, durch den Befehl zurück.</span><span class="sxs-lookup"><span data-stu-id="890dd-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="890dd-294">Für einige dieser Vorgänge (wie einfügen und Löschen von Datensätzen) wird der Prozess, der den Vorgang angefordert wird, die entsprechenden Berechtigungen verfügen, in der Datenbank aufweist.</span><span class="sxs-lookup"><span data-stu-id="890dd-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="890dd-295">Dies ist deshalb für Produktionsdatenbanken, die Sie häufig einen Benutzernamen und ein Kennwort angeben, wenn Sie eine Verbindung mit der Datenbank herstellen müssen.</span><span class="sxs-lookup"><span data-stu-id="890dd-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="890dd-296">Es gibt Dutzende von SQL-Befehle, aber sie alle folgen einem Muster wie die Befehle, die Sie hier sehen.</span><span class="sxs-lookup"><span data-stu-id="890dd-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="890dd-297">Sie können SQL-Befehlen erstellen Sie die Datenbanktabellen, die Anzahl der Datensätze in einer Tabelle, Preise berechnen und viele weitere Vorgänge ausführen.</span><span class="sxs-lookup"><span data-stu-id="890dd-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>


### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="890dd-298">Hinzufügen von Markup zum Anzeigen der Daten</span><span class="sxs-lookup"><span data-stu-id="890dd-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="890dd-299">In der `<head>` festlegen-Inhalt des Elements der `<title>` Element zu "Movies":</span><span class="sxs-lookup"><span data-stu-id="890dd-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="890dd-300">In der `<body>` -Element der Seite, fügen Sie Folgendes hinzu:</span><span class="sxs-lookup"><span data-stu-id="890dd-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="890dd-301">Das ist alles.</span><span class="sxs-lookup"><span data-stu-id="890dd-301">That's it.</span></span> <span data-ttu-id="890dd-302">Die `grid` Variablen ist der Wert, der Sie erstellt haben, bei der Erstellung der `WebGrid` -Objekt im Code oben.</span><span class="sxs-lookup"><span data-stu-id="890dd-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="890dd-303">Klicken Sie in der WebMatrix-Strukturansicht mit der rechten Maustaste in der Seite, und wählen **in Browser starten**.</span><span class="sxs-lookup"><span data-stu-id="890dd-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="890dd-304">Sie sehen etwa wie auf dieser Seite:</span><span class="sxs-lookup"><span data-stu-id="890dd-304">You'll see something like this page:</span></span>

![Standardausgabe WebGrid-Hilfsprogramm aus der Tabelle Filme](displaying-data/_static/image18.png)

<span data-ttu-id="890dd-306">Klicken Sie auf Link einer Spaltenüberschrift nach dieser Spalte sortiert.</span><span class="sxs-lookup"><span data-stu-id="890dd-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="890dd-307">Durch Klicken auf eine Spaltenüberschrift sortieren kann, ist ein Feature, das in integriert ist die **WebGrid** Helper.</span><span class="sxs-lookup"><span data-stu-id="890dd-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="890dd-308">Die `GetHtml` -Methode, wie der Name schon sagt, generiert das Markup, das Daten anzeigt.</span><span class="sxs-lookup"><span data-stu-id="890dd-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="890dd-309">In der Standardeinstellung die `GetHtml` Methode generiert ein HTML-Code `<table>` Element.</span><span class="sxs-lookup"><span data-stu-id="890dd-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="890dd-310">(Wenn Sie möchten, können Sie das Rendering überprüfen, indem Sie den Quellcode der Seite im Browser ansehen.)</span><span class="sxs-lookup"><span data-stu-id="890dd-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="890dd-311">Ändern das Aussehen des Rasters</span><span class="sxs-lookup"><span data-stu-id="890dd-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="890dd-312">Mithilfe der `WebGrid` Hilfsprogramm wie eben durchgeführt ist einfach, aber die resultierende Anzeige ist einfacher.</span><span class="sxs-lookup"><span data-stu-id="890dd-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="890dd-313">Die `WebGrid` Hilfsprogramm bietet Optionen, mit denen Sie steuern, wie die Daten angezeigt werden können.</span><span class="sxs-lookup"><span data-stu-id="890dd-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="890dd-314">Es gibt viel zu viele in diesem Tutorial untersuchen, aber in diesem Abschnitt erhalten Sie einen Überblick über einige dieser Optionen.</span><span class="sxs-lookup"><span data-stu-id="890dd-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="890dd-315">Einige zusätzliche Optionen werden in späteren Tutorials dieser Serie behandelt.</span><span class="sxs-lookup"><span data-stu-id="890dd-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="890dd-316">Angeben der einzelne Spalten zum Anzeigen</span><span class="sxs-lookup"><span data-stu-id="890dd-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="890dd-317">Um zu starten, können Sie angeben, dass nur bestimmte Spalten angezeigt werden soll.</span><span class="sxs-lookup"><span data-stu-id="890dd-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="890dd-318">Standardmäßig lässt sich sagen, wie Sie gesehen haben, im Raster werden alle vier Spalten aus der *Filme* Tabelle.</span><span class="sxs-lookup"><span data-stu-id="890dd-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="890dd-319">In der *Movies.cshtml* Datei, ersetzen Sie die `@grid.GetHtml()` Markup, das Sie gerade hinzugefügt, durch Folgendes haben:</span><span class="sxs-lookup"><span data-stu-id="890dd-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="890dd-320">Um dem Hilfsobjekt die anzuzeigenden Spalten zu sagen, Sie enthalten eine `columns` -Parameter für die `GetHtml` -Methode und übergeben Sie eine Auflistung von Spalten.</span><span class="sxs-lookup"><span data-stu-id="890dd-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="890dd-321">In der Auflistung geben Sie jede Spalte einschließen.</span><span class="sxs-lookup"><span data-stu-id="890dd-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="890dd-322">Geben Sie eine einzelne Spalte zum Anzeigen von einschließlich einer `grid.Column` Objekt, und übergeben Sie den Namen der Datenspalte.</span><span class="sxs-lookup"><span data-stu-id="890dd-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="890dd-323">(Diese Spalten müssen in die Ergebnisse der SQL-Abfrage enthalten sein, die `WebGrid` Hilfsprogramm kann nicht angezeigt werden Spalten, die nicht von der Abfrage zurückgegeben wurden.)</span><span class="sxs-lookup"><span data-stu-id="890dd-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="890dd-324">Starten Sie den *Movies.cshtml* Seite im Browser erneut, und dieses Mal erhalten Sie eine Anzeige wie folgt (Beachten Sie, dass kein ID-Spalte angezeigt wird):</span><span class="sxs-lookup"><span data-stu-id="890dd-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![WebGrid-angezeigt werden, nur ausgewählte Spalten](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="890dd-326">Ändern des Aussehens des Rasters</span><span class="sxs-lookup"><span data-stu-id="890dd-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="890dd-327">Es gibt einigen Weitere Optionen für die Anzeige von Spalten, von die einige in späteren Tutorials in dieser Gruppe beschrieben wird.</span><span class="sxs-lookup"><span data-stu-id="890dd-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="890dd-328">Jetzt werden in diesem Abschnitt Möglichkeiten vorgestellt in dem Sie das Raster als Ganzes formatieren können.</span><span class="sxs-lookup"><span data-stu-id="890dd-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="890dd-329">In der `<head>` Abschnitt der Seite unmittelbar vor dem `</head>` markieren, fügen Sie die folgenden `<style>` Element:</span><span class="sxs-lookup"><span data-stu-id="890dd-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="890dd-330">Dieser CSS-Markup definiert Klassen, die mit dem Namen `grid`, `head`und so weiter.</span><span class="sxs-lookup"><span data-stu-id="890dd-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="890dd-331">Sie können auch diese Stildefinitionen einfügen, in einer separaten *CSS* Datei, und verknüpfen, die auf der Seite.</span><span class="sxs-lookup"><span data-stu-id="890dd-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="890dd-332">(In der Tat müssen Sie das weiter unten in dieser tutorialreihe tun.) Aber um die Dinge für dieses Tutorial zu vereinfachen, in der gleichen Seite, die die Daten anzeigt.</span><span class="sxs-lookup"><span data-stu-id="890dd-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="890dd-333">Nutzen Sie jetzt die `WebGrid` Hilfsprogramm mithilfe dieser Stil-Klassen.</span><span class="sxs-lookup"><span data-stu-id="890dd-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="890dd-334">Das Hilfsprogramm hat eine Reihe von Eigenschaften (z. B. `tableStyle`) für genau diesen Zweck, Sie einen Klassennamen der CSS-Stil auf diese zuweisen und Klassennamens gerendert wird, im Rahmen von Markup, das vom Hilfsprogramm gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="890dd-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="890dd-335">Ändern der `grid.GetHtml` Markup so, dass die It nun wie im folgenden Code sieht:</span><span class="sxs-lookup"><span data-stu-id="890dd-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="890dd-336">Was neu ist, dass Sie hinzugefügt haben `tableStyle`, `headerStyle`, und `alternatingRowStyle` Parameter für die `GetHtml` Methode.</span><span class="sxs-lookup"><span data-stu-id="890dd-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="890dd-337">Diese Parameter haben den Namen der CSS-Formate festgelegt wurde, die Sie vorhin hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="890dd-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="890dd-338">Führen Sie die Seite, und dieses Mal sehen Sie ein Raster, das wesentlich einfacher als vor dem aussieht:</span><span class="sxs-lookup"><span data-stu-id="890dd-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![WebGrid-Anzeige, die CSS-Klassennamen festgelegten Parametern enthält.](displaying-data/_static/image20.png)

<span data-ttu-id="890dd-340">Festzustellen, was die `GetHtml` Methode, die generiert werden, sehen Sie sich den Quellcode der Seite im Browser.</span><span class="sxs-lookup"><span data-stu-id="890dd-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="890dd-341">Wir gehen hier nicht, aber der wichtigste Punkt ist, die durch Angabe von Parametern wie `tableStyle`, Sie verursacht das Raster zum Generieren von HTML-Tags wie folgt:</span><span class="sxs-lookup"><span data-stu-id="890dd-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="890dd-342">Die `<table>` Tag hatte eine `class` Attribut hinzugefügt wird, die auf eine CSS-Regeln verweist, die Sie zuvor hinzugefügt haben.</span><span class="sxs-lookup"><span data-stu-id="890dd-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="890dd-343">Dieser Code zeigt das grundlegende Muster &mdash; unterschiedliche Parameter für die `GetHtml` können Sie ein Verweis auf CSS-Klassen, die die Methode dann zusammen mit dem Markup generiert.</span><span class="sxs-lookup"><span data-stu-id="890dd-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="890dd-344">Wie Sie die CSS-Klassen verwenden, bleibt Ihnen überlassen.</span><span class="sxs-lookup"><span data-stu-id="890dd-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="890dd-345">Hinzufügen von Paging</span><span class="sxs-lookup"><span data-stu-id="890dd-345">Adding Paging</span></span>

<span data-ttu-id="890dd-346">Als der letzten Aufgabe in diesem Tutorial fügen Sie Paging in das Raster.</span><span class="sxs-lookup"><span data-stu-id="890dd-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="890dd-347">Zurzeit ist kein Problem, um alle Ihre Filme auf einmal anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="890dd-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="890dd-348">Aber wenn Sie Hunderte von Filmen hinzugefügt haben, würden die angezeigte Seite lang erhalten.</span><span class="sxs-lookup"><span data-stu-id="890dd-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="890dd-349">Ändern Sie im Seitencode, die Zeile, die erstellt die `WebGrid` Objekt in den folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="890dd-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="890dd-350">Bevor Sie der einzige Unterschied ist, dass Sie hinzugefügt haben eine `rowsPerPage` Parameter, der auf 3 festgelegt ist.</span><span class="sxs-lookup"><span data-stu-id="890dd-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="890dd-351">Führen Sie die Seite.</span><span class="sxs-lookup"><span data-stu-id="890dd-351">Run the page.</span></span> <span data-ttu-id="890dd-352">Das Raster zeigt 3 Zeilen an eine Zeit plus der Navigationslinks, die auf Ihrer Seite über den Filmen in Ihrer Datenbank zu ermöglichen:</span><span class="sxs-lookup"><span data-stu-id="890dd-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![WebGrid-Anzeige mit auslagerungen](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="890dd-354">Als Nächstes kommen</span><span class="sxs-lookup"><span data-stu-id="890dd-354">Coming Up Next</span></span>

<span data-ttu-id="890dd-355">Im nächsten Tutorial erfahren Sie, wie Razor und c#-Code verwenden, um Benutzereingaben in einem Formular abgerufen werden.</span><span class="sxs-lookup"><span data-stu-id="890dd-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="890dd-356">Sie müssen ein Suchfeld auf der Seite "Movies" hinzufügen, damit Sie Filme nach Titel oder "Genre" finden können.</span><span class="sxs-lookup"><span data-stu-id="890dd-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="890dd-357">Vollständige Liste für die Seite "Movies"</span><span class="sxs-lookup"><span data-stu-id="890dd-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="890dd-358">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="890dd-358">Additional Resources</span></span>

- [<span data-ttu-id="890dd-359">Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax</span><span class="sxs-lookup"><span data-stu-id="890dd-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="890dd-360">[Zurück](intro-to-web-pages-programming.md)
> [Weiter](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="890dd-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
