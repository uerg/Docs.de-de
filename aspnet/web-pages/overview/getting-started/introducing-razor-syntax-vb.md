---
uid: web-pages/overview/getting-started/introducing-razor-syntax-vb
title: "Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax (Visual Basic) | Microsoft Docs"
author: tfitzmac
description: "In diesem Anhang werden Ihnen einen Überblick über die Programmierung mit ASP.NET Web Pages in Visual Basic mit der Razor-Syntax."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: 5da59646-e973-41cd-88a9-c6b2c0594027
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-vb
msc.type: authoredcontent
ms.openlocfilehash: 8f5d223a5944d8adb9fe65c89e87829d18d1c7ee
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-visual-basic"></a><span data-ttu-id="83f20-103">Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="83f20-103">Introduction to ASP.NET Web Programming Using the Razor Syntax (Visual Basic)</span></span>
====================
<span data-ttu-id="83f20-104">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="83f20-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="83f20-105">In diesem Artikel können Sie einen Überblick über die Programmierung mit ASP.NET Web Pages mit Razor-Syntax und Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="83f20-105">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax and Visual Basic.</span></span> <span data-ttu-id="83f20-106">ASP.NET ist die Technologie von Microsoft für die Ausführung von dynamischen Webseiten auf Webservern.</span><span class="sxs-lookup"><span data-stu-id="83f20-106">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span>
> 
> <span data-ttu-id="83f20-107">**Lernen Sie**:</span><span class="sxs-lookup"><span data-stu-id="83f20-107">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="83f20-108">Das obere 8 Programmiertipps für die ersten Schritte bei der Programmierung von ASP.NET Web Pages mit Razor-Syntax.</span><span class="sxs-lookup"><span data-stu-id="83f20-108">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="83f20-109">Grundlegende Programmierkonzepte erläutert, die Sie benötigen.</span><span class="sxs-lookup"><span data-stu-id="83f20-109">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="83f20-110">Welche Server ASP.NET-Code und Razor-Syntax ist zu.</span><span class="sxs-lookup"><span data-stu-id="83f20-110">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="83f20-111">Versionen der Software</span><span class="sxs-lookup"><span data-stu-id="83f20-111">Software versions</span></span>
> 
> 
> - <span data-ttu-id="83f20-112">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="83f20-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="83f20-113">Dieses Lernprogramm funktioniert auch mit ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="83f20-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>


<span data-ttu-id="83f20-114">Die meisten Beispiele für die Verwendung von ASP.NET Web Pages mit Razor-Syntax verwenden c#.</span><span class="sxs-lookup"><span data-stu-id="83f20-114">Most examples of using ASP.NET Web Pages with Razor syntax use C#.</span></span> <span data-ttu-id="83f20-115">Die Razor-Syntax unterstützt aber auch Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="83f20-115">But the Razor syntax also supports Visual Basic.</span></span> <span data-ttu-id="83f20-116">Um eine ASP.NET-Webseite in Visual Basic programmieren, erstellen Sie eine Webseite mit einem *vbhtml* Erweiterung, und klicken Sie dann Visual Basic-Code hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="83f20-116">To program an ASP.NET web page in Visual Basic, you create a web page with a *.vbhtml* filename extension, and then add Visual Basic code.</span></span> <span data-ttu-id="83f20-117">Dieser Artikel bietet Ihnen einen Überblick über das Arbeiten mit der Sprache Visual Basic und die Syntax zum Erstellen von ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="83f20-117">This article gives you an overview of working with the Visual Basic language and syntax to create ASP.NET Webpages.</span></span>

> [!NOTE]
> <span data-ttu-id="83f20-118">Die Standardvorlagen für die Website für Microsoft WebMatrix (**Bakery**, **Fotogalerie**, und **Starter Site**usw.) in c# und Visual Basic-Versionen verfügbar sind.</span><span class="sxs-lookup"><span data-stu-id="83f20-118">The default website templates for Microsoft WebMatrix (**Bakery**, **Photo Gallery**, and **Starter Site**, etc.) are available in C# and Visual Basic versions.</span></span> <span data-ttu-id="83f20-119">Sie können die Visual Basic-Vorlagen von als NuGet-Pakete installieren.</span><span class="sxs-lookup"><span data-stu-id="83f20-119">You can install the Visual Basic templates by as NuGet packages.</span></span> <span data-ttu-id="83f20-120">Websitevorlagen installiert sind, im Stammordner der Website in einem Ordner namens *Microsoft Templates*.</span><span class="sxs-lookup"><span data-stu-id="83f20-120">Website templates are installed in the root folder of your site in a folder named *Microsoft Templates*.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="83f20-121">Der obere 8 Tipps für die Programmierung</span><span class="sxs-lookup"><span data-stu-id="83f20-121">The Top 8 Programming Tips</span></span>

<span data-ttu-id="83f20-122">Dieser Abschnitt enthält einige Tipps, die Sie unbedingt benötigen wissen Sie beim Einstieg in ASP.NET Servercode mithilfe der Razor-Syntax schreiben.</span><span class="sxs-lookup"><span data-stu-id="83f20-122">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="83f20-123">1. Fügen Sie Code, um eine Seite mit dem @-Zeichen</span><span class="sxs-lookup"><span data-stu-id="83f20-123">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="83f20-124">Die `@` Inlineausdrücke Single-Anweisungsblöcken und mit mehreren Anweisungen Blöcke beginnt:</span><span class="sxs-lookup"><span data-stu-id="83f20-124">The `@` character starts inline expressions, single-statement blocks, and multi-statement blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample1.vbhtml)]

<span data-ttu-id="83f20-125">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-125">The result displayed in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-vb/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="83f20-127">**HTML-Codierung**</span><span class="sxs-lookup"><span data-stu-id="83f20-127">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="83f20-128">Beim Anzeigen von Inhalt in einer Seite mit den `@` Zeichen, wie in den vorherigen Beispielen ASP.NET HTML-Codierung der Ausgabe.</span><span class="sxs-lookup"><span data-stu-id="83f20-128">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="83f20-129">Dies ersetzt reservierte HTML-Zeichen (z. B. `<` und `>` und `&`) mit Codes, mit denen die Zeichen als Zeichen statt als HTML-Tags oder Entitäten interpretiert wird auf einer Webseite angezeigt werden können.</span><span class="sxs-lookup"><span data-stu-id="83f20-129">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="83f20-130">Ohne HTML-Codierung, die Ausgabe aus dem Servercode möglicherweise nicht richtig angezeigt und kann eine Seite, um Sicherheitsrisiken machen.</span><span class="sxs-lookup"><span data-stu-id="83f20-130">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="83f20-131">Wenn Ihr Ziel ist, um HTML-Markup auszugeben, die RFID-Transponder als Markup gerendert wird (z. B. `<p></p>` für einen Absatz oder `<em></em>` Text hervorgehoben), finden Sie im Abschnitt [Kombinieren von Text, Markup und Code in Codeblöcken](#BM_CombiningTextMarkupAndCode) weiter unten in diesem Artikel.</span><span class="sxs-lookup"><span data-stu-id="83f20-131">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="83f20-132">Erfahren Sie mehr über die HTML-Codierung [arbeiten mit HTML-Formularen in ASP.NET Web Pages-Websites](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="83f20-132">You can read more about HTML encoding in [Working with HTML Forms in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-with-codeend-code"></a><span data-ttu-id="83f20-133">2. Sie setzen die Codeblöcke mit Code... End-Code</span><span class="sxs-lookup"><span data-stu-id="83f20-133">2. You enclose code blocks with Code...End Code</span></span>

<span data-ttu-id="83f20-134">Ein Codeblock enthält eine oder mehrere codeanweisungen und wird mit den Schlüsselwörtern eingeschlossen `Code` und `End Code`.</span><span class="sxs-lookup"><span data-stu-id="83f20-134">A code block includes one or more code statements and is enclosed with the keywords `Code` and `End Code`.</span></span> <span data-ttu-id="83f20-135">Platzieren Sie das öffnende `Code` Schlüsselwort unmittelbar nach der `@` Zeichen &#8212; es kann nicht Leerzeichen dazwischen.</span><span class="sxs-lookup"><span data-stu-id="83f20-135">Place the opening `Code` keyword immediately after the `@` character &#8212; there can't be whitespace between them.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample2.vbhtml)]

<span data-ttu-id="83f20-136">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-136">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-vb/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-line-break"></a><span data-ttu-id="83f20-138">3. Innerhalb eines Blocks am Ende jeder codeanweisung mit Zeilenumbrüchen</span><span class="sxs-lookup"><span data-stu-id="83f20-138">3. Inside a block, you end each code statement with a line break</span></span>

<span data-ttu-id="83f20-139">In einem Visual Basic-Code-Block beendet jede Anweisung mit einem Zeilenumbruch.</span><span class="sxs-lookup"><span data-stu-id="83f20-139">In a Visual Basic code block, each statement ends with a line break.</span></span> <span data-ttu-id="83f20-140">(Später in diesem Artikel wird eine Möglichkeit, eine lange codeanweisung in mehrere Zeilen umbrochen werden soll, falls erforderlich angezeigt.)</span><span class="sxs-lookup"><span data-stu-id="83f20-140">(Later in the article you'll see a way to wrap a long code statement into multiple lines if needed.)</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample3.vbhtml)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="83f20-141">4. Sie können Variablen verwenden, um Werte zu speichern</span><span class="sxs-lookup"><span data-stu-id="83f20-141">4. You use variables to store values</span></span>

<span data-ttu-id="83f20-142">Sie können Werte in Speichern einer *Variable*, einschließlich Zeichenfolgen, Zahlen und Datumsangaben. Sie erstellen eine neue Variable mit dem `Dim` Schlüsselwort.</span><span class="sxs-lookup"><span data-stu-id="83f20-142">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `Dim` keyword.</span></span> <span data-ttu-id="83f20-143">Sie können Variablenwerte einfügen, direkt in eine Seite mit `@`.</span><span class="sxs-lookup"><span data-stu-id="83f20-143">You can insert variable values directly in a page using `@`.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample4.vbhtml)]

<span data-ttu-id="83f20-144">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-144">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-vb/_static/image3.jpg)

### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="83f20-146">5. Sie setzen literale Zeichenfolgenwerte in Anführungszeichen</span><span class="sxs-lookup"><span data-stu-id="83f20-146">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="83f20-147">Ein *Zeichenfolge* ist eine Folge von Zeichen, die als Text behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="83f20-147">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="83f20-148">Um eine Zeichenfolge angeben, schließen Sie ihn in doppelte Anführungszeichen:</span><span class="sxs-lookup"><span data-stu-id="83f20-148">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample5.vbhtml)]

<span data-ttu-id="83f20-149">Fügen Sie zum Einbetten von Anführungszeichen in einen Zeichenfolgenwert, zwei doppelte Anführungszeichen ein.</span><span class="sxs-lookup"><span data-stu-id="83f20-149">To embed double quotation marks within a string value, insert two double quotation mark characters.</span></span> <span data-ttu-id="83f20-150">Wenn Sie das doppelte Anführungszeichen einmal in der Seitenausgabe angezeigt werden soll, geben Sie ihn als `""` innerhalb der Anführungszeichen-Zeichenfolge ein, und wenn es zweimal angezeigt werden sollen, geben Sie ihn als `""""` innerhalb der Zeichenfolge in Anführungszeichen.</span><span class="sxs-lookup"><span data-stu-id="83f20-150">If you want the double quotation character to appear once in the page output, enter it as `""` within the quoted string, and if you want it to appear twice, enter it as `""""` within the quoted string.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample6.vbhtml)]

<span data-ttu-id="83f20-151">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-151">The result displayed in a browser:</span></span>

![Razor-Img4](introducing-razor-syntax-vb/_static/image4.jpg)

### <a name="6-visual-basic-code-is-not-case-sensitive"></a><span data-ttu-id="83f20-153">6. Visual Basic-Code ist nicht in der Groß-/Kleinschreibung unterschieden</span><span class="sxs-lookup"><span data-stu-id="83f20-153">6. Visual Basic code is not case sensitive</span></span>

<span data-ttu-id="83f20-154">Es ist nicht die Sprache Visual Basic Groß-/Kleinschreibung beachtet.</span><span class="sxs-lookup"><span data-stu-id="83f20-154">The Visual Basic language is not case sensitive.</span></span> <span data-ttu-id="83f20-155">Programmierung Schlüsselwörter (z. B. `Dim`, `If`, und `True`) und Variablennamen (z. B. `myString`, oder `subTotal`) können in jedem Fall geschrieben werden.</span><span class="sxs-lookup"><span data-stu-id="83f20-155">Programming keywords (like `Dim`, `If`, and `True`) and variable names (like `myString`, or `subTotal`) can be written in any case.</span></span>

<span data-ttu-id="83f20-156">Die folgenden Codezeilen die Variable einen Wert zuweisen `lastname` mit einem Kleinbuchstaben benennen, und klicken Sie dann den Wert den Variablen auf der Seite mit einem Großbuchstaben Namen ausgeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-156">The following lines of code assign a value to the variable `lastname` using a lowercase name, and then output the variable value to the page using an uppercase name.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample7.vbhtml)]

<span data-ttu-id="83f20-157">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-157">The result displayed in a browser:</span></span>

![vb-syntax-5](introducing-razor-syntax-vb/_static/image5.jpg)

### <a name="7-much-of-your-coding-involves-working-with-objects"></a><span data-ttu-id="83f20-159">7. Viele der Codierung umfasst das Arbeiten mit Objekten</span><span class="sxs-lookup"><span data-stu-id="83f20-159">7. Much of your coding involves working with objects</span></span>

<span data-ttu-id="83f20-160">Ein Objekt darstellt, die Sie programmieren können, mit etwas &#8212; eine Seite, ein Textfeld, eine Datei, ein Bild, einer webanforderung, eine e-Mail-Nachricht, einen Kundendatensatz (Datenbankzeile) usw. Objekte verfügen über Eigenschaften, die beschreiben, ihre Eigenschaften &#8212; ein Text-Box-Objekt verfügt über eine `Text` Eigenschaft Request-Objekt hat eine `Url` -Eigenschaft, um eine e-Mail-Nachricht wurde ein `From` -Eigenschaft und eine Customer-Objekt hat eine `FirstName` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="83f20-160">An object represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics &#8212; a text box object has a `Text` property, a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="83f20-161">Objekte verfügen außerdem über Methoden, die die &quot;Verben&quot; ausführen.</span><span class="sxs-lookup"><span data-stu-id="83f20-161">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="83f20-162">Beispiele für ein Objekt "Datei" `Save` -Methode, ein Image-Objekt `Rotate` -Methode, und ein e-Mail-Objekt `Send` Methode.</span><span class="sxs-lookup"><span data-stu-id="83f20-162">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="83f20-163">Arbeiten Sie werden häufig mit der `Request` -Objekt, das Sie die Informationen wie die Werte des Formulars erhalten Felder auf der Seite (Textfelder, usw.), welche Art von Browser, die URL der Seite, die Identität des Benutzers usw. angefordert. In diesem Beispiel wird gezeigt, wie Eigenschaften für den Zugriff auf die `Request` -Objekt und das Aufrufen der `MapPath` Methode der `Request` -Objekt, das Sie den absoluten Pfad der Seite auf dem Server erhalten:</span><span class="sxs-lookup"><span data-stu-id="83f20-163">You'll often work with the `Request` object, which gives you information like the values of form fields on the page (text boxes, etc.), what type of browser made the request, the URL of the page, the user identity, etc. This example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample8.html)]

<span data-ttu-id="83f20-164">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-164">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-vb/_static/image6.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="83f20-166">8. Sie können Code schreiben, mit der Entscheidungen</span><span class="sxs-lookup"><span data-stu-id="83f20-166">8. You can write code that makes decisions</span></span>

<span data-ttu-id="83f20-167">Ein wichtiges Feature von dynamischen Webseiten ist, dass Sie ermitteln können, was Sie basierend auf Bedingungen.</span><span class="sxs-lookup"><span data-stu-id="83f20-167">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="83f20-168">Die gängigste Methode hierfür ist, mit der `If` Anweisung (und optional `Else` Anweisung).</span><span class="sxs-lookup"><span data-stu-id="83f20-168">The most common way to do this is with the `If` statement (and optional `Else` statement).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample9.vbhtml)]

<span data-ttu-id="83f20-169">Die Anweisung `If IsPost` ist eine schnelle Möglichkeit der Verfassung `If IsPost = True`.</span><span class="sxs-lookup"><span data-stu-id="83f20-169">The statement `If IsPost` is a shorthand way of writing `If IsPost = True`.</span></span> <span data-ttu-id="83f20-170">Zusammen mit `If` Anweisungen, es gibt eine Vielzahl von Methoden zum Testen Bedingungen, wiederholen Sie diesen Schritt Codeblöcke und usw., die beschrieben werden weiter unten in diesem Artikel.</span><span class="sxs-lookup"><span data-stu-id="83f20-170">Along with `If` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="83f20-171">Das Ergebnis in einem Browser angezeigt (nach dem Klicken auf **Absenden**):</span><span class="sxs-lookup"><span data-stu-id="83f20-171">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-vb/_static/image7.jpg)

> [!TIP] 
> 
> <span data-ttu-id="83f20-173">**HTTP-GET und POST-Methoden und die IsPost-Eigenschaft**</span><span class="sxs-lookup"><span data-stu-id="83f20-173">**HTTP GET and POST Methods and the IsPost Property**</span></span>
> 
> <span data-ttu-id="83f20-174">Das Protokoll für Webseiten (HTTP) unterstützt eine beschränkte Anzahl der Methoden (&quot;Verben&quot;), werden verwendet, um die Anforderungen an den Server zu senden.</span><span class="sxs-lookup"><span data-stu-id="83f20-174">The protocol used for web pages (HTTP) supports a very limited number of methods (&quot;verbs&quot;) that are used to make requests to the server.</span></span> <span data-ttu-id="83f20-175">Die zwei gängigsten sind "GET", die zum Lesen einer Seite verwendet wird, und POST, die verwendet wird, um eine Seite zu senden.</span><span class="sxs-lookup"><span data-stu-id="83f20-175">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="83f20-176">Im Allgemeinen zum ersten Mal, das ein Benutzer eine Seite anfordert, GET mithilfe die Seite angefordert.</span><span class="sxs-lookup"><span data-stu-id="83f20-176">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="83f20-177">Wenn der Benutzer in einem Formular ausfüllt und klickt dann auf **Absenden**, der Browser sendet eine POST-Anforderung an den Server.</span><span class="sxs-lookup"><span data-stu-id="83f20-177">If the user fills in a form and then clicks **Submit**, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="83f20-178">Web-Programmierung ist es oft hilfreich zu wissen, ob eine Seite als GET oder POST-Methode angefordert werden, damit Sie wissen, wie auf die Seite zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="83f20-178">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="83f20-179">In ASP.NET Web Pages, die Sie verwenden die `IsPost` Eigenschaft, um festzustellen, ob eine Anforderung eine Get- oder POST-Methode ist.</span><span class="sxs-lookup"><span data-stu-id="83f20-179">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="83f20-180">Wenn die Anforderung einen Beitrag ist der `IsPost` Eigenschaft gibt "true" zurück, und Sie können z. B. das Lesen der Werte von Textfeldern in einem Formular.</span><span class="sxs-lookup"><span data-stu-id="83f20-180">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="83f20-181">Viele Sie sehen Beispiele veranschaulichen das Verarbeiten der Seite unterschiedlich je nach dem Wert des `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="83f20-181">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="83f20-182">Ein einfaches Codebeispiel</span><span class="sxs-lookup"><span data-stu-id="83f20-182">A Simple Code Example</span></span>

<span data-ttu-id="83f20-183">Dieses Verfahren wird gezeigt, wie eine Seite zu erstellen, die grundlegende Programmiertechniken veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="83f20-183">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="83f20-184">Im Beispiel erstellen Sie eine Seite, mit dem Benutzer, die zwei Zahlen eingeben, und fügt sie hinzu und das Ergebnis wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="83f20-184">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="83f20-185">Klicken Sie im Editor, erstellen Sie eine neue Datei, und nennen sie *AddNumbers.vbhtml*.</span><span class="sxs-lookup"><span data-stu-id="83f20-185">In your editor, create a new file and name it *AddNumbers.vbhtml*.</span></span>
2. <span data-ttu-id="83f20-186">Kopieren Sie den folgenden Code und Markup in die Seite, und Ersetzen Sie dabei alle Elemente bereits in der Seite an.</span><span class="sxs-lookup"><span data-stu-id="83f20-186">Copy the following code and markup into the page, replacing anything already in the page.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample10.vbhtml)]

    <span data-ttu-id="83f20-187">Hier sind einige Punkte beachten:</span><span class="sxs-lookup"><span data-stu-id="83f20-187">Here are some things for you to note:</span></span>

    - <span data-ttu-id="83f20-188">Die `@` beginnt die erste Codeblock auf der Seite, und ihm vorausgeht der `totalMessage` Variable eingebettet, das am unteren Rand.</span><span class="sxs-lookup"><span data-stu-id="83f20-188">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable embedded near the bottom.</span></span>
    - <span data-ttu-id="83f20-189">Der Block am oberen Rand der Seite "steht in `Code...End Code`.</span><span class="sxs-lookup"><span data-stu-id="83f20-189">The block at the top of the page is enclosed in `Code...End Code`.</span></span>
    - <span data-ttu-id="83f20-190">Die Variablen `total`, `num1`, `num2`, und `totalMessage` mehrere Nummern und eine Zeichenfolge zu speichern.</span><span class="sxs-lookup"><span data-stu-id="83f20-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="83f20-191">Zugewiesene Wert Literalzeichenfolge der `totalMessage` Variable ist in doppelte Anführungszeichen.</span><span class="sxs-lookup"><span data-stu-id="83f20-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="83f20-192">Da Visual Basic-Code nicht wenn Groß-/Kleinschreibung beachtet, ist die `totalMessage` Variablen am unteren Rand der Seite verwendet wird, muss seinen Namen nur die Schreibweise der Variablendeklaration am oberen Rand der Seite überein.</span><span class="sxs-lookup"><span data-stu-id="83f20-192">Because Visual Basic code is not case sensitive, when the `totalMessage` variable is used near the bottom of the page, its name only needs to match the spelling of the variable declaration at the top of the page.</span></span> <span data-ttu-id="83f20-193">Die Groß-/Kleinschreibung spielt keine Rolle.</span><span class="sxs-lookup"><span data-stu-id="83f20-193">The casing doesn't matter.</span></span>
    - <span data-ttu-id="83f20-194">Der Ausdruck `num1.AsInt()`  +  `num2.AsInt()` wird gezeigt, wie zum Arbeiten mit Objekten und Methoden.</span><span class="sxs-lookup"><span data-stu-id="83f20-194">The expression `num1.AsInt()` + `num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="83f20-195">Die `AsInt` -Methode für jede Variable konvertiert die Zeichenfolge, die von einem Benutzer eingegeben wird, auf eine ganze Zahl (Integer), die hinzugefügt werden können.</span><span class="sxs-lookup"><span data-stu-id="83f20-195">The `AsInt` method on each variable converts the string entered by a user to a whole number (an integer) that can be added.</span></span>
    - <span data-ttu-id="83f20-196">Die `<form>` Tag enthält eine `method="post"` Attribut.</span><span class="sxs-lookup"><span data-stu-id="83f20-196">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="83f20-197">Dies gibt an, wenn der Benutzer klickt auf **hinzufügen**, wird die Seite an den Server mithilfe der HTTP POST-Methode gesendet werden.</span><span class="sxs-lookup"><span data-stu-id="83f20-197">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="83f20-198">Wenn die Seite übermittelt wird, den Code `If IsPost` True ergibt, die bedingte code ausgeführt wird, das Ergebnis der Addition der Zahlen anzeigen.</span><span class="sxs-lookup"><span data-stu-id="83f20-198">When the page is submitted, the code `If IsPost` evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="83f20-199">Speichern Sie die Seite, und führen Sie es in einem Browser.</span><span class="sxs-lookup"><span data-stu-id="83f20-199">Save the page and run it in a browser.</span></span> <span data-ttu-id="83f20-200">(Stellen Sie sicher, dass die Seite ist ausgewählt, der **Dateien** Arbeitsbereich vor der Ausführung.) Geben Sie zwei ganzen Zahlen, und klicken Sie dann auf die **hinzufügen** Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="83f20-200">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span>

    ![Razor-Img7](introducing-razor-syntax-vb/_static/image8.jpg)

## <a name="visual-basic-language-and-syntax"></a><span data-ttu-id="83f20-202">Visual Basic-Sprache und Syntax</span><span class="sxs-lookup"><span data-stu-id="83f20-202">Visual Basic Language and Syntax</span></span>

<span data-ttu-id="83f20-203">Zuvor haben Sie gesehen ein einfaches Beispiel zum Erstellen einer ASP.NET-Webseite und wie Sie HTML-Markup Servercode hinzufügen können.</span><span class="sxs-lookup"><span data-stu-id="83f20-203">Earlier you saw a basic example of how to create an ASP.NET web page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="83f20-204">Hier erfahren Sie die Grundlagen der Verwendung von Visual Basic ASP.NET Servercode mithilfe der Razor-Syntax &#8212;schreiben; d. h. die Programmierung Sprachregeln.</span><span class="sxs-lookup"><span data-stu-id="83f20-204">Here you'll learn the basics of using Visual Basic to write ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="83f20-205">Wenn Sie bereits Erfahrung mit Programmierung (insbesondere, wenn Sie C, C++, c#, Visual Basic oder JavaScript verwendet haben), werden viele der hier Sie lesen vertraut sein.</span><span class="sxs-lookup"><span data-stu-id="83f20-205">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="83f20-206">Sie benötigen wahrscheinlich nur mit wie Markup in WebMatrix-Code hinzugefügt wird vertraut *vbhtml* Dateien.</span><span class="sxs-lookup"><span data-stu-id="83f20-206">You'll probably need to familiarize yourself only with how WebMatrix code is added to markup in *.vbhtml* files.</span></span>

### <a id="BM_CombiningTextMarkupAndCode"></a><span data-ttu-id="83f20-207">Kombinieren von Text, Markup und Code in Codeblöcken</span><span class="sxs-lookup"><span data-stu-id="83f20-207">Combining text, markup, and code in code blocks</span></span>

<span data-ttu-id="83f20-208">In Server-Codeblöcke sollten Sie häufig zur Ausgabe von Text und Markup auf der Seite.</span><span class="sxs-lookup"><span data-stu-id="83f20-208">In server code blocks, you'll often want to output text and markup to the page.</span></span> <span data-ttu-id="83f20-209">Wenn ein servercodeblock Text enthält, die sich nicht Code und, stattdessen gerendert werden soll, ist, muss ASP.NET Code, in die Text unterscheiden können.</span><span class="sxs-lookup"><span data-stu-id="83f20-209">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="83f20-210">Dafür stehen verschiedene Möglichkeiten zur Verfügung:</span><span class="sxs-lookup"><span data-stu-id="83f20-210">There are several ways to do this.</span></span>

- <span data-ttu-id="83f20-211">Schließen Sie den Text in ein HTML-Block-Element, z. B. `<p></p>` oder `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="83f20-211">Enclose the text in an HTML block element like `<p></p>` or `<em></em>`:</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample11.vbhtml)]

    <span data-ttu-id="83f20-212">Das HTML-Element kann Text, zusätzliche HTML-Elemente und Servercode Ausdrücke enthalten.</span><span class="sxs-lookup"><span data-stu-id="83f20-212">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="83f20-213">Wenn ASP.NET sieht das öffnende HTML-Tag (z. B. `<p>`), rendert alles, was das Element und dessen Inhalt als für den Browser (und löst die Servercode Ausdrücke) ist.</span><span class="sxs-lookup"><span data-stu-id="83f20-213">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything the element and its content as is to the browser (and resolves the server-code expressions).</span></span>

- <span data-ttu-id="83f20-214">Verwenden der `@:` Operator oder die `<text>` Element.</span><span class="sxs-lookup"><span data-stu-id="83f20-214">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="83f20-215">Die `@:` gibt eine einzige Codezeile Inhalt, der nur-Text oder HTML-Tags werden nicht übereinstimmende; enthält die `<text>` Element einschließt, mehrere Zeilen ausgegeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-215">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="83f20-216">Diese Optionen eignen sich nicht als Teil der Ausgabe ein HTML-Elements gerendert werden sollen.</span><span class="sxs-lookup"><span data-stu-id="83f20-216">These options are useful when you don't want to render an HTML element as part of the output.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample12.vbhtml)]

    <span data-ttu-id="83f20-217">Im folgenden Beispiel wird die im vorherige Beispiel wiederholt allerdings verwendet ein einzelnes Paar von `<text>` Tags aus, um den Text zum Rendern einschließen.</span><span class="sxs-lookup"><span data-stu-id="83f20-217">The following example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample13.vbhtml)]

    <span data-ttu-id="83f20-218">Im folgenden Beispiel die `<text>` und `</text>` Tags einschließen drei Zeilen, die alle einige nicht enthaltene Text und HTML-Tags, die nicht übereinstimmenden haben (`<br />`), sowie der Servercode und übereinstimmende HTML-Tags.</span><span class="sxs-lookup"><span data-stu-id="83f20-218">In the following example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="83f20-219">Erneut, können Sie auch jede Zeile einzeln mit voranstellen der `@:` Operator; beide Weise funktionieren.</span><span class="sxs-lookup"><span data-stu-id="83f20-219">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample14.vbhtml)]

    > [!NOTE]
    > <span data-ttu-id="83f20-220">Wenn Sie Text Ausgabe wie in diesem Abschnitt &#8212;dargestellt; ein HTML-Element mit dem `@:` -Operator, oder die `<text>` -Element &#8212; ASP.NET die Ausgabe nicht HTML-codiert.</span><span class="sxs-lookup"><span data-stu-id="83f20-220">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="83f20-221">(Wie bereits erwähnt, wird die Ausgabe der Codeausdrücke Server und Server Codeblöcke, die vorangestellt werden von ASP.NET codiert `@`, außer in die Sonderfälle, in diesem Abschnitt erwähnt wurden.)</span><span class="sxs-lookup"><span data-stu-id="83f20-221">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="83f20-222">Whitespace</span><span class="sxs-lookup"><span data-stu-id="83f20-222">Whitespace</span></span>

<span data-ttu-id="83f20-223">Zusätzliche Leerzeichen in einer Anweisung (und außerhalb von einem Zeichenfolgenliteral) wirken sich nicht auf die Anweisung aus:</span><span class="sxs-lookup"><span data-stu-id="83f20-223">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample15.vbhtml)]

### <a name="breaking-long-statements-into-multiple-lines"></a><span data-ttu-id="83f20-224">Lange Anweisungen in mehrere Zeilen aufgeteilt</span><span class="sxs-lookup"><span data-stu-id="83f20-224">Breaking long statements into multiple lines</span></span>

<span data-ttu-id="83f20-225">Mehrere Zeilen eine lange codeanweisung unterbrechen möchten, verwenden Sie den Unterstrich `_` (in Visual Basic wird aufgerufen, die *Fortsetzungszeichen*) nach jeder Zeile des Codes.</span><span class="sxs-lookup"><span data-stu-id="83f20-225">You can break a long code statement into multiple lines by using the underscore character `_` (which in Visual Basic is called the *continuation character*) after each line of code.</span></span> <span data-ttu-id="83f20-226">Fügen Sie um eine Anweisung auf die nächste Zeile unterbrechen, am Ende der Zeile ein Leerzeichen und dann das Fortsetzungszeichen aus.</span><span class="sxs-lookup"><span data-stu-id="83f20-226">To break a statement onto the next line, at the end of the line add a space and then the continuation character.</span></span> <span data-ttu-id="83f20-227">Fortsetzen Sie die Anweisung bei der nächsten Zeile.</span><span class="sxs-lookup"><span data-stu-id="83f20-227">Continue the statement on the next line.</span></span> <span data-ttu-id="83f20-228">Sie können die Anweisungen auf so viele Zeilen wie erforderlich, um die Lesbarkeit zu verbessern umschließen.</span><span class="sxs-lookup"><span data-stu-id="83f20-228">You can wrap statements onto as many lines as you need to improve readability.</span></span> <span data-ttu-id="83f20-229">Die folgenden Anweisungen entsprechen:</span><span class="sxs-lookup"><span data-stu-id="83f20-229">The following statements are the same:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample16.vbhtml)]

<span data-ttu-id="83f20-230">Allerdings können keine Sie eine Zeile in der Mitte ein Zeichenfolgenliteral umschließen.</span><span class="sxs-lookup"><span data-stu-id="83f20-230">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="83f20-231">Im folgende Beispiel funktioniert nicht:</span><span class="sxs-lookup"><span data-stu-id="83f20-231">The following example doesn't work:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample17.vbhtml)]

<span data-ttu-id="83f20-232">Eine lange Zeichenfolge kombinieren, die mehrere Zeilen wie der Code oben umbrochen wird, müssen Sie verwenden die *Verkettungsoperator* (`&`), die Sie später in diesem Artikel sehen.</span><span class="sxs-lookup"><span data-stu-id="83f20-232">To combine a long string that wraps to multiple lines like the above code, you would need to use the *concatenation operator* (`&`), which you'll see later in this article.</span></span>

### <a name="code-comments"></a><span data-ttu-id="83f20-233">Codekommentare</span><span class="sxs-lookup"><span data-stu-id="83f20-233">Code comments</span></span>

<span data-ttu-id="83f20-234">Kommentare können Sie die Anmerkungen zu dieser selbst oder von anderen Benutzern zu lassen.</span><span class="sxs-lookup"><span data-stu-id="83f20-234">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="83f20-235">Kommentare für Razor-Syntax vorangestellt `@*` und enden mit `*@`.</span><span class="sxs-lookup"><span data-stu-id="83f20-235">Razor syntax comments are prefixed with `@*` and end with `*@`.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-vb/samples/sample18.cshtml)]

<span data-ttu-id="83f20-236">In Codeblöcken können Sie die Kommentare der Razor-Syntax, oder Sie können normale Visual Basic-Kommentarzeichen, also ein einfaches Anführungszeichen (`'`) als Präfix für jede Zeile.</span><span class="sxs-lookup"><span data-stu-id="83f20-236">Within code blocks you can use the Razor syntax comments, or you can use ordinary Visual Basic comment character, which is a single quote (`'`) prefixed to each line.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample19.vbhtml)]

## <a name="variables"></a><span data-ttu-id="83f20-237">Variablen</span><span class="sxs-lookup"><span data-stu-id="83f20-237">Variables</span></span>

<span data-ttu-id="83f20-238">Eine Variable ist ein benanntes Objekt, mit denen Sie Daten zu speichern.</span><span class="sxs-lookup"><span data-stu-id="83f20-238">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="83f20-239">Variablen beliebig benennen, aber der Name muss mit einem alphanumerischen Zeichen beginnen und darf keine Leerzeichen oder reservierte Zeichen.</span><span class="sxs-lookup"><span data-stu-id="83f20-239">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span> <span data-ttu-id="83f20-240">Zuvor haben Sie gesehen, ist die Groß-/Kleinschreibung der Buchstaben im Namen einer Variablen in Visual Basic unerheblich.</span><span class="sxs-lookup"><span data-stu-id="83f20-240">In Visual Basic, as you saw earlier, the case of the letters in a variable name doesn't matter.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="83f20-241">Variablen und Datentypen</span><span class="sxs-lookup"><span data-stu-id="83f20-241">Variables and data types</span></span>

<span data-ttu-id="83f20-242">Eine Variable kann einen bestimmten Datentyp besitzen, der angibt, welche Art von Daten in der Variablen gespeichert ist.</span><span class="sxs-lookup"><span data-stu-id="83f20-242">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="83f20-243">Können Sie Zeichenfolgenvariablen, in denen Zeichenfolgenwerte gespeichert haben (z. B. &quot;Hello World&quot;), Ganzzahlvariablen, die ganzzahlige Werte (z. B. 3 oder 79) speichern und Datenvariablen, die Datumswerte in einer Vielzahl von Formaten (z. B. 4/12/2012 oder März 2009 speichern ).</span><span class="sxs-lookup"><span data-stu-id="83f20-243">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="83f20-244">Und es gibt viele andere Datentypen, die Sie verwenden können.</span><span class="sxs-lookup"><span data-stu-id="83f20-244">And there are many other data types you can use.</span></span>

<span data-ttu-id="83f20-245">Sie haben jedoch keinen Typ für eine Variable angeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-245">However, you don't have to specify a type for a variable.</span></span> <span data-ttu-id="83f20-246">In den meisten Fällen können ASP.NET herausfinden, welche basierend auf wie die Daten in die Variable verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="83f20-246">In most cases ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="83f20-247">(Gelegentlich müssen Sie einen Typ angeben, sehen Sie Beispiele, in denen dies "true".)</span><span class="sxs-lookup"><span data-stu-id="83f20-247">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="83f20-248">Um eine Variable zu deklarieren, ohne Angabe eines Typs verwenden `Dim` plus den Variablennamen ein (z. B. `Dim myVar`).</span><span class="sxs-lookup"><span data-stu-id="83f20-248">To declare a variable without specifying a type, use `Dim` plus the variable name (for instance, `Dim myVar`).</span></span> <span data-ttu-id="83f20-249">Um eine Variable mit einem Typ zu deklarieren, verwenden `Dim` plus den Variablennamen ein, gefolgt von `As` , und klicken Sie dann den Typnamen (z. B. `Dim myVar As String`).</span><span class="sxs-lookup"><span data-stu-id="83f20-249">To declare a variable with a type, use `Dim` plus the variable name, followed by `As` and then the type name (for instance, `Dim myVar As String`).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample20.vbhtml)]

<span data-ttu-id="83f20-250">Das folgende Beispiel zeigt einige Inlineausdrücke, die die Variablen in einer Webseite verwenden.</span><span class="sxs-lookup"><span data-stu-id="83f20-250">The following example shows some inline expressions that use the variables in a web page.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample21.vbhtml)]

<span data-ttu-id="83f20-251">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-251">The result displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-vb/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="83f20-253">Konvertieren und Testen von Datentypen</span><span class="sxs-lookup"><span data-stu-id="83f20-253">Converting and testing data types</span></span>

<span data-ttu-id="83f20-254">Obwohl ASP.NET einen Datentyp in der Regel automatisch ermitteln kann, in einigen Fällen kann es nicht.</span><span class="sxs-lookup"><span data-stu-id="83f20-254">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="83f20-255">Aus diesem Grund müssen Sie die ASP.NET unterstützen auf, indem Sie eine explizite Konvertierung durchführen.</span><span class="sxs-lookup"><span data-stu-id="83f20-255">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="83f20-256">Auch wenn Sie keine Typen konvertiert haben, ist es manchmal hilfreich, testen, um festzustellen, welche Art von Daten Sie arbeiten können.</span><span class="sxs-lookup"><span data-stu-id="83f20-256">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="83f20-257">Der häufigste Fall ist, Sie eine Zeichenfolge in einen anderen Typ wie z. B. in eine ganze Zahl oder ein Datum zu konvertieren müssen.</span><span class="sxs-lookup"><span data-stu-id="83f20-257">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="83f20-258">Das folgende Beispiel zeigt einem typischen Fall, in dem eine Zeichenfolge in eine Zahl konvertiert werden muss.</span><span class="sxs-lookup"><span data-stu-id="83f20-258">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample22.vbhtml)]

<span data-ttu-id="83f20-259">– In der Regel eine Benutzereingabe werden Sie als Zeichenfolgen.</span><span class="sxs-lookup"><span data-stu-id="83f20-259">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="83f20-260">Auch wenn Sie den Benutzer zur Eingabe einer Zahl aufgefordert haben und auch wenn sie eine Ziffer eingegeben haben, wenn Benutzereingaben gesendet wird und Sie im Code lesen, sind die Daten im Zeichenfolgenformat.</span><span class="sxs-lookup"><span data-stu-id="83f20-260">Even if you've prompted the user to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="83f20-261">Aus diesem Grund müssen Sie die Zeichenfolge in eine Zahl konvertieren.</span><span class="sxs-lookup"><span data-stu-id="83f20-261">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="83f20-262">Im Beispiel wenn Sie versuchen, die Durchführung von Berechnungen auf den Werten ohne zu konvertieren, führt der folgende Fehler, da ASP.NET die beiden Zeichenfolgen hinzufügen können:</span><span class="sxs-lookup"><span data-stu-id="83f20-262">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

`Cannot implicitly convert type 'string' to 'int'.`

<span data-ttu-id="83f20-263">Um die Werte zu einer ganzen Zahl zu konvertieren, rufen Sie die `AsInt` Methode.</span><span class="sxs-lookup"><span data-stu-id="83f20-263">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="83f20-264">Wenn die Konvertierung erfolgreich ist, können Sie dann die Zahlen hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="83f20-264">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="83f20-265">Die folgende Tabelle enthält einige allgemeine Konvertierung und Test-Methoden für die Variablen an.</span><span class="sxs-lookup"><span data-stu-id="83f20-265">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="83f20-266">**Methode**</span><span class="sxs-lookup"><span data-stu-id="83f20-266">**Method**</span></span> | <span data-ttu-id="83f20-267">**Beschreibung**</span><span class="sxs-lookup"><span data-stu-id="83f20-267">**Description**</span></span> | <span data-ttu-id="83f20-268">**Beispiel**</span><span class="sxs-lookup"><span data-stu-id="83f20-268">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="83f20-269">Konvertiert eine Zeichenfolge, die eine ganze Zahl darstellt (z. B. &quot;593&quot;) in eine ganze Zahl.</span><span class="sxs-lookup"><span data-stu-id="83f20-269">Converts a string that represents a whole number (like &quot;593&quot;) to an integer.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample23.vb)] |
| `AsBool(), IsBool()` | <span data-ttu-id="83f20-270">Konvertiert eine Zeichenfolge wie &quot;"true"&quot; oder &quot;"false"&quot; zu einem Boolean-Typ.</span><span class="sxs-lookup"><span data-stu-id="83f20-270">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample24.vb)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="83f20-271">Konvertiert eine Zeichenfolge, die einen decimal-Wert wie &quot;1.3&quot; oder &quot;7.439&quot; in eine Gleitkommazahl.</span><span class="sxs-lookup"><span data-stu-id="83f20-271">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample25.vb)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="83f20-272">Konvertiert eine Zeichenfolge, die einen decimal-Wert wie &quot;1.3&quot; oder &quot;7.439&quot; in eine Dezimalzahl.</span><span class="sxs-lookup"><span data-stu-id="83f20-272">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="83f20-273">(In ASP.NET ist eine Dezimalzahl genauer als eine Gleitkommazahl.)</span><span class="sxs-lookup"><span data-stu-id="83f20-273">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample26.vb)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="83f20-274">Konvertiert eine Zeichenfolge, die einen Wert für Datum und Uhrzeit der ASP.NET darstellt `DateTime` Typ.</span><span class="sxs-lookup"><span data-stu-id="83f20-274">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample27.vb)] |
| `ToString()` | <span data-ttu-id="83f20-275">Konvertiert von einem beliebigen anderen Datentyp in eine Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="83f20-275">Converts any other data type to a string.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample28.vb)] |

## <a name="operators"></a><span data-ttu-id="83f20-276">Operatoren</span><span class="sxs-lookup"><span data-stu-id="83f20-276">Operators</span></span>

<span data-ttu-id="83f20-277">Ein Operator ist ein Schlüsselwort oder ein Zeichen, die ASP.NET, welche Art von Befehl zum Ausführen in einem Ausdruck mitteilt.</span><span class="sxs-lookup"><span data-stu-id="83f20-277">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="83f20-278">Visual Basic unterstützt viele Operatoren, jedoch müssen Sie nur wenige für den Einstieg in die Entwicklung von ASP.NET Web Pages zu erkennen.</span><span class="sxs-lookup"><span data-stu-id="83f20-278">Visual Basic supports many operators, but you only need to recognize a few to get started developing ASP.NET web pages.</span></span> <span data-ttu-id="83f20-279">In der folgenden Tabelle werden die am häufigsten verwendeten Operatoren zusammengefasst.</span><span class="sxs-lookup"><span data-stu-id="83f20-279">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="83f20-280">**Operator**</span><span class="sxs-lookup"><span data-stu-id="83f20-280">**Operator**</span></span> | <span data-ttu-id="83f20-281">**Beschreibung**</span><span class="sxs-lookup"><span data-stu-id="83f20-281">**Description**</span></span> | <span data-ttu-id="83f20-282">**Beispiele**</span><span class="sxs-lookup"><span data-stu-id="83f20-282">**Examples**</span></span> |
| --- | --- | --- |
| `+ - * /` | <span data-ttu-id="83f20-283">Mathematische Operatoren, die in numerischen Ausdrücken verwendet.</span><span class="sxs-lookup"><span data-stu-id="83f20-283">Math operators used in numerical expressions.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample29.vb)] |
| `=` | <span data-ttu-id="83f20-284">Zuweisung und auf Gleichheit.</span><span class="sxs-lookup"><span data-stu-id="83f20-284">Assignment and equality.</span></span> <span data-ttu-id="83f20-285">Je nach Kontext entweder weist den Wert auf der rechten Seite einer Anweisung mit dem Objekt auf der linken Seite oder die Werte hinsichtlich ihrer Gleichheit überprüft.</span><span class="sxs-lookup"><span data-stu-id="83f20-285">Depending on context, either assigns the value on the right side of a statement to the object on the left side, or checks the values for equality.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample30.vb)] |
| `<>` | <span data-ttu-id="83f20-286">Ungleichheit.</span><span class="sxs-lookup"><span data-stu-id="83f20-286">Inequality.</span></span> <span data-ttu-id="83f20-287">Gibt `True` , wenn die Werte nicht gleich sind.</span><span class="sxs-lookup"><span data-stu-id="83f20-287">Returns `True` if the values are not equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample31.vb)] |
| `< > <= >=` | <span data-ttu-id="83f20-288">Kleiner als, größer als, kleiner als oder gleich und größer als oder gleich.</span><span class="sxs-lookup"><span data-stu-id="83f20-288">Less than, greater than, less than or equal, and greater than or equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample32.vb)] |
| `&` | <span data-ttu-id="83f20-289">Verkettung verwendet wird, um Zeichenfolgen zu verknüpfen.</span><span class="sxs-lookup"><span data-stu-id="83f20-289">Concatenation, which is used to join strings.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample33.vbhtml)] |
| `+= -=` | <span data-ttu-id="83f20-290">Die Inkrement- und Dekrement-Operatoren, die Addition und Subtraktion 1 (bzw.) aus einer Variablen.</span><span class="sxs-lookup"><span data-stu-id="83f20-290">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample34.vb)] |
| `.` | <span data-ttu-id="83f20-291">Punkt.</span><span class="sxs-lookup"><span data-stu-id="83f20-291">Dot.</span></span> <span data-ttu-id="83f20-292">Verwendet, um Objekte und ihre Eigenschaften und Methoden zu unterscheiden.</span><span class="sxs-lookup"><span data-stu-id="83f20-292">Used to distinguish objects and their properties and methods.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample35.vb)] |
| `()` | <span data-ttu-id="83f20-293">Klammern.</span><span class="sxs-lookup"><span data-stu-id="83f20-293">Parentheses.</span></span> <span data-ttu-id="83f20-294">Verwendet, um gruppenausdrücken zum Übergeben von Parametern zu Methoden und den Zugriff auf Member des Arrays und Sammlungen.</span><span class="sxs-lookup"><span data-stu-id="83f20-294">Used to group expressions, to pass parameters to methods, and to access members of arrays and collections.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample36.vbhtml)] |
| `Not` | <span data-ttu-id="83f20-295">Nicht.</span><span class="sxs-lookup"><span data-stu-id="83f20-295">Not.</span></span> <span data-ttu-id="83f20-296">Kehrt einen true-Wert auf "false" und umgekehrt.</span><span class="sxs-lookup"><span data-stu-id="83f20-296">Reverses a true value to false and vice versa.</span></span> <span data-ttu-id="83f20-297">In der Regel als eine schnelle Möglichkeit zum Testen verwendet `False` (d. h. für nicht `True`).</span><span class="sxs-lookup"><span data-stu-id="83f20-297">Typically used as a shorthand way to test for `False` (that is, for not `True`).</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample37.vb)] |
| `AndAlso OrElse` | <span data-ttu-id="83f20-298">Logisches AND und Bedingungen, die verwendet werden, um die Verknüpfung zusammen.</span><span class="sxs-lookup"><span data-stu-id="83f20-298">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample38.vb)] |

## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="83f20-299">Arbeiten mit Datei- und Ordnerpfade in Code</span><span class="sxs-lookup"><span data-stu-id="83f20-299">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="83f20-300">Sie werden häufig in Ihrem Code mit der Datei- und Ordnerpfade arbeiten.</span><span class="sxs-lookup"><span data-stu-id="83f20-300">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="83f20-301">Hier ist ein Beispiel der physischen Ordnerstruktur für eine Website auf, wie er auf dem Entwicklungscomputer angezeigt werden kann:</span><span class="sxs-lookup"><span data-stu-id="83f20-301">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="83f20-302">Hier sind einige wichtige Details über URLs und Pfade:</span><span class="sxs-lookup"><span data-stu-id="83f20-302">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="83f20-303">Eine URL beginnt entweder mit einen Domänennamen (`http://www.example.com`) oder einen Servernamen (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="83f20-303">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="83f20-304">Eine URL entspricht einem physischen Pfad auf einem Hostcomputer.</span><span class="sxs-lookup"><span data-stu-id="83f20-304">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="83f20-305">Beispielsweise `http://myserver` entspricht dem Ordner möglicherweise *C:\websites\mywebsite* auf dem Server.</span><span class="sxs-lookup"><span data-stu-id="83f20-305">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="83f20-306">Ein virtueller Pfad ist die Abkürzung für Pfade im Code ohne den vollständigen Pfad angeben zu müssen.</span><span class="sxs-lookup"><span data-stu-id="83f20-306">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="83f20-307">Es umfasst den Teil einer URL, die die Domäne bzw. den Servernamen folgt.</span><span class="sxs-lookup"><span data-stu-id="83f20-307">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="83f20-308">Bei Verwendung von virtuellen Pfaden können Sie Ihren Code zu einer anderen Domäne oder Server verschieben, ohne zum Aktualisieren der Pfade.</span><span class="sxs-lookup"><span data-stu-id="83f20-308">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="83f20-309">Hier ist ein Beispiel, um die Unterschiede zu verstehen:</span><span class="sxs-lookup"><span data-stu-id="83f20-309">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="83f20-310">Vollständige URL</span><span class="sxs-lookup"><span data-stu-id="83f20-310">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="83f20-311">Servername</span><span class="sxs-lookup"><span data-stu-id="83f20-311">Server name</span></span> | <span data-ttu-id="83f20-312">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="83f20-312">*mycompanyserver*</span></span> |
| <span data-ttu-id="83f20-313">Virtueller Pfad</span><span class="sxs-lookup"><span data-stu-id="83f20-313">Virtual path</span></span> | <span data-ttu-id="83f20-314">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="83f20-314">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="83f20-315">Physischer Pfad</span><span class="sxs-lookup"><span data-stu-id="83f20-315">Physical path</span></span> | <span data-ttu-id="83f20-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="83f20-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="83f20-317">Das virtuelle Stammverzeichnis ist /, wie der Stamm von Laufwerk C: Laufwerk ist \.</span><span class="sxs-lookup"><span data-stu-id="83f20-317">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="83f20-318">(Virtuelle Ordnerpfaden verwenden immer Schrägstriche). Der virtuelle Pfad eines Ordners verfügt nicht über den gleichen Namen wie der physische Ordner haben; Es kann ein Alias sein.</span><span class="sxs-lookup"><span data-stu-id="83f20-318">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="83f20-319">(Auf Produktionsservern, der virtuelle Pfad selten einen genaue physischen Pfad entspricht.)</span><span class="sxs-lookup"><span data-stu-id="83f20-319">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="83f20-320">Bei der Arbeit mit Dateien und Ordnern im Code müssen Sie manchmal verweisen auf den physischen Pfad und in einigen Fällen einen virtuellen Pfad, je nachdem welche Objekte mit dem Sie arbeiten.</span><span class="sxs-lookup"><span data-stu-id="83f20-320">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="83f20-321">ASP.NET bietet Ihnen diese Tools zum Arbeiten mit Datei- und Ordnerpfade in Code: der `Server.MapPath` -Methode, und die `~` Operator und `Href` Methode.</span><span class="sxs-lookup"><span data-stu-id="83f20-321">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="83f20-322">Konvertieren von virtuellen und physischen Pfade: die Server.MapPath-Methode</span><span class="sxs-lookup"><span data-stu-id="83f20-322">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="83f20-323">Der `Server.MapPath` Methode konvertiert einen virtuellen Pfad (z. B. */default.cshtml*) in einen absoluten physischen Pfad (z. B. *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="83f20-323">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="83f20-324">Mithilfe dieser Methode können Sie jederzeit einen vollständigen physischen Pfad.</span><span class="sxs-lookup"><span data-stu-id="83f20-324">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="83f20-325">Ein typisches Beispiel ist beim Lesen oder Schreiben einer Textdatei oder einer Bilddatei auf dem Webserver.</span><span class="sxs-lookup"><span data-stu-id="83f20-325">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="83f20-326">Der absoluten physischen Pfad der Website auf einer hosting-Site-Server in der Regel nicht kennen, damit diese Methode konvertieren des Pfads können Sie kennen, den virtuellen Pfad – auf den entsprechenden Pfad auf dem Server für Sie.</span><span class="sxs-lookup"><span data-stu-id="83f20-326">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="83f20-327">Sie den virtuellen Pfad zu einer Datei oder eines Ordners an die Methode übergeben, und gibt den physischen Pfad zurück:</span><span class="sxs-lookup"><span data-stu-id="83f20-327">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample39.vbhtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="83f20-328">Verweisen auf das virtuelle Stammverzeichnis: der ~-Operator und Href-Methode</span><span class="sxs-lookup"><span data-stu-id="83f20-328">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="83f20-329">In einem *cshtml* oder *vbhtml* -Datei können Sie verweisen, den virtuellen Stamm Pfad mit der `~` Operator.</span><span class="sxs-lookup"><span data-stu-id="83f20-329">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="83f20-330">Dies ist sehr nützlich, da Sie Seiten, an einem Standort navigieren können, und alle Links, die sie zu anderen Seiten enthalten nicht unterbrochen.</span><span class="sxs-lookup"><span data-stu-id="83f20-330">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="83f20-331">Es ist auch hilfreich, für den Fall, dass Sie Ihre Website jemals an einen anderen Speicherort verschieben.</span><span class="sxs-lookup"><span data-stu-id="83f20-331">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="83f20-332">Hier einige Beispiele:</span><span class="sxs-lookup"><span data-stu-id="83f20-332">Here are some examples:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample40.vbhtml)]

<span data-ttu-id="83f20-333">Wenn die Website `http://myserver/myapp`, sieht wie ASP.NET diese Pfade behandelt wird, wenn die Seite ausgeführt wird:</span><span class="sxs-lookup"><span data-stu-id="83f20-333">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="83f20-334">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="83f20-334">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="83f20-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="83f20-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="83f20-336">(Diese Pfade nicht tatsächlich als die Werte der Variablen angezeigt, aber ASP.NET Pfade behandelt, als wäre, was waren ist.)</span><span class="sxs-lookup"><span data-stu-id="83f20-336">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="83f20-337">Sie können die `~` Operator im Servercode (wie oben) und im Markup, wie folgt:</span><span class="sxs-lookup"><span data-stu-id="83f20-337">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample41.html)]

<span data-ttu-id="83f20-338">Verwenden Sie im Markup der `~` Operator, um Pfade zu Ressourcen, wie Bilddateien, andere Webseiten und CSS-Dateien zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="83f20-338">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="83f20-339">Wenn die Seite ausgeführt wird, über die Seite "" (Code und Markup) sucht und behebt alle ASP.NET die `~` Verweise auf den entsprechenden Pfad.</span><span class="sxs-lookup"><span data-stu-id="83f20-339">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="83f20-340">Bedingter Logik und Schleifen</span><span class="sxs-lookup"><span data-stu-id="83f20-340">Conditional Logic and Loops</span></span>

<span data-ttu-id="83f20-341">Servercode ASP.NET ermöglicht Ihnen die Durchführung von Aufgaben basierend auf Bedingungen und Schreiben Sie Code, durch die Anweisung wiederholt, die eine bestimmte Anzahl von Malen, also code, der eine Schleife ausgeführt wird).</span><span class="sxs-lookup"><span data-stu-id="83f20-341">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="83f20-342">Testen von Bedingungen</span><span class="sxs-lookup"><span data-stu-id="83f20-342">Testing conditions</span></span>

<span data-ttu-id="83f20-343">Um eine einfache Bedingung zu testen, Sie verwenden, die `If...Then` Anweisung, die zurückkehrt `True` oder `False` anhand eines Tests, die Sie angeben:</span><span class="sxs-lookup"><span data-stu-id="83f20-343">To test a simple condition you use the `If...Then` statement, which returns `True` or `False` based on a test you specify:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample42.vbhtml)]

<span data-ttu-id="83f20-344">Die `If` Schlüsselwort beginnt einen Block.</span><span class="sxs-lookup"><span data-stu-id="83f20-344">The `If` keyword starts a block.</span></span> <span data-ttu-id="83f20-345">Der eigentliche Test (Bedingung) folgt die `If` -Schlüsselwort und gibt "true" oder "false".</span><span class="sxs-lookup"><span data-stu-id="83f20-345">The actual test (condition) follows the `If` keyword and returns true or false.</span></span> <span data-ttu-id="83f20-346">Die `If` Anweisung endet mit `Then`.</span><span class="sxs-lookup"><span data-stu-id="83f20-346">The `If` statement ends with `Then`.</span></span> <span data-ttu-id="83f20-347">Die Anweisungen, die ausgeführt werden, wenn der Test "true" ist durch eingeschlossen sind `If` und `End If`.</span><span class="sxs-lookup"><span data-stu-id="83f20-347">The statements that will run if the test is true are enclosed by `If` and `End If`.</span></span> <span data-ttu-id="83f20-348">Ein `If` Anweisung zählen eine `Else` Datenblock, Anweisungen, die ausgeführt werden, wenn die Bedingung "false" ist:</span><span class="sxs-lookup"><span data-stu-id="83f20-348">An `If` statement can include an `Else` block that specifies statements to run if the condition is false:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample43.vbhtml)]

<span data-ttu-id="83f20-349">Wenn ein `If` Anweisung beginnt einen Codeblock, müssen Sie keine verwenden die normalen `Code...End Code` Anweisungen, um die Blöcke einzuschließen.</span><span class="sxs-lookup"><span data-stu-id="83f20-349">If an `If` statement starts a code block, you don't have to use the normal `Code...End Code` statements to include the blocks.</span></span> <span data-ttu-id="83f20-350">Sie können einfach hinzufügen `@` an den Block, und er funktioniert.</span><span class="sxs-lookup"><span data-stu-id="83f20-350">You can just add `@` to the block, and it will work.</span></span> <span data-ttu-id="83f20-351">Dieser Ansatz funktioniert mit `If` sowie andere Visual Basic-Schlüsselwörter, deren Codeblöcke, einschließlich folgen Programmierung `For`, `For Each`, `Do While`usw.</span><span class="sxs-lookup"><span data-stu-id="83f20-351">This approach works with `If` as well as other Visual Basic programming keywords that are followed by code blocks, including `For`, `For Each`, `Do While`, etc.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample44.vbhtml)]

<span data-ttu-id="83f20-352">Sie können mehrere Bedingungen, die mit einem oder mehreren hinzufügen `ElseIf` blockiert:</span><span class="sxs-lookup"><span data-stu-id="83f20-352">You can add multiple conditions using one or more `ElseIf` blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample45.vbhtml)]

<span data-ttu-id="83f20-353">In diesem Beispiel, wenn das erste Problem, das in der `If` Block ist nicht "true", die `ElseIf` Bedingung aktiviert ist.</span><span class="sxs-lookup"><span data-stu-id="83f20-353">In this example, if the first condition in the `If` block is not true, the `ElseIf` condition is checked.</span></span> <span data-ttu-id="83f20-354">Wenn diese Bedingung erfüllt ist, die Anweisungen in der `ElseIf` Block ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="83f20-354">If that condition is met, the statements in the `ElseIf` block are executed.</span></span> <span data-ttu-id="83f20-355">Wenn keine der Bedingungen erfüllt sind, die Anweisungen in der `Else` Block ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="83f20-355">If none of the conditions are met, the statements in the `Else` block are executed.</span></span> <span data-ttu-id="83f20-356">Sie können eine beliebige Anzahl von hinzufügen `ElseIf` blockiert, und schließen Sie dann mit einer `Else` Blockieren der &quot;alles andere&quot; Bedingung.</span><span class="sxs-lookup"><span data-stu-id="83f20-356">You can add any number of `ElseIf` blocks, and then close with an `Else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="83f20-357">Um eine große Anzahl von Bedingungen zu testen, verwenden Sie eine `Select Case` blockieren:</span><span class="sxs-lookup"><span data-stu-id="83f20-357">To test a large number of conditions, use a `Select Case` block:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample46.vbhtml)]

<span data-ttu-id="83f20-358">Der zu testende Wert steht in Klammern (in dem Beispiel wird die Variable Wochentag).</span><span class="sxs-lookup"><span data-stu-id="83f20-358">The value to test is in parentheses (in the example, the weekday variable).</span></span> <span data-ttu-id="83f20-359">Jeder einzelne Test verwendet einen `Case` -Anweisung, die einen Wert enthält.</span><span class="sxs-lookup"><span data-stu-id="83f20-359">Each individual test uses a `Case` statement that lists a value.</span></span> <span data-ttu-id="83f20-360">Wenn der Wert des einem `Case` Anweisung entspricht dem Testwert, den Code in diesem `Case` Block ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="83f20-360">If the value of a `Case` statement matches the test value, the code in that `Case` block is executed.</span></span>

<span data-ttu-id="83f20-361">Das Ergebnis der letzten beiden bedingte Blöcke in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-361">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-vb/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="83f20-363">Durchlaufen von code</span><span class="sxs-lookup"><span data-stu-id="83f20-363">Looping code</span></span>

<span data-ttu-id="83f20-364">Sie müssen häufig die gleichen Anweisungen wiederholt ausführen.</span><span class="sxs-lookup"><span data-stu-id="83f20-364">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="83f20-365">Dazu müssen Sie Schleifen.</span><span class="sxs-lookup"><span data-stu-id="83f20-365">You do this by looping.</span></span> <span data-ttu-id="83f20-366">Beispielsweise führen Sie häufig die gleichen Anweisungen für jedes Element in einer Auflistung von Daten.</span><span class="sxs-lookup"><span data-stu-id="83f20-366">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="83f20-367">Wenn Sie wissen, genau wie oft Sie in einer Schleife möchten, können Sie eine `For` Schleife.</span><span class="sxs-lookup"><span data-stu-id="83f20-367">If you know exactly how many times you want to loop, you can use a `For` loop.</span></span> <span data-ttu-id="83f20-368">Diese Art von Schleife ist besonders hilfreich für das Zählen von oben oder nach unten zählen:</span><span class="sxs-lookup"><span data-stu-id="83f20-368">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample47.vbhtml)]

<span data-ttu-id="83f20-369">Die Schleife beginnt mit der `For` Schlüsselwort, gefolgt von drei Elementen:</span><span class="sxs-lookup"><span data-stu-id="83f20-369">The loop begins with the `For` keyword, followed by three elements:</span></span>

- <span data-ttu-id="83f20-370">Unmittelbar nach der `For` -Anweisung, deklarieren Sie eine Zählervariable (Sie müssen nicht verwenden `Dim`) und geben Sie den Bereich an, wie in `i = 10 to 20`.</span><span class="sxs-lookup"><span data-stu-id="83f20-370">Immediately after the `For` statement, you declare a counter variable (you don't have to use `Dim`) and then indicate the range, as in `i = 10 to 20`.</span></span> <span data-ttu-id="83f20-371">Dies bedeutet, dass die Variable `i` Zähler bei 10 beginnt und weiterhin bis 20 (inklusiv) erreicht.</span><span class="sxs-lookup"><span data-stu-id="83f20-371">This means the variable `i` will start counting at 10 and continue until it reaches 20 (inclusive).</span></span>
- <span data-ttu-id="83f20-372">Zwischen der `For` und `Next` Anweisungen befindet sich der Inhalt des Blocks.</span><span class="sxs-lookup"><span data-stu-id="83f20-372">Between the `For` and `Next` statements is the content of the block.</span></span> <span data-ttu-id="83f20-373">Dies kann eine oder mehrere Code-Anweisungen mit jeder Schleife enthalten.</span><span class="sxs-lookup"><span data-stu-id="83f20-373">This can contain one or more code statements that execute with each loop.</span></span>
- <span data-ttu-id="83f20-374">Die `Next i` Anweisung wird die Schleife beendet.</span><span class="sxs-lookup"><span data-stu-id="83f20-374">The `Next i` statement ends the loop.</span></span> <span data-ttu-id="83f20-375">Der Zähler wird inkrementiert, und startet die nächste Iteration der Schleife.</span><span class="sxs-lookup"><span data-stu-id="83f20-375">It increments the counter and starts the next iteration of the loop.</span></span>

<span data-ttu-id="83f20-376">Die Zeile des Codes zwischen den `For` und `Next` Zeilen enthält den Code, der für jede Iteration der Schleife ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="83f20-376">The line of code between the `For` and `Next` lines contains the code that runs for each iteration of the loop.</span></span> <span data-ttu-id="83f20-377">Das Markup erstellt einen neuen Absatz (`<p>` Element) jedes Mal und fügt eine Zeile an die Ausgabe des Werts eines i (Leistungsindikator).</span><span class="sxs-lookup"><span data-stu-id="83f20-377">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of i (the counter).</span></span> <span data-ttu-id="83f20-378">Wenn Sie auf dieser Seite ausführen, erstellt das Beispiel 11 Zeilen, die die Ausgabe mit dem Text in jeder Zeile an, mit dem Element angezeigt.</span><span class="sxs-lookup"><span data-stu-id="83f20-378">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-vb/_static/image11.jpg)

<span data-ttu-id="83f20-380">Wenn Sie eine Auflistung oder ein Array verwenden, verwenden Sie häufig eine `For Each` Schleife.</span><span class="sxs-lookup"><span data-stu-id="83f20-380">If you're working with a collection or array, you often use a `For Each` loop.</span></span> <span data-ttu-id="83f20-381">Eine Auflistung ist eine Gruppe von ähnliche Objekte, und die `For Each` Schleife können, Sie aus einer Aufgabe für jedes Element in der Auflistung führen.</span><span class="sxs-lookup"><span data-stu-id="83f20-381">A collection is a group of similar objects, and the `For Each` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="83f20-382">Diese Art der Schleife eignet sich für Auflistungen, da im Gegensatz zu einer `For` Schleifen, Sie müssen keine Erhöhen des Zählerwerts und einzuschränken.</span><span class="sxs-lookup"><span data-stu-id="83f20-382">This type of loop is convenient for collections, because unlike a `For` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="83f20-383">Stattdessen die `For Each` Schleife Code wird einfach in der Auflistung fortgesetzt, bis er abgeschlossen ist.</span><span class="sxs-lookup"><span data-stu-id="83f20-383">Instead, the `For Each` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="83f20-384">In diesem Beispiel gibt die Elemente in der `Request.ServerVariables` Auflistung (enthält Informationen zu Ihrem Webserver).</span><span class="sxs-lookup"><span data-stu-id="83f20-384">This example returns the items in the `Request.ServerVariables` collection (which contains information about your web server).</span></span> <span data-ttu-id="83f20-385">Er verwendet ein `For Each` Schleife, um den Namen der einzelnen Elemente anzeigen, indem Sie beim Erstellen eines neuen `<li>` Element in einem HTML-Aufzählung.</span><span class="sxs-lookup"><span data-stu-id="83f20-385">It uses a `For Each` loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample48.vbhtml)]

<span data-ttu-id="83f20-386">Die `For Each` Schlüsselwort folgt durch eine Variable, die ein einzelnes Element in der Auflistung darstellt (im Beispiel `myItem`), gefolgt von den `In` Schlüsselwort, gefolgt von der Auflistung durchlaufen werden sollen.</span><span class="sxs-lookup"><span data-stu-id="83f20-386">The `For Each` keyword is followed by a variable that represents a single item in the collection (in the example, `myItem`), followed by the `In` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="83f20-387">Im Hauptteil der `For Each` Schleife, können Sie das aktuelle Element über die Variable, die Sie zuvor deklarierte zugreifen.</span><span class="sxs-lookup"><span data-stu-id="83f20-387">In the body of the `For Each` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-vb/_static/image12.jpg)

<span data-ttu-id="83f20-389">Verwenden Sie zum Erstellen einer allgemeinen Schleife die `Do While` Anweisung:</span><span class="sxs-lookup"><span data-stu-id="83f20-389">To create a more general-purpose loop, use the `Do While` statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample49.vbhtml)]

<span data-ttu-id="83f20-390">Diese Schleife beginnt mit der `Do While` Schlüsselwort, gefolgt von einer Bedingung, gefolgt von den Block zu wiederholen.</span><span class="sxs-lookup"><span data-stu-id="83f20-390">This loop begins with the `Do While` keyword, followed by a condition, followed by the block to repeat.</span></span> <span data-ttu-id="83f20-391">Schleifen in der Regel erhöhen (hinzufügen) oder dekrementiert (subtrahiert) eine Variable oder ein Objekt, das für die Inventur verwendet.</span><span class="sxs-lookup"><span data-stu-id="83f20-391">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="83f20-392">Im Beispiel die `+=` Operator fügt 1 auf den Wert einer Variablen bei jeder Ausführung die Schleife.</span><span class="sxs-lookup"><span data-stu-id="83f20-392">In the example, the `+=` operator adds 1 to the value of a variable each time the loop runs.</span></span> <span data-ttu-id="83f20-393">(Um eine Variable in einer Schleife zu verringern, die nach-unten zählt, verwenden Sie die Dekrement-Operators `-=`.)</span><span class="sxs-lookup"><span data-stu-id="83f20-393">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`.)</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="83f20-394">Objekte und Auflistungen</span><span class="sxs-lookup"><span data-stu-id="83f20-394">Objects and Collections</span></span>

<span data-ttu-id="83f20-395">Nahezu alles in einer ASP.NET-Website handelt es sich um ein Objekt, einschließlich der Webseite selbst.</span><span class="sxs-lookup"><span data-stu-id="83f20-395">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="83f20-396">Dieser Abschnitt beschreibt einige wichtige Objekte, die Sie häufig in Ihrem Code arbeiten mit.</span><span class="sxs-lookup"><span data-stu-id="83f20-396">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="83f20-397">Seite "-Objekte</span><span class="sxs-lookup"><span data-stu-id="83f20-397">Page objects</span></span>

<span data-ttu-id="83f20-398">Das grundlegendste Objekt in ASP.NET ist die Seite.</span><span class="sxs-lookup"><span data-stu-id="83f20-398">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="83f20-399">Sie können die Eigenschaften der Page-Objekt direkt ohne alle qualifizierenden Objekt zugreifen.</span><span class="sxs-lookup"><span data-stu-id="83f20-399">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="83f20-400">Der folgende Code Ruft den Dateipfad mit der Seite mit den `Request` Objekt von der Seite ":</span><span class="sxs-lookup"><span data-stu-id="83f20-400">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample50.vbhtml)]

<span data-ttu-id="83f20-401">Können Sie Eigenschaften der `Page` Objekt, eine Vielzahl von Informationen, wie z. B. abgerufen werden:</span><span class="sxs-lookup"><span data-stu-id="83f20-401">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="83f20-402">`Request`</span><span class="sxs-lookup"><span data-stu-id="83f20-402">`Request`.</span></span> <span data-ttu-id="83f20-403">Wie Sie bereits gesehen haben, ist dies eine Auflistung von Informationen über die aktuelle Anforderung, z. B., welche Art von Browser, die URL der Seite, die Identität des Benutzers usw. angefordert.</span><span class="sxs-lookup"><span data-stu-id="83f20-403">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="83f20-404">`Response`</span><span class="sxs-lookup"><span data-stu-id="83f20-404">`Response`.</span></span> <span data-ttu-id="83f20-405">Dies ist eine Auflistung von Informationen über die Antwort (Seite), die an den Browser gesendet wird, wenn der Code ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="83f20-405">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="83f20-406">Beispielsweise können Sie diese Eigenschaft verwenden, um Informationen in die Antwort zu schreiben.</span><span class="sxs-lookup"><span data-stu-id="83f20-406">For example, you can use this property to write information into the response.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample51.vbhtml)]

### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="83f20-407">Auflistungsobjekte (Arrays und Wörterbücher)</span><span class="sxs-lookup"><span data-stu-id="83f20-407">Collection objects (arrays and dictionaries)</span></span>

<span data-ttu-id="83f20-408">Eine Auflistung ist eine Gruppe von Objekten desselben Typs, z. B. eine Auflistung von `Customer` Objekte aus einer Datenbank.</span><span class="sxs-lookup"><span data-stu-id="83f20-408">A collection is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="83f20-409">ASP.NET enthält viele integrierte Auflistungen, wie z. B. die `Request.Files` Auflistung.</span><span class="sxs-lookup"><span data-stu-id="83f20-409">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="83f20-410">Sie werden häufig in Auflistungen mit Daten arbeiten.</span><span class="sxs-lookup"><span data-stu-id="83f20-410">You'll often work with data in collections.</span></span> <span data-ttu-id="83f20-411">Zwei allgemeine Auflistungstypen sind die *Array* und *Wörterbuch*.</span><span class="sxs-lookup"><span data-stu-id="83f20-411">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="83f20-412">Ein Array ist nützlich, wenn Sie eine Auflistung mit ähnlichen Elementen gespeichert werden soll, aber nicht, erstellen Sie eine separate Variable, um jedes Element möchten:</span><span class="sxs-lookup"><span data-stu-id="83f20-412">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample52.vbhtml)]

<span data-ttu-id="83f20-413">Bei Arrays, Sie deklarieren einen bestimmten Datentyp z. B. `String`, `Integer`, oder `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="83f20-413">With arrays, you declare a specific data type, such as `String`, `Integer`, or `DateTime`.</span></span> <span data-ttu-id="83f20-414">Um anzugeben, dass die Variable kann ein Array enthalten, Sie fügen Sie dem Variablennamen in der Deklaration Klammern (z. B. `Dim myVar() As String`).</span><span class="sxs-lookup"><span data-stu-id="83f20-414">To indicate that the variable can contain an array, you add parentheses to the variable name in the declaration (such as `Dim myVar() As String`).</span></span> <span data-ttu-id="83f20-415">Sie erreichen die Elemente in einem Array mit ihrer Position (Index) oder mithilfe der `For Each` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="83f20-415">You can access items in an array using their position (index) or by using the `For Each` statement.</span></span> <span data-ttu-id="83f20-416">Array Indizes sind nullbasiert &#8212; Das heißt, das erste Element ist an position 0, das zweite Element ist an Position 1 und So weiter.</span><span class="sxs-lookup"><span data-stu-id="83f20-416">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample53.vbhtml)]

<span data-ttu-id="83f20-417">Sie können die Anzahl der Elemente in einem Array zu bestimmen, durch Abrufen der `Length` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="83f20-417">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="83f20-418">Die Position eines bestimmten Elements im Array abrufen (d. h., das Array gesucht werden soll), verwenden die `Array.IndexOf` Methode.</span><span class="sxs-lookup"><span data-stu-id="83f20-418">To get the position of a specific item in the array (that is, to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="83f20-419">Sie können auch Aktionen wie Reverse den Inhalt eines Arrays (die `Array.Reverse` Methode) oder Sortieren des Inhalts (die `Array.Sort` Methode).</span><span class="sxs-lookup"><span data-stu-id="83f20-419">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="83f20-420">Die Ausgabe von der String-Array-Code in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="83f20-420">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-vb/_static/image13.jpg)

<span data-ttu-id="83f20-422">Ein Wörterbuch ist eine Auflistung von Schlüssel/Wert-Paaren, in dem Sie den Schlüssel (oder den Namen) festlegen oder Abrufen des entsprechenden Werts eingeben:</span><span class="sxs-lookup"><span data-stu-id="83f20-422">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample54.vbhtml)]

<span data-ttu-id="83f20-423">Um ein Wörterbuch zu erstellen, verwenden Sie die `New` Schlüsselwort, um anzugeben, dass Sie ein neues erstellen `Dictionary` Objekt.</span><span class="sxs-lookup"><span data-stu-id="83f20-423">To create a dictionary, you use the `New` keyword to indicate that you're creating a new `Dictionary` object.</span></span> <span data-ttu-id="83f20-424">Sie können ein Wörterbuch zuweisen, um eine Variable mit dem `Dim` Schlüsselwort.</span><span class="sxs-lookup"><span data-stu-id="83f20-424">You can assign a dictionary to a variable using the `Dim` keyword.</span></span> <span data-ttu-id="83f20-425">Sie geben die Datentypen der Elemente im Wörterbuch mithilfe von Klammern ( `( )` ).</span><span class="sxs-lookup"><span data-stu-id="83f20-425">You indicate the data types of the items in the dictionary using parentheses ( `( )` ).</span></span> <span data-ttu-id="83f20-426">Am Ende der Deklaration müssen Sie einen anderen Klammernpaar, hinzufügen, da es sich tatsächlich um eine Methode handelt, die ein neues Wörterbuch erstellt.</span><span class="sxs-lookup"><span data-stu-id="83f20-426">At the end of the declaration, you must add another pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="83f20-427">Um Elemente zum Wörterbuch hinzuzufügen, rufen Sie die `Add` Methode der Variablen Wörterbuch (`myScores` in diesem Fall), und klicken Sie dann einen Schlüssel und einen Wert angeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-427">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="83f20-428">Alternativ können Sie Klammern verwenden, um den Schlüssel an, und führen Sie eine einfache Zuweisung, wie im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="83f20-428">Alternatively, you can use parentheses to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample55.vbhtml)]

<span data-ttu-id="83f20-429">Um einen Wert aus dem Wörterbuch zu erhalten, geben Sie den Schlüssel in Klammern:</span><span class="sxs-lookup"><span data-stu-id="83f20-429">To get a value from the dictionary, you specify the key in parentheses:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample56.vbhtml)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="83f20-430">Aufrufen von Methoden mit Parametern</span><span class="sxs-lookup"><span data-stu-id="83f20-430">Calling Methods with Parameters</span></span>

<span data-ttu-id="83f20-431">Wie Sie weiter oben in diesem Artikel gesehen haben, verfügen über die Objekte, denen Sie Programmieren mit Methoden aus.</span><span class="sxs-lookup"><span data-stu-id="83f20-431">As you saw earlier in this article, the objects that you program with have methods.</span></span> <span data-ttu-id="83f20-432">Z. B. eine `Database` Objekt möglicherweise ein `Database.Connect` Methode.</span><span class="sxs-lookup"><span data-stu-id="83f20-432">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="83f20-433">Viele Methoden verfügen auch über einen oder mehrere Parameter.</span><span class="sxs-lookup"><span data-stu-id="83f20-433">Many methods also have one or more parameters.</span></span> <span data-ttu-id="83f20-434">Ein *Parameter* ist ein Wert, den Sie an eine Methode übergeben, die Methode zum Abschließen des Tasks zu aktivieren.</span><span class="sxs-lookup"><span data-stu-id="83f20-434">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="83f20-435">Betrachten Sie z. B. eine Deklaration für die `Request.MapPath` Methode, die drei Parameter akzeptiert:</span><span class="sxs-lookup"><span data-stu-id="83f20-435">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-vb[Main](introducing-razor-syntax-vb/samples/sample57.vb)]

<span data-ttu-id="83f20-436">Diese Methode gibt den physischen Pfad auf dem Server, der entspricht einem angegebenen virtuellen Pfad zurück.</span><span class="sxs-lookup"><span data-stu-id="83f20-436">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="83f20-437">Die drei Parameter für die Methode sind `virtualPath`, `baseVirtualDir`, und `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="83f20-437">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="83f20-438">(Beachten Sie, dass in der Deklaration der Parameter mit den Datentypen der Daten aufgeführt sind, die sie akzeptieren.) Wenn Sie diese Methode aufrufen, müssen Sie Werte für alle drei Parameter angeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-438">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="83f20-439">Wenn Sie Visual Basic mit Razor-Syntax verwenden, stehen Ihnen zwei Optionen zum Übergeben von Parametern an eine Methode: *Positionsparameter* oder *benannte Parameter*.</span><span class="sxs-lookup"><span data-stu-id="83f20-439">When you're using Visual Basic with the Razor syntax, you have two options for passing parameters to a method: *positional parameters* or *named parameters*.</span></span> <span data-ttu-id="83f20-440">Zum Aufrufen einer Methode mit positionelle Parameter übergeben Sie die Parameter in einer festen Reihenfolge, die in der Deklaration der Methode angegeben ist.</span><span class="sxs-lookup"><span data-stu-id="83f20-440">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="83f20-441">(Sie würden diese Reihenfolge in der Regel wissen, lesen Sie die Dokumentation für die Methode.) Sie müssen die Reihenfolge einhalten, und Sie können nicht fortfahren, einen der Parameter &#8212; Wenn es sich bei Bedarf können Sie eine leere Zeichenfolge übergeben (`""`) oder null für einen positionelle Parameter, die Sie für einen Wert besitzen.</span><span class="sxs-lookup"><span data-stu-id="83f20-441">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or null for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="83f20-442">Im folgende Beispiel wird angenommen, Sie haben einen Ordner namens *Skripts* auf Ihrer Website.</span><span class="sxs-lookup"><span data-stu-id="83f20-442">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="83f20-443">Der Code Ruft die `Request.MapPath` -Methode auf und übergibt Werte für die drei Parameter in der richtigen Reihenfolge.</span><span class="sxs-lookup"><span data-stu-id="83f20-443">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="83f20-444">Anschließend wird den resultierenden zugeordneten Pfad angezeigt.</span><span class="sxs-lookup"><span data-stu-id="83f20-444">It then displays the resulting mapped path.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample58.vbhtml)]

<span data-ttu-id="83f20-445">Wenn viele Parameter für eine Methode vorhanden sind, können Sie Ihren Code übersichtlichere und besser lesbar behalten, mithilfe von benannten Parametern.</span><span class="sxs-lookup"><span data-stu-id="83f20-445">When there are many parameters for a method, you can keep your code cleaner and more readable by using named parameters.</span></span> <span data-ttu-id="83f20-446">Zum Aufrufen einer Methode, die benannte Parameter verwenden, geben Sie den Parameternamen, gefolgt von `:=` und geben Sie den Wert.</span><span class="sxs-lookup"><span data-stu-id="83f20-446">To call a method using named parameters, specify the parameter name followed by `:=` and then provide the value.</span></span> <span data-ttu-id="83f20-447">Ein Vorteil der benannten Parameter ist, dass Sie sie in beliebiger Reihenfolge hinzufügen können, Sie möchten.</span><span class="sxs-lookup"><span data-stu-id="83f20-447">An advantage of named parameters is that you can add them in any order you want.</span></span> <span data-ttu-id="83f20-448">(Ein Nachteil ist, dass der Methodenaufruf nicht als compact ist).</span><span class="sxs-lookup"><span data-stu-id="83f20-448">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="83f20-449">Im folgenden Beispiel wird die gleiche Methode wie oben beschrieben, aber verwendet benannte Parameter, um die Werte anzugeben:</span><span class="sxs-lookup"><span data-stu-id="83f20-449">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample59.vbhtml)]

<span data-ttu-id="83f20-450">Wie Sie sehen können, werden die Parameter in einer anderen Reihenfolge übergeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-450">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="83f20-451">Wenn Sie im vorherigen Beispiel und in diesem Beispiel ausführen, müssen sie den gleichen Wert zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="83f20-451">However, if you run the previous example and this example, they'll return the same value.</span></span>

## <a name="handling-errors"></a><span data-ttu-id="83f20-452">Behandeln von Fehlern</span><span class="sxs-lookup"><span data-stu-id="83f20-452">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="83f20-453">Try-Catch-Anweisungen</span><span class="sxs-lookup"><span data-stu-id="83f20-453">Try-Catch statements</span></span>

<span data-ttu-id="83f20-454">Sie müssen häufig Anweisungen in Ihrem Code, die außerhalb Ihrer Kontrolle Gründen fehlschlagen.</span><span class="sxs-lookup"><span data-stu-id="83f20-454">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="83f20-455">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="83f20-455">For example:</span></span>

- <span data-ttu-id="83f20-456">Wenn Ihr Code versucht zu öffnen, erstellen, lesen oder Schreiben einer Datei alle Arten von Fehlern auftreten können.</span><span class="sxs-lookup"><span data-stu-id="83f20-456">If your code tries to open, create, read, or write a file, all sorts of errors might occur.</span></span> <span data-ttu-id="83f20-457">Die gewünschte Datei ist möglicherweise nicht vorhanden, wird sie möglicherweise gesperrt, der Code möglicherweise nicht über Berechtigungen verfügen, und so weiter.</span><span class="sxs-lookup"><span data-stu-id="83f20-457">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="83f20-458">Auf ähnliche Weise, wenn Ihr Code versucht, zum Aktualisieren von Datensätzen in einer Datenbank, treten möglicherweise Berechtigungsprobleme, kann die Verbindung mit der Datenbank gelöscht werden, die zu speichernden Daten möglicherweise ungültige usw.</span><span class="sxs-lookup"><span data-stu-id="83f20-458">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="83f20-459">Programmiertechnisch gesehen Situationen heißen *Ausnahmen*.</span><span class="sxs-lookup"><span data-stu-id="83f20-459">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="83f20-460">Wenn im Code eine Ausnahme auftritt, generiert er (auslöst) eine Fehlermeldung angezeigt, d. h., bestenfalls störende für Benutzer.</span><span class="sxs-lookup"><span data-stu-id="83f20-460">If your code encounters an exception, it generates (throws) an error message that is, at best, annoying to users.</span></span>

![Razor-Img14](introducing-razor-syntax-vb/_static/image14.jpg)

<span data-ttu-id="83f20-462">In Situationen, in denen Code kann Ausnahmen auftreten, und um Fehlermeldungen dieses Typs zu vermeiden, können Sie `Try/Catch` Anweisungen.</span><span class="sxs-lookup"><span data-stu-id="83f20-462">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `Try/Catch` statements.</span></span> <span data-ttu-id="83f20-463">In der `Try` -Anweisung, führen Sie den Code, der Sie eingecheckt haben.</span><span class="sxs-lookup"><span data-stu-id="83f20-463">In the `Try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="83f20-464">Eine oder mehrere `Catch` -Anweisungen, die Sie suchen können, für bestimmte Fehler (bestimmte Arten von Ausnahmen), die möglicherweise aufgetreten sind.</span><span class="sxs-lookup"><span data-stu-id="83f20-464">In one or more `Catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="83f20-465">Sie können beliebig viele einschließen `Catch` Anweisungen, als Sie benötigen nach Fehlern suchen, die Sie vorhersehen können.</span><span class="sxs-lookup"><span data-stu-id="83f20-465">You can include as many `Catch` statements as you need to look for errors that you're anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="83f20-466">Es wird empfohlen, dass Sie, verwenden vermeiden die `Response.Redirect` Methode in `Try/Catch` Anweisungen, da es einen Ausnahmefehler, auf der Seite verursachen kann.</span><span class="sxs-lookup"><span data-stu-id="83f20-466">We recommend that you avoid using the `Response.Redirect` method in `Try/Catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="83f20-467">Das folgende Beispiel zeigt eine Seite, die bei der ersten Anforderung eine Textdatei erstellt und zeigt dann eine Schaltfläche, die dem Benutzer die Datei öffnen kann.</span><span class="sxs-lookup"><span data-stu-id="83f20-467">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="83f20-468">Im Beispiel verwendet absichtlich einen falscher Dateiname, sodass er eine Ausnahme auslöst.</span><span class="sxs-lookup"><span data-stu-id="83f20-468">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="83f20-469">Der Code enthält `Catch` -Anweisungen für die zwei möglichen Ausnahmen: `FileNotFoundException`, Dies tritt ein, wenn der Dateiname beschädigt ist und `DirectoryNotFoundException`, Dies tritt ein, wenn ASP.NET den Ordner selbst finden kann.</span><span class="sxs-lookup"><span data-stu-id="83f20-469">The code includes `Catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="83f20-470">(Sie können kommentieren Sie eine Anweisung im Beispiel um anzuzeigen, wie er ausgeführt wird, wenn alles ordnungsgemäß funktioniert.)</span><span class="sxs-lookup"><span data-stu-id="83f20-470">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="83f20-471">Wenn Ihr Code die Ausnahme behandelt hat nicht, sehen Sie eine Fehlerseite, wie der vorherige Screenshot.</span><span class="sxs-lookup"><span data-stu-id="83f20-471">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="83f20-472">Allerdings die `Try/Catch` Abschnitt wird verhindert, dass den Benutzer sehen diese Arten von Fehlern.</span><span class="sxs-lookup"><span data-stu-id="83f20-472">However, the `Try/Catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample60.vbhtml)]

## <a name="additional-resources"></a><span data-ttu-id="83f20-473">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="83f20-473">Additional Resources</span></span>

### <a name="reference-documentation"></a><span data-ttu-id="83f20-474">Referenzdokumentation</span><span class="sxs-lookup"><span data-stu-id="83f20-474">Reference Documentation</span></span>

- [<span data-ttu-id="83f20-475">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="83f20-475">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)
- [<span data-ttu-id="83f20-476">Visual Basic-Sprache</span><span class="sxs-lookup"><span data-stu-id="83f20-476">Visual Basic Language</span></span>](https://msdn.microsoft.com/library/2x7h1hfk.aspx)
