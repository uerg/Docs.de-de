---
uid: web-pages/overview/getting-started/introducing-razor-syntax-c
title: Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax (c#) | Microsoft Docs
author: tfitzmac
description: In diesem Kapitel können Sie einen Überblick über die Programmierung mit ASP.NET Web Pages mit Razor-Syntax. ASP.NET ist die Technologie von Microsoft für die Ausführung von dynamischen Web Pa...
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: aa67d304-583b-4bf8-a231-195656cfb587
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-c
msc.type: authoredcontent
ms.openlocfilehash: 430033c06df74cc3661c40ca7f7bd9244cd257c9
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 04/06/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-c"></a><span data-ttu-id="feab8-104">Einführung in ASP.NET-Webprogrammierung mithilfe der Razor-Syntax (c#)</span><span class="sxs-lookup"><span data-stu-id="feab8-104">Introduction to ASP.NET Web Programming Using the Razor Syntax (C#)</span></span>
====================
<span data-ttu-id="feab8-105">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="feab8-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="feab8-106">In diesem Artikel können Sie einen Überblick über die Programmierung mit ASP.NET Web Pages mit Razor-Syntax.</span><span class="sxs-lookup"><span data-stu-id="feab8-106">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax.</span></span> <span data-ttu-id="feab8-107">ASP.NET ist die Technologie von Microsoft für die Ausführung von dynamischen Webseiten auf Webservern.</span><span class="sxs-lookup"><span data-stu-id="feab8-107">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span> <span data-ttu-id="feab8-108">Dieser Artikel konzentriert sich auf die mithilfe der C#-Programmiersprache.</span><span class="sxs-lookup"><span data-stu-id="feab8-108">This articles focuses on using the C# programming language.</span></span>
> 
> <span data-ttu-id="feab8-109">**Lernen Sie**:</span><span class="sxs-lookup"><span data-stu-id="feab8-109">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="feab8-110">Das obere 8 Programmiertipps für die ersten Schritte bei der Programmierung von ASP.NET Web Pages mit Razor-Syntax.</span><span class="sxs-lookup"><span data-stu-id="feab8-110">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="feab8-111">Grundlegende Programmierkonzepte erläutert, die Sie benötigen.</span><span class="sxs-lookup"><span data-stu-id="feab8-111">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="feab8-112">Welche Server ASP.NET-Code und Razor-Syntax ist zu.</span><span class="sxs-lookup"><span data-stu-id="feab8-112">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="feab8-113">Versionen der Software</span><span class="sxs-lookup"><span data-stu-id="feab8-113">Software versions</span></span>
> 
> 
> - <span data-ttu-id="feab8-114">ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="feab8-114">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="feab8-115">Dieses Lernprogramm funktioniert auch mit ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="feab8-115">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="feab8-116">Der obere 8 Tipps für die Programmierung</span><span class="sxs-lookup"><span data-stu-id="feab8-116">The Top 8 Programming Tips</span></span>

<span data-ttu-id="feab8-117">Dieser Abschnitt enthält einige Tipps, die Sie unbedingt benötigen wissen Sie beim Einstieg in ASP.NET Servercode mithilfe der Razor-Syntax schreiben.</span><span class="sxs-lookup"><span data-stu-id="feab8-117">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

> [!NOTE]
> <span data-ttu-id="feab8-118">Die Razor-Syntax basiert auf der C#-Programmiersprache, und das ist die Sprache, die am häufigsten mit ASP.NET Web Pages verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-118">The Razor syntax is based on the C# programming language, and that's the language that's used most often with ASP.NET Web Pages.</span></span> <span data-ttu-id="feab8-119">Die Razor-Syntax unterstützt jedoch auch der Sprache Visual Basic, und die übrigen Funktionen, die Sie sehen, dass Sie auch in Visual Basic ausführen können.</span><span class="sxs-lookup"><span data-stu-id="feab8-119">However, the Razor syntax also supports the Visual Basic language, and everything you see you can also do in Visual Basic.</span></span> <span data-ttu-id="feab8-120">Weitere Informationen finden Sie im Anhang [Visual Basic-Sprache und-Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span><span class="sxs-lookup"><span data-stu-id="feab8-120">For details, see the appendix [Visual Basic Language and Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span></span>


<span data-ttu-id="feab8-121">Sie finden weitere Informationen über die meisten dieser Programmiertechniken später in diesem Artikel.</span><span class="sxs-lookup"><span data-stu-id="feab8-121">You can find more details about most of these programming techniques later in the article.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="feab8-122">1. Fügen Sie Code, um eine Seite mit dem @-Zeichen</span><span class="sxs-lookup"><span data-stu-id="feab8-122">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="feab8-123">Die `@` Inlineausdrücke, einzelne Anweisungsblöcken und mit mehreren Anweisungen Blöcke beginnt:</span><span class="sxs-lookup"><span data-stu-id="feab8-123">The `@` character starts inline expressions, single statement blocks, and multi-statement blocks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample1.html)]

<span data-ttu-id="feab8-124">Dies ist, wie diese Anweisungen aussieht, wenn die Seite in einem Browser ausgeführt wird:</span><span class="sxs-lookup"><span data-stu-id="feab8-124">This is what these statements look like when the page runs in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-c/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="feab8-126">**HTML-Codierung**</span><span class="sxs-lookup"><span data-stu-id="feab8-126">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="feab8-127">Beim Anzeigen von Inhalt in einer Seite mit den `@` Zeichen, wie in den vorherigen Beispielen ASP.NET HTML-Codierung der Ausgabe.</span><span class="sxs-lookup"><span data-stu-id="feab8-127">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="feab8-128">Dies ersetzt reservierte HTML-Zeichen (z. B. `<` und `>` und `&`) mit Codes, mit denen die Zeichen als Zeichen statt als HTML-Tags oder Entitäten interpretiert wird auf einer Webseite angezeigt werden können.</span><span class="sxs-lookup"><span data-stu-id="feab8-128">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="feab8-129">Ohne HTML-Codierung, die Ausgabe aus dem Servercode möglicherweise nicht richtig angezeigt und kann eine Seite, um Sicherheitsrisiken machen.</span><span class="sxs-lookup"><span data-stu-id="feab8-129">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="feab8-130">Wenn Ihr Ziel ist, um HTML-Markup auszugeben, die RFID-Transponder als Markup gerendert wird (z. B. `<p></p>` für einen Absatz oder `<em></em>` Text hervorgehoben), finden Sie im Abschnitt [Kombinieren von Text, Markup und Code in Codeblöcken](#BM_CombiningTextMarkupAndCode) weiter unten in diesem Artikel.</span><span class="sxs-lookup"><span data-stu-id="feab8-130">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="feab8-131">Erfahren Sie mehr über die HTML-Codierung [arbeiten mit Formularen](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="feab8-131">You can read more about HTML encoding in [Working with Forms](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-in-braces"></a><span data-ttu-id="feab8-132">2. Sie einschließen Codeblöcke in Klammern</span><span class="sxs-lookup"><span data-stu-id="feab8-132">2. You enclose code blocks in braces</span></span>

<span data-ttu-id="feab8-133">Ein *Codeblock* enthält eine oder mehrere codeanweisungen und in geschweifte Klammern eingeschlossen ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-133">A *code block* includes one or more code statements and is enclosed in braces.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample2.html)]

<span data-ttu-id="feab8-134">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="feab8-134">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-c/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-semicolon"></a><span data-ttu-id="feab8-136">3. Innerhalb eines Blocks am Ende jeder Anweisung mit einem Semikolon</span><span class="sxs-lookup"><span data-stu-id="feab8-136">3. Inside a block, you end each code statement with a semicolon</span></span>

<span data-ttu-id="feab8-137">Jede Anweisung vollständige Code muss in einem Codeblock mit einem Semikolon enden.</span><span class="sxs-lookup"><span data-stu-id="feab8-137">Inside a code block, each complete code statement must end with a semicolon.</span></span> <span data-ttu-id="feab8-138">Inline-Ausdrücke enden nicht mit einem Semikolon.</span><span class="sxs-lookup"><span data-stu-id="feab8-138">Inline expressions don't end with a semicolon.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample3.html)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="feab8-139">4. Sie können Variablen verwenden, um Werte zu speichern</span><span class="sxs-lookup"><span data-stu-id="feab8-139">4. You use variables to store values</span></span>

<span data-ttu-id="feab8-140">Sie können Werte in Speichern einer *Variable*, einschließlich Zeichenfolgen, Zahlen und Datumsangaben. Sie erstellen eine neue Variable mit dem `var` Schlüsselwort.</span><span class="sxs-lookup"><span data-stu-id="feab8-140">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `var` keyword.</span></span> <span data-ttu-id="feab8-141">Sie können Variablenwerte einfügen, direkt in eine Seite mit `@`.</span><span class="sxs-lookup"><span data-stu-id="feab8-141">You can insert variable values directly in a page using `@`.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample4.html)]

<span data-ttu-id="feab8-142">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="feab8-142">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-c/_static/image3.jpg)

<a id="ID_StringLiterals"></a>
### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="feab8-144">5. Sie setzen literale Zeichenfolgenwerte in Anführungszeichen</span><span class="sxs-lookup"><span data-stu-id="feab8-144">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="feab8-145">Ein *Zeichenfolge* ist eine Folge von Zeichen, die als Text behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-145">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="feab8-146">Um eine Zeichenfolge angeben, schließen Sie ihn in doppelte Anführungszeichen:</span><span class="sxs-lookup"><span data-stu-id="feab8-146">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample5.cshtml)]

<span data-ttu-id="feab8-147">Wenn die Zeichenfolge, die Sie anzeigen möchten, einen umgekehrten Schrägstrich enthält ( `\` ) oder doppelte Anführungszeichen ( `"` ), verwenden eine *wörtliche Zeichenfolgenliteral* , vorangestellt ist die `@` Operator.</span><span class="sxs-lookup"><span data-stu-id="feab8-147">If the string that you want to display contains a backslash character ( `\` ) or double quotation marks ( `"` ), use a *verbatim string literal* that's prefixed with the `@` operator.</span></span> <span data-ttu-id="feab8-148">(In c# ist die \ Zeichen hat besondere Bedeutung, es sei denn, Sie wörtliche Zeichenfolge literal verwenden.)</span><span class="sxs-lookup"><span data-stu-id="feab8-148">(In C#, the \ character has special meaning unless you use a verbatim string literal.)</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample6.html)]

<span data-ttu-id="feab8-149">Einbetten von doppelten Anführungszeichen, ein wörtliches Zeichenfolgenliteral verwenden, und wiederholen die Anführungszeichen:</span><span class="sxs-lookup"><span data-stu-id="feab8-149">To embed double quotation marks, use a verbatim string literal and repeat the quotation marks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample7.html)]

<span data-ttu-id="feab8-150">So sieht das Ergebnis der Verwendung in beiden Beispielen auf einer Seite aus:</span><span class="sxs-lookup"><span data-stu-id="feab8-150">Here's the result of using both of these examples in a page:</span></span>

![Razor-Img4](introducing-razor-syntax-c/_static/image4.jpg)

> [!NOTE]
> <span data-ttu-id="feab8-152">Beachten Sie, dass die `@` Zeichen wird verwendet, um ausführliche zeichenfolgeliterale in c# zu markieren und Markieren von Code in ASP.NET-Seiten.</span><span class="sxs-lookup"><span data-stu-id="feab8-152">Notice that the `@` character is used both to mark verbatim string literals in C# and to mark code in ASP.NET pages.</span></span>


### <a name="6-code-is-case-sensitive"></a><span data-ttu-id="feab8-153">6. Code wird die Groß-/Kleinschreibung unterschieden</span><span class="sxs-lookup"><span data-stu-id="feab8-153">6. Code is case sensitive</span></span>

<span data-ttu-id="feab8-154">C#-Schlüsselwörter (z. B. `var`, `true`, und `if`) und Variablennamen wird nach Groß-/Kleinschreibung beachtet.</span><span class="sxs-lookup"><span data-stu-id="feab8-154">In C#, keywords (like `var`, `true`, and `if`) and variable names are case sensitive.</span></span> <span data-ttu-id="feab8-155">Die folgenden Codezeilen erstellen Sie zwei unterschiedliche Variablen `lastName` und `LastName.`</span><span class="sxs-lookup"><span data-stu-id="feab8-155">The following lines of code create two different variables, `lastName` and `LastName.`</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample8.cshtml)]

<span data-ttu-id="feab8-156">Wenn Sie eine Variable deklarieren `var lastName = "Smith";` und wenn Sie versuchen, diese Variable auf Ihrer Seite als verweisen `@LastName`, ein Fehler verursacht, weil `LastName` wird nicht erkannt werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-156">If you declare a variable as `var lastName = "Smith";` and if you try to reference that variable in your page as `@LastName`, an error results because `LastName` won't be recognized.</span></span>

> [!NOTE]
> <span data-ttu-id="feab8-157">In Visual Basic-Schlüsselwörter und Variablen werden *nicht* Groß-/Kleinschreibung beachtet.</span><span class="sxs-lookup"><span data-stu-id="feab8-157">In Visual Basic, keywords and variables are *not* case sensitive.</span></span>


### <a name="7-much-of-your-coding-involves-objects"></a><span data-ttu-id="feab8-158">7. Viele der Codierung umfasst Objekte</span><span class="sxs-lookup"><span data-stu-id="feab8-158">7. Much of your coding involves objects</span></span>

<span data-ttu-id="feab8-159">Ein *Objekt* darstellt, die Sie programmieren können, mit etwas &#8212; eine Seite, ein Textfeld, eine Datei, ein Bild, einer webanforderung, eine e-Mail-Nachricht, einen Kundendatensatz (Datenbankzeile) usw. Objekte verfügen über Eigenschaften, die ihren Merkmalen beschreiben, sodass Sie lesen oder ändern können &#8212; ein Text-Box-Objekt hat eine `Text` -Eigenschaft (neben anderen), eine Request-Objekt hat eine `Url` -Eigenschaft, um eine e-Mail-Nachricht wurde ein `From` -Eigenschaft eine Customer-Objekt verfügt über eine `FirstName` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="feab8-159">An *object* represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics and that you can read or change &#8212; a text box object has a `Text` property (among others), a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="feab8-160">Objekte verfügen außerdem über Methoden, die die &quot;Verben&quot; ausführen.</span><span class="sxs-lookup"><span data-stu-id="feab8-160">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="feab8-161">Beispiele für ein Objekt "Datei" `Save` -Methode, ein Image-Objekt `Rotate` -Methode, und ein e-Mail-Objekt `Send` Methode.</span><span class="sxs-lookup"><span data-stu-id="feab8-161">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="feab8-162">Arbeiten Sie werden häufig mit den `Request` -Objekt, wo Sie Informationen wie die Werte von Textfeldern (Formularfelder) auf der Seite, welche Art von Browser, die URL der Seite, die Identität des Benutzers usw. angefordert. Im folgende Beispiel wird gezeigt, wie Eigenschaften für den Zugriff auf die `Request` -Objekt und das Aufrufen der `MapPath` Methode der `Request` -Objekt, das Sie den absoluten Pfad der Seite auf dem Server erhalten:</span><span class="sxs-lookup"><span data-stu-id="feab8-162">You'll often work with the `Request` object, which gives you information like the values of text boxes (form fields) on the page, what type of browser made the request, the URL of the page, the user identity, etc. The following example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample9.html)]

<span data-ttu-id="feab8-163">Das Ergebnis in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="feab8-163">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-c/_static/image5.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="feab8-165">8. Sie können Code schreiben, mit der Entscheidungen</span><span class="sxs-lookup"><span data-stu-id="feab8-165">8. You can write code that makes decisions</span></span>

<span data-ttu-id="feab8-166">Ein wichtiges Feature von dynamischen Webseiten ist, dass Sie ermitteln können, was Sie basierend auf Bedingungen.</span><span class="sxs-lookup"><span data-stu-id="feab8-166">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="feab8-167">Die gängigste Methode hierfür ist, mit der `if` Anweisung (und optional `else` Anweisung).</span><span class="sxs-lookup"><span data-stu-id="feab8-167">The most common way to do this is with the `if` statement (and optional `else` statement).</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample10.cshtml)]

<span data-ttu-id="feab8-168">Die Anweisung `if(IsPost)` ist eine schnelle Möglichkeit der Verfassung `if(IsPost == true)`.</span><span class="sxs-lookup"><span data-stu-id="feab8-168">The statement `if(IsPost)` is a shorthand way of writing `if(IsPost == true)`.</span></span> <span data-ttu-id="feab8-169">Zusammen mit `if` Anweisungen, es gibt eine Vielzahl von Methoden zum Testen Bedingungen, wiederholen Sie diesen Schritt Codeblöcke und usw., die beschrieben werden weiter unten in diesem Artikel.</span><span class="sxs-lookup"><span data-stu-id="feab8-169">Along with `if` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="feab8-170">Das Ergebnis in einem Browser angezeigt (nach dem Klicken auf **Absenden**):</span><span class="sxs-lookup"><span data-stu-id="feab8-170">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor-Img6](introducing-razor-syntax-c/_static/image6.jpg)

> [!TIP] 
> 
> <a id="SB_HttpGetPost"></a>
> ### <a name="http-get-and-post-methods-and-the-ispost-property"></a><span data-ttu-id="feab8-172">HTTP-GET und POST-Methoden und die IsPost-Eigenschaft</span><span class="sxs-lookup"><span data-stu-id="feab8-172">HTTP GET and POST Methods and the IsPost Property</span></span>
> 
> <span data-ttu-id="feab8-173">Das Protokoll für Webseiten (HTTP) unterstützt eine beschränkte Anzahl von Methoden (Verben), die verwendet werden, um die Anforderungen an den Server zu senden.</span><span class="sxs-lookup"><span data-stu-id="feab8-173">The protocol used for web pages (HTTP) supports a very limited number of methods (verbs) that are used to make requests to the server.</span></span> <span data-ttu-id="feab8-174">Die zwei gängigsten sind "GET", die zum Lesen einer Seite verwendet wird, und POST, die verwendet wird, um eine Seite zu senden.</span><span class="sxs-lookup"><span data-stu-id="feab8-174">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="feab8-175">Im Allgemeinen zum ersten Mal, das ein Benutzer eine Seite anfordert, GET mithilfe die Seite angefordert.</span><span class="sxs-lookup"><span data-stu-id="feab8-175">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="feab8-176">Wenn der Benutzer in einem Formular ausfüllt und klickt dann auf eine Schaltfläche "Absenden", sendet der Browser eine POST-Anforderung an dem Server an.</span><span class="sxs-lookup"><span data-stu-id="feab8-176">If the user fills in a form and then clicks a submit button, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="feab8-177">Web-Programmierung ist es oft hilfreich zu wissen, ob eine Seite als GET oder POST-Methode angefordert werden, damit Sie wissen, wie auf die Seite zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="feab8-177">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="feab8-178">In ASP.NET Web Pages, die Sie verwenden die `IsPost` Eigenschaft, um festzustellen, ob eine Anforderung eine Get- oder POST-Methode ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-178">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="feab8-179">Wenn die Anforderung einen Beitrag ist der `IsPost` Eigenschaft gibt "true" zurück, und Sie können z. B. das Lesen der Werte von Textfeldern in einem Formular.</span><span class="sxs-lookup"><span data-stu-id="feab8-179">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="feab8-180">Viele Sie sehen Beispiele veranschaulichen das Verarbeiten der Seite unterschiedlich je nach dem Wert des `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="feab8-180">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="feab8-181">Ein einfaches Codebeispiel</span><span class="sxs-lookup"><span data-stu-id="feab8-181">A Simple Code Example</span></span>

<span data-ttu-id="feab8-182">Dieses Verfahren wird gezeigt, wie eine Seite zu erstellen, die grundlegende Programmiertechniken veranschaulicht.</span><span class="sxs-lookup"><span data-stu-id="feab8-182">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="feab8-183">Im Beispiel erstellen Sie eine Seite, mit dem Benutzer, die zwei Zahlen eingeben, und fügt sie hinzu und das Ergebnis wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="feab8-183">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="feab8-184">Klicken Sie im Editor, erstellen Sie eine neue Datei, und nennen sie *AddNumbers.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="feab8-184">In your editor, create a new file and name it *AddNumbers.cshtml*.</span></span>
2. <span data-ttu-id="feab8-185">Kopieren Sie den folgenden Code und Markup in die Seite, und Ersetzen Sie dabei alle Elemente bereits in der Seite an.</span><span class="sxs-lookup"><span data-stu-id="feab8-185">Copy the following code and markup into the page, replacing anything already in the page.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample11.cshtml)]

    <span data-ttu-id="feab8-186">Hier sind einige Punkte beachten:</span><span class="sxs-lookup"><span data-stu-id="feab8-186">Here are some things for you to note:</span></span>

    - <span data-ttu-id="feab8-187">Die `@` beginnt die erste Codeblock auf der Seite, und ihm vorausgeht der `totalMessage` Variable, die am unteren Rand der Seite eingebettet ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-187">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable that's embedded near the bottom of the page.</span></span>
    - <span data-ttu-id="feab8-188">Der Block am oberen Rand der Seite "ist in der geschweiften Klammern eingeschlossen.</span><span class="sxs-lookup"><span data-stu-id="feab8-188">The block at the top of the page is enclosed in braces.</span></span>
    - <span data-ttu-id="feab8-189">Beenden alle Zeilen im-Block oben mit einem Semikolon.</span><span class="sxs-lookup"><span data-stu-id="feab8-189">In the block at the top, all lines end with a semicolon.</span></span>
    - <span data-ttu-id="feab8-190">Die Variablen `total`, `num1`, `num2`, und `totalMessage` mehrere Nummern und eine Zeichenfolge zu speichern.</span><span class="sxs-lookup"><span data-stu-id="feab8-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="feab8-191">Zugewiesene Wert Literalzeichenfolge der `totalMessage` Variable ist in doppelte Anführungszeichen.</span><span class="sxs-lookup"><span data-stu-id="feab8-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="feab8-192">Da der Code Groß-/Kleinschreibung beachtet, wenn die `totalMessage` Variablen am unteren Rand der Seite verwendet wird, muss der Namen der Variablen am Anfang genau entsprechen.</span><span class="sxs-lookup"><span data-stu-id="feab8-192">Because the code is case-sensitive, when the `totalMessage` variable is used near the bottom of the page, its name must match the variable at the top exactly.</span></span>
    - <span data-ttu-id="feab8-193">Der Ausdruck `num1.AsInt() + num2.AsInt()` wird gezeigt, wie zum Arbeiten mit Objekten und Methoden.</span><span class="sxs-lookup"><span data-stu-id="feab8-193">The expression `num1.AsInt() + num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="feab8-194">Die `AsInt` -Methode für jede Variable konvertiert die Zeichenfolge, die von einem Benutzer auf eine Zahl (Integer) eingegeben wird, sodass Sie arithmetische Operationen darauf ausführen können.</span><span class="sxs-lookup"><span data-stu-id="feab8-194">The `AsInt` method on each variable converts the string entered by a user to a number (an integer) so that you can perform arithmetic on it.</span></span>
    - <span data-ttu-id="feab8-195">Die `<form>` Tag enthält eine `method="post"` Attribut.</span><span class="sxs-lookup"><span data-stu-id="feab8-195">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="feab8-196">Dies gibt an, wenn der Benutzer klickt auf **hinzufügen**, wird die Seite an den Server mithilfe der HTTP POST-Methode gesendet werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-196">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="feab8-197">Wenn die Seite übermittelt wird, die `if(IsPost)` Test True ergibt, die bedingte code ausgeführt wird, das Ergebnis der Addition der Zahlen anzeigen.</span><span class="sxs-lookup"><span data-stu-id="feab8-197">When the page is submitted, the `if(IsPost)` test evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="feab8-198">Speichern Sie die Seite, und führen Sie es in einem Browser.</span><span class="sxs-lookup"><span data-stu-id="feab8-198">Save the page and run it in a browser.</span></span> <span data-ttu-id="feab8-199">(Stellen Sie sicher, dass die Seite ist ausgewählt, der **Dateien** Arbeitsbereich vor der Ausführung.) Geben Sie zwei ganzen Zahlen, und klicken Sie dann auf die **hinzufügen** Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="feab8-199">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span> 

    ![Razor-Img7](introducing-razor-syntax-c/_static/image7.jpg)

## <a name="basic-programming-concepts"></a><span data-ttu-id="feab8-201">Grundlegende Programmierkonzepte</span><span class="sxs-lookup"><span data-stu-id="feab8-201">Basic Programming Concepts</span></span>

<span data-ttu-id="feab8-202">Dieser Artikel bietet eine Übersicht über ASP.NET Web programmieren.</span><span class="sxs-lookup"><span data-stu-id="feab8-202">This article provides you with an overview of ASP.NET web programming.</span></span> <span data-ttu-id="feab8-203">Es ist nicht für eine gründlichere Überprüfung, nur einen kurzen Überblick über die Programmierkonzepte erläutert, die am häufigsten verwendet werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-203">It isn't an exhaustive examination, just a quick tour through the programming concepts you'll use most often.</span></span> <span data-ttu-id="feab8-204">Deshalb werden nahezu jede Option, die Sie benötigen, um zu beginnen mit ASP.NET Web Pages behandelt.</span><span class="sxs-lookup"><span data-stu-id="feab8-204">Even so, it covers almost everything you'll need to get started with ASP.NET Web Pages.</span></span>

<span data-ttu-id="feab8-205">Erste jedoch ein wenig technische Hintergrundinformationen.</span><span class="sxs-lookup"><span data-stu-id="feab8-205">But first, a little technical background.</span></span>

### <a name="the-razor-syntax-server-code-and-aspnet"></a><span data-ttu-id="feab8-206">Die Razor-Syntax, Servercode und ASP.NET</span><span class="sxs-lookup"><span data-stu-id="feab8-206">The Razor Syntax, Server Code, and ASP.NET</span></span>

<span data-ttu-id="feab8-207">Razor-Syntax ist eine einfache Programmiersyntax zum Einbetten von serverbasiertem Code in einer Webseite.</span><span class="sxs-lookup"><span data-stu-id="feab8-207">Razor syntax is a simple programming syntax for embedding server-based code in a web page.</span></span> <span data-ttu-id="feab8-208">Auf einer Webseite, die die Razor-Syntax verwendet, es gibt zwei Arten von Inhalten: Clientcode Inhalt und Server.</span><span class="sxs-lookup"><span data-stu-id="feab8-208">In a web page that uses the Razor syntax, there are two kinds of content: client content and server code.</span></span> <span data-ttu-id="feab8-209">Clientinhalt ist die Dinge, die in Webseiten mit sind: HTML-Markup (Elemente), Informationen wie z. B. CSS, Stil vielleicht einige Clientskript, z. B. JavaScript und nur-Text.</span><span class="sxs-lookup"><span data-stu-id="feab8-209">Client content is the stuff you're used to in web pages: HTML markup (elements), style information such as CSS, maybe some client script such as JavaScript, and plain text.</span></span>

<span data-ttu-id="feab8-210">Razor-Syntax können Sie diese Clients Inhalte Servercode hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="feab8-210">Razor syntax lets you add server code to this client content.</span></span> <span data-ttu-id="feab8-211">Ist es Servercode auf der Seite, führt der Server diesen Code zuerst, bevor die Seite an den Browser gesendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-211">If there's server code in the page, the server runs that code first, before it sends the page to the browser.</span></span> <span data-ttu-id="feab8-212">Auf dem Server ausführen, kann der Code Aufgaben ausführen, die viel komplexer mit Clientinhalt eigenständig verwendet, z. B. den Zugriff auf serverbasierte Datenbanken werden können.</span><span class="sxs-lookup"><span data-stu-id="feab8-212">By running on the server, the code can perform tasks that can be a lot more complex to do using client content alone, like accessing server-based databases.</span></span> <span data-ttu-id="feab8-213">Der wichtigste Servercode kann Clientinhalt dynamisch erstellen &#8212; können sie HTML-Markup oder andere Inhalte nebenbei generieren und senden Sie sie an den Browser sowie die statische HTML-Elemente, die die Seite enthalten kann.</span><span class="sxs-lookup"><span data-stu-id="feab8-213">Most importantly, server code can dynamically create client content &#8212; it can generate HTML markup or other content on the fly and then send it to the browser along with any static HTML that the page might contain.</span></span> <span data-ttu-id="feab8-214">Aus Sicht des Browsers unterscheidet sich Clients Inhalte, die von Ihr Servercode generiert wird nicht als andere Client-Inhalt.</span><span class="sxs-lookup"><span data-stu-id="feab8-214">From the browser's perspective, client content that's generated by your server code is no different than any other client content.</span></span> <span data-ttu-id="feab8-215">Wie Sie bereits gesehen haben, ist der Code, der erforderlich ist ganz einfach.</span><span class="sxs-lookup"><span data-stu-id="feab8-215">As you've already seen, the server code that's required is quite simple.</span></span>

<span data-ttu-id="feab8-216">ASP.NET-Webseiten, die die Razor-Syntax enthalten spezielle Dateierweiterung aufweisen (*cshtml* oder *vbhtml*).</span><span class="sxs-lookup"><span data-stu-id="feab8-216">ASP.NET web pages that include the Razor syntax have a special file extension (*.cshtml* or *.vbhtml*).</span></span> <span data-ttu-id="feab8-217">Der Server erkennt diese Erweiterungen, führt den Code, der mit Razor-Syntax markiert ist, und klicken Sie dann die Seite an den Browser sendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-217">The server recognizes these extensions, runs the code that's marked with Razor syntax, and then sends the page to the browser.</span></span>

### <a name="where-does-aspnet-fit-in"></a><span data-ttu-id="feab8-218">Wann ist ASP.NET angebracht?</span><span class="sxs-lookup"><span data-stu-id="feab8-218">Where does ASP.NET fit in?</span></span>

<span data-ttu-id="feab8-219">Razor-Syntax basiert auf eine Technologie von Microsoft ASP.NET,, die wiederum auf Microsoft .NET Framework basiert.</span><span class="sxs-lookup"><span data-stu-id="feab8-219">Razor syntax is based on a technology from Microsoft called ASP.NET, which in turn is based on the Microsoft .NET Framework.</span></span> <span data-ttu-id="feab8-220">.NET Framework ist ein großer, umfassende Programmierframework von Microsoft für nahezu jede Art von Computer-Anwendung entwickeln.</span><span class="sxs-lookup"><span data-stu-id="feab8-220">The.NET Framework is a big, comprehensive programming framework from Microsoft for developing virtually any type of computer application.</span></span> <span data-ttu-id="feab8-221">ASP.NET ist der Teil von .NET Framework, die speziell für das Erstellen von Webanwendungen konzipiert ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-221">ASP.NET is the part of the .NET Framework that's specifically designed for creating web applications.</span></span> <span data-ttu-id="feab8-222">Entwickler haben ASP.NET verwendet, um vielen der größten und höchsten Datenverkehr Websites in der ganzen Welt erstellt.</span><span class="sxs-lookup"><span data-stu-id="feab8-222">Developers have used ASP.NET to create many of the largest and highest-traffic websites in the world.</span></span> <span data-ttu-id="feab8-223">(Jederzeit finden Sie unter der Dateinamenerweiterung *aspx* als Teil der URL, an einem Standort müssen Sie wissen, dass die Website mithilfe von ASP.NET geschrieben wurde.)</span><span class="sxs-lookup"><span data-stu-id="feab8-223">(Any time you see the file-name extension *.aspx* as part of the URL in a site, you'll know that the site was written using ASP.NET.)</span></span>

<span data-ttu-id="feab8-224">Die Razor-Syntax erhalten Sie die gesamte Leistung von ASP.NET, verwendet jedoch eine vereinfachte Syntax, die einfacher zu erfahren, ob Sie Anfänger sind und Sie produktiv stellt, wenn Sie ein Experte sind.</span><span class="sxs-lookup"><span data-stu-id="feab8-224">The Razor syntax gives you all the power of ASP.NET, but using a simplified syntax that's easier to learn if you're a beginner and that makes you more productive if you're an expert.</span></span> <span data-ttu-id="feab8-225">Obwohl diese Syntax einfach zu verwenden ist, bedeutet die Familie Beziehung mit ASP.NET und .NET Framework, wie Ihre Websites komplexer werden, Sie die Leistungsfähigkeit von der größeren Frameworks zur Verfügung haben.</span><span class="sxs-lookup"><span data-stu-id="feab8-225">Even though this syntax is simple to use, its family relationship to ASP.NET and the .NET Framework means that as your websites become more sophisticated, you have the power of the larger frameworks available to you.</span></span>

![Razor-Img8](introducing-razor-syntax-c/_static/image8.jpg)

> [!TIP] 
> 
> <span data-ttu-id="feab8-227">**Klassen und Instanzen**</span><span class="sxs-lookup"><span data-stu-id="feab8-227">**Classes and Instances**</span></span>
> 
> <span data-ttu-id="feab8-228">Servercode ASP.NET verwendet die Objekte, die wiederum auf der Idee von Klassen erstellt werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-228">ASP.NET server code uses objects, which are in turn built on the idea of classes.</span></span> <span data-ttu-id="feab8-229">Die Klasse ist die Definition oder einer Vorlage für ein Objekt.</span><span class="sxs-lookup"><span data-stu-id="feab8-229">The class is the definition or template for an object.</span></span> <span data-ttu-id="feab8-230">Angenommen, eine Anwendung enthält eine `Customer` Klasse, definiert die Eigenschaften und Methoden, die alle Kundenobjekt benötigt.</span><span class="sxs-lookup"><span data-stu-id="feab8-230">For example, an application might contain a `Customer` class that defines the properties and methods that any customer object needs.</span></span>
> 
> <span data-ttu-id="feab8-231">Wenn die Anwendung mit der tatsächlichen Kundeninformationen arbeiten muss, erstellt er eine Instanz von (oder *instanziiert*) ein Customer-Objekt.</span><span class="sxs-lookup"><span data-stu-id="feab8-231">When the application needs to work with actual customer information, it creates an instance of (or *instantiates*) a customer object.</span></span> <span data-ttu-id="feab8-232">Jedes einzelner Kunden ist eine separate Instanz von der `Customer` Klasse.</span><span class="sxs-lookup"><span data-stu-id="feab8-232">Each individual customer is a separate instance of the `Customer` class.</span></span> <span data-ttu-id="feab8-233">Jede Instanz unterstützt die gleichen Eigenschaften und Methoden, aber die Eigenschaftswerte für jede Instanz in der Regel unterschiedlich sind, da jedes Kundenobjekt eindeutig ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-233">Every instance supports the same properties and methods, but the property values for each instance are typically different, because each customer object is unique.</span></span> <span data-ttu-id="feab8-234">In einer Customer-Objekt das `LastName` Eigenschaft möglicherweise "Smith"; in einem anderen Customer-Objekt, das `LastName` Eigenschaft möglicherweise "Jones."</span><span class="sxs-lookup"><span data-stu-id="feab8-234">In one customer object, the `LastName` property might be "Smith"; in another customer object, the `LastName` property might be "Jones."</span></span>
> 
> <span data-ttu-id="feab8-235">Ebenso jeder einzelnen Webseite an Ihrem Standort ist ein `Page` -Objekt, das eine Instanz ist die `Page` Klasse.</span><span class="sxs-lookup"><span data-stu-id="feab8-235">Similarly, any individual web page in your site is a `Page` object that's an instance of the `Page` class.</span></span> <span data-ttu-id="feab8-236">Eine Schaltfläche auf der Seite ist eine `Button` -Objekt, das eine Instanz von der `Button` -Klasse, und So weiter.</span><span class="sxs-lookup"><span data-stu-id="feab8-236">A button on the page is a `Button` object that is an instance of the `Button` class, and so on.</span></span> <span data-ttu-id="feab8-237">Jede Instanz verfügt über eigene Eigenschaften, aber alle basieren auf was in der Klassendefinition für das Objekt angegeben wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-237">Each instance has its own characteristics, but they all are based on what's specified in the object's class definition.</span></span>


## <a name="basic-syntax"></a><span data-ttu-id="feab8-238">Allgemeine Syntax</span><span class="sxs-lookup"><span data-stu-id="feab8-238">Basic Syntax</span></span>

<span data-ttu-id="feab8-239">Zuvor haben Sie gesehen ein einfaches Beispiel zum Erstellen einer ASP.NET Web Pages-Seite, und wie Sie HTML-Markup Servercode hinzufügen können.</span><span class="sxs-lookup"><span data-stu-id="feab8-239">Earlier you saw a basic example of how to create an ASP.NET Web Pages page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="feab8-240">Hier erfahren Sie die Grundlagen zum Schreiben von ASP.NET Servercode mithilfe der Razor-Syntax &#8212; , also programming Sprachregeln.</span><span class="sxs-lookup"><span data-stu-id="feab8-240">Here you'll learn the basics of writing ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="feab8-241">Wenn Sie bereits Erfahrung mit Programmierung (insbesondere, wenn Sie C, C++, c#, Visual Basic oder JavaScript verwendet haben), werden viele der hier Sie lesen vertraut sein.</span><span class="sxs-lookup"><span data-stu-id="feab8-241">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="feab8-242">Sie benötigen wahrscheinlich Kennenlernen nur wie Markup im Servercode hinzugefügt wird *cshtml* Dateien.</span><span class="sxs-lookup"><span data-stu-id="feab8-242">You'll probably need to familiarize yourself only with how server code is added to markup in *.cshtml* files.</span></span>

<a id="BM_CombiningTextMarkupAndCode"></a>
### <a name="combining-text-markup-and-code-in-code-blocks"></a><span data-ttu-id="feab8-243">Kombinieren von Text, Markup und Code in Codeblöcken</span><span class="sxs-lookup"><span data-stu-id="feab8-243">Combining Text, Markup, and Code in Code Blocks</span></span>

<span data-ttu-id="feab8-244">In Server-Codeblöcke möchten Sie häufig Ausgabe Text oder Markup (oder beides) auf der Seite.</span><span class="sxs-lookup"><span data-stu-id="feab8-244">In server code blocks, you often want to output text or markup (or both) to the page.</span></span> <span data-ttu-id="feab8-245">Wenn ein servercodeblock Text enthält, die sich nicht Code und, stattdessen gerendert werden soll, ist, muss ASP.NET Code, in die Text unterscheiden können.</span><span class="sxs-lookup"><span data-stu-id="feab8-245">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="feab8-246">Dafür stehen verschiedene Möglichkeiten zur Verfügung:</span><span class="sxs-lookup"><span data-stu-id="feab8-246">There are several ways to do this.</span></span>

- <span data-ttu-id="feab8-247">Schließen Sie den Text in ein HTML-Element, z. B. `<p></p>` oder `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="feab8-247">Enclose the text in an HTML element like `<p></p>` or `<em></em>`:</span></span>   

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample12.cshtml)]

    <span data-ttu-id="feab8-248">Das HTML-Element kann Text, zusätzliche HTML-Elemente und Servercode Ausdrücke enthalten.</span><span class="sxs-lookup"><span data-stu-id="feab8-248">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="feab8-249">Wenn ASP.NET sieht das öffnende HTML-Tag (z. B. `<p>`), alle Elemente einschließlich des-Elements gerendert und seinen Inhalt als an den Browser, darin Codeausdrücke auf dem Server aufgelöst wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-249">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything including the element and its content as is to the browser, resolving server-code expressions as it goes.</span></span>
- <span data-ttu-id="feab8-250">Verwenden der `@:` Operator oder die `<text>` Element.</span><span class="sxs-lookup"><span data-stu-id="feab8-250">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="feab8-251">Die `@:` gibt eine einzige Codezeile Inhalt, der nur-Text oder HTML-Tags werden nicht übereinstimmende; enthält die `<text>` Element einschließt, mehrere Zeilen ausgegeben.</span><span class="sxs-lookup"><span data-stu-id="feab8-251">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="feab8-252">Diese Optionen eignen sich nicht als Teil der Ausgabe ein HTML-Elements gerendert werden sollen.</span><span class="sxs-lookup"><span data-stu-id="feab8-252">These options are useful when you don't want to render an HTML element as part of the output.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample13.cshtml)]

    <span data-ttu-id="feab8-253">Wenn Sie mehrere Zeilen Text oder HTML-Tags, die nicht übereinstimmenden ausgeben möchten, können Sie jede Zeile mit voranstellen `@:`, oder Sie können einschließen, die Zeile in einer `<text>` Element.</span><span class="sxs-lookup"><span data-stu-id="feab8-253">If you want to output multiple lines of text or unmatched HTML tags, you can precede each line with `@:`, or you can enclose the line in a `<text>` element.</span></span> <span data-ttu-id="feab8-254">Wie die `@:` Operator`<text>` Tags von ASP.NET zum Identifizieren von Textinhalt verwendet werden und werden nie in der Seitenausgabe gerendert.</span><span class="sxs-lookup"><span data-stu-id="feab8-254">Like the `@:` operator,`<text>` tags are used by ASP.NET to identify text content and are never rendered in the page output.</span></span>

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample14.cshtml)]

    <span data-ttu-id="feab8-255">Im erste Beispiel wird die im vorherige Beispiel wiederholt aber verwendet ein einzelnes Paar von `<text>` Tags aus, um den Text zum Rendern einschließen.</span><span class="sxs-lookup"><span data-stu-id="feab8-255">The first example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span> <span data-ttu-id="feab8-256">Im zweiten Beispiel die `<text>` und `</text>` Tags einschließen drei Zeilen, die alle einige nicht enthaltene Text und HTML-Tags, die nicht übereinstimmenden haben (`<br />`), sowie der Servercode und übereinstimmende HTML-Tags.</span><span class="sxs-lookup"><span data-stu-id="feab8-256">In the second example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="feab8-257">Erneut, können Sie auch jede Zeile einzeln mit voranstellen der `@:` Operator; beide Weise funktionieren.</span><span class="sxs-lookup"><span data-stu-id="feab8-257">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    > [!NOTE]
    > <span data-ttu-id="feab8-258">Wenn Sie Text ausgeben, wie in diesem Abschnitt dargestellt &#8212; mithilfe eines HTML-Elements der `@:` -Operator, oder die `<text>` Element &#8212; ASP.NET die Ausgabe nicht HTML-codiert.</span><span class="sxs-lookup"><span data-stu-id="feab8-258">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="feab8-259">(Wie bereits erwähnt, wird die Ausgabe der Codeausdrücke Server und Server Codeblöcke, die vorangestellt werden von ASP.NET codiert `@`, außer in die Sonderfälle, in diesem Abschnitt erwähnt wurden.)</span><span class="sxs-lookup"><span data-stu-id="feab8-259">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="feab8-260">Whitespace</span><span class="sxs-lookup"><span data-stu-id="feab8-260">Whitespace</span></span>

<span data-ttu-id="feab8-261">Zusätzliche Leerzeichen in einer Anweisung (und außerhalb von einem Zeichenfolgenliteral) wirken sich nicht auf die Anweisung aus:</span><span class="sxs-lookup"><span data-stu-id="feab8-261">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample15.cshtml)]

<span data-ttu-id="feab8-262">Ein Zeilenumbruch in einer Anweisung hat keine Auswirkungen auf die Anweisung, und Sie können Anweisungen zur besseren Lesbarkeit umbrochen.</span><span class="sxs-lookup"><span data-stu-id="feab8-262">A line break in a statement has no effect on the statement, and you can wrap statements for readability.</span></span> <span data-ttu-id="feab8-263">Die folgenden Anweisungen entsprechen:</span><span class="sxs-lookup"><span data-stu-id="feab8-263">The following statements are the same:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample16.cshtml)]

<span data-ttu-id="feab8-264">Allerdings können keine Sie eine Zeile in der Mitte ein Zeichenfolgenliteral umschließen.</span><span class="sxs-lookup"><span data-stu-id="feab8-264">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="feab8-265">Im folgende Beispiel funktioniert nicht:</span><span class="sxs-lookup"><span data-stu-id="feab8-265">The following example doesn't work:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample17.cshtml)]

<span data-ttu-id="feab8-266">Es gibt zwei Optionen, um eine lange Zeichenfolge zu kombinieren, die mehrere Zeilen wie der Code oben umbrochen wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-266">To combine a long string that wraps to multiple lines like the above code, there are two options.</span></span> <span data-ttu-id="feab8-267">Sie können den Verkettungsoperator (`+`), die Sie später in diesem Artikel sehen.</span><span class="sxs-lookup"><span data-stu-id="feab8-267">You can use the concatenation operator (`+`), which you'll see later in this article.</span></span> <span data-ttu-id="feab8-268">Sie können auch die `@` Zeichen wörtliche Zeichenfolge literal erstellt, wie Sie weiter oben in diesem Artikel gesehen haben.</span><span class="sxs-lookup"><span data-stu-id="feab8-268">You can also use the `@` character to create a verbatim string literal, as you saw earlier in this article.</span></span> <span data-ttu-id="feab8-269">Ausführliche zeichenfolgeliterale kann über mehrere Zeilen umbrochen werden:</span><span class="sxs-lookup"><span data-stu-id="feab8-269">You can break verbatim string literals across lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample18.cshtml)]

### <a name="code-and-markup-comments"></a><span data-ttu-id="feab8-270">Code (und Markup) Kommentare</span><span class="sxs-lookup"><span data-stu-id="feab8-270">Code (and Markup) Comments</span></span>

<span data-ttu-id="feab8-271">Kommentare können Sie die Anmerkungen zu dieser selbst oder von anderen Benutzern zu lassen.</span><span class="sxs-lookup"><span data-stu-id="feab8-271">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="feab8-272">Sie können auch damit deaktivieren (*kommentieren*) einen Abschnitt des Codes oder die Markuperweiterung, die Sie nicht ausgeführt werden soll, aber auf Ihrer Seite wird beibehalten möchten.</span><span class="sxs-lookup"><span data-stu-id="feab8-272">They also allow you to disable (*comment out*) a section of code or markup that you don't want to run but want to keep in your page for the time being.</span></span>

<span data-ttu-id="feab8-273">Es unterscheidet sich kommentieren die Syntax für Razor-Code und HTML-Markup.</span><span class="sxs-lookup"><span data-stu-id="feab8-273">There's different commenting syntax for Razor code and for HTML markup.</span></span> <span data-ttu-id="feab8-274">Wie bei allen Razor-Code Razor Kommentare werden verarbeitet (und dann entfernt) auf dem Server, bevor die Seite an den Browser gesendet wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-274">As with all Razor code, Razor comments are processed (and then removed) on the server before the page is sent to the browser.</span></span> <span data-ttu-id="feab8-275">Aus diesem Grund kann die Kommentierung Razor-Syntax Kommentare in den Code (oder sogar in das Markup) zu konzentrieren, die Sie sehen können, wenn Sie die Datei bearbeiten, aber, die Benutzern nicht angezeigt, selbst im Quellcode für die Seite.</span><span class="sxs-lookup"><span data-stu-id="feab8-275">Therefore, the Razor commenting syntax lets you put comments into the code (or even into the markup) that you can see when you edit the file, but that users don't see, even in the page source.</span></span>

<span data-ttu-id="feab8-276">Für ASP.NET Razor-Kommentare, starten Sie den Kommentar mit `@*` und enden mit `*@`.</span><span class="sxs-lookup"><span data-stu-id="feab8-276">For ASP.NET Razor comments, you start the comment with `@*` and end it with `*@`.</span></span> <span data-ttu-id="feab8-277">Der Kommentar kann auf eine oder mehrere Zeilen:</span><span class="sxs-lookup"><span data-stu-id="feab8-277">The comment can be on one line or multiple lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample19.cshtml)]

<span data-ttu-id="feab8-278">Hier ist ein Kommentar innerhalb eines Codeblocks:</span><span class="sxs-lookup"><span data-stu-id="feab8-278">Here is a comment within a code block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample20.cshtml)]

<span data-ttu-id="feab8-279">Hier wird die gleichen Codeblock, mit der Codezeile auskommentiert, damit er keine ausgeführt werden:</span><span class="sxs-lookup"><span data-stu-id="feab8-279">Here is the same block of code, with the line of code commented out so that it won't run:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample21.cshtml)]

<span data-ttu-id="feab8-280">In einem Codeblock können Sie als Alternative zur Verwendung von Razor-Kommentar-Syntax, die Kommentare Syntax der Programmiersprache verwenden, die Sie, die wie z. B. c# verwenden:</span><span class="sxs-lookup"><span data-stu-id="feab8-280">Inside a code block, as an alternative to using Razor comment syntax, you can use the commenting syntax of the programming language you're using, such as C#:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample22.cshtml)]

<span data-ttu-id="feab8-281">In c# einzeilige Kommentare vorangestellt werden die `//` Zeichen und mehrzeilige Kommentare beginnen mit `/*` und enden mit `*/`.</span><span class="sxs-lookup"><span data-stu-id="feab8-281">In C#, single-line comments are preceded by the `//` characters, and multi-line comments begin with `/*` and end with `*/`.</span></span> <span data-ttu-id="feab8-282">(Wie bei der Razor-Kommentare werden C#-Kommentare nicht im Browser gerendert.)</span><span class="sxs-lookup"><span data-stu-id="feab8-282">(As with Razor comments, C# comments are not rendered to the browser.)</span></span>

<span data-ttu-id="feab8-283">Für Markup wie Sie möglicherweise wissen, können Sie einen HTML-Kommentar erstellen:</span><span class="sxs-lookup"><span data-stu-id="feab8-283">For markup, as you probably know, you can create an HTML comment:</span></span>

[!code-xml[Main](introducing-razor-syntax-c/samples/sample23.xml)]

<span data-ttu-id="feab8-284">Beginnen Sie mit HTML-Kommentare `<!--` Zeichen und enden mit `-->`.</span><span class="sxs-lookup"><span data-stu-id="feab8-284">HTML comments start with `<!--` characters and end with `-->`.</span></span> <span data-ttu-id="feab8-285">HTML-Kommentare können zum umschließen, nicht nur Text, sondern auch alle HTML-Markup, die Sie auf der Seite beibehalten wollen, aber nicht gerendert werden soll.</span><span class="sxs-lookup"><span data-stu-id="feab8-285">You can use HTML comments to surround not only text, but also any HTML markup that you may want to keep in the page but don't want to render.</span></span> <span data-ttu-id="feab8-286">Diese HTML-Kommentar wird den gesamten Inhalt des Tags und die darin enthaltenen Text ausblenden:</span><span class="sxs-lookup"><span data-stu-id="feab8-286">This HTML comment will hide the entire content of the tags and the text they contain:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample24.html)]

<span data-ttu-id="feab8-287">Im Gegensatz zu Razor-Kommentare, HTML-Kommentare *sind* auf der Seite gerendert und dem Benutzer können sie durch Anzeigen der Quellcode für die Seite angezeigt.</span><span class="sxs-lookup"><span data-stu-id="feab8-287">Unlike Razor comments, HTML comments *are* rendered to the page and the user can see them by viewing the page source.</span></span>

<span data-ttu-id="feab8-288">Razor weist Einschränkungen auf geschachtelten Blöcken von c#.</span><span class="sxs-lookup"><span data-stu-id="feab8-288">Razor has limitations on nested blocks of C#.</span></span> <span data-ttu-id="feab8-289">Weitere Informationen finden Sie unter [mit dem Namen C#-Variablen und geschachtelten Blöcke aufgeteilt Code generieren](http://aspnetwebstack.codeplex.com/workitem/1914)</span><span class="sxs-lookup"><span data-stu-id="feab8-289">For more information see [Named C# Variables and Nested Blocks Generate Broken Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span></span>

## <a name="variables"></a><span data-ttu-id="feab8-290">Variablen</span><span class="sxs-lookup"><span data-stu-id="feab8-290">Variables</span></span>

<span data-ttu-id="feab8-291">Eine Variable ist ein benanntes Objekt, mit denen Sie Daten zu speichern.</span><span class="sxs-lookup"><span data-stu-id="feab8-291">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="feab8-292">Variablen beliebig benennen, aber der Name muss mit einem alphanumerischen Zeichen beginnen und darf keine Leerzeichen oder reservierte Zeichen.</span><span class="sxs-lookup"><span data-stu-id="feab8-292">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="feab8-293">Variablen und Datentypen</span><span class="sxs-lookup"><span data-stu-id="feab8-293">Variables and Data Types</span></span>

<span data-ttu-id="feab8-294">Eine Variable kann einen bestimmten Datentyp besitzen, der angibt, welche Art von Daten in der Variablen gespeichert ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-294">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="feab8-295">Können Sie Zeichenfolgenvariablen, in denen Zeichenfolgenwerte gespeichert haben (z. B. &quot;Hello World&quot;), Ganzzahlvariablen, die ganzzahlige Werte (z. B. 3 oder 79) speichern und Datenvariablen, die Datumswerte in einer Vielzahl von Formaten (z. B. 4/12/2012 oder März 2009 speichern ).</span><span class="sxs-lookup"><span data-stu-id="feab8-295">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="feab8-296">Und es gibt viele andere Datentypen, die Sie verwenden können.</span><span class="sxs-lookup"><span data-stu-id="feab8-296">And there are many other data types you can use.</span></span>

<span data-ttu-id="feab8-297">Allerdings müssen Sie in der Regel einen Typ für eine Variable angeben.</span><span class="sxs-lookup"><span data-stu-id="feab8-297">However, you generally don't have to specify a type for a variable.</span></span> <span data-ttu-id="feab8-298">In den meisten Fällen, können ASP.NET herausfinden, welche basierend auf wie die Daten in die Variable verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-298">Most of the time, ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="feab8-299">(Gelegentlich müssen Sie einen Typ angeben, sehen Sie Beispiele, in denen dies "true".)</span><span class="sxs-lookup"><span data-stu-id="feab8-299">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="feab8-300">Sie deklarieren eine Variable mit dem `var` Schlüsselwort (sofern Sie nicht möchten, dass für einen Typ angegeben wird) oder mit dem Namen des Typs:</span><span class="sxs-lookup"><span data-stu-id="feab8-300">You declare a variable using the `var` keyword (if you don't want to specify a type) or by using the name of the type:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample25.cshtml)]

<span data-ttu-id="feab8-301">Das folgende Beispiel zeigt einige typischen Verwendungen von Variablen in einer Webseite:</span><span class="sxs-lookup"><span data-stu-id="feab8-301">The following example shows some typical uses of variables in a web page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample26.cshtml)]

<span data-ttu-id="feab8-302">Wenn Sie die vorherigen Beispielen auf einer Seite kombinieren, sehen Sie diese in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="feab8-302">If you combine the previous examples in a page, you see this displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-c/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="feab8-304">Konvertieren und Testen von Datentypen</span><span class="sxs-lookup"><span data-stu-id="feab8-304">Converting and Testing Data Types</span></span>

<span data-ttu-id="feab8-305">Obwohl ASP.NET einen Datentyp in der Regel automatisch ermitteln kann, in einigen Fällen kann es nicht.</span><span class="sxs-lookup"><span data-stu-id="feab8-305">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="feab8-306">Aus diesem Grund müssen Sie die ASP.NET unterstützen auf, indem Sie eine explizite Konvertierung durchführen.</span><span class="sxs-lookup"><span data-stu-id="feab8-306">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="feab8-307">Auch wenn Sie keine Typen konvertiert haben, ist es manchmal hilfreich, testen, um festzustellen, welche Art von Daten Sie arbeiten können.</span><span class="sxs-lookup"><span data-stu-id="feab8-307">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="feab8-308">Der häufigste Fall ist, Sie eine Zeichenfolge in einen anderen Typ wie z. B. in eine ganze Zahl oder ein Datum zu konvertieren müssen.</span><span class="sxs-lookup"><span data-stu-id="feab8-308">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="feab8-309">Das folgende Beispiel zeigt einem typischen Fall, in dem eine Zeichenfolge in eine Zahl konvertiert werden muss.</span><span class="sxs-lookup"><span data-stu-id="feab8-309">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample27.cshtml)]

<span data-ttu-id="feab8-310">– In der Regel eine Benutzereingabe werden Sie als Zeichenfolgen.</span><span class="sxs-lookup"><span data-stu-id="feab8-310">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="feab8-311">Auch wenn Sie Benutzer zur Eingabe einer Zahl aufgefordert haben und auch wenn sie eine Ziffer eingegeben haben, wenn Benutzereingaben gesendet wird und Sie im Code lesen, sind die Daten im Zeichenfolgenformat.</span><span class="sxs-lookup"><span data-stu-id="feab8-311">Even if you've prompted users to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="feab8-312">Aus diesem Grund müssen Sie die Zeichenfolge in eine Zahl konvertieren.</span><span class="sxs-lookup"><span data-stu-id="feab8-312">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="feab8-313">Im Beispiel wenn Sie versuchen, die Durchführung von Berechnungen auf den Werten ohne zu konvertieren, führt der folgende Fehler, da ASP.NET die beiden Zeichenfolgen hinzufügen können:</span><span class="sxs-lookup"><span data-stu-id="feab8-313">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

<span data-ttu-id="feab8-314">*Typ "Zeichenfolge" in "Int" kann nicht implizit konvertiert werden.*</span><span class="sxs-lookup"><span data-stu-id="feab8-314">*Cannot implicitly convert type 'string' to 'int'.*</span></span>

<span data-ttu-id="feab8-315">Um die Werte zu einer ganzen Zahl zu konvertieren, rufen Sie die `AsInt` Methode.</span><span class="sxs-lookup"><span data-stu-id="feab8-315">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="feab8-316">Wenn die Konvertierung erfolgreich ist, können Sie dann die Zahlen hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="feab8-316">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="feab8-317">Die folgende Tabelle enthält einige allgemeine Konvertierung und Test-Methoden für die Variablen an.</span><span class="sxs-lookup"><span data-stu-id="feab8-317">The following table lists some common conversion and test methods for variables.</span></span>


|   <span data-ttu-id="feab8-318"><strong>Methode</strong></span><span class="sxs-lookup"><span data-stu-id="feab8-318"><strong>Method</strong></span></span>    |                                                                              <span data-ttu-id="feab8-319"><strong>Beschreibung</strong></span><span class="sxs-lookup"><span data-stu-id="feab8-319"><strong>Description</strong></span></span>                                                                              |                         <span data-ttu-id="feab8-320"><strong>Beispiel</strong></span><span class="sxs-lookup"><span data-stu-id="feab8-320"><strong>Example</strong></span></span>                         |
|------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
|      `AsInt(), IsInt()`      |                                                      <span data-ttu-id="feab8-321">Konvertiert eine Zeichenfolge, die eine ganze Zahl (z. B. "593") in eine ganze Zahl darstellt.</span><span class="sxs-lookup"><span data-stu-id="feab8-321">Converts a string that represents a whole number (like "593") to an integer.</span></span>                                                      |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample28.cs)]   |
|     `AsBool(), IsBool()`     |                                                    <span data-ttu-id="feab8-322">Konvertiert eine Zeichenfolge wie &quot;"true"&quot; oder &quot;"false"&quot; zu einem Boolean-Typ.</span><span class="sxs-lookup"><span data-stu-id="feab8-322">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span>                                                     |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample29.cs)]   |
|    `AsFloat(), IsFloat()`    |                                    <span data-ttu-id="feab8-323">Konvertiert eine Zeichenfolge, die einen decimal-Wert wie &quot;1.3&quot; oder &quot;7.439&quot; in eine Gleitkommazahl.</span><span class="sxs-lookup"><span data-stu-id="feab8-323">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span>                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample30.cs)]   |
|  `AsDecimal(), IsDecimal()`  | <span data-ttu-id="feab8-324">Konvertiert eine Zeichenfolge, die einen decimal-Wert wie &quot;1.3&quot; oder &quot;7.439&quot; in eine Dezimalzahl.</span><span class="sxs-lookup"><span data-stu-id="feab8-324">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="feab8-325">(In ASP.NET ist eine Dezimalzahl genauer als eine Gleitkommazahl.)</span><span class="sxs-lookup"><span data-stu-id="feab8-325">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample31.cs)]   |
| `AsDateTime(), IsDateTime()` |                                                <span data-ttu-id="feab8-326">Konvertiert eine Zeichenfolge, die einen Wert für Datum und Uhrzeit der ASP.NET darstellt `DateTime` Typ.</span><span class="sxs-lookup"><span data-stu-id="feab8-326">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span>                                                 |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample32.cs)]   |
|         `ToString()`         |                                                                       <span data-ttu-id="feab8-327">Konvertiert von einem beliebigen anderen Datentyp in eine Zeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="feab8-327">Converts any other data type to a string.</span></span>                                                                        | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample33.js)] |

## <a name="operators"></a><span data-ttu-id="feab8-328">Operatoren</span><span class="sxs-lookup"><span data-stu-id="feab8-328">Operators</span></span>

<span data-ttu-id="feab8-329">Ein Operator ist ein Schlüsselwort oder ein Zeichen, die ASP.NET, welche Art von Befehl zum Ausführen in einem Ausdruck mitteilt.</span><span class="sxs-lookup"><span data-stu-id="feab8-329">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="feab8-330">Der C#-Sprache (und die Razor-Syntax, die darauf basieren) unterstützt viele Operatoren müssen Sie nur wenige für den Einstieg zu erkennen.</span><span class="sxs-lookup"><span data-stu-id="feab8-330">The C# language (and the Razor syntax that's based on it) supports many operators, but you only need to recognize a few to get started.</span></span> <span data-ttu-id="feab8-331">In der folgenden Tabelle werden die am häufigsten verwendeten Operatoren zusammengefasst.</span><span class="sxs-lookup"><span data-stu-id="feab8-331">The following table summarizes the most common operators.</span></span>


|   <span data-ttu-id="feab8-332"><strong>Operator</strong></span><span class="sxs-lookup"><span data-stu-id="feab8-332"><strong>Operator</strong></span></span>    |                                                                     <span data-ttu-id="feab8-333"><strong>Beschreibung</strong></span><span class="sxs-lookup"><span data-stu-id="feab8-333"><strong>Description</strong></span></span>                                                                     |                        <span data-ttu-id="feab8-334"><strong>Beispiele</strong></span><span class="sxs-lookup"><span data-stu-id="feab8-334"><strong>Examples</strong></span></span>                         |
|--------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
|        <span data-ttu-id="feab8-335">`+` `-` `*` `/`</span><span class="sxs-lookup"><span data-stu-id="feab8-335">`+` `-` `*` `/`</span></span>         |                                                            <span data-ttu-id="feab8-336">Mathematische Operatoren, die in numerischen Ausdrücken verwendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-336">Math operators used in numerical expressions.</span></span>                                                             |    [!code-css[Main](introducing-razor-syntax-c/samples/sample34.css)]    |
|              `=`               |                                    <span data-ttu-id="feab8-337">Zuweisung.</span><span class="sxs-lookup"><span data-stu-id="feab8-337">Assignment.</span></span> <span data-ttu-id="feab8-338">Weist den Wert auf der rechten Seite einer Anweisung mit dem Objekt auf der linken Seite an.</span><span class="sxs-lookup"><span data-stu-id="feab8-338">Assigns the value on the right side of a statement to the object on the left side.</span></span>                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample35.cs)]   |
|              `==`              |                      <span data-ttu-id="feab8-339">Gleichheit.</span><span class="sxs-lookup"><span data-stu-id="feab8-339">Equality.</span></span> <span data-ttu-id="feab8-340">Gibt `true` , wenn die Werte gleich sind.</span><span class="sxs-lookup"><span data-stu-id="feab8-340">Returns `true` if the values are equal.</span></span> <span data-ttu-id="feab8-341">(Beachten Sie den Unterschied zwischen der `=` Operator und die `==` Operator.)</span><span class="sxs-lookup"><span data-stu-id="feab8-341">(Notice the distinction between the `=` operator and the `==` operator.)</span></span>                      |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample36.cs)]   |
|              `!=`              |                                                       <span data-ttu-id="feab8-342">Ungleichheit.</span><span class="sxs-lookup"><span data-stu-id="feab8-342">Inequality.</span></span> <span data-ttu-id="feab8-343">Gibt `true` , wenn die Werte nicht gleich sind.</span><span class="sxs-lookup"><span data-stu-id="feab8-343">Returns `true` if the values are not equal.</span></span>                                                        |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample37.cs)]   |
|          `< > <= >=`           |                                               <span data-ttu-id="feab8-344">Kleiner-als, größer-als, kleiner oder gleich und größer oder gleich.</span><span class="sxs-lookup"><span data-stu-id="feab8-344">Less-than, greater-than, less-than-or-equal, and greater-than-or-equal.</span></span>                                                |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample38.cs)]   |
|              `+`               | <span data-ttu-id="feab8-345">Verkettung verwendet wird, um Zeichenfolgen zu verknüpfen.</span><span class="sxs-lookup"><span data-stu-id="feab8-345">Concatenation, which is used to join strings.</span></span> <span data-ttu-id="feab8-346">ASP.NET ist den Unterschied zwischen diesen Operator und dem Additionsoperator basierend auf den Datentyp des Ausdrucks bekannt.</span><span class="sxs-lookup"><span data-stu-id="feab8-346">ASP.NET knows the difference between this operator and the addition operator based on the data type of the expression.</span></span> |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample39.cs)]   |
|           <span data-ttu-id="feab8-347">`+=` `-=`</span><span class="sxs-lookup"><span data-stu-id="feab8-347">`+=` `-=`</span></span>            |                                   <span data-ttu-id="feab8-348">Die Inkrement- und Dekrement-Operatoren, die Addition und Subtraktion 1 (bzw.) aus einer Variablen.</span><span class="sxs-lookup"><span data-stu-id="feab8-348">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span>                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample40.cs)]   |
|              `.`               |                                                  <span data-ttu-id="feab8-349">Punkt.</span><span class="sxs-lookup"><span data-stu-id="feab8-349">Dot.</span></span> <span data-ttu-id="feab8-350">Verwendet, um Objekte und ihre Eigenschaften und Methoden zu unterscheiden.</span><span class="sxs-lookup"><span data-stu-id="feab8-350">Used to distinguish objects and their properties and methods.</span></span>                                                  |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample41.cs)]   |
|              `()`              |                                              <span data-ttu-id="feab8-351">Klammern.</span><span class="sxs-lookup"><span data-stu-id="feab8-351">Parentheses.</span></span> <span data-ttu-id="feab8-352">Gruppierungsausdrücke sowie zum Übergeben von Parametern für Methoden verwendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-352">Used to group expressions and to pass parameters to methods.</span></span>                                               | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample42.js)] |
|              `[]`              |                                                    <span data-ttu-id="feab8-353">Klammern.</span><span class="sxs-lookup"><span data-stu-id="feab8-353">Brackets.</span></span> <span data-ttu-id="feab8-354">Für den Zugriff auf Werte in Arrays oder Auflistungen verwendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-354">Used for accessing values in arrays or collections.</span></span>                                                     |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample43.cs)]   |
|              `!`               |               <span data-ttu-id="feab8-355">Nicht.</span><span class="sxs-lookup"><span data-stu-id="feab8-355">Not.</span></span> <span data-ttu-id="feab8-356">Kehrt eine `true` Wert `false` und umgekehrt.</span><span class="sxs-lookup"><span data-stu-id="feab8-356">Reverses a `true` value to `false` and vice versa.</span></span> <span data-ttu-id="feab8-357">In der Regel als eine schnelle Möglichkeit zum Testen verwendet `false` (d. h. für nicht `true`).</span><span class="sxs-lookup"><span data-stu-id="feab8-357">Typically used as a shorthand way to test for `false` (that is, for not `true`).</span></span>               |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample44.cs)]   |
| <span data-ttu-id="feab8-358">`&&` <code>&#124;&#124;</code></span><span class="sxs-lookup"><span data-stu-id="feab8-358">`&&` <code>&#124;&#124;</code></span></span> |                                                   <span data-ttu-id="feab8-359">Logisches AND und Bedingungen, die verwendet werden, um die Verknüpfung zusammen.</span><span class="sxs-lookup"><span data-stu-id="feab8-359">Logical AND and OR, which are used to link conditions together.</span></span>                                                    |   [!code-csharp[Main](introducing-razor-syntax-c/samples/sample45.cs)]   |

<a id="ID_WorkingWithFileAndFolderPaths"></a>
## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="feab8-360">Arbeiten mit Datei- und Ordnerpfade in Code</span><span class="sxs-lookup"><span data-stu-id="feab8-360">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="feab8-361">Sie werden häufig in Ihrem Code mit der Datei- und Ordnerpfade arbeiten.</span><span class="sxs-lookup"><span data-stu-id="feab8-361">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="feab8-362">Hier ist ein Beispiel der physischen Ordnerstruktur für eine Website auf, wie er auf dem Entwicklungscomputer angezeigt werden kann:</span><span class="sxs-lookup"><span data-stu-id="feab8-362">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="feab8-363">Hier sind einige wichtige Details über URLs und Pfade:</span><span class="sxs-lookup"><span data-stu-id="feab8-363">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="feab8-364">Eine URL beginnt entweder mit einen Domänennamen (`http://www.example.com`) oder einen Servernamen (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="feab8-364">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="feab8-365">Eine URL entspricht einem physischen Pfad auf einem Hostcomputer.</span><span class="sxs-lookup"><span data-stu-id="feab8-365">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="feab8-366">Beispielsweise `http://myserver` entspricht dem Ordner möglicherweise *C:\websites\mywebsite* auf dem Server.</span><span class="sxs-lookup"><span data-stu-id="feab8-366">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="feab8-367">Ein virtueller Pfad ist die Abkürzung für Pfade im Code ohne den vollständigen Pfad angeben zu müssen.</span><span class="sxs-lookup"><span data-stu-id="feab8-367">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="feab8-368">Es umfasst den Teil einer URL, die die Domäne bzw. den Servernamen folgt.</span><span class="sxs-lookup"><span data-stu-id="feab8-368">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="feab8-369">Bei Verwendung von virtuellen Pfaden können Sie Ihren Code zu einer anderen Domäne oder Server verschieben, ohne zum Aktualisieren der Pfade.</span><span class="sxs-lookup"><span data-stu-id="feab8-369">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="feab8-370">Hier ist ein Beispiel, um die Unterschiede zu verstehen:</span><span class="sxs-lookup"><span data-stu-id="feab8-370">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="feab8-371">Vollständige URL</span><span class="sxs-lookup"><span data-stu-id="feab8-371">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="feab8-372">Servername</span><span class="sxs-lookup"><span data-stu-id="feab8-372">Server name</span></span> | <span data-ttu-id="feab8-373">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="feab8-373">*mycompanyserver*</span></span> |
| <span data-ttu-id="feab8-374">Virtueller Pfad</span><span class="sxs-lookup"><span data-stu-id="feab8-374">Virtual path</span></span> | <span data-ttu-id="feab8-375">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="feab8-375">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="feab8-376">Physischer Pfad</span><span class="sxs-lookup"><span data-stu-id="feab8-376">Physical path</span></span> | <span data-ttu-id="feab8-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="feab8-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="feab8-378">Das virtuelle Stammverzeichnis ist /, wie der Stamm von Laufwerk C: Laufwerk ist \.</span><span class="sxs-lookup"><span data-stu-id="feab8-378">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="feab8-379">(Virtuelle Ordnerpfaden verwenden immer Schrägstriche). Der virtuelle Pfad eines Ordners verfügt nicht über den gleichen Namen wie der physische Ordner haben; Es kann ein Alias sein.</span><span class="sxs-lookup"><span data-stu-id="feab8-379">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="feab8-380">(Auf Produktionsservern, der virtuelle Pfad selten einen genaue physischen Pfad entspricht.)</span><span class="sxs-lookup"><span data-stu-id="feab8-380">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="feab8-381">Bei der Arbeit mit Dateien und Ordnern im Code müssen Sie manchmal verweisen auf den physischen Pfad und in einigen Fällen einen virtuellen Pfad, je nachdem welche Objekte mit dem Sie arbeiten.</span><span class="sxs-lookup"><span data-stu-id="feab8-381">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="feab8-382">ASP.NET bietet Ihnen diese Tools zum Arbeiten mit Datei- und Ordnerpfade in Code: der `Server.MapPath` -Methode, und die `~` Operator und `Href` Methode.</span><span class="sxs-lookup"><span data-stu-id="feab8-382">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="feab8-383">Konvertieren von virtuellen und physischen Pfade: die Server.MapPath-Methode</span><span class="sxs-lookup"><span data-stu-id="feab8-383">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="feab8-384">Der `Server.MapPath` Methode konvertiert einen virtuellen Pfad (z. B. */default.cshtml*) in einen absoluten physischen Pfad (z. B. *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="feab8-384">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="feab8-385">Mithilfe dieser Methode können Sie jederzeit einen vollständigen physischen Pfad.</span><span class="sxs-lookup"><span data-stu-id="feab8-385">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="feab8-386">Ein typisches Beispiel ist beim Lesen oder Schreiben einer Textdatei oder einer Bilddatei auf dem Webserver.</span><span class="sxs-lookup"><span data-stu-id="feab8-386">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="feab8-387">Der absoluten physischen Pfad der Website auf einer hosting-Site-Server in der Regel nicht kennen, damit diese Methode konvertieren des Pfads können Sie kennen, den virtuellen Pfad – auf den entsprechenden Pfad auf dem Server für Sie.</span><span class="sxs-lookup"><span data-stu-id="feab8-387">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="feab8-388">Sie den virtuellen Pfad zu einer Datei oder eines Ordners an die Methode übergeben, und gibt den physischen Pfad zurück:</span><span class="sxs-lookup"><span data-stu-id="feab8-388">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample46.cshtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="feab8-389">Verweisen auf das virtuelle Stammverzeichnis: der ~-Operator und Href-Methode</span><span class="sxs-lookup"><span data-stu-id="feab8-389">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="feab8-390">In einem *cshtml* oder *vbhtml* -Datei können Sie verweisen, den virtuellen Stamm Pfad mit der `~` Operator.</span><span class="sxs-lookup"><span data-stu-id="feab8-390">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="feab8-391">Dies ist sehr nützlich, da Sie Seiten, an einem Standort navigieren können, und alle Links, die sie zu anderen Seiten enthalten nicht unterbrochen.</span><span class="sxs-lookup"><span data-stu-id="feab8-391">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="feab8-392">Es ist auch hilfreich, für den Fall, dass Sie Ihre Website jemals an einen anderen Speicherort verschieben.</span><span class="sxs-lookup"><span data-stu-id="feab8-392">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="feab8-393">Hier einige Beispiele:</span><span class="sxs-lookup"><span data-stu-id="feab8-393">Here are some examples:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample47.cshtml)]

<span data-ttu-id="feab8-394">Wenn die Website `http://myserver/myapp`, sieht wie ASP.NET diese Pfade behandelt wird, wenn die Seite ausgeführt wird:</span><span class="sxs-lookup"><span data-stu-id="feab8-394">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="feab8-395">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="feab8-395">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="feab8-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="feab8-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="feab8-397">(Diese Pfade nicht tatsächlich als die Werte der Variablen angezeigt, aber ASP.NET Pfade behandelt, als wäre, was waren ist.)</span><span class="sxs-lookup"><span data-stu-id="feab8-397">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="feab8-398">Sie können die `~` Operator im Servercode (wie oben) und im Markup, wie folgt:</span><span class="sxs-lookup"><span data-stu-id="feab8-398">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample48.html)]

<span data-ttu-id="feab8-399">Verwenden Sie im Markup der `~` Operator, um Pfade zu Ressourcen, wie Bilddateien, andere Webseiten und CSS-Dateien zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="feab8-399">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="feab8-400">Wenn die Seite ausgeführt wird, über die Seite "" (Code und Markup) sucht und behebt alle ASP.NET die `~` Verweise auf den entsprechenden Pfad.</span><span class="sxs-lookup"><span data-stu-id="feab8-400">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="feab8-401">Bedingter Logik und Schleifen</span><span class="sxs-lookup"><span data-stu-id="feab8-401">Conditional Logic and Loops</span></span>

<span data-ttu-id="feab8-402">Servercode für ASP.NET können Sie die Aufgaben, die basierend auf Bedingungen und Schreiben Sie Code, durch die Anweisung wiederholt eine bestimmte Anzahl von Malen (d. h. Code, der eine Schleife ausgeführt wird).</span><span class="sxs-lookup"><span data-stu-id="feab8-402">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times (that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="feab8-403">Testen von Bedingungen</span><span class="sxs-lookup"><span data-stu-id="feab8-403">Testing Conditions</span></span>

<span data-ttu-id="feab8-404">Um eine einfache Bedingung zu testen, Sie verwenden, die `if` -Anweisung, die einen Test gibt "true" oder "false" Grundlage Sie angeben:</span><span class="sxs-lookup"><span data-stu-id="feab8-404">To test a simple condition you use the `if` statement, which returns true or false based on a test you specify:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample49.cshtml)]

<span data-ttu-id="feab8-405">Die `if` Schlüsselwort beginnt einen Block.</span><span class="sxs-lookup"><span data-stu-id="feab8-405">The `if` keyword starts a block.</span></span> <span data-ttu-id="feab8-406">Die tatsächliche Test (Bedingung) wird in Klammern angegeben und gibt "true" oder "false" zurück.</span><span class="sxs-lookup"><span data-stu-id="feab8-406">The actual test (condition) is in parentheses and returns true or false.</span></span> <span data-ttu-id="feab8-407">Die Anweisungen, die ausgeführt werden, wenn der Test "true" ist, werden in Klammern eingeschlossen.</span><span class="sxs-lookup"><span data-stu-id="feab8-407">The statements that run if the test is true are enclosed in braces.</span></span> <span data-ttu-id="feab8-408">Ein `if` Anweisung zählen eine `else` Datenblock, Anweisungen, die ausgeführt werden, wenn die Bedingung "false" ist:</span><span class="sxs-lookup"><span data-stu-id="feab8-408">An `if` statement can include an `else` block that specifies statements to run if the condition is false:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample50.cshtml)]

<span data-ttu-id="feab8-409">Sie können mehrere Bedingungen mit hinzufügen ein `else if` blockieren:</span><span class="sxs-lookup"><span data-stu-id="feab8-409">You can add multiple conditions using an `else if` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample51.cshtml)]

<span data-ttu-id="feab8-410">In diesem Beispiel, wenn die erste in der If-Bedingung Block ist nicht "true", die `else if` Bedingung aktiviert ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-410">In this example, if the first condition in the if block is not true, the `else if` condition is checked.</span></span> <span data-ttu-id="feab8-411">Wenn diese Bedingung erfüllt ist, die Anweisungen in der `else if` Block ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-411">If that condition is met, the statements in the `else if` block are executed.</span></span> <span data-ttu-id="feab8-412">Wenn keine der Bedingungen erfüllt sind, die Anweisungen in der `else` Block ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="feab8-412">If none of the conditions are met, the statements in the `else` block are executed.</span></span> <span data-ttu-id="feab8-413">Sie können eine beliebige Anzahl von ElseIf hinzufügen blockiert, und schließen Sie dann mit einer `else` Blockieren der &quot;alles andere&quot; Bedingung.</span><span class="sxs-lookup"><span data-stu-id="feab8-413">You can add any number of else if blocks, and then close with an `else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="feab8-414">Um eine große Anzahl von Bedingungen zu testen, verwenden Sie eine `switch` blockieren:</span><span class="sxs-lookup"><span data-stu-id="feab8-414">To test a large number of conditions, use a `switch` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample52.cshtml)]

<span data-ttu-id="feab8-415">Der zu testende Wert steht in Klammern (im Beispiel die `weekday` Variable).</span><span class="sxs-lookup"><span data-stu-id="feab8-415">The value to test is in parentheses (in the example, the `weekday` variable).</span></span> <span data-ttu-id="feab8-416">Jeder einzelne Test verwendet einen `case` -Anweisung, die mit einem Doppelpunkt (:)) endet.</span><span class="sxs-lookup"><span data-stu-id="feab8-416">Each individual test uses a `case` statement that ends with a colon (:).</span></span> <span data-ttu-id="feab8-417">Wenn der Wert des einem `case` Anweisung entspricht dem Testwert, der Code in diesem Fall Block wird ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="feab8-417">If the value of a `case` statement matches the test value, the code in that case block is executed.</span></span> <span data-ttu-id="feab8-418">Schließen Sie jede Case-Anweisung mit einem `break` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="feab8-418">You close each case statement with a `break` statement.</span></span> <span data-ttu-id="feab8-419">(Wenn Sie vergessen, jede Unterbrechung einschließt `case` blockieren, den Code von der nächsten `case` Anweisung wird auch ausgeführt werden.) Ein `switch` Block verfügt oft über eine `default` Anweisung als bei der letzten Fall einen &quot;alles andere&quot; Option, die ausgeführt wird, wenn keine der anderen Fälle zutrifft.</span><span class="sxs-lookup"><span data-stu-id="feab8-419">(If you forget to include break in each `case` block, the code from the next `case` statement will run also.) A `switch` block often has a `default` statement as the last case for an &quot;everything else&quot; option that runs if none of the other cases are true.</span></span>

<span data-ttu-id="feab8-420">Das Ergebnis der letzten beiden bedingte Blöcke in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="feab8-420">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-c/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="feab8-422">Durchlaufen von Code</span><span class="sxs-lookup"><span data-stu-id="feab8-422">Looping Code</span></span>

<span data-ttu-id="feab8-423">Sie müssen häufig die gleichen Anweisungen wiederholt ausführen.</span><span class="sxs-lookup"><span data-stu-id="feab8-423">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="feab8-424">Dazu müssen Sie Schleifen.</span><span class="sxs-lookup"><span data-stu-id="feab8-424">You do this by looping.</span></span> <span data-ttu-id="feab8-425">Beispielsweise führen Sie häufig die gleichen Anweisungen für jedes Element in einer Auflistung von Daten.</span><span class="sxs-lookup"><span data-stu-id="feab8-425">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="feab8-426">Wenn Sie wissen, genau wie oft Sie in einer Schleife möchten, können Sie eine `for` Schleife.</span><span class="sxs-lookup"><span data-stu-id="feab8-426">If you know exactly how many times you want to loop, you can use a `for` loop.</span></span> <span data-ttu-id="feab8-427">Diese Art von Schleife ist besonders hilfreich für das Zählen von oben oder nach unten zählen:</span><span class="sxs-lookup"><span data-stu-id="feab8-427">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample53.html)]

<span data-ttu-id="feab8-428">Die Schleife beginnt mit der `for` Schlüsselwort, gefolgt von drei Anweisungen in Klammern ein, jeweils mit einem Semikolon beendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-428">The loop begins with the `for` keyword, followed by three statements in parentheses, each terminated with a semicolon.</span></span>

- <span data-ttu-id="feab8-429">Innerhalb der Klammern, die erste Anweisung (`var i=10;`) einen Leistungsindikator erstellt und initialisiert es mit 10.</span><span class="sxs-lookup"><span data-stu-id="feab8-429">Inside the parentheses, the first statement (`var i=10;`) creates a counter and initializes it to 10.</span></span> <span data-ttu-id="feab8-430">Müssen Sie die Namen des Zählers nicht `i` &#8212; können Sie beliebige Variable.</span><span class="sxs-lookup"><span data-stu-id="feab8-430">You don't have to name the counter `i` &#8212; you can use any variable.</span></span> <span data-ttu-id="feab8-431">Wenn die `for` Schleife ausgeführt wird, wird automatisch erhöht.</span><span class="sxs-lookup"><span data-stu-id="feab8-431">When the `for` loop runs, the counter is automatically incremented.</span></span>
- <span data-ttu-id="feab8-432">Die zweite Anweisung (`i < 21;`) legt die Bedingung wie weit Sie zählen möchten.</span><span class="sxs-lookup"><span data-stu-id="feab8-432">The second statement (`i < 21;`) sets the condition for how far you want to count.</span></span> <span data-ttu-id="feab8-433">In diesem Fall möchten Sie es auf ein Maximum von 20 wechseln (d. h. passiert während der Indikator kleiner als 21 befindet).</span><span class="sxs-lookup"><span data-stu-id="feab8-433">In this case, you want it to go to a maximum of 20 (that is, keep going while the counter is less than 21).</span></span>
- <span data-ttu-id="feab8-434">Die dritte Anweisung (`i++` ) verwendet eine Inkrementoperator, der einfach angibt, dass der Leistungsindikator hinzugefügt wird bei jeder Ausführung die Schleife 1 aufweisen sollte.</span><span class="sxs-lookup"><span data-stu-id="feab8-434">The third statement (`i++` ) uses an increment operator, which simply specifies that the counter should have 1 added to it each time the loop runs.</span></span>

<span data-ttu-id="feab8-435">Ist Sie in der geschweiften Klammern der Code, der für jede Iteration der Schleife ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="feab8-435">Inside the braces is the code that will run for each iteration of the loop.</span></span> <span data-ttu-id="feab8-436">Das Markup erstellt einen neuen Absatz (`<p>` Element) jedes Mal und fügt eine Zeile an die Ausgabe des Werts eines `i` (Leistungsindikator).</span><span class="sxs-lookup"><span data-stu-id="feab8-436">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of `i` (the counter).</span></span> <span data-ttu-id="feab8-437">Wenn Sie auf dieser Seite ausführen, erstellt das Beispiel 11 Zeilen, die die Ausgabe mit dem Text in jeder Zeile an, mit dem Element angezeigt.</span><span class="sxs-lookup"><span data-stu-id="feab8-437">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-c/_static/image11.jpg)

<span data-ttu-id="feab8-439">Wenn Sie eine Auflistung oder ein Array verwenden, verwenden Sie häufig eine `foreach` Schleife.</span><span class="sxs-lookup"><span data-stu-id="feab8-439">If you're working with a collection or array, you often use a `foreach` loop.</span></span> <span data-ttu-id="feab8-440">Eine Auflistung ist eine Gruppe von ähnliche Objekte, und die `foreach` Schleife können, Sie aus einer Aufgabe für jedes Element in der Auflistung führen.</span><span class="sxs-lookup"><span data-stu-id="feab8-440">A collection is a group of similar objects, and the `foreach` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="feab8-441">Diese Art der Schleife eignet sich für Auflistungen, da im Gegensatz zu einer `for` Schleifen, Sie müssen keine Erhöhen des Zählerwerts und einzuschränken.</span><span class="sxs-lookup"><span data-stu-id="feab8-441">This type of loop is convenient for collections, because unlike a `for` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="feab8-442">Stattdessen die `foreach` Schleife Code wird einfach in der Auflistung fortgesetzt, bis er abgeschlossen ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-442">Instead, the `foreach` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="feab8-443">Der folgende Code gibt beispielsweise die Elemente in der `Request.ServerVariables` Auflistung, die ein Objekt, das Informationen über den Webserver enthält.</span><span class="sxs-lookup"><span data-stu-id="feab8-443">For example, the following code returns the items in the `Request.ServerVariables` collection, which is an object that contains information about your web server.</span></span> <span data-ttu-id="feab8-444">Er verwendet ein `foreac` h-Schleife, um den Namen der einzelnen Elemente anzeigen, indem Sie beim Erstellen eines neuen `<li>` Element in einem HTML-Aufzählung.</span><span class="sxs-lookup"><span data-stu-id="feab8-444">It uses a `foreac` h loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample54.html)]

<span data-ttu-id="feab8-445">Die `foreach` Schlüsselwort durch Klammern gefolgt wird, bei denen Sie Deklarieren einer Variable, die ein einzelnes Element in der Auflistung darstellt (im Beispiel `var item`), gefolgt von den `in` Schlüsselwort, gefolgt von der Auflistung durchlaufen werden sollen.</span><span class="sxs-lookup"><span data-stu-id="feab8-445">The `foreach` keyword is followed by parentheses where you declare a variable that represents a single item in the collection (in the example, `var item`), followed by the `in` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="feab8-446">Im Hauptteil der `foreach` Schleife, können Sie das aktuelle Element über die Variable, die Sie zuvor deklarierte zugreifen.</span><span class="sxs-lookup"><span data-stu-id="feab8-446">In the body of the `foreach` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-c/_static/image12.jpg)

<span data-ttu-id="feab8-448">Verwenden Sie zum Erstellen einer allgemeinen Schleife die `while` Anweisung:</span><span class="sxs-lookup"><span data-stu-id="feab8-448">To create a more general-purpose loop, use the `while` statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample55.cshtml)]

<span data-ttu-id="feab8-449">Ein `while` Schleife beginnt mit der `while` Schlüsselwort, gefolgt von Klammern geben Sie, wie lange die Schleife fortgesetzt wird (hier für solange `countNum` ist kleiner als 50), klicken Sie dann den Block zu wiederholen.</span><span class="sxs-lookup"><span data-stu-id="feab8-449">A `while` loop begins with the `while` keyword, followed by parentheses where you specify how long the loop continues (here, for as long as `countNum` is less than 50), then the block to repeat.</span></span> <span data-ttu-id="feab8-450">Schleifen in der Regel erhöhen (hinzufügen) oder dekrementiert (subtrahiert) eine Variable oder ein Objekt, das für die Inventur verwendet.</span><span class="sxs-lookup"><span data-stu-id="feab8-450">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="feab8-451">Im Beispiel die `+=` Operator fügt 1 bis `countNum` bei jeder Ausführung die Schleife.</span><span class="sxs-lookup"><span data-stu-id="feab8-451">In the example, the `+=` operator adds 1 to `countNum` each time the loop runs.</span></span> <span data-ttu-id="feab8-452">(Um eine Variable in einer Schleife zu verringern, die nach-unten zählt, verwenden Sie die Dekrement-Operators `-=`).</span><span class="sxs-lookup"><span data-stu-id="feab8-452">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`).</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="feab8-453">Objekte und Auflistungen</span><span class="sxs-lookup"><span data-stu-id="feab8-453">Objects and Collections</span></span>

<span data-ttu-id="feab8-454">Nahezu alles in einer ASP.NET-Website handelt es sich um ein Objekt, einschließlich der Webseite selbst.</span><span class="sxs-lookup"><span data-stu-id="feab8-454">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="feab8-455">Dieser Abschnitt beschreibt einige wichtige Objekte, die Sie häufig in Ihrem Code arbeiten mit.</span><span class="sxs-lookup"><span data-stu-id="feab8-455">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="feab8-456">Seite "-Objekte</span><span class="sxs-lookup"><span data-stu-id="feab8-456">Page Objects</span></span>

<span data-ttu-id="feab8-457">Das grundlegendste Objekt in ASP.NET ist die Seite.</span><span class="sxs-lookup"><span data-stu-id="feab8-457">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="feab8-458">Sie können die Eigenschaften der Page-Objekt direkt ohne alle qualifizierenden Objekt zugreifen.</span><span class="sxs-lookup"><span data-stu-id="feab8-458">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="feab8-459">Der folgende Code Ruft den Dateipfad mit der Seite mit den `Request` Objekt von der Seite ":</span><span class="sxs-lookup"><span data-stu-id="feab8-459">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample56.cshtml)]

<span data-ttu-id="feab8-460">Zum erleichtern deutlich, dass verweisen auf Eigenschaften und Methoden für das Seitenobjekt der aktuellen Optional können Sie das Schlüsselwort `this` zur Darstellung der Page-Objekt in Ihrem Code.</span><span class="sxs-lookup"><span data-stu-id="feab8-460">To make it clear that you're referencing properties and methods on the current page object, you can optionally use the keyword `this` to represent the page object in your code.</span></span> <span data-ttu-id="feab8-461">Hier wird im vorangehenden Codebeispiel wird mit `this` zur Darstellung der Seite hinzugefügt:</span><span class="sxs-lookup"><span data-stu-id="feab8-461">Here is the previous code example, with `this` added to represent the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample57.cshtml)]

<span data-ttu-id="feab8-462">Können Sie Eigenschaften der `Page` Objekt, eine Vielzahl von Informationen, wie z. B. abgerufen werden:</span><span class="sxs-lookup"><span data-stu-id="feab8-462">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="feab8-463">`Request`</span><span class="sxs-lookup"><span data-stu-id="feab8-463">`Request`.</span></span> <span data-ttu-id="feab8-464">Wie Sie bereits gesehen haben, ist dies eine Auflistung von Informationen über die aktuelle Anforderung, z. B., welche Art von Browser, die URL der Seite, die Identität des Benutzers usw. angefordert.</span><span class="sxs-lookup"><span data-stu-id="feab8-464">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="feab8-465">`Response`</span><span class="sxs-lookup"><span data-stu-id="feab8-465">`Response`.</span></span> <span data-ttu-id="feab8-466">Dies ist eine Auflistung von Informationen über die Antwort (Seite), die an den Browser gesendet wird, wenn der Code ausgeführt wurde.</span><span class="sxs-lookup"><span data-stu-id="feab8-466">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="feab8-467">Beispielsweise können Sie diese Eigenschaft verwenden, um Informationen in die Antwort zu schreiben.</span><span class="sxs-lookup"><span data-stu-id="feab8-467">For example, you can use this property to write information into the response.</span></span> 

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample58.cshtml)]

<a id="ID_CollectionsAndObjects"></a>
### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="feab8-468">Auflistungsobjekte (Arrays und Wörterbücher)</span><span class="sxs-lookup"><span data-stu-id="feab8-468">Collection Objects (Arrays and Dictionaries)</span></span>

<span data-ttu-id="feab8-469">Ein *Auflistung* ist eine Gruppe von Objekten desselben Typs, z. B. eine Auflistung von `Customer` Objekte aus einer Datenbank.</span><span class="sxs-lookup"><span data-stu-id="feab8-469">A *collection* is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="feab8-470">ASP.NET enthält viele integrierte Auflistungen, wie z. B. die `Request.Files` Auflistung.</span><span class="sxs-lookup"><span data-stu-id="feab8-470">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="feab8-471">Sie werden häufig in Auflistungen mit Daten arbeiten.</span><span class="sxs-lookup"><span data-stu-id="feab8-471">You'll often work with data in collections.</span></span> <span data-ttu-id="feab8-472">Zwei allgemeine Auflistungstypen sind die *Array* und *Wörterbuch*.</span><span class="sxs-lookup"><span data-stu-id="feab8-472">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="feab8-473">Ein Array ist nützlich, wenn Sie eine Auflistung mit ähnlichen Elementen gespeichert werden soll, aber nicht, erstellen Sie eine separate Variable, um jedes Element möchten:</span><span class="sxs-lookup"><span data-stu-id="feab8-473">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample59.cshtml)]

<span data-ttu-id="feab8-474">Bei Arrays, Sie deklarieren einen bestimmten Datentyp z. B. `string`, `int`, oder `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="feab8-474">With arrays, you declare a specific data type, such as `string`, `int`, or `DateTime`.</span></span> <span data-ttu-id="feab8-475">Um anzugeben, dass die Variable kann ein Array enthalten, Sie Klammern zur Deklaration hinzufügen (z. B. `string[]` oder `int[]`).</span><span class="sxs-lookup"><span data-stu-id="feab8-475">To indicate that the variable can contain an array, you add brackets to the declaration (such as `string[]` or `int[]`).</span></span> <span data-ttu-id="feab8-476">Sie erreichen die Elemente in einem Array mit ihrer Position (Index) oder mithilfe der `foreach` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="feab8-476">You can access items in an array using their position (index) or by using the `foreach` statement.</span></span> <span data-ttu-id="feab8-477">Arrayindizes sind nullbasiert &#8212; , also das erste Element ist an position 0, das zweite Element ist an Position 1 und So weiter.</span><span class="sxs-lookup"><span data-stu-id="feab8-477">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample60.cshtml)]

<span data-ttu-id="feab8-478">Sie können die Anzahl der Elemente in einem Array zu bestimmen, durch Abrufen der `Length` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="feab8-478">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="feab8-479">Um die Position eines bestimmten Elements im Array (Suchen Sie das Array) abzurufen, verwenden die `Array.IndexOf` Methode.</span><span class="sxs-lookup"><span data-stu-id="feab8-479">To get the position of a specific item in the array (to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="feab8-480">Sie können auch Aktionen wie Reverse den Inhalt eines Arrays (die `Array.Reverse` Methode) oder Sortieren des Inhalts (die `Array.Sort` Methode).</span><span class="sxs-lookup"><span data-stu-id="feab8-480">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="feab8-481">Die Ausgabe von der String-Array-Code in einem Browser angezeigt:</span><span class="sxs-lookup"><span data-stu-id="feab8-481">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-c/_static/image13.jpg)

<span data-ttu-id="feab8-483">Ein Wörterbuch ist eine Auflistung von Schlüssel/Wert-Paaren, in dem Sie den Schlüssel (oder den Namen) festlegen oder Abrufen des entsprechenden Werts eingeben:</span><span class="sxs-lookup"><span data-stu-id="feab8-483">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample61.cshtml)]

<span data-ttu-id="feab8-484">Um ein Wörterbuch zu erstellen, verwenden Sie die `new` Schlüsselwort, um anzugeben, dass Sie ein neues Dictionary-Objekt erstellen.</span><span class="sxs-lookup"><span data-stu-id="feab8-484">To create a dictionary, you use the `new` keyword to indicate that you're creating a new dictionary object.</span></span> <span data-ttu-id="feab8-485">Sie können ein Wörterbuch zuweisen, um eine Variable mit dem `var` Schlüsselwort.</span><span class="sxs-lookup"><span data-stu-id="feab8-485">You can assign a dictionary to a variable using the `var` keyword.</span></span> <span data-ttu-id="feab8-486">Sie geben die Datentypen der Elemente im Wörterbuch mithilfe der spitzen Klammern ( `< >` ).</span><span class="sxs-lookup"><span data-stu-id="feab8-486">You indicate the data types of the items in the dictionary using angle brackets ( `< >` ).</span></span> <span data-ttu-id="feab8-487">Am Ende der Deklaration müssen Sie ein Klammernpaar, hinzufügen, da es sich tatsächlich um eine Methode handelt, die ein neues Wörterbuch erstellt.</span><span class="sxs-lookup"><span data-stu-id="feab8-487">At the end of the declaration, you must add a pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="feab8-488">Um Elemente zum Wörterbuch hinzuzufügen, rufen Sie die `Add` Methode der Variablen Wörterbuch (`myScores` in diesem Fall), und klicken Sie dann einen Schlüssel und einen Wert angeben.</span><span class="sxs-lookup"><span data-stu-id="feab8-488">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="feab8-489">Alternativ können Sie eckige Klammern verwenden, um den Schlüssel an, und führen Sie eine einfache Zuweisung, wie im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="feab8-489">Alternatively, you can use square brackets to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample62.cs)]

<span data-ttu-id="feab8-490">Um einen Wert aus dem Wörterbuch zu erhalten, geben Sie den Schlüssel in Klammern:</span><span class="sxs-lookup"><span data-stu-id="feab8-490">To get a value from the dictionary, you specify the key in brackets:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample63.cs)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="feab8-491">Aufrufen von Methoden mit Parametern</span><span class="sxs-lookup"><span data-stu-id="feab8-491">Calling Methods with Parameters</span></span>

<span data-ttu-id="feab8-492">Wie Sie weiter oben in diesem Artikel lesen, können die Objekte, denen Programmierung mit Methoden verfügen.</span><span class="sxs-lookup"><span data-stu-id="feab8-492">As you read earlier in this article, the objects that you program with can have methods.</span></span> <span data-ttu-id="feab8-493">Z. B. eine `Database` Objekt möglicherweise ein `Database.Connect` Methode.</span><span class="sxs-lookup"><span data-stu-id="feab8-493">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="feab8-494">Viele Methoden verfügen auch über einen oder mehrere Parameter.</span><span class="sxs-lookup"><span data-stu-id="feab8-494">Many methods also have one or more parameters.</span></span> <span data-ttu-id="feab8-495">Ein *Parameter* ist ein Wert, den Sie an eine Methode übergeben, die Methode zum Abschließen des Tasks zu aktivieren.</span><span class="sxs-lookup"><span data-stu-id="feab8-495">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="feab8-496">Betrachten Sie z. B. eine Deklaration für die `Request.MapPath` Methode, die drei Parameter akzeptiert:</span><span class="sxs-lookup"><span data-stu-id="feab8-496">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample64.cs)]

<span data-ttu-id="feab8-497">(Die Zeile wurde umgebrochen, um sie besser lesbar zu machen.</span><span class="sxs-lookup"><span data-stu-id="feab8-497">(The line has been wrapped to make it more readable.</span></span> <span data-ttu-id="feab8-498">Denken Sie daran, dass Sie Zeilenumbrüche aufnehmen können, fast jeden Ort, mit der Ausnahme, die innerhalb von Zeichenfolgen, in Anführungszeichen eingeschlossen werden.)</span><span class="sxs-lookup"><span data-stu-id="feab8-498">Remember that you can put line breaks almost any place except inside strings that are enclosed in quotation marks.)</span></span>

<span data-ttu-id="feab8-499">Diese Methode gibt den physischen Pfad auf dem Server, der entspricht einem angegebenen virtuellen Pfad zurück.</span><span class="sxs-lookup"><span data-stu-id="feab8-499">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="feab8-500">Die drei Parameter für die Methode sind `virtualPath`, `baseVirtualDir`, und `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="feab8-500">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="feab8-501">(Beachten Sie, dass in der Deklaration der Parameter mit den Datentypen der Daten aufgeführt sind, die sie akzeptieren.) Wenn Sie diese Methode aufrufen, müssen Sie Werte für alle drei Parameter angeben.</span><span class="sxs-lookup"><span data-stu-id="feab8-501">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="feab8-502">Die Razor-Syntax erhalten Sie zwei Optionen zum Übergeben von Parametern an eine Methode: *Positionsparameter* und *benannte Parameter*.</span><span class="sxs-lookup"><span data-stu-id="feab8-502">The Razor syntax gives you two options for passing parameters to a method: *positional parameters* and *named parameters*.</span></span> <span data-ttu-id="feab8-503">Zum Aufrufen einer Methode mit positionelle Parameter übergeben Sie die Parameter in einer festen Reihenfolge, die in der Deklaration der Methode angegeben ist.</span><span class="sxs-lookup"><span data-stu-id="feab8-503">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="feab8-504">(Sie würden diese Reihenfolge in der Regel wissen, lesen Sie die Dokumentation für die Methode.) Führen Sie die Reihenfolge, und Sie können nicht fortfahren, einen der Parameter &#8212; Wenn erforderlich, eine leere Zeichenfolge übergeben (`""`) oder `null` für einen positionelle Parameter, die Sie für einen Wert besitzen.</span><span class="sxs-lookup"><span data-stu-id="feab8-504">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or `null` for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="feab8-505">Im folgende Beispiel wird angenommen, Sie haben einen Ordner namens *Skripts* auf Ihrer Website.</span><span class="sxs-lookup"><span data-stu-id="feab8-505">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="feab8-506">Der Code Ruft die `Request.MapPath` -Methode auf und übergibt Werte für die drei Parameter in der richtigen Reihenfolge.</span><span class="sxs-lookup"><span data-stu-id="feab8-506">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="feab8-507">Anschließend wird den resultierenden zugeordneten Pfad angezeigt.</span><span class="sxs-lookup"><span data-stu-id="feab8-507">It then displays the resulting mapped path.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample65.cshtml)]

<span data-ttu-id="feab8-508">Wenn eine Methode viele Parameter verfügt, können Sie den Code besser lesbar behalten mithilfe von benannten Parametern.</span><span class="sxs-lookup"><span data-stu-id="feab8-508">When a method has many parameters, you can keep your code more readable by using named parameters.</span></span> <span data-ttu-id="feab8-509">Zum Aufrufen einer Methode, die benannte Parameter verwenden, geben Sie einen Doppelpunkt (:)), und klicken Sie dann den Wert der Name des Parameters folgen.</span><span class="sxs-lookup"><span data-stu-id="feab8-509">To call a method using named parameters, you specify the parameter name followed by a colon (:), and then the value.</span></span> <span data-ttu-id="feab8-510">Der Vorteil der benannten Parameter ist, dass Sie sie in beliebiger Reihenfolge übergeben können Sie die gewünschte.</span><span class="sxs-lookup"><span data-stu-id="feab8-510">The advantage of named parameters is that you can pass them in any order you want.</span></span> <span data-ttu-id="feab8-511">(Ein Nachteil ist, dass der Methodenaufruf nicht als compact ist).</span><span class="sxs-lookup"><span data-stu-id="feab8-511">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="feab8-512">Im folgenden Beispiel wird die gleiche Methode wie oben beschrieben, aber verwendet benannte Parameter, um die Werte anzugeben:</span><span class="sxs-lookup"><span data-stu-id="feab8-512">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample66.cshtml)]

<span data-ttu-id="feab8-513">Wie Sie sehen können, werden die Parameter in einer anderen Reihenfolge übergeben.</span><span class="sxs-lookup"><span data-stu-id="feab8-513">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="feab8-514">Wenn Sie im vorherigen Beispiel und in diesem Beispiel ausführen, müssen sie den gleichen Wert zurückgeben.</span><span class="sxs-lookup"><span data-stu-id="feab8-514">However, if you run the previous example and this example, they'll return the same value.</span></span>

<a id="ID_HandlingErrors"></a>
## <a name="handling-errors"></a><span data-ttu-id="feab8-515">Behandeln von Fehlern</span><span class="sxs-lookup"><span data-stu-id="feab8-515">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="feab8-516">Try-Catch-Anweisungen</span><span class="sxs-lookup"><span data-stu-id="feab8-516">Try-Catch Statements</span></span>

<span data-ttu-id="feab8-517">Sie müssen häufig Anweisungen in Ihrem Code, die außerhalb Ihrer Kontrolle Gründen fehlschlagen.</span><span class="sxs-lookup"><span data-stu-id="feab8-517">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="feab8-518">Zum Beispiel:</span><span class="sxs-lookup"><span data-stu-id="feab8-518">For example:</span></span>

- <span data-ttu-id="feab8-519">Wenn Ihr Code versucht, erstellen oder eine Datei zuzugreifen, können alle Arten von Fehlern auftreten.</span><span class="sxs-lookup"><span data-stu-id="feab8-519">If your code tries to create or access a file, all sorts of errors might occur.</span></span> <span data-ttu-id="feab8-520">Die gewünschte Datei ist möglicherweise nicht vorhanden, wird sie möglicherweise gesperrt, der Code möglicherweise nicht über Berechtigungen verfügen, und so weiter.</span><span class="sxs-lookup"><span data-stu-id="feab8-520">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="feab8-521">Auf ähnliche Weise, wenn Ihr Code versucht, zum Aktualisieren von Datensätzen in einer Datenbank, treten möglicherweise Berechtigungsprobleme, kann die Verbindung mit der Datenbank gelöscht werden, die zu speichernden Daten möglicherweise ungültige usw.</span><span class="sxs-lookup"><span data-stu-id="feab8-521">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="feab8-522">Programmiertechnisch gesehen Situationen heißen *Ausnahmen*.</span><span class="sxs-lookup"><span data-stu-id="feab8-522">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="feab8-523">Wenn im Code eine Ausnahme auftritt, generiert er (auslöst) eine Fehlermeldung angezeigt, die Benutzern bestenfalls störende:</span><span class="sxs-lookup"><span data-stu-id="feab8-523">If your code encounters an exception, it generates (throws) an error message that's, at best, annoying to users:</span></span>

![Razor-Img14](introducing-razor-syntax-c/_static/image14.jpg)

<span data-ttu-id="feab8-525">In Situationen, in denen Code kann Ausnahmen auftreten, und um Fehlermeldungen dieses Typs zu vermeiden, können Sie `try/catch` Anweisungen.</span><span class="sxs-lookup"><span data-stu-id="feab8-525">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `try/catch` statements.</span></span> <span data-ttu-id="feab8-526">In der `try` -Anweisung, führen Sie den Code, der Sie eingecheckt haben.</span><span class="sxs-lookup"><span data-stu-id="feab8-526">In the `try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="feab8-527">Eine oder mehrere `catch` -Anweisungen, die Sie suchen können, für bestimmte Fehler (bestimmte Arten von Ausnahmen), die möglicherweise aufgetreten sind.</span><span class="sxs-lookup"><span data-stu-id="feab8-527">In one or more `catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="feab8-528">Sie können beliebig viele einschließen `catch` Anweisungen, wie Sie nach Fehlern zu suchen, die Sie vorhersehen werden müssen.</span><span class="sxs-lookup"><span data-stu-id="feab8-528">You can include as many `catch` statements as you need to look for errors that you are anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="feab8-529">Es wird empfohlen, dass Sie, verwenden vermeiden die `Response.Redirect` Methode in `try/catch` Anweisungen, da es einen Ausnahmefehler, auf der Seite verursachen kann.</span><span class="sxs-lookup"><span data-stu-id="feab8-529">We recommend that you avoid using the `Response.Redirect` method in `try/catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="feab8-530">Das folgende Beispiel zeigt eine Seite, die bei der ersten Anforderung eine Textdatei erstellt und zeigt dann eine Schaltfläche, die dem Benutzer die Datei öffnen kann.</span><span class="sxs-lookup"><span data-stu-id="feab8-530">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="feab8-531">Im Beispiel verwendet absichtlich einen falscher Dateiname, sodass er eine Ausnahme auslöst.</span><span class="sxs-lookup"><span data-stu-id="feab8-531">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="feab8-532">Der Code enthält `catch` -Anweisungen für die zwei möglichen Ausnahmen: `FileNotFoundException`, Dies tritt ein, wenn der Dateiname beschädigt ist und `DirectoryNotFoundException`, Dies tritt ein, wenn ASP.NET den Ordner selbst finden kann.</span><span class="sxs-lookup"><span data-stu-id="feab8-532">The code includes `catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="feab8-533">(Sie können kommentieren Sie eine Anweisung im Beispiel um anzuzeigen, wie er ausgeführt wird, wenn alles ordnungsgemäß funktioniert.)</span><span class="sxs-lookup"><span data-stu-id="feab8-533">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="feab8-534">Wenn Ihr Code die Ausnahme behandelt hat nicht, sehen Sie eine Fehlerseite, wie der vorherige Screenshot.</span><span class="sxs-lookup"><span data-stu-id="feab8-534">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="feab8-535">Allerdings die `try/catch` Abschnitt wird verhindert, dass den Benutzer sehen diese Arten von Fehlern.</span><span class="sxs-lookup"><span data-stu-id="feab8-535">However, the `try/catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample67.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="feab8-536">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="feab8-536">Additional Resources</span></span>

<span data-ttu-id="feab8-537">**Programmierung mit Visual Basic**</span><span class="sxs-lookup"><span data-stu-id="feab8-537">**Programming with Visual Basic**</span></span>


[<span data-ttu-id="feab8-538">Anhang: Visual Basic-Sprache und Syntax</span><span class="sxs-lookup"><span data-stu-id="feab8-538">Appendix: Visual Basic Language and Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkId=202908)


<span data-ttu-id="feab8-539">**Referenzdokumentation**</span><span class="sxs-lookup"><span data-stu-id="feab8-539">**Reference Documentation**</span></span>


[<span data-ttu-id="feab8-540">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="feab8-540">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)

[<span data-ttu-id="feab8-541">C#-Sprache</span><span class="sxs-lookup"><span data-stu-id="feab8-541">C# Language</span></span>](https://msdn.microsoft.com/library/kx37x362.aspx)
