---
uid: web-pages/overview/data/working-with-files
title: Arbeiten mit Dateien an einem Standort der ASP.NET Web Pages (Razor) | Microsoft Docs
author: tfitzmac
description: In diesem Kapitel wird erläutert, wie lesen, schreiben, anfügen, löschen und Hochladen von Dateien.
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/20/2014
ms.topic: article
ms.assetid: eee916e4-ba4c-439a-a24e-68df7d45a569
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/data/working-with-files
msc.type: authoredcontent
ms.openlocfilehash: 0f119f8fb4873e55292203f21a2efd8f26793ae4
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 04/10/2018
ms.locfileid: "28040222"
---
<a name="working-with-files-in-an-aspnet-web-pages-razor-site"></a><span data-ttu-id="f305d-103">Arbeiten mit Dateien an einem Standort der ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="f305d-103">Working with Files in an ASP.NET Web Pages (Razor) Site</span></span>
====================
<span data-ttu-id="f305d-104">durch [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="f305d-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="f305d-105">In diesem Artikel wird erläutert, wie lesen, schreiben, anfügen, löschen und Hochladen von Dateien an einem Standort der ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="f305d-105">This article explains how to read, write, append, delete, and upload files in an ASP.NET Web Pages (Razor) site.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="f305d-106">Wenn zum Hochladen von Images und bearbeitet werden sollen (z. B. kippen, oder passen Sie sie), finden Sie unter [arbeiten mit Bildern in einer ASP.NET Web Pages-Website](https://go.microsoft.com/fwlink/?LinkId=202897).</span><span class="sxs-lookup"><span data-stu-id="f305d-106">If you want to upload images and manipulate them (for example, flip or resize them), see [Working with Images in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202897).</span></span>
> 
> 
> <span data-ttu-id="f305d-107">**Lernen Sie:**</span><span class="sxs-lookup"><span data-stu-id="f305d-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="f305d-108">Informationen zum Erstellen einer Textdatei und Daten in sie schreiben.</span><span class="sxs-lookup"><span data-stu-id="f305d-108">How to create a text file and write data to it.</span></span>
> - <span data-ttu-id="f305d-109">Wie Sie Daten an eine vorhandene Datei anfügen.</span><span class="sxs-lookup"><span data-stu-id="f305d-109">How to append data to an existing file.</span></span>
> - <span data-ttu-id="f305d-110">Informationen zum Lesen einer Datei und daraus anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="f305d-110">How to read a file and display from it.</span></span>
> - <span data-ttu-id="f305d-111">Vorgehensweise: Löschen von Dateien von einer Website.</span><span class="sxs-lookup"><span data-stu-id="f305d-111">How to delete files from a website.</span></span>
> - <span data-ttu-id="f305d-112">Wie können Benutzer eine oder mehrere Dateien hochladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-112">How to let users upload one file or multiple files.</span></span>
> 
> <span data-ttu-id="f305d-113">Hierbei handelt es sich um den ASP.NET Programmieren von Funktionen, die im Artikel:</span><span class="sxs-lookup"><span data-stu-id="f305d-113">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="f305d-114">Die `File` -Objekt, das eine Möglichkeit zum Verwalten von Dateien bereitstellt.</span><span class="sxs-lookup"><span data-stu-id="f305d-114">The `File` object, which provides a way to manage files.</span></span>
> - <span data-ttu-id="f305d-115">Die `FileUpload` Helper.</span><span class="sxs-lookup"><span data-stu-id="f305d-115">The `FileUpload` helper.</span></span>
> - <span data-ttu-id="f305d-116">Die `Path` -Objekt, das Methoden bereitstellt, mit denen Sie den Pfad und den Dateinamen zu ändern.</span><span class="sxs-lookup"><span data-stu-id="f305d-116">The `Path` object, which provides methods that let you manipulate path and file names.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="f305d-117">In diesem Lernprogramm verwendeten Versionen der Software</span><span class="sxs-lookup"><span data-stu-id="f305d-117">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="f305d-118">ASP.NET Web Pages (Razor) 2</span><span class="sxs-lookup"><span data-stu-id="f305d-118">ASP.NET Web Pages (Razor) 2</span></span>
> - <span data-ttu-id="f305d-119">WebMatrix 2</span><span class="sxs-lookup"><span data-stu-id="f305d-119">WebMatrix 2</span></span>
>   
> 
> <span data-ttu-id="f305d-120">Dieses Lernprogramm funktioniert auch mit WebMatrix-3.</span><span class="sxs-lookup"><span data-stu-id="f305d-120">This tutorial also works with WebMatrix 3.</span></span>


<a id="Creating_a_Text_File"></a>
## <a name="creating-a-text-file-and-writing-data-to-it"></a><span data-ttu-id="f305d-121">Erstellen einer Textdatei, und Schreiben von Daten zu</span><span class="sxs-lookup"><span data-stu-id="f305d-121">Creating a Text File and Writing Data to It</span></span>

<span data-ttu-id="f305d-122">Zusätzlich zur Verwendung einer Datenbank in Ihrer Website, können Sie mit Dateien arbeiten.</span><span class="sxs-lookup"><span data-stu-id="f305d-122">In addition to using a database in your website, you might work with files.</span></span> <span data-ttu-id="f305d-123">Sie können z. B. Textdateien als eine einfache Möglichkeit zum Speichern von Daten für den Standort verwenden.</span><span class="sxs-lookup"><span data-stu-id="f305d-123">For example, you might use text files as a simple way to store data for the site.</span></span> <span data-ttu-id="f305d-124">(Eine Textdatei, die verwendet wird, zum Speichern von Daten bezeichnet eine *Flatfile*.) Textdateien kann in verschiedenen Formaten wie z. B. *".txt"*, *XML*, oder *CSV* (durch Trennzeichen getrennte Werte).</span><span class="sxs-lookup"><span data-stu-id="f305d-124">(A text file that's used to store data is sometimes called a *flat file*.) Text files can be in different formats, like *.txt*, *.xml*, or *.csv* (comma-delimited values).</span></span>

<span data-ttu-id="f305d-125">Wenn Sie Daten in einer Textdatei speichern möchten, können Sie mithilfe der `File.WriteAllText` Methode zum Angeben der zu erstellenden Datei und die Daten zu schreiben.</span><span class="sxs-lookup"><span data-stu-id="f305d-125">If you want to store data in a text file, you can use the `File.WriteAllText` method to specify the file to create and the data to write to it.</span></span> <span data-ttu-id="f305d-126">In diesem Verfahren erstellen Sie eine Seite, die ein einfaches Formular mit drei enthält `input` Elemente (Vorname, Nachname und e-Mail-Adresse) und ein **Absenden** Schaltfläche.</span><span class="sxs-lookup"><span data-stu-id="f305d-126">In this procedure, you'll create a page that contains a simple form with three `input` elements (first name, last name, and email address) and a **Submit** button.</span></span> <span data-ttu-id="f305d-127">Wenn der Benutzer das Formular sendet, müssen Sie die Eingaben des Benutzers in einer Textdatei speichern.</span><span class="sxs-lookup"><span data-stu-id="f305d-127">When the user submits the form, you'll store the user's input in a text file.</span></span>

1. <span data-ttu-id="f305d-128">Erstellen Sie einen neuen Ordner namens *App\_Daten*, sofern er nicht bereits vorhanden.</span><span class="sxs-lookup"><span data-stu-id="f305d-128">Create a new folder named *App\_Data*, if it doesn't exist already.</span></span>
2. <span data-ttu-id="f305d-129">Erstellen Sie im Stammverzeichnis der Website eine neue Datei namens *UserData.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f305d-129">At the root of your website, create a new file named *UserData.cshtml*.</span></span>
3. <span data-ttu-id="f305d-130">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="f305d-130">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample1.cshtml)]

    <span data-ttu-id="f305d-131">Das HTML-Markup erstellt ein Formular mit drei Textfelder.</span><span class="sxs-lookup"><span data-stu-id="f305d-131">The HTML markup creates the form with the three text boxes.</span></span> <span data-ttu-id="f305d-132">Verwenden Sie im Code die `IsPost` Eigenschaft, um zu bestimmen, ob die Seite übermittelt wurde, bevor Sie die Verarbeitung gestartet.</span><span class="sxs-lookup"><span data-stu-id="f305d-132">In the code, you use the `IsPost` property to determine whether the page has been submitted before you start processing.</span></span>

    <span data-ttu-id="f305d-133">Die erste Aufgabe wird zum Abrufen der Benutzereingabe und Variablen zuweisen.</span><span class="sxs-lookup"><span data-stu-id="f305d-133">The first task is to get the user input and assign it to variables.</span></span> <span data-ttu-id="f305d-134">Der Code verkettet die Werte von zwei separate Variablen klicken Sie dann in eine durch Trennzeichen getrennte Zeichenfolge, die dann in eine andere Variable gespeichert werden.</span><span class="sxs-lookup"><span data-stu-id="f305d-134">The code then concatenates the values of the separate variables into one comma-delimited string, which is then stored in a different variable.</span></span> <span data-ttu-id="f305d-135">Beachten Sie, dass das Komma als Trennzeichen eine Zeichenfolge in Anführungszeichen (","), da buchstäblich ein Komma in großen Zeichenfolge einzubettenden, die Sie erstellen.</span><span class="sxs-lookup"><span data-stu-id="f305d-135">Notice that the comma separator is a string contained in quotation marks (","), because you're literally embedding a comma into the big string that you're creating.</span></span> <span data-ttu-id="f305d-136">Fügen Sie am Ende der Daten, die Sie verketten, `Environment.NewLine`.</span><span class="sxs-lookup"><span data-stu-id="f305d-136">At the end of the data that you concatenate together, you add `Environment.NewLine`.</span></span> <span data-ttu-id="f305d-137">Dies fügt einen Zeilenumbruch (ein neue Zeilenumbruchzeichen).</span><span class="sxs-lookup"><span data-stu-id="f305d-137">This adds a line break (a newline character).</span></span> <span data-ttu-id="f305d-138">Was Sie mit der alle diese Verkettung erstellen, ist eine Zeichenfolge, die wie folgt aussieht:</span><span class="sxs-lookup"><span data-stu-id="f305d-138">What you're creating with all this concatenation is a string that looks like this:</span></span>

    [!code-css[Main](working-with-files/samples/sample2.css)]

    <span data-ttu-id="f305d-139">(Mit einem unsichtbaren Zeilenumbruch am Ende.)</span><span class="sxs-lookup"><span data-stu-id="f305d-139">(With an invisible line break at the end.)</span></span>

    <span data-ttu-id="f305d-140">Anschließend erstellen Sie eine Variable (`dataFile`), die den Speicherort und Namen der Datei zum Speichern der Daten enthält.</span><span class="sxs-lookup"><span data-stu-id="f305d-140">You then create a variable (`dataFile`) that contains the location and name of the file to store the data in.</span></span> <span data-ttu-id="f305d-141">Festlegen des Speicherorts erfordert einige besondere Behandlung.</span><span class="sxs-lookup"><span data-stu-id="f305d-141">Setting the location requires some special handling.</span></span> <span data-ttu-id="f305d-142">Auf Websites, ist es kein empfehlenswertes Verfahren, die in absolute Pfade wie im Code finden Sie unter *C:\Folder\File.txt* für Dateien auf dem Webserver.</span><span class="sxs-lookup"><span data-stu-id="f305d-142">In websites, it's a bad practice to refer in code to absolute paths like *C:\Folder\File.txt* for files on the web server.</span></span> <span data-ttu-id="f305d-143">Wenn eine Website verschoben wird, wird ein absoluter Pfad falsch sein.</span><span class="sxs-lookup"><span data-stu-id="f305d-143">If a website is moved, an absolute path will be wrong.</span></span> <span data-ttu-id="f305d-144">Darüber hinaus kennen nicht für eine gehostete Website (im Gegensatz zur auf Ihrem eigenen Computer) Sie in der Regel sogar der richtige Pfad, wenn Sie den Code schreiben.</span><span class="sxs-lookup"><span data-stu-id="f305d-144">Moreover, for a hosted site (as opposed to on your own computer) you typically don't even know what the correct path is when you're writing the code.</span></span>

    <span data-ttu-id="f305d-145">Aber in einigen Fällen (z. B. Now, für das Schreiben einer Datei) benötigen Sie einen vollständigen Pfad.</span><span class="sxs-lookup"><span data-stu-id="f305d-145">But sometimes (like now, for writing a file) you do need a complete path.</span></span> <span data-ttu-id="f305d-146">Die Lösung besteht darin, mithilfe der `MapPath` Methode der `Server` Objekt.</span><span class="sxs-lookup"><span data-stu-id="f305d-146">The solution is to use the `MapPath` method of the `Server` object.</span></span> <span data-ttu-id="f305d-147">Dies gibt den vollständigen Pfad zu Ihrer Website zurück.</span><span class="sxs-lookup"><span data-stu-id="f305d-147">This returns the complete path to your website.</span></span> <span data-ttu-id="f305d-148">Beim Abrufen des Pfads für den Stammordner der Website, Sie als Benutzer der `~` Operator (Represen die Website des virtuellen Stamm), `MapPath`.</span><span class="sxs-lookup"><span data-stu-id="f305d-148">To get the path for the website root, you user the `~` operator (to represen the site's virtual root) to `MapPath`.</span></span> <span data-ttu-id="f305d-149">(Sie können auch einen Unterordnernamen, übergeben, z. B. *~/App\_Daten /*, um den Pfad für diese Unterordner abzurufen.) Sie können dann verketten Zusatzinformationen auf unabhängig von die Methode zurückgegeben wird, um einen vollständigen Pfad zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="f305d-149">(You can also pass a subfolder name to it, like *~/App\_Data/*, to get the path for that subfolder.) You can then concatenate additional information onto whatever the method returns in order to create a complete path.</span></span> <span data-ttu-id="f305d-150">In diesem Beispiel fügen Sie einen Dateinamen an.</span><span class="sxs-lookup"><span data-stu-id="f305d-150">In this example, you add a file name.</span></span> <span data-ttu-id="f305d-151">(Weitere Informationen zum Arbeiten mit Datei- und Ordnerpfade in [Einführung in ASP.NET Web Pages Programmierung mithilfe der Razor-Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span><span class="sxs-lookup"><span data-stu-id="f305d-151">(You can read more about how to work with file and folder paths in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span></span>

    <span data-ttu-id="f305d-152">Die Datei wird gespeichert, der *App\_Daten* Ordner.</span><span class="sxs-lookup"><span data-stu-id="f305d-152">The file is saved in the *App\_Data* folder.</span></span> <span data-ttu-id="f305d-153">Dieser Ordner ist ein spezieller Ordner, die zum Speichern von Datendateien, verwendet wird, wie beschrieben in ASP.NET [Einführung in die Arbeit mit einer Datenbank in ASP.NET Web Pages-Websites](https://go.microsoft.com/fwlink/?LinkId=195209).</span><span class="sxs-lookup"><span data-stu-id="f305d-153">This folder is a special folder in ASP.NET that's used to store data files, as described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=195209).</span></span>

    <span data-ttu-id="f305d-154">Die `WriteAllText` Methode der `File` Objekt schreibt die Daten in der Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-154">The `WriteAllText` method of the `File` object writes the data to the file.</span></span> <span data-ttu-id="f305d-155">Diese Methode akzeptiert zwei Parameter: den Namen (Pfad) der Datei zum Schreiben und die tatsächlichen Daten schreiben.</span><span class="sxs-lookup"><span data-stu-id="f305d-155">This method takes two parameters: the name (with path) of the file to write to, and the actual data to write.</span></span> <span data-ttu-id="f305d-156">Beachten Sie, dass mit der Namen des ersten Parameters ein `@` Zeichen als Präfix.</span><span class="sxs-lookup"><span data-stu-id="f305d-156">Notice that the name of the first parameter has an `@` character as a prefix.</span></span> <span data-ttu-id="f305d-157">Dies weist ASP.NET, die Sie eine wörtliche Zeichenfolge bereitgestellt, und Zeichen wie "/" nicht auf besondere Weise interpretiert werden soll.</span><span class="sxs-lookup"><span data-stu-id="f305d-157">This tells ASP.NET that you're providing a verbatim string literal, and that characters like "/" should not be interpreted in special ways.</span></span> <span data-ttu-id="f305d-158">(Weitere Informationen finden Sie unter [Einführung in ASP.NET Web-Programmierung mithilfe der Razor-Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span><span class="sxs-lookup"><span data-stu-id="f305d-158">(For more information, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=195205#ID_WorkingWithFileAndFolderPaths).)</span></span>

    > [!NOTE]
    > <span data-ttu-id="f305d-159">In der Reihenfolge für den Code zum Speichern von Dateien in den *App\_Daten* Ordner der Anwendung muss Lese-/ Schreibzugriff Berechtigungen für diesen Ordner.</span><span class="sxs-lookup"><span data-stu-id="f305d-159">In order for your code to save files in the *App\_Data* folder, the application needs read-write permissions for that folder.</span></span> <span data-ttu-id="f305d-160">Auf Ihrem Computer ist dies nicht in der Regel ein Problem.</span><span class="sxs-lookup"><span data-stu-id="f305d-160">On your development computer this is not typically an issue.</span></span> <span data-ttu-id="f305d-161">Wenn Sie Ihre Website mit einem Hostinganbieter Webserver veröffentlichen, müssen Sie diese Berechtigungen explizit festzulegen.</span><span class="sxs-lookup"><span data-stu-id="f305d-161">However, when you publish your site to a hosting provider's web server, you might need to explicitly set those permissions.</span></span> <span data-ttu-id="f305d-162">Wenn Sie diesen Code auf einem Hostinganbieter Server ausführen und Fehler gemeldet werden, überprüfen Sie mit dem Hostinganbieter, herauszufinden, wie diese Berechtigungen festlegen.</span><span class="sxs-lookup"><span data-stu-id="f305d-162">If you run this code on a hosting provider's server and get errors, check with the hosting provider to find out how to set those permissions.</span></span>

- <span data-ttu-id="f305d-163">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="f305d-163">Run the page in a browser.</span></span> 

    ![](working-with-files/_static/image1.jpg)
- <span data-ttu-id="f305d-164">Geben Sie Werte in die Felder aus, und klicken Sie dann auf **Absenden**.</span><span class="sxs-lookup"><span data-stu-id="f305d-164">Enter values into the fields and then click **Submit**.</span></span>
- <span data-ttu-id="f305d-165">Schließen Sie den Browser.</span><span class="sxs-lookup"><span data-stu-id="f305d-165">Close the browser.</span></span>
- <span data-ttu-id="f305d-166">Kehren Sie zum zurück, und aktualisieren Sie die Ansicht.</span><span class="sxs-lookup"><span data-stu-id="f305d-166">Return to the project and refresh the view.</span></span>
- <span data-ttu-id="f305d-167">Öffnen der *data.txt* Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-167">Open the *data.txt* file.</span></span> <span data-ttu-id="f305d-168">Die Daten, die Sie in das Formular gesendet werden, in der Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-168">The data you submitted in the form is in the file.</span></span> 

    ![[Image]](working-with-files/_static/image2.jpg)
- <span data-ttu-id="f305d-170">Schließen der *data.txt* Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-170">Close the *data.txt* file.</span></span>

<a id="Appending_Data"></a>
## <a name="appending-data-to-an-existing-file"></a><span data-ttu-id="f305d-171">Daten an eine vorhandene Datei anfügen</span><span class="sxs-lookup"><span data-stu-id="f305d-171">Appending Data to an Existing File</span></span>

<span data-ttu-id="f305d-172">Im vorherigen Beispiel, verwendet Sie `WriteAllText` zum Erstellen einer Textdatei, die nur ein Teil der Daten in diese Dateien enthält.</span><span class="sxs-lookup"><span data-stu-id="f305d-172">In the previous example, you used `WriteAllText` to create a text file that's got just one piece of data in it.</span></span> <span data-ttu-id="f305d-173">Wenn Sie die Methode erneut aufrufen und den Dateinamen zu übergeben, wird die vorhandene Datei vollständig überschrieben.</span><span class="sxs-lookup"><span data-stu-id="f305d-173">If you call the method again and pass it the same file name, the existing file is completely overwritten.</span></span> <span data-ttu-id="f305d-174">Allerdings nach der Erstellung einer Datei häufig neue Daten an das Ende der Datei hinzufügen möchten.</span><span class="sxs-lookup"><span data-stu-id="f305d-174">However, after you've created a file you often want to add new data to the end of the file.</span></span> <span data-ttu-id="f305d-175">Erreichen Sie, dass die Verwendung der `AppendAllText` Methode der `File` Objekt.</span><span class="sxs-lookup"><span data-stu-id="f305d-175">You can do that using the `AppendAllText` method of the `File` object.</span></span>

1. <span data-ttu-id="f305d-176">Stellen Sie auf der Website, eine Kopie der *UserData.cshtml* Datei, und nennen Sie die Kopie *UserDataMultiple.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f305d-176">In the website, make a copy of the *UserData.cshtml* file and name the copy *UserDataMultiple.cshtml*.</span></span>
2. <span data-ttu-id="f305d-177">Ersetzen Sie vor dem Öffnen den Codeblock `<!DOCTYPE html>` Tag mit den folgenden Codeblock:</span><span class="sxs-lookup"><span data-stu-id="f305d-177">Replace the code block before the opening `<!DOCTYPE html>` tag with the following code block:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample3.cshtml)]

    <span data-ttu-id="f305d-178">Dieser Code enthält eine Änderung aus dem vorherigen Beispiel.</span><span class="sxs-lookup"><span data-stu-id="f305d-178">This code has one change in it from the previous example.</span></span> <span data-ttu-id="f305d-179">Anstatt `WriteAllText`, verwendet er `the AppendAllText` Methode.</span><span class="sxs-lookup"><span data-stu-id="f305d-179">Instead of using `WriteAllText`, it uses `the AppendAllText` method.</span></span> <span data-ttu-id="f305d-180">Die Methoden sind ähnlich, außer dass `AppendAllText` fügt die Daten an das Ende der Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-180">The methods are similar, except that `AppendAllText` adds the data to the end of the file.</span></span> <span data-ttu-id="f305d-181">Wie bei `WriteAllText`, `AppendAllText` erstellt die Datei aus, wenn er nicht bereits vorhanden.</span><span class="sxs-lookup"><span data-stu-id="f305d-181">As with `WriteAllText`, `AppendAllText` creates the file if it doesn't already exist.</span></span>
3. <span data-ttu-id="f305d-182">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="f305d-182">Run the page in a browser.</span></span>
4. <span data-ttu-id="f305d-183">Geben Sie Werte für die Felder aus, und klicken Sie dann auf **Absenden**.</span><span class="sxs-lookup"><span data-stu-id="f305d-183">Enter values for the fields and then click **Submit**.</span></span>
5. <span data-ttu-id="f305d-184">Fügen Sie weitere Daten hinzu, und senden Sie das Formular erneut.</span><span class="sxs-lookup"><span data-stu-id="f305d-184">Add more data and submit the form again.</span></span>
6. <span data-ttu-id="f305d-185">Zurück zu Ihrem Projekt rechten Maustaste auf den Projektordner, und klicken Sie dann auf **aktualisieren**.</span><span class="sxs-lookup"><span data-stu-id="f305d-185">Return to your project, right-click the project folder, and then click **Refresh**.</span></span>
7. <span data-ttu-id="f305d-186">Öffnen der *data.txt* Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-186">Open the *data.txt* file.</span></span> <span data-ttu-id="f305d-187">Es enthält nun die neuen Daten, die Sie gerade eingegeben haben.</span><span class="sxs-lookup"><span data-stu-id="f305d-187">It now contains the new data that you just entered.</span></span> 

    ![[Image]](working-with-files/_static/image3.jpg)

<a id="Reading_and_Displaying_Data"></a>
## <a name="reading-and-displaying-data-from-a-file"></a><span data-ttu-id="f305d-189">Lesen und Anzeigen von Daten aus einer Datei</span><span class="sxs-lookup"><span data-stu-id="f305d-189">Reading and Displaying Data from a File</span></span>

<span data-ttu-id="f305d-190">Auch wenn Sie keine Daten in eine Textdatei schreiben müssen, müssen Sie möglicherweise gelegentlich zum Lesen von Daten aus einer.</span><span class="sxs-lookup"><span data-stu-id="f305d-190">Even if you don't need to write data to a text file, you'll probably sometimes need to read data from one.</span></span> <span data-ttu-id="f305d-191">Zu diesem Zweck können Sie erneut die `File` Objekt.</span><span class="sxs-lookup"><span data-stu-id="f305d-191">To do this, you can again use the `File` object.</span></span> <span data-ttu-id="f305d-192">Können Sie die `File` Objekt jede Zeile einzeln gelesen (getrennt durch Zeilenumbrüche) oder zum Lesen einzelnen Artikels unabhängig davon, wie sie getrennt sind.</span><span class="sxs-lookup"><span data-stu-id="f305d-192">You can use the `File` object to read each line individually (separated by line breaks) or to read individual item no matter how they're separated.</span></span>

<span data-ttu-id="f305d-193">Dieses Verfahren wird gezeigt, wie zum Lesen und Anzeigen der Daten, die Sie im vorherigen Beispiel erstellt.</span><span class="sxs-lookup"><span data-stu-id="f305d-193">This procedure shows you how to read and display the data that you created in the previous example.</span></span>

1. <span data-ttu-id="f305d-194">Erstellen Sie im Stammverzeichnis der Website eine neue Datei namens *DisplayData.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f305d-194">At the root of your website, create a new file named *DisplayData.cshtml*.</span></span>
2. <span data-ttu-id="f305d-195">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="f305d-195">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample4.cshtml)]

    <span data-ttu-id="f305d-196">Der Code beginnt mit dem Lesen der Datei, die Sie im vorherigen Beispiel erstellt, in einer Variablen namens haben `userData`, mithilfe dieser Methodenaufruf:</span><span class="sxs-lookup"><span data-stu-id="f305d-196">The code starts by reading the file that you created in the previous example into a variable named `userData`, using this method call:</span></span>

    [!code-css[Main](working-with-files/samples/sample5.css)]

    <span data-ttu-id="f305d-197">Der Code dazu befindet sich innerhalb einer `if` Anweisung.</span><span class="sxs-lookup"><span data-stu-id="f305d-197">The code to do this is inside an `if` statement.</span></span> <span data-ttu-id="f305d-198">Wenn Sie eine Datei zu lesen möchten, ist es eine gute Idee, verwenden Sie die `File.Exists` Methode, um zunächst zu bestimmen, ob die Datei verfügbar ist.</span><span class="sxs-lookup"><span data-stu-id="f305d-198">When you want to read a file, it's a good idea to use the `File.Exists` method to determine first whether the file is available.</span></span> <span data-ttu-id="f305d-199">Der Code überprüft außerdem, ob die Datei leer ist.</span><span class="sxs-lookup"><span data-stu-id="f305d-199">The code also checks whether the file is empty.</span></span>

    <span data-ttu-id="f305d-200">Der Text der Seite enthält zwei `foreach` Schleifen, die andere geschachtelten.</span><span class="sxs-lookup"><span data-stu-id="f305d-200">The body of the page contains two `foreach` loops, one nested inside the other.</span></span> <span data-ttu-id="f305d-201">Die äußere `foreach` Schleife Ruft eine Zeile zu einem Zeitpunkt aus der Datendatei.</span><span class="sxs-lookup"><span data-stu-id="f305d-201">The outer `foreach` loop gets one line at a time from the data file.</span></span> <span data-ttu-id="f305d-202">In diesem Fall werden die Zeilen durch Zeilenumbrüche in der Datei definiert &#8212; also ist jedes Datenelement in einer eigenen Zeile.</span><span class="sxs-lookup"><span data-stu-id="f305d-202">In this case, the lines are defined by line breaks in the file &#8212; that is, each data item is on its own line.</span></span> <span data-ttu-id="f305d-203">Die äußere Schleife erstellt ein neues Element (`<li>` Element) in eine geordnete Liste (`<ol>` Element).</span><span class="sxs-lookup"><span data-stu-id="f305d-203">The outer loop creates a new item (`<li>` element) inside an ordered list (`<ol>` element).</span></span>

    <span data-ttu-id="f305d-204">Die innere Schleife teilt jede Datenzeile in Elemente (Felder) mit einem Komma als Trennzeichen.</span><span class="sxs-lookup"><span data-stu-id="f305d-204">The inner loop splits each data line into items (fields) using a comma as a delimiter.</span></span> <span data-ttu-id="f305d-205">(Basierend auf dem vorhergehenden Beispiel, das bedeutet, dass jede Zeile drei Felder enthält &#8212; der Vorname, Nachname und e-Mail-Adresse, jeweils durch ein Komma getrennt.) Die innere Schleife erstellt außerdem eine `<ul>` Liste und zeigt eine Liste Element für jedes Feld in der Datenzeile.</span><span class="sxs-lookup"><span data-stu-id="f305d-205">(Based on the previous example, this means that each line contains three fields &#8212; the first name, last name, and email address, each separated by a comma.) The inner loop also creates a `<ul>` list and displays one list item for each field in the data line.</span></span>

    <span data-ttu-id="f305d-206">Der Code wird veranschaulicht, wie zwei Typen von Daten, ein Array verwenden und die `char` -Datentyp.</span><span class="sxs-lookup"><span data-stu-id="f305d-206">The code illustrates how to use two data types, an array and the `char` data type.</span></span> <span data-ttu-id="f305d-207">Das Array ist erforderlich, da die `File.ReadAllLines` -Methode Daten als Array zurückgibt.</span><span class="sxs-lookup"><span data-stu-id="f305d-207">The array is required because the `File.ReadAllLines` method returns data as an array.</span></span> <span data-ttu-id="f305d-208">Die `char` -Datentyp ist erforderlich, da die `Split` Methode gibt ein `array` in dem jedes Element des Typs ist `char`.</span><span class="sxs-lookup"><span data-stu-id="f305d-208">The `char` data type is required because the `Split` method returns an `array` in which each element is of the type `char`.</span></span> <span data-ttu-id="f305d-209">(Weitere Informationen zu Arrays finden Sie unter [Einführung in ASP.NET Web-Programmierung mithilfe der Razor-Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#ID_CollectionsAndObjects).)</span><span class="sxs-lookup"><span data-stu-id="f305d-209">(For information about arrays, see [Introduction to ASP.NET Web Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890#ID_CollectionsAndObjects).)</span></span>
3. <span data-ttu-id="f305d-210">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="f305d-210">Run the page in a browser.</span></span> <span data-ttu-id="f305d-211">Die Daten für den vorherigen Beispielen eingegebene werden angezeigt.</span><span class="sxs-lookup"><span data-stu-id="f305d-211">The data you entered for the previous examples is displayed.</span></span> 

    ![[Image]](working-with-files/_static/image4.jpg)

> [!TIP] 
> 
> <span data-ttu-id="f305d-213">**Anzeigen von Daten aus einer Microsoft Excel durch Trennzeichen getrennte Datei**</span><span class="sxs-lookup"><span data-stu-id="f305d-213">**Displaying Data from a Microsoft Excel Comma-Delimited File**</span></span>
> 
> <span data-ttu-id="f305d-214">Verwenden Sie Microsoft Excel, um die Daten in eine Tabelle als eine durch Trennzeichen getrennte Datei zu speichern (*CSV* Datei).</span><span class="sxs-lookup"><span data-stu-id="f305d-214">You can use Microsoft Excel to save the data contained in a spreadsheet as a comma-delimited file (*.csv* file).</span></span> <span data-ttu-id="f305d-215">In diesem Fall wird die Datei im nur-Text nicht in Excel-Format gespeichert.</span><span class="sxs-lookup"><span data-stu-id="f305d-215">When you do, the file is saved in plain text, not in Excel format.</span></span> <span data-ttu-id="f305d-216">Jede Zeile im Arbeitsblatt wird getrennt durch einen Zeilenumbruch in der Textdatei, und jedes Datenelement wird durch ein Komma getrennt.</span><span class="sxs-lookup"><span data-stu-id="f305d-216">Each row in the spreadsheet is separated by a line break in the text file, and each data item is separated by a comma.</span></span> <span data-ttu-id="f305d-217">Den Code im vorherigen Beispiel gezeigt können Sie eine durch Trennzeichen getrennten Excel-Datei lesen, indem Sie einfach den Namen der Datendatei im Code zu ändern.</span><span class="sxs-lookup"><span data-stu-id="f305d-217">You can use the code shown in the previous example to read an Excel comma-delimited file just by changing the name of the data file in your code.</span></span>


<a id="Deleting_Files"></a>
## <a name="deleting-files"></a><span data-ttu-id="f305d-218">Löschen von Dateien</span><span class="sxs-lookup"><span data-stu-id="f305d-218">Deleting Files</span></span>

<span data-ttu-id="f305d-219">Um Dateien aus der Website löschen, können Sie die `File.Delete` Methode.</span><span class="sxs-lookup"><span data-stu-id="f305d-219">To delete files from your website, you can use the `File.Delete` method.</span></span> <span data-ttu-id="f305d-220">Dieses Verfahren wird gezeigt, wie können Benutzer ein Bild löschen (*jpg* Datei) aus einer *Bilder* Ordner, wenn sie wissen, dass der Name der Datei.</span><span class="sxs-lookup"><span data-stu-id="f305d-220">This procedure shows how to let users delete an image (*.jpg* file) from an *images* folder if they know the name of the file.</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="f305d-221">**Wichtige** In eine Produktionswebsite Sie in der Regel einschränken, wer darf hat, um die Daten zu ändern.</span><span class="sxs-lookup"><span data-stu-id="f305d-221">**Important** In a production website, you typically restrict who's allowed to make changes to the data.</span></span> <span data-ttu-id="f305d-222">Informationen zum Einrichten der Mitgliedschaft und zu den Möglichkeiten zum Autorisieren von Benutzern für Aufgaben auf der Website finden Sie unter [Sicherheit hinzufügen und die Mitgliedschaft in einer ASP.NET Web Pages-Website](https://go.microsoft.com/fwlink/?LinkId=202904).</span><span class="sxs-lookup"><span data-stu-id="f305d-222">For information about how to set up membership and about ways to authorize users to perform tasks on the site, see [Adding Security and Membership to an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202904).</span></span>


1. <span data-ttu-id="f305d-223">Erstellen Sie auf der Website einen Unterordner namens *Bilder*.</span><span class="sxs-lookup"><span data-stu-id="f305d-223">In the website, create a subfolder named *images*.</span></span>
2. <span data-ttu-id="f305d-224">Kopieren einer oder mehrerer *jpg* Dateien in der *Bilder* Ordner.</span><span class="sxs-lookup"><span data-stu-id="f305d-224">Copy one or more *.jpg* files into the *images* folder.</span></span>
3. <span data-ttu-id="f305d-225">Erstellen Sie im Stammverzeichnis der Website, eine neue Datei namens *FileDelete.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f305d-225">In the root of the website, create a new file named *FileDelete.cshtml*.</span></span>
4. <span data-ttu-id="f305d-226">Ersetzen Sie den vorhandenen Inhalt durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="f305d-226">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample6.cshtml)]

    <span data-ttu-id="f305d-227">Diese Seite enthält eine Form, in dem Benutzer den Namen einer Bilddatei eingeben können.</span><span class="sxs-lookup"><span data-stu-id="f305d-227">This page contains a form where users can enter the name of an image file.</span></span> <span data-ttu-id="f305d-228">Sie geben Sie nicht die *jpg* Dateinamenerweiterung; durch Beschränken der Dateiname sieht, Sie Hilfe verhindert, dass Benutzer beliebige Dateien auf Ihrer Website gelöscht.</span><span class="sxs-lookup"><span data-stu-id="f305d-228">They don't enter the *.jpg* file-name extension; by restricting the file name like this, you help prevents users from deleting arbitrary files on your site.</span></span>

    <span data-ttu-id="f305d-229">Der Code liest den Dateinamen an, dass der Benutzer eingegeben hat, und klicken Sie dann einen vollständigen Pfad erstellt.</span><span class="sxs-lookup"><span data-stu-id="f305d-229">The code reads the file name that the user has entered and then constructs a complete path.</span></span> <span data-ttu-id="f305d-230">So erstellen Sie den Pfad, verwendet der Code den Pfad der aktuellen Website (wie vom der `Server.MapPath` Methode), die *Bilder* Ordnernamen, den Namen, den der Benutzer bereitgestellt hat und ".jpg" als Literalzeichenfolge.</span><span class="sxs-lookup"><span data-stu-id="f305d-230">To create the path, the code uses the current website path (as returned by the `Server.MapPath` method), the *images* folder name, the name that the user has provided, and ".jpg" as a literal string.</span></span>

    <span data-ttu-id="f305d-231">So löschen Sie die Datei, die der Code Ruft die `File.Delete` -Methode, und übergeben sie den vollständigen Pfad, die Sie soeben erstellt haben.</span><span class="sxs-lookup"><span data-stu-id="f305d-231">To delete the file, the code calls the `File.Delete` method, passing it the full path that you just constructed.</span></span> <span data-ttu-id="f305d-232">Am Ende das Markup zeigt Code für eine bestätigungsmeldung an, dass die Datei gelöscht wurde.</span><span class="sxs-lookup"><span data-stu-id="f305d-232">At the end of the markup, code displays a confirmation message that the file was deleted.</span></span>
5. <span data-ttu-id="f305d-233">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="f305d-233">Run the page in a browser.</span></span> 

    ![[Image]](working-with-files/_static/image5.jpg)
6. <span data-ttu-id="f305d-235">Geben Sie den Namen der Datei zu löschen, und klicken Sie dann auf **Absenden**.</span><span class="sxs-lookup"><span data-stu-id="f305d-235">Enter the name of the file to delete and then click **Submit**.</span></span> <span data-ttu-id="f305d-236">Wenn die Datei gelöscht wurde, wird der Name der Datei am unteren Rand der Seite angezeigt.</span><span class="sxs-lookup"><span data-stu-id="f305d-236">If the file was deleted, the name of the file is displayed at the bottom of the page.</span></span>

<a id="Letting_Users_Upload_a_File"></a>
## <a name="letting-users-upload-a-file"></a><span data-ttu-id="f305d-237">Infrastrukturcode Benutzer Hochladen einer Datei</span><span class="sxs-lookup"><span data-stu-id="f305d-237">Letting Users Upload a File</span></span>

<span data-ttu-id="f305d-238">Die `FileUpload` -Hilfsobjekt ermöglicht Benutzern, die Dateien auf Ihre Website hochzuladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-238">The `FileUpload` helper lets users upload files to your website.</span></span> <span data-ttu-id="f305d-239">Im folgenden Verfahren wird gezeigt, wie können Benutzer, die eine einzelne Datei hochladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-239">The procedure below shows you how to let users upload a single file.</span></span>

1. <span data-ttu-id="f305d-240">Der ASP.NET Web Helpers Library zu Ihrer Website hinzufügen, wie in beschrieben [installieren-Hilfsprogramme in einer ASP.NET Web Pages-Website](https://go.microsoft.com/fwlink/?LinkId=252372), wenn Sie zuvor hinzugefügt haben.</span><span class="sxs-lookup"><span data-stu-id="f305d-240">Add the ASP.NET Web Helpers Library to your website as described in [Installing Helpers in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=252372), if you didn't add it previously.</span></span>
2. <span data-ttu-id="f305d-241">In der *App\_Daten* Ordner, erstellen Sie ein neues einen Ordner, und nennen Sie sie *UploadedFiles*.</span><span class="sxs-lookup"><span data-stu-id="f305d-241">In the *App\_Data* folder, create a new a folder and name it *UploadedFiles*.</span></span>
3. <span data-ttu-id="f305d-242">Erstellen Sie im Stammverzeichnis, eine neue Datei namens *FileUpload.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f305d-242">In the root, create a new file named *FileUpload.cshtml*.</span></span>
4. <span data-ttu-id="f305d-243">Ersetzen Sie den vorhandenen Inhalt auf der Seite mit den folgenden:</span><span class="sxs-lookup"><span data-stu-id="f305d-243">Replace the existing content in the page with the following:</span></span> 

    [!code-cshtml[Main](working-with-files/samples/sample7.cshtml)]

    <span data-ttu-id="f305d-244">Der Body-Teil der Seite "verwendet die `FileUpload` Hilfsmethode zum Erstellen der Upload-Feld und die Schaltflächen, die Sie wahrscheinlich mit vertraut sind:</span><span class="sxs-lookup"><span data-stu-id="f305d-244">The body portion of the page uses the `FileUpload` helper to create the upload box and buttons that you're probably familiar with:</span></span>

    ![[Image]](working-with-files/_static/image6.jpg)

    <span data-ttu-id="f305d-246">Die Eigenschaften, die Sie, für festlegen die `FileUpload` Helper angeben, dass Sie ein einzelnes Kontrollkästchen für die Datei hochladen möchten, und dass die Schaltfläche "Absenden" gelesen werden soll **hochladen**.</span><span class="sxs-lookup"><span data-stu-id="f305d-246">The properties that you set for the `FileUpload` helper specify that you want a single box for the file to upload and that you want the submit button to read **Upload**.</span></span> <span data-ttu-id="f305d-247">(Sie werden später in diesem Artikel weitere Felder hinzufügen.)</span><span class="sxs-lookup"><span data-stu-id="f305d-247">(You'll add more boxes later in the article.)</span></span>

    <span data-ttu-id="f305d-248">Wenn der Benutzer klickt **hochladen**, der Code am oberen Rand der Seite ruft die Datei ab und speichert es.</span><span class="sxs-lookup"><span data-stu-id="f305d-248">When the user clicks **Upload**, the code at the top of the page gets the file and saves it.</span></span> <span data-ttu-id="f305d-249">Die `Request` -Objekt, das Sie normalerweise verwenden, um das Abrufen von Werten aus Formularfelder verfügt auch über eine `Files` Array, das die Datei (oder Dateien) enthält, die hochgeladen worden sein.</span><span class="sxs-lookup"><span data-stu-id="f305d-249">The `Request` object that you normally use to get values from form fields also has a `Files` array that contains the file (or files) that have been uploaded.</span></span> <span data-ttu-id="f305d-250">Sie können einzelne Dateien aus bestimmten Positionen im Array abrufen &#8212; zum Abrufen der ersten hochgeladenen Datei erhalten Sie z. B. `Request.Files[0]`, um die zweite Datei abzurufen erhalten Sie `Request.Files[1]`und so weiter.</span><span class="sxs-lookup"><span data-stu-id="f305d-250">You can get individual files out of specific positions in the array &#8212; for example, to get the first uploaded file, you get `Request.Files[0]`, to get the second file, you get `Request.Files[1]`, and so on.</span></span> <span data-ttu-id="f305d-251">(Beachten Sie, dass bei der Programmierung zählen in der Regel auf 0 (null) beginnt.)</span><span class="sxs-lookup"><span data-stu-id="f305d-251">(Remember that in programming, counting usually starts at zero.)</span></span>

    <span data-ttu-id="f305d-252">Wenn Sie eine hochgeladene Datei abzurufen, Sie fügen Sie ihn in einer Variablen (hier `uploadedFile`), damit Sie diese bearbeiten können.</span><span class="sxs-lookup"><span data-stu-id="f305d-252">When you fetch an uploaded file, you put it in a variable (here, `uploadedFile`) so that you can manipulate it.</span></span> <span data-ttu-id="f305d-253">Den Namen der hochgeladenen Datei ermitteln möchten, erhalten Sie nur seine `FileName` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="f305d-253">To determine the name of the uploaded file, you just get its `FileName` property.</span></span> <span data-ttu-id="f305d-254">Jedoch, wenn der Benutzer wird eine Datei hochgeladen, `FileName` enthält den Namen des Benutzers ursprünglichen, darunter den vollständigen Pfad.</span><span class="sxs-lookup"><span data-stu-id="f305d-254">However, when the user uploads a file, `FileName` contains the user's original name, which includes the entire path.</span></span> <span data-ttu-id="f305d-255">Es kann wie folgt aussehen:</span><span class="sxs-lookup"><span data-stu-id="f305d-255">It might look like this:</span></span>

    <span data-ttu-id="f305d-256">*C:\Users\Public\Sample.txt*</span><span class="sxs-lookup"><span data-stu-id="f305d-256">*C:\Users\Public\Sample.txt*</span></span>

    <span data-ttu-id="f305d-257">Sie soll nicht alle dieser Pfadinformationen allerdings, da dies der Pfad auf dem Computer des Benutzers, nicht für den Server ist.</span><span class="sxs-lookup"><span data-stu-id="f305d-257">You don't want all that path information, though, because that's the path on the user's computer, not for your server.</span></span> <span data-ttu-id="f305d-258">Sie möchten nur den tatsächlichen Dateinamen (*"Sample.txt"*).</span><span class="sxs-lookup"><span data-stu-id="f305d-258">You just want the actual file name (*Sample.txt*).</span></span> <span data-ttu-id="f305d-259">Sie müssen nur die Datei aus einem Pfad entfernen können, mithilfe der `Path.GetFileName` -Methode, wie folgt:</span><span class="sxs-lookup"><span data-stu-id="f305d-259">You can strip out just the file from a path by using the `Path.GetFileName` method, like this:</span></span>

    [!code-csharp[Main](working-with-files/samples/sample8.cs)]

    <span data-ttu-id="f305d-260">Die `Path` Objekt ist ein Hilfsprogramm, das eine Reihe von Methoden wie folgt, die Sie verwenden können verfügt, um Pfade zu entfernen, Kombinieren von Pfaden und so weiter.</span><span class="sxs-lookup"><span data-stu-id="f305d-260">The `Path` object is a utility that has a number of methods like this that you can use to strip paths, combine paths, and so on.</span></span>

    <span data-ttu-id="f305d-261">Nachdem Sie den Namen der hochgeladenen Datei gelangt sind, können Sie erstellen einen neuen Pfad für die hochgeladene Datei auf Ihrer Website gespeichert werden soll.</span><span class="sxs-lookup"><span data-stu-id="f305d-261">Once you've gotten the name of the uploaded file, you can build a new path for where you want to store the uploaded file in your website.</span></span> <span data-ttu-id="f305d-262">In diesem Fall kombinieren Sie `Server.MapPath`, die Ordnernamen (*App\_Daten/UploadedFiles*), und der neu blanker Dateiname, erstellen Sie einen neuen Pfad.</span><span class="sxs-lookup"><span data-stu-id="f305d-262">In this case, you combine `Server.MapPath`, the folder names (*App\_Data/UploadedFiles*), and the newly stripped file name to create a new path.</span></span> <span data-ttu-id="f305d-263">Rufen Sie dann der hochgeladenen Datei `SaveAs` Methode, um die Datei zu speichern.</span><span class="sxs-lookup"><span data-stu-id="f305d-263">You can then call the uploaded file's `SaveAs` method to actually save the file.</span></span>
5. <span data-ttu-id="f305d-264">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="f305d-264">Run the page in a browser.</span></span> 

    ![[Image]](working-with-files/_static/image7.jpg)
6. <span data-ttu-id="f305d-266">Klicken Sie auf **Durchsuchen** und wählen Sie dann eine Datei zum Hochladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-266">Click **Browse** and then select a file to upload.</span></span> 

    ![[Image]](working-with-files/_static/image8.jpg)

    <span data-ttu-id="f305d-268">Das Textfeld neben der **Durchsuchen** Schaltfläche enthält den Pfad und den Dateispeicherort.</span><span class="sxs-lookup"><span data-stu-id="f305d-268">The text box next to the **Browse** button will contain the path and file location.</span></span>

    ![[Image]](working-with-files/_static/image9.jpg)
7. <span data-ttu-id="f305d-270">Klicken Sie auf **Hochladen**.</span><span class="sxs-lookup"><span data-stu-id="f305d-270">Click **Upload**.</span></span>
8. <span data-ttu-id="f305d-271">Klicken Sie auf der Website mit der rechten Maustaste in des Projektordners, und klicken Sie dann auf **aktualisieren**.</span><span class="sxs-lookup"><span data-stu-id="f305d-271">In the website, right-click the project folder and then click **Refresh**.</span></span>
9. <span data-ttu-id="f305d-272">Öffnen der *UploadedFiles* Ordner.</span><span class="sxs-lookup"><span data-stu-id="f305d-272">Open the *UploadedFiles* folder.</span></span> <span data-ttu-id="f305d-273">Die Datei, die Sie hochgeladen haben, wird im Ordner "".</span><span class="sxs-lookup"><span data-stu-id="f305d-273">The file that you uploaded is in the folder.</span></span> 

    ![[Image]](working-with-files/_static/image10.jpg)

<a id="Letting_Users_Upload_Multiple_Files"></a>
## <a name="letting-users-upload-multiple-files"></a><span data-ttu-id="f305d-275">Ermöglicht Benutzern Hochladen mehrerer Dateien</span><span class="sxs-lookup"><span data-stu-id="f305d-275">Letting Users Upload Multiple Files</span></span>

<span data-ttu-id="f305d-276">Im vorherigen Beispiel können Sie Benutzer, die eine Datei hochladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-276">In the previous example, you let users upload one file.</span></span> <span data-ttu-id="f305d-277">Sie können jedoch die `FileUpload` Hilfsfunktion, um mehrere Dateien gleichzeitig hochladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-277">But you can use the `FileUpload` helper to upload more than one file at a time.</span></span> <span data-ttu-id="f305d-278">Dies ist praktisch für Szenarien wie beim Hochladen von Fotos, in denen Hochladen einer Datei zu einem Zeitpunkt aufwändig ist.</span><span class="sxs-lookup"><span data-stu-id="f305d-278">This is handy for scenarios like uploading photos, where uploading one file at a time is tedious.</span></span> <span data-ttu-id="f305d-279">(Finden Sie Informationen zum Hochladen von Fotos in [arbeiten mit Bildern in einer ASP.NET Web Pages-Website](https://go.microsoft.com/fwlink/?LinkId=202897).) Dieses Beispiel zeigt, wie Benutzer, die zwei gleichzeitig hochladen können, obwohl Sie das gleiche Verfahren verwenden können, um mehr als die hochzuladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-279">(You can read about uploading photos in [Working with Images in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=202897).) This example shows how to let users upload two at a time, although you can use the same technique to upload more than that.</span></span>

1. <span data-ttu-id="f305d-280">Der ASP.NET Web Helpers Library zu Ihrer Website hinzufügen, wie in beschrieben [installieren-Hilfsprogramme in einer ASP.NET Web Pages-Website](https://go.microsoft.com/fwlink/?LinkId=252372), sofern Sie noch nicht geschehen.</span><span class="sxs-lookup"><span data-stu-id="f305d-280">Add the ASP.NET Web Helpers Library to your website as described in [Installing Helpers in an ASP.NET Web Pages Site](https://go.microsoft.com/fwlink/?LinkId=252372), if you haven't already.</span></span>
2. <span data-ttu-id="f305d-281">Erstellen Sie eine neue Seite mit dem Namen *FileUploadMultiple.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="f305d-281">Create a new page named *FileUploadMultiple.cshtml*.</span></span>
3. <span data-ttu-id="f305d-282">Ersetzen Sie den vorhandenen Inhalt auf der Seite mit den folgenden:</span><span class="sxs-lookup"><span data-stu-id="f305d-282">Replace the existing content in the page with the following:</span></span>  

    [!code-cshtml[Main](working-with-files/samples/sample9.cshtml)]

    <span data-ttu-id="f305d-283">In diesem Beispiel wird die `FileUpload` Helper im Hauptteil der Seite "ist so konfiguriert, dass Benutzer, die zwei Dateien hochladen können standardmäßig.</span><span class="sxs-lookup"><span data-stu-id="f305d-283">In this example, the `FileUpload` helper in the body of the page is configured to let users upload two files by default.</span></span> <span data-ttu-id="f305d-284">Da `allowMoreFilesToBeAdded` festgelegt ist, um `true`, die-Hilfsmethode rendert einen Link zum Benutzer, die weitere Upload-Felder hinzufügen:</span><span class="sxs-lookup"><span data-stu-id="f305d-284">Because `allowMoreFilesToBeAdded` is set to `true`, the helper renders a link that lets user add more upload boxes:</span></span>

    ![[Image]](working-with-files/_static/image11.jpg)

    <span data-ttu-id="f305d-286">Um die Dateien zu verarbeiten, die der Benutzer hochgeladen, der Code verwendet das gleiche grundlegende Verfahren, die Sie im vorherigen Beispiel verwendet &#8212; Abrufen einer Datei aus `Request.Files` und speichern Sie sie.</span><span class="sxs-lookup"><span data-stu-id="f305d-286">To process the files that the user uploads, the code uses the same basic technique that you used in the previous example &#8212; get a file from `Request.Files` and then save it.</span></span> <span data-ttu-id="f305d-287">(Einschließlich verschiedene Dinge müssen Sie tun können, um den richtigen Dateinamen und Pfad.) Die Innovation diesmal ist, dass der Benutzer möglicherweise mehrerer Dateien hochladen und viele nicht kennen.</span><span class="sxs-lookup"><span data-stu-id="f305d-287">(Including the various things you need to do to get the right file name and path.) The innovation this time is that the user might be uploading multiple files and you don't know many.</span></span> <span data-ttu-id="f305d-288">Um herauszufinden, erhalten Sie `Request.Files.Count`.</span><span class="sxs-lookup"><span data-stu-id="f305d-288">To find out, you can get `Request.Files.Count`.</span></span>

    <span data-ttu-id="f305d-289">Mit dieser Nummer in Hand, können Sie eine Schleife über `Request.Files`, jede Datei wiederum abrufen und speichern Sie sie.</span><span class="sxs-lookup"><span data-stu-id="f305d-289">With this number in hand, you can loop through `Request.Files`, fetch each file in turn, and save it.</span></span> <span data-ttu-id="f305d-290">Wenn eine bekannte Anzahl von Malen durch eine Auflistung in einer Schleife werden sollen, können Sie eine `for` Schleife wie folgt:</span><span class="sxs-lookup"><span data-stu-id="f305d-290">When you want to loop a known number of times through a collection, you can use a `for` loop, like this:</span></span>

    [!code-csharp[Main](working-with-files/samples/sample10.cs)]

    <span data-ttu-id="f305d-291">Die Variable `i` ist ein temporärer Zähler, die aus 0 (null), um die Obergrenze aufgenommen werden, festlegen.</span><span class="sxs-lookup"><span data-stu-id="f305d-291">The variable `i` is just a temporary counter that will go from zero to whatever upper limit you set.</span></span> <span data-ttu-id="f305d-292">In diesem Fall wird die Obergrenze für die Anzahl der Dateien.</span><span class="sxs-lookup"><span data-stu-id="f305d-292">In this case, the upper limit is the number of files.</span></span> <span data-ttu-id="f305d-293">Da der Zähler auf 0 (null), wie für das zählen in Szenarien in ASP.NET wird gestartet wird, die Obergrenze weist jedoch tatsächlich um eins kleiner als die Anzahl der Dateien.</span><span class="sxs-lookup"><span data-stu-id="f305d-293">But because the counter starts at zero, as is typical for counting scenarios in ASP.NET, the upper limit is actually one less than the file count.</span></span> <span data-ttu-id="f305d-294">(Wenn drei Dateien hochgeladen wurden, ist die Anzahl 0 (null) auf 2.)</span><span class="sxs-lookup"><span data-stu-id="f305d-294">(If three files are uploaded, the count is zero to 2.)</span></span>

    <span data-ttu-id="f305d-295">Die `uploadedCount` Variable summiert die Dateien, die erfolgreich hochgeladen und gespeichert werden.</span><span class="sxs-lookup"><span data-stu-id="f305d-295">The `uploadedCount` variable totals all the files that are successfully uploaded and saved.</span></span> <span data-ttu-id="f305d-296">Dieser Code Konten für die Möglichkeit, die eine erwartete Datei möglicherweise nicht hochgeladen werden können.</span><span class="sxs-lookup"><span data-stu-id="f305d-296">This code accounts for the possibility that an expected file may not be able to be uploaded.</span></span>
4. <span data-ttu-id="f305d-297">Führen Sie die Seite in einem Browser aus.</span><span class="sxs-lookup"><span data-stu-id="f305d-297">Run the page in a browser.</span></span> <span data-ttu-id="f305d-298">Der Browser zeigt die zwei Upload-Feldern und die Seite.</span><span class="sxs-lookup"><span data-stu-id="f305d-298">The browser displays the page and its two upload boxes.</span></span>
5. <span data-ttu-id="f305d-299">Wählen Sie zwei Dateien hochladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-299">Select two files to upload.</span></span>
6. <span data-ttu-id="f305d-300">Klicken Sie auf **fügen Sie eine andere Datei**.</span><span class="sxs-lookup"><span data-stu-id="f305d-300">Click **Add another file**.</span></span> <span data-ttu-id="f305d-301">Die Seite zeigt ein Feld "Neues hochladen".</span><span class="sxs-lookup"><span data-stu-id="f305d-301">The page displays a new upload box.</span></span> 

    ![[Image]](working-with-files/_static/image12.jpg)
7. <span data-ttu-id="f305d-303">Klicken Sie auf **Hochladen**.</span><span class="sxs-lookup"><span data-stu-id="f305d-303">Click **Upload**.</span></span>
8. <span data-ttu-id="f305d-304">Klicken Sie auf der Website mit der rechten Maustaste in des Projektordners, und klicken Sie dann auf **aktualisieren**.</span><span class="sxs-lookup"><span data-stu-id="f305d-304">In the website, right-click the project folder and then click **Refresh**.</span></span>
9. <span data-ttu-id="f305d-305">Öffnen der *UploadedFiles* sehen Sie die Dateien erfolgreich hochgeladen.</span><span class="sxs-lookup"><span data-stu-id="f305d-305">Open the *UploadedFiles* folder to see the successfully uploaded files.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="f305d-306">Zusätzliche Ressourcen</span><span class="sxs-lookup"><span data-stu-id="f305d-306">Additional Resources</span></span>


[<span data-ttu-id="f305d-307">Arbeiten mit Bildern in einer ASP.NET Web Pages-Website</span><span class="sxs-lookup"><span data-stu-id="f305d-307">Working with Images in an ASP.NET Web Pages Site</span></span>](https://go.microsoft.com/fwlink/?LinkId=202897)

[<span data-ttu-id="f305d-308">Exportieren als CSV-Datei</span><span class="sxs-lookup"><span data-stu-id="f305d-308">Exporting to a CSV File</span></span>](https://msdn.microsoft.com/library/ms155919.aspx)
