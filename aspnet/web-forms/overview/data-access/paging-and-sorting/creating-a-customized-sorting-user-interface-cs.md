---
uid: web-forms/overview/data-access/paging-and-sorting/creating-a-customized-sorting-user-interface-cs
title: Erstellen einer angepassten Benutzeroberfläche zum Sortieren (c#) | Microsoft-Dokumentation
author: rick-anderson
description: Wenn Daten mit einer langen Liste sortiert werden, es kann sehr hilfreich sein, gruppieren Sie verknüpfte Daten durch die Einführung von Trennzeichen für Zeilen. In diesem Tutorial sehen, dass wie ASK erstellen...
ms.author: aspnetcontent
ms.date: 08/15/2006
ms.assetid: 6f81b633-9d01-4e52-ae4a-2ea6bc109475
msc.legacyurl: /web-forms/overview/data-access/paging-and-sorting/creating-a-customized-sorting-user-interface-cs
msc.type: authoredcontent
ms.openlocfilehash: 96f52b3caf812fcfe82012c2de091acb91b8e0ae
ms.sourcegitcommit: b28cd0313af316c051c2ff8549865bff67f2fbb4
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 07/05/2018
ms.locfileid: "37842331"
---
<a name="creating-a-customized-sorting-user-interface-c"></a><span data-ttu-id="eda07-104">Erstellen einer angepassten Benutzeroberfläche zum Sortieren (c#)</span><span class="sxs-lookup"><span data-stu-id="eda07-104">Creating a Customized Sorting User Interface (C#)</span></span>
====================
<span data-ttu-id="eda07-105">durch [Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="eda07-105">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="eda07-106">[Beispiel-App herunter](http://download.microsoft.com/download/9/c/1/9c1d03ee-29ba-4d58-aa1a-f201dcc822ea/ASPNET_Data_Tutorial_27_CS.exe) oder [PDF-Datei herunterladen](creating-a-customized-sorting-user-interface-cs/_static/datatutorial27cs1.pdf)</span><span class="sxs-lookup"><span data-stu-id="eda07-106">[Download Sample App](http://download.microsoft.com/download/9/c/1/9c1d03ee-29ba-4d58-aa1a-f201dcc822ea/ASPNET_Data_Tutorial_27_CS.exe) or [Download PDF](creating-a-customized-sorting-user-interface-cs/_static/datatutorial27cs1.pdf)</span></span>

> <span data-ttu-id="eda07-107">Wenn Daten mit einer langen Liste sortiert werden, es kann sehr hilfreich sein, gruppieren Sie verknüpfte Daten durch die Einführung von Trennzeichen für Zeilen.</span><span class="sxs-lookup"><span data-stu-id="eda07-107">When displaying a long list of sorted data, it can be very helpful to group related data by introducing separator rows.</span></span> <span data-ttu-id="eda07-108">In diesem Tutorial sehen wir, wie Sie solche eine Sortierung Benutzeroberfläche zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="eda07-108">In this tutorial we'll see how to create such a sorting user interface.</span></span>


## <a name="introduction"></a><span data-ttu-id="eda07-109">Einführung</span><span class="sxs-lookup"><span data-stu-id="eda07-109">Introduction</span></span>

<span data-ttu-id="eda07-110">Wenn mit einer langen Liste sortierter Daten vorhanden sind nur wenige unterschiedliche Werte in der sortierten Spalte an, ein Benutzer finden es möglicherweise schwierig, genau die Unterschied Grenzen auftreten zu erkennen.</span><span class="sxs-lookup"><span data-stu-id="eda07-110">When displaying a long list of sorted data where there are only a handful of different values in the sorted column, an end user might find it hard to discern where, exactly, the difference boundaries occur.</span></span> <span data-ttu-id="eda07-111">Beispielsweise sind 81 Produkte in der Datenbank, aber nur neun andere Kategorie-Optionen (acht eindeutigen Kategorien sowie die `NULL` Option).</span><span class="sxs-lookup"><span data-stu-id="eda07-111">For example, there are 81 products in the database, but only nine different category choices (eight unique categories plus the `NULL` option).</span></span> <span data-ttu-id="eda07-112">Betrachten Sie den Fall eines Benutzers ist daran interessiert, untersuchen die Produkte, die unter der Kategorie "Seafood" fallen.</span><span class="sxs-lookup"><span data-stu-id="eda07-112">Consider the case of a user who is interested in examining the products that fall under the Seafood category.</span></span> <span data-ttu-id="eda07-113">Von einer Seite, die auflistet *alle* der Produkte in einer einzelnen GridView, kann der Benutzer entscheiden, ihr am sinnvollsten ist, zum Sortieren der Ergebnisse nach Kategorie, die gruppiert werden alle Produkte Seafood zusammen.</span><span class="sxs-lookup"><span data-stu-id="eda07-113">From a page that lists *all* of the products in a single GridView, the user might decide her best bet is to sort the results by category, which will group together all of the Seafood products together.</span></span> <span data-ttu-id="eda07-114">Nach dem Sortieren nach Kategorie, muss der Benutzer über der Liste zu suchen gesucht, in denen die Produkte Seafood gruppiert beginnen und enden.</span><span class="sxs-lookup"><span data-stu-id="eda07-114">After sorting by category, the user then needs to hunt through the list, looking for where the Seafood-grouped products start and end.</span></span> <span data-ttu-id="eda07-115">Da die Ergebnisse nach alphabetischer Reihenfolge sortiert werden der Kategoriename der Seafood Produkte zu finden, ist nicht schwierig, aber erfordert er dennoch genau überprüft die Liste der Elemente im Raster.</span><span class="sxs-lookup"><span data-stu-id="eda07-115">Since the results are ordered alphabetically by the category name finding the Seafood products is not difficult, but it still requires closely scanning the list of items in the grid.</span></span>

<span data-ttu-id="eda07-116">Damit können die Grenzen zwischen sortierte Gruppen markieren, nutzen viele Websites eine Benutzeroberfläche, die ein Trennzeichen zwischen Gruppen hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="eda07-116">To help highlight the boundaries between sorted groups, many websites employ a user interface that adds a separator between such groups.</span></span> <span data-ttu-id="eda07-117">Trennzeichen, wie in Abbildung 1 dargestellten ermöglicht Benutzern schneller eine bestimmte Gruppe suchen und identifizieren die Grenzen, als auch ermitteln, welche unterschiedlichen Gruppen in den Daten vorhanden sind.</span><span class="sxs-lookup"><span data-stu-id="eda07-117">Separators like the ones shown in Figure 1 enables a user to more quickly find a particular group and identify its boundaries, as well as ascertain what distinct groups exist in the data.</span></span>


<span data-ttu-id="eda07-118">[![Jede Kategoriegruppe ist eindeutig identifiziert](creating-a-customized-sorting-user-interface-cs/_static/image2.png)](creating-a-customized-sorting-user-interface-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="eda07-118">[![Each Category Group is Clearly Identified](creating-a-customized-sorting-user-interface-cs/_static/image2.png)](creating-a-customized-sorting-user-interface-cs/_static/image1.png)</span></span>

<span data-ttu-id="eda07-119">**Abbildung 1**: Jede Kategoriegruppe ist eindeutig identifiziert ([klicken Sie, um das Bild in voller Größe anzeigen](creating-a-customized-sorting-user-interface-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="eda07-119">**Figure 1**: Each Category Group is Clearly Identified ([Click to view full-size image](creating-a-customized-sorting-user-interface-cs/_static/image3.png))</span></span>


<span data-ttu-id="eda07-120">In diesem Tutorial sehen wir, wie Sie solche eine Sortierung Benutzeroberfläche zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="eda07-120">In this tutorial we'll see how to create such a sorting user interface.</span></span>

## <a name="step-1-creating-a-standard-sortable-gridview"></a><span data-ttu-id="eda07-121">Schritt 1: Erstellen einer standardmäßigen, sortierbar GridView-Ansicht</span><span class="sxs-lookup"><span data-stu-id="eda07-121">Step 1: Creating a Standard, Sortable GridView</span></span>

<span data-ttu-id="eda07-122">Bevor wir Gewusst wie: verbessern die GridView, um die erweiterte Sortierung Oberfläche bereitzustellen, zu untersuchen, können Sie s, erstellen Sie zunächst eine standard, sortierbare GridView, die die Produkte aufgelistet werden.</span><span class="sxs-lookup"><span data-stu-id="eda07-122">Before we explore how to augment the GridView to provide the enhanced sorting interface, let s first create a standard, sortable GridView that lists the products.</span></span> <span data-ttu-id="eda07-123">Öffnen Sie zunächst die `CustomSortingUI.aspx` auf der Seite die `PagingAndSorting` Ordner.</span><span class="sxs-lookup"><span data-stu-id="eda07-123">Start by opening the `CustomSortingUI.aspx` page in the `PagingAndSorting` folder.</span></span> <span data-ttu-id="eda07-124">Hinzufügen einer GridView-Ansicht auf der Seite, legen Sie dessen `ID` Eigenschaft `ProductList`, und binden sie an eine neue "ObjectDataSource".</span><span class="sxs-lookup"><span data-stu-id="eda07-124">Add a GridView to the page, set its `ID` property to `ProductList`, and bind it to a new ObjectDataSource.</span></span> <span data-ttu-id="eda07-125">Konfigurieren Sie mit dem ObjectDataSource-Steuerelement die `ProductsBLL` Klasse s `GetProducts()` Methode zum Auswählen von Datensätzen.</span><span class="sxs-lookup"><span data-stu-id="eda07-125">Configure the ObjectDataSource to use the `ProductsBLL` class s `GetProducts()` method for selecting records.</span></span>

<span data-ttu-id="eda07-126">Konfigurieren Sie anschließend die GridView, nur enthält die `ProductName`, `CategoryName`, `SupplierName`, und `UnitPrice` BoundFields und CheckBoxField nicht mehr unterstützt.</span><span class="sxs-lookup"><span data-stu-id="eda07-126">Next, configure the GridView such that it only contains the `ProductName`, `CategoryName`, `SupplierName`, and `UnitPrice` BoundFields and the Discontinued CheckBoxField.</span></span> <span data-ttu-id="eda07-127">Konfigurieren Sie abschließend die GridView zur Unterstützung von Sortierung durch Aktivieren des Kontrollkästchens Sortieren aktivieren, in das GridView-s-Smarttag (oder durch Festlegen seiner `AllowSorting` Eigenschaft `true`).</span><span class="sxs-lookup"><span data-stu-id="eda07-127">Finally, configure the GridView to support sorting by checking the Enable Sorting checkbox in the GridView s smart tag (or by setting its `AllowSorting` property to `true`).</span></span> <span data-ttu-id="eda07-128">Nach dem Herstellen dieser Ergänzungen zu den `CustomSortingUI.aspx` Seite deklarative Markup sollte etwa wie folgt aussehen:</span><span class="sxs-lookup"><span data-stu-id="eda07-128">After making these additions to the `CustomSortingUI.aspx` page, the declarative markup should look similar to the following:</span></span>


[!code-aspx[Main](creating-a-customized-sorting-user-interface-cs/samples/sample1.aspx)]

<span data-ttu-id="eda07-129">Nehmen Sie einen Moment Zeit, um unseren Fortschritt bisher in einem Browser anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="eda07-129">Take a moment to view our progress thus far in a browser.</span></span> <span data-ttu-id="eda07-130">Abbildung 2 zeigt das sortierbare GridView, wenn die Daten nach Kategorie in alphabetischer Reihenfolge sortiert ist.</span><span class="sxs-lookup"><span data-stu-id="eda07-130">Figure 2 shows the sortable GridView when its data is sorted by category in alphabetical order.</span></span>


<span data-ttu-id="eda07-131">[![Der sortierbare GridView-s werden Daten nach Kategorie sortiert.](creating-a-customized-sorting-user-interface-cs/_static/image5.png)](creating-a-customized-sorting-user-interface-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="eda07-131">[![The Sortable GridView s Data is Ordered by Category](creating-a-customized-sorting-user-interface-cs/_static/image5.png)](creating-a-customized-sorting-user-interface-cs/_static/image4.png)</span></span>

<span data-ttu-id="eda07-132">**Abbildung 2**: das sortierbare GridView s, die Daten nach Kategorie sortiert werden ([klicken Sie, um das Bild in voller Größe anzeigen](creating-a-customized-sorting-user-interface-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="eda07-132">**Figure 2**: The Sortable GridView s Data is Ordered by Category ([Click to view full-size image](creating-a-customized-sorting-user-interface-cs/_static/image6.png))</span></span>


## <a name="step-2-exploring-techniques-for-adding-the-separator-rows"></a><span data-ttu-id="eda07-133">Schritt 2: Untersuchen von Techniken für das Trennzeichen für Zeilen hinzufügen</span><span class="sxs-lookup"><span data-stu-id="eda07-133">Step 2: Exploring Techniques for Adding the Separator Rows</span></span>

<span data-ttu-id="eda07-134">Mit generischen, sortierbar GridView vollständige übrig bleibt, um die Trennzeichen für Zeilen in der GridView vor jeder eindeutige sortierten Gruppe hinzufügen zu können.</span><span class="sxs-lookup"><span data-stu-id="eda07-134">With the generic, sortable GridView complete, all that remains is to be able to add the separator rows in the GridView before each unique sorted group.</span></span> <span data-ttu-id="eda07-135">Aber wie können solche Zeilen eingefügt werden in der GridView?</span><span class="sxs-lookup"><span data-stu-id="eda07-135">But how can such rows be injected into the GridView?</span></span> <span data-ttu-id="eda07-136">Im Wesentlichen, wir benötigen zum iterieren durch die GridView-s-Zeilen bestimmen, in denen die Unterschiede zwischen den Werten in der sortierten Spalte auftreten, und fügen Sie das entsprechende Trennzeichen-Zeile hinzu.</span><span class="sxs-lookup"><span data-stu-id="eda07-136">Essentially, we need to iterate through the GridView s rows, determine where the differences occur between the values in the sorted column, and then add the appropriate separator row.</span></span> <span data-ttu-id="eda07-137">Bei Informationen zu diesem Problem scheint es natürlich, dass die Lösung an einer beliebigen Stelle in den GridView-s liegt `RowDataBound` -Ereignishandler.</span><span class="sxs-lookup"><span data-stu-id="eda07-137">When thinking about this problem, it seems natural that the solution lies somewhere in the GridView s `RowDataBound` event handler.</span></span> <span data-ttu-id="eda07-138">Wie wir unter den [benutzerdefinierte Formatierung basierend auf Daten](../custom-formatting/custom-formatting-based-upon-data-cs.md) Tutorial dieser Ereignishandler wird häufig verwendet, wenn es sich bei anhand von Daten aus der Zeile s auf Zeilenebene Formatierung anwenden.</span><span class="sxs-lookup"><span data-stu-id="eda07-138">As we discussed in the [Custom Formatting Based Upon Data](../custom-formatting/custom-formatting-based-upon-data-cs.md) tutorial, this event handler is commonly used when applying row-level formatting based on the row s data.</span></span> <span data-ttu-id="eda07-139">Allerdings die `RowDataBound` -Ereignishandler ist nicht die Lösung hier, wie Zeilen an die GridView programmgesteuert aus diesem Ereignishandler hinzugefügt werden können.</span><span class="sxs-lookup"><span data-stu-id="eda07-139">However, the `RowDataBound` event handler is not the solution here, as rows cannot be added to the GridView programmatically from this event handler.</span></span> <span data-ttu-id="eda07-140">Das GridView-s `Rows` -Auflistung, in der Tat schreibgeschützt ist.</span><span class="sxs-lookup"><span data-stu-id="eda07-140">The GridView s `Rows` collection, in fact, is read-only.</span></span>

<span data-ttu-id="eda07-141">Zum Hinzufügen von zusätzlicher Zeilen an die GridView gibt es drei Möglichkeiten zur Auswahl:</span><span class="sxs-lookup"><span data-stu-id="eda07-141">To add additional rows to the GridView we have three choices:</span></span>

- <span data-ttu-id="eda07-142">Fügen Sie diese Metadaten Trennzeichen für Zeilen, auf die tatsächlichen Daten, die an die GridView gebunden ist</span><span class="sxs-lookup"><span data-stu-id="eda07-142">Add these metadata separator rows to the actual data that is bound to the GridView</span></span>
- <span data-ttu-id="eda07-143">Nachdem Sie auf die Daten die GridView gebunden wurde, hinzufügen, zusätzliche `TableRow` Instanzen der GridView-s steuern Auflistung</span><span class="sxs-lookup"><span data-stu-id="eda07-143">After the GridView has been bound to the data, add additional `TableRow` instances to the GridView s control collection</span></span>
- <span data-ttu-id="eda07-144">Erstellen Sie ein benutzerdefiniertes Serversteuerelement, das erweitert des GridView-Steuerelements und überschreibt die Methoden, die verantwortlich für die Erstellung der GridView-s-Struktur</span><span class="sxs-lookup"><span data-stu-id="eda07-144">Create a custom server control that extends the GridView control and overrides those methods responsible for constructing the GridView s structure</span></span>

<span data-ttu-id="eda07-145">Erstellen ein benutzerdefiniertes Serversteuerelement wäre am besten, wenn diese Funktionalität auf viele Webseiten oder über mehrere Websites erforderlich war.</span><span class="sxs-lookup"><span data-stu-id="eda07-145">Creating a custom server control would be the best approach if this functionality was needed on many web pages or across several websites.</span></span> <span data-ttu-id="eda07-146">Es wäre jedoch einiges an Code und eine gründliche Untersuchung in die Tiefe der die interne Funktionsweise des GridView-s gelten.</span><span class="sxs-lookup"><span data-stu-id="eda07-146">However, it would entail quite a bit of code and a thorough exploration into the depths of the GridView s internal workings.</span></span> <span data-ttu-id="eda07-147">Aus diesem Grund werden wir die Option für dieses Tutorial nicht berücksichtigt.</span><span class="sxs-lookup"><span data-stu-id="eda07-147">Therefore, we'll not consider that option for this tutorial.</span></span>

<span data-ttu-id="eda07-148">Die anderen beiden Optionen Hinzufügen von Trennzeichen für Zeilen, die tatsächlichen Daten, an die GridView gebunden, und Bearbeiten von der GridView-s-steuerelementauflistung nach dessen wurde gebunden – das Problem anders Angriffe und eine Erläuterung bedürfen.</span><span class="sxs-lookup"><span data-stu-id="eda07-148">The other two options adding separator rows to the actual data being bound to the GridView and manipulating the GridView s control collection after its been bound - attack the problem differently and merit a discussion.</span></span>

## <a name="adding-rows-to-the-data-bound-to-the-gridview"></a><span data-ttu-id="eda07-149">Hinzufügen von Zeilen auf die Daten an die GridView gebunden</span><span class="sxs-lookup"><span data-stu-id="eda07-149">Adding Rows to the Data Bound to the GridView</span></span>

<span data-ttu-id="eda07-150">Wenn die GridView mit einer Datenquelle gebunden ist, erstellt es einen `GridViewRow` für jeden Datensatz von der Datenquelle zurückgegeben.</span><span class="sxs-lookup"><span data-stu-id="eda07-150">When the GridView is bound to a data source, it creates a `GridViewRow` for each record returned by the data source.</span></span> <span data-ttu-id="eda07-151">Aus diesem Grund können wir die erforderlichen Trennzeichen für Zeilen einfügen, durch Trennzeichen Datensätze an die Datenquelle hinzufügen, bevor er an die GridView gebunden.</span><span class="sxs-lookup"><span data-stu-id="eda07-151">Therefore, we can inject the needed separator rows by adding separator records to the data source before binding it to the GridView.</span></span> <span data-ttu-id="eda07-152">Abbildung 3 veranschaulicht dieses Konzept.</span><span class="sxs-lookup"><span data-stu-id="eda07-152">Figure 3 illustrates this concept.</span></span>


![Eine Technik wird das Hinzufügen von Trennzeichen für Zeilen mit der Datenquelle](creating-a-customized-sorting-user-interface-cs/_static/image7.png)

<span data-ttu-id="eda07-154">**Abbildung 3**: eine Technik wird das Hinzufügen von Trennzeichen für Zeilen mit der Datenquelle</span><span class="sxs-lookup"><span data-stu-id="eda07-154">**Figure 3**: One Technique Involves Adding Separator Rows to the Data Source</span></span>


<span data-ttu-id="eda07-155">Ich verwende Begriff Trennzeichen Datensätze in Anführungszeichen, da es keine spezielle Trennzeichen für Datensatz; Stattdessen müssen wir irgendwie flag, die ein bestimmter Datensatz in der Datenquelle als Trennzeichen und nicht als eine Zeile für normale Daten dient.</span><span class="sxs-lookup"><span data-stu-id="eda07-155">I use the term separator records in quotes because there is no special separator record; rather, we must somehow flag that a particular record in the data source serves as a separator rather than a normal data row.</span></span> <span data-ttu-id="eda07-156">Für unseren Beispielen wir re-Bindung eine `ProductsDataTable` Instanz an die GridView, das aus besteht `ProductRows`.</span><span class="sxs-lookup"><span data-stu-id="eda07-156">For our examples, we re binding a `ProductsDataTable` instance to the GridView, which is composed of `ProductRows`.</span></span> <span data-ttu-id="eda07-157">Wir können einen Datensatz als eine Trennzeichenzeile kennzeichnen, durch Festlegen der `CategoryID` Eigenschaft `-1` (da es sich um eine solche eine Wert konnte t normalerweise vorhanden ist).</span><span class="sxs-lookup"><span data-stu-id="eda07-157">We might flag a record as a separator row by setting its `CategoryID` property to `-1` (since such a value couldn t exist normally).</span></span>

<span data-ttu-id="eda07-158">Um dieses Verfahren nutzen zu können, müssen wir d die folgenden Schritte ausführen:</span><span class="sxs-lookup"><span data-stu-id="eda07-158">To utilize this technique we d need to perform the following steps:</span></span>

1. <span data-ttu-id="eda07-159">Programmgesteuertes Abrufen von Daten an die GridView zu binden (eine `ProductsDataTable` Instanz)</span><span class="sxs-lookup"><span data-stu-id="eda07-159">Programmatically retrieve the data to bind to the GridView (a `ProductsDataTable` instance)</span></span>
2. <span data-ttu-id="eda07-160">Sortieren Sie die Daten basierend auf der GridView-s `SortExpression` und `SortDirection` Eigenschaften</span><span class="sxs-lookup"><span data-stu-id="eda07-160">Sort the data based on the GridView s `SortExpression` and `SortDirection` properties</span></span>
3. <span data-ttu-id="eda07-161">Durchlaufen der `ProductsRows` in die `ProductsDataTable`, suchen, wo die Unterschiede in der sortierten Spalte liegen</span><span class="sxs-lookup"><span data-stu-id="eda07-161">Iterate through the `ProductsRows` in the `ProductsDataTable`, looking for where the differences in the sorted column lie</span></span>
4. <span data-ttu-id="eda07-162">Einfügen von an jeder Gruppengrenze,, einen Trennzeichen für Datensatz `ProductsRow` Instanz in die Datentabelle, einen, der s `CategoryID` festgelegt `-1` (oder beliebige Bezeichnung erfassungsebene wurde, um einen Datensatz als Trennzeichen für Datensatz markieren)</span><span class="sxs-lookup"><span data-stu-id="eda07-162">At each group boundary, inject a separator record `ProductsRow` instance into the DataTable, one that has it s `CategoryID` set to `-1` (or whatever designation was decided upon to mark a record as a separator record )</span></span>
5. <span data-ttu-id="eda07-163">Nach dem Einfügen der Trennzeichen für Zeilen, binden Sie die Daten an die GridView programmgesteuert</span><span class="sxs-lookup"><span data-stu-id="eda07-163">After injecting the separator rows, programmatically bind the data to the GridView</span></span>

<span data-ttu-id="eda07-164">Zusätzlich zu den folgenden fünf Schritte aus, wir d müssen auch einen Ereignishandler für das GridView-s bereitstellen `RowDataBound` Ereignis.</span><span class="sxs-lookup"><span data-stu-id="eda07-164">In addition to these five steps, we d also need to provide an event handler for the GridView s `RowDataBound` event.</span></span> <span data-ttu-id="eda07-165">Hier prüfen wir d jeweils `DataRow` und ermitteln, ob es ein Trennzeichen wurde eine Zeile, deren `CategoryID` Einstellung wurde `-1`.</span><span class="sxs-lookup"><span data-stu-id="eda07-165">Here, we d check each `DataRow` and determine if it was a separator row, one whose `CategoryID` setting was `-1`.</span></span> <span data-ttu-id="eda07-166">Wenn dies der Fall ist, möchten wir d wahrscheinlich passen Sie die Formatierung oder den Text in die Zellen wird angezeigt.</span><span class="sxs-lookup"><span data-stu-id="eda07-166">If so, we d probably want to adjust its formatting or the text displayed in the cell(s).</span></span>

<span data-ttu-id="eda07-167">Verwendung dieser Technik für Grenzen für die Sortierung einfügen etwas mehr Arbeit als oben beschrieben, wie Sie auch einen Ereignishandler für das GridView-s bereitstellen müssen `Sorting` Ereignis und beibehalten zu verfolgen, der die `SortExpression` und `SortDirection` Werte.</span><span class="sxs-lookup"><span data-stu-id="eda07-167">Using this technique for injecting the sorting group boundaries requires a bit more work than outlined above, as you need to also provide an event handler for the GridView s `Sorting` event and keep track of the `SortExpression` and `SortDirection` values.</span></span>

## <a name="manipulating-the-gridview-s-control-collection-after-it-s-been-databound"></a><span data-ttu-id="eda07-168">Bearbeiten der GridView-s-Auflistung nach dem sie steuern s wurde an Daten gebunden</span><span class="sxs-lookup"><span data-stu-id="eda07-168">Manipulating the GridView s Control Collection After It s Been Databound</span></span>

<span data-ttu-id="eda07-169">Anstatt die Daten vor dem Binden an die GridView-messaging, wir können die Trennzeichen für Zeilen hinzufügen *nach* die Daten an die GridView gebunden wurde.</span><span class="sxs-lookup"><span data-stu-id="eda07-169">Rather than messaging the data before binding it to the GridView, we can add the separator rows *after* the data has been bound to the GridView.</span></span> <span data-ttu-id="eda07-170">Der Prozess der Datenbindung erstellt, der Steuerelementhierarchie GridView s, die in der Praxis ist einfach eine `Table` Instanz besteht aus einer Auflistung von Zeilen, von denen jede aus einer Auflistung von Zellen besteht.</span><span class="sxs-lookup"><span data-stu-id="eda07-170">The process of data binding builds up the GridView s control hierarchy, which in reality is simply a `Table` instance composed of a collection of rows, each of which is composed of a collection of cells.</span></span> <span data-ttu-id="eda07-171">Insbesondere die steuerelementauflistung des GridView-s enthält eine `Table` Objekt am Stamm, eine `GridViewRow` (ergibt sich aus der `TableRow` Klasse) für jeden Datensatz in die `DataSource` an die GridView gebunden und ein `TableCell` Objekt in jeder `GridViewRow` Instanz für jedes Datenfeld in der `DataSource`.</span><span class="sxs-lookup"><span data-stu-id="eda07-171">Specifically, the GridView s control collection contains a `Table` object at its root, a `GridViewRow` (which is derived from the `TableRow` class) for each record in the `DataSource` bound to the GridView, and a `TableCell` object in each `GridViewRow` instance for each data field in the `DataSource`.</span></span>

<span data-ttu-id="eda07-172">Um Trennzeichen für Zeilen zwischen den einzelnen sortieren Gruppen hinzuzufügen, können wir diese Steuerelementhierarchie direkt bearbeiten, nachdem es erstellt wurde.</span><span class="sxs-lookup"><span data-stu-id="eda07-172">To add separator rows between each sorting group, we can directly manipulate this control hierarchy once it has been created.</span></span> <span data-ttu-id="eda07-173">Wir können sicher sein, dass die Hierarchie der GridView-s-Steuerelemente zum letzten Mal mit der Zeit erstellt wurde, die die Seite gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="eda07-173">We can be confident that the GridView s control hierarchy has been created for the last time by the time the page is being rendered.</span></span> <span data-ttu-id="eda07-174">Dieser Ansatz aus diesem Grund überschreibt die `Page` Klasse s `Render` Methode, die an diesem Punkt-die Steuerelementhierarchie GridView s endgültige aktualisiert wird, um die erforderlichen Trennzeichen für Zeilen enthalten.</span><span class="sxs-lookup"><span data-stu-id="eda07-174">Therefore, this approach overrides the `Page` class s `Render` method, at which point the GridView s final control hierarchy is updated to include the needed separator rows.</span></span> <span data-ttu-id="eda07-175">Dieser Prozess wird in Abbildung 4 dargestellt.</span><span class="sxs-lookup"><span data-stu-id="eda07-175">Figure 4 illustrates this process.</span></span>


<span data-ttu-id="eda07-176">[![Eine alternative Methode bearbeitet die Hierarchie der GridView-s-Steuerelemente](creating-a-customized-sorting-user-interface-cs/_static/image9.png)](creating-a-customized-sorting-user-interface-cs/_static/image8.png)</span><span class="sxs-lookup"><span data-stu-id="eda07-176">[![An Alternate Technique Manipulates the GridView s Control Hierarchy](creating-a-customized-sorting-user-interface-cs/_static/image9.png)](creating-a-customized-sorting-user-interface-cs/_static/image8.png)</span></span>

<span data-ttu-id="eda07-177">**Abbildung 4**: eine alternative Methode ändert das GridView-s-Serversteuerelement-Hierarchie ([klicken Sie, um das Bild in voller Größe anzeigen](creating-a-customized-sorting-user-interface-cs/_static/image10.png))</span><span class="sxs-lookup"><span data-stu-id="eda07-177">**Figure 4**: An Alternate Technique Manipulates the GridView s Control Hierarchy ([Click to view full-size image](creating-a-customized-sorting-user-interface-cs/_static/image10.png))</span></span>


<span data-ttu-id="eda07-178">In diesem Tutorial wird dieser zweite Ansatz verwendet, um die Sortierung benutzererfahrung anpassen.</span><span class="sxs-lookup"><span data-stu-id="eda07-178">For this tutorial, we'll use this latter approach to customize the sorting user experience.</span></span>

> [!NOTE]
> <span data-ttu-id="eda07-179">Der Code ich m darstellen, in diesem Tutorial basiert auf dem Beispiel unter [Teemu Keiski](http://aspadvice.com/blogs/joteke/default.aspx) s Blogeintrag [spielen etwas mit GridView sortieren Gruppierung](http://aspadvice.com/blogs/joteke/archive/2006/02/11/15130.aspx).</span><span class="sxs-lookup"><span data-stu-id="eda07-179">The code I m presenting in this tutorial is based on the example provided in [Teemu Keiski](http://aspadvice.com/blogs/joteke/default.aspx) s blog entry, [Playing a Bit with GridView Sort Grouping](http://aspadvice.com/blogs/joteke/archive/2006/02/11/15130.aspx).</span></span>


## <a name="step-3-adding-the-separator-rows-to-the-gridview-s-control-hierarchy"></a><span data-ttu-id="eda07-180">Schritt 3: Hinzufügen von Trennzeichen für Zeilen, die Hierarchie der GridView-s-Steuerelemente</span><span class="sxs-lookup"><span data-stu-id="eda07-180">Step 3: Adding the Separator Rows to the GridView s Control Hierarchy</span></span>

<span data-ttu-id="eda07-181">Da möchten wir nur die Trennzeichen für Zeilen die Hierarchie der GridView-s-Steuerelemente hinzufügen, nachdem seine Steuerelementhierarchie erstellt und zum letzten Mal auf dieser Seite finden Sie unter erstellt wurde, sollten Sie diese Ergänzung am Ende des Lebenszyklus der Seite, aber vor der tatsächlichen GridView c ausführen quellcodeverwaltung-Hierarchie ist in HTML gerendert.</span><span class="sxs-lookup"><span data-stu-id="eda07-181">Since we only want to add the separator rows to the GridView s control hierarchy after its control hierarchy has been created and created for the last time on that page visit, we want to perform this addition at the end of the page lifecycle, but before the actual GridView control hierarchy has been rendered into HTML.</span></span> <span data-ttu-id="eda07-182">Der neueste Punkt an der wir dies erreichen können, ist die `Page` Klasse s `Render` -Ereignis, das wir in unserem Code-Behind-Klasse, die mit die folgende Methodensignatur überschreiben können:</span><span class="sxs-lookup"><span data-stu-id="eda07-182">The latest possible point at which we can accomplish this is the `Page` class s `Render` event, which we can override in our code-behind class using the following method signature:</span></span>


[!code-csharp[Main](creating-a-customized-sorting-user-interface-cs/samples/sample2.cs)]

<span data-ttu-id="eda07-183">Wenn die `Page` s, die ursprüngliche Klasse `Render` Methode wird aufgerufen, `base.Render(writer)` jedes der Steuerelemente auf der Seite gerendert wird, generiert das Markup, das basierend auf ihrer Hierarchie.</span><span class="sxs-lookup"><span data-stu-id="eda07-183">When the `Page` class s original `Render` method is invoked `base.Render(writer)` each of the controls in the page will be rendered, generating the markup based on their control hierarchy.</span></span> <span data-ttu-id="eda07-184">Daher ist es zwingend erforderlich, dass wir beide rufen `base.Render(writer)`, damit die Seite gerendert wird, und dass wir das GridView-s bearbeiten-Steuerelementhierarchie vor dem Aufruf `base.Render(writer)`, sodass die GridView-s-Steuerelementhierarchie vor dem Trennzeichen für Zeilen hinzugefügt wurden s wurde gerendert.</span><span class="sxs-lookup"><span data-stu-id="eda07-184">Therefore it is imperative that we both call `base.Render(writer)`, so that the page is rendered, and that we manipulate the GridView s control hierarchy prior to calling `base.Render(writer)`, so that the separator rows have been added to the GridView s control hierarchy before it s been rendered.</span></span>

<span data-ttu-id="eda07-185">Zum Einfügen der Gruppenheader sortieren müssen wir sicherstellen, dass der Benutzer angefordert hat, dass die Daten sortiert werden.</span><span class="sxs-lookup"><span data-stu-id="eda07-185">To inject the sort group headers we first need to ensure that the user has requested that the data be sorted.</span></span> <span data-ttu-id="eda07-186">In der Standardeinstellung der GridView-s-Inhalt nicht sortiert sind, und daher Wir raten t muss jede Gruppe sortieren Header eingeben.</span><span class="sxs-lookup"><span data-stu-id="eda07-186">By default, the GridView s contents are not sorted, and therefore we don t need to enter any group sorting headers.</span></span>

> [!NOTE]
> <span data-ttu-id="eda07-187">Wenn Sie möchten die GridView nach einer bestimmten Spalte sortiert werden, wenn die Seite zuerst geladen wird, rufen Sie die GridView-s `Sort` Methode ersten Besuch der Seite (aber nicht bei nachfolgenden Postbacks).</span><span class="sxs-lookup"><span data-stu-id="eda07-187">If you want the GridView to be sorted by a particular column when the page is first loaded, call the GridView s `Sort` method on the first page visit (but not on subsequent postbacks).</span></span> <span data-ttu-id="eda07-188">Um dies zu erreichen, fügen Sie diesen Aufruf in die `Page_Load` Ereignishandler innerhalb einer `if (!Page.IsPostBack)` bedingte.</span><span class="sxs-lookup"><span data-stu-id="eda07-188">To accomplish this, add this call in the `Page_Load` event handler within an `if (!Page.IsPostBack)` conditional.</span></span> <span data-ttu-id="eda07-189">Verweisen zurück auf die [Paging und Sortieren von Berichtsdaten](paging-and-sorting-report-data-cs.md) Tutorial Informationen Weitere Informationen zu den `Sort` Methode.</span><span class="sxs-lookup"><span data-stu-id="eda07-189">Refer back to the [Paging and Sorting Report Data](paging-and-sorting-report-data-cs.md) tutorial information for more on the `Sort` method.</span></span>


<span data-ttu-id="eda07-190">Vorausgesetzt, dass die Daten sortiert wurden, unsere nächste Aufgabe ist, um zu bestimmen, welche Spalten, klicken Sie dann Werte, die Unterschiede in der Spalte s nach Zeilen zu durchsuchen und die Daten sortiert wurde.</span><span class="sxs-lookup"><span data-stu-id="eda07-190">Assuming that the data has been sorted, our next task is to determine what column the data was sorted by and then to scan the rows looking for differences in that column s values.</span></span> <span data-ttu-id="eda07-191">Der folgende Code stellt sicher, dass die Daten sortiert wurden, und sucht nach der Spalte, die mit der die Daten sortiert wurden:</span><span class="sxs-lookup"><span data-stu-id="eda07-191">The following code ensures that the data has been sorted and finds the column by which the data has been sorted:</span></span>


[!code-csharp[Main](creating-a-customized-sorting-user-interface-cs/samples/sample3.cs)]

<span data-ttu-id="eda07-192">Wenn das GridView hat noch sein sortiert, die GridView s `SortExpression` Eigenschaft wurde wird nicht festgelegt.</span><span class="sxs-lookup"><span data-stu-id="eda07-192">If the GridView has yet to be sorted, the GridView s `SortExpression` property will not have been set.</span></span> <span data-ttu-id="eda07-193">Daher möchten wir nur die Trennzeichen für Zeilen hinzugefügt werden soll, wenn diese Eigenschaft einen Wert verfügt.</span><span class="sxs-lookup"><span data-stu-id="eda07-193">Therefore, we only want to add the separator rows if this property has some value.</span></span> <span data-ttu-id="eda07-194">Wenn dies der Fall ist, müssen wir als Nächstes den Index der Spalte zu bestimmen, mit dem die Daten sortiert wurde.</span><span class="sxs-lookup"><span data-stu-id="eda07-194">If it does, we next need to determine the index of the column by which the data was sorted.</span></span> <span data-ttu-id="eda07-195">Dies geschieht durch Durchlaufen der GridView-s `Columns` Auflistung, die nach der Spalte gesucht, deren `SortExpression` Eigenschaft gleich die GridView-s `SortExpression` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eda07-195">This is accomplished by looping through the GridView s `Columns` collection, searching for the column whose `SortExpression` property equals the GridView s `SortExpression` property.</span></span> <span data-ttu-id="eda07-196">Zusätzlich zu den Spaltenindex s, nehmen wir außerdem die `HeaderText` -Eigenschaft, die verwendet wird, wenn die Trennzeichen für Zeilen anzeigen.</span><span class="sxs-lookup"><span data-stu-id="eda07-196">In addition to the column s index, we also grab the `HeaderText` property, which is used when displaying the separator rows.</span></span>

<span data-ttu-id="eda07-197">Der Index der Spalte mit der die Daten sortiert sind, ist der letzte Schritt zum Aufzählen von den Zeilen der GridView.</span><span class="sxs-lookup"><span data-stu-id="eda07-197">With the index of the column by which the data is sorted, the final step is to enumerate the rows of the GridView.</span></span> <span data-ttu-id="eda07-198">Für jede Zeile müssen wir bestimmen, ob s der vorherigen Zeile s sortiert Spaltenwert der Wert des s sortierte Spalte unterscheidet.</span><span class="sxs-lookup"><span data-stu-id="eda07-198">For each row we need to determine whether the sorted column s value differs from the previous row s sorted column s value.</span></span> <span data-ttu-id="eda07-199">Wenn wir also eine neue einfügen müssen `GridViewRow` -Instanz in die Hierarchie der Steuerelemente.</span><span class="sxs-lookup"><span data-stu-id="eda07-199">If so, we need to inject a new `GridViewRow` instance into the control hierarchy.</span></span> <span data-ttu-id="eda07-200">Dies erfolgt durch den folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="eda07-200">This is accomplished with the following code:</span></span>


[!code-csharp[Main](creating-a-customized-sorting-user-interface-cs/samples/sample4.cs)]

<span data-ttu-id="eda07-201">Dieser Code beginnt, durch Programmgesteuertes Verweisen auf die `Table` Objekt am Stamm der GridView-s-Steuerelementhierarchie gefunden, und erstellen Sie eine Zeichenfolgenvariable mit dem Namen `lastValue`.</span><span class="sxs-lookup"><span data-stu-id="eda07-201">This code starts by programmatically referencing the `Table` object found at the root of the GridView s control hierarchy and creating a string variable named `lastValue`.</span></span> <span data-ttu-id="eda07-202">`lastValue` Dient zum Wert der aktuellen Zeile s sortiert Spalte mit der vorherigen s-Zeilenwert verglichen werden soll.</span><span class="sxs-lookup"><span data-stu-id="eda07-202">`lastValue` is used to compare the current row s sorted column value with the previous row s value.</span></span> <span data-ttu-id="eda07-203">Anschließend wird das GridView-s `Rows` Auflistung aufgezählt und für jede Zeile befindet sich der Wert, der die sortierte Spalte in der `currentValue` Variable.</span><span class="sxs-lookup"><span data-stu-id="eda07-203">Next, the GridView s `Rows` collection is enumerated and for each row the value of the sorted column is stored in the `currentValue` variable.</span></span>

> [!NOTE]
> <span data-ttu-id="eda07-204">Zum Bestimmen des Werts der Spalte sortiert s bestimmten Zeile die Zelle s verwende `Text` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eda07-204">To determine the value of the particular row s sorted column I use the cell s `Text` property.</span></span> <span data-ttu-id="eda07-205">Dies eignet sich gut für BoundFields, werden jedoch nicht wie gewünscht für von TemplateFields, CheckBoxFields, arbeiten und so weiter.</span><span class="sxs-lookup"><span data-stu-id="eda07-205">This works well for BoundFields, but will not work as desired for TemplateFields, CheckBoxFields, and so on.</span></span> <span data-ttu-id="eda07-206">Wir werden wie alternative GridView-Felder in Kürze berücksichtigen betrachten.</span><span class="sxs-lookup"><span data-stu-id="eda07-206">We'll look at how to account for alternate GridView fields shortly.</span></span>


<span data-ttu-id="eda07-207">Die `currentValue` und `lastValue` Variablen werden dann verglichen.</span><span class="sxs-lookup"><span data-stu-id="eda07-207">The `currentValue` and `lastValue` variables are then compared.</span></span> <span data-ttu-id="eda07-208">Wenn Unterschiede festgestellt werden, müssen wir eine neue Trennzeichenzeile für die Hierarchie der Steuerelemente hinzu.</span><span class="sxs-lookup"><span data-stu-id="eda07-208">If they differ we need to add a new separator row to the control hierarchy.</span></span> <span data-ttu-id="eda07-209">Dies wird erreicht, indem bestimmt den Index des der `GridViewRow` in die `Table` s-Objekt `Rows` -Auflistung, Erstellen neuer `GridViewRow` und `TableCell` Instanzen und zum anschließenden Hinzufügen der `TableCell` und `GridViewRow` auf der Hierarchie der Steuerelemente.</span><span class="sxs-lookup"><span data-stu-id="eda07-209">This is accomplished by determining the index of the `GridViewRow` in the `Table` object s `Rows` collection, creating new `GridViewRow` and `TableCell` instances, and then adding the `TableCell` and `GridViewRow` to the control hierarchy.</span></span>

<span data-ttu-id="eda07-210">Beachten Sie, dass das Trennzeichen für s, die einzelne Zeile `TableCell` ist so formatiert, dass es sich um die gesamte Breite des GridView umfasst mit formatiert wird die `SortHeaderRowStyle` CSS-Klasse, und verfügt über seine `Text` Eigenschaft z. B., dass sowohl die Gruppe für das Sortieren angezeigt (z. B. Kategorie) zu benennen und der Gruppe-s-Wert (z. B. Getränke).</span><span class="sxs-lookup"><span data-stu-id="eda07-210">Note that the separator row s lone `TableCell` is formatted such that it spans the entire width of the GridView, is formatted using the `SortHeaderRowStyle` CSS class, and has its `Text` property such that it shows both the sort group name (such as Category ) and the group s value (such as Beverages ).</span></span> <span data-ttu-id="eda07-211">Zum Schluss `lastValue` wird aktualisiert, um den Wert der `currentValue`.</span><span class="sxs-lookup"><span data-stu-id="eda07-211">Finally, `lastValue` is updated to the value of `currentValue`.</span></span>

<span data-ttu-id="eda07-212">Die CSS-Klasse, die zum Formatieren der Sortierung Gruppenkopfzeile verwendet `SortHeaderRowStyle` in angegeben werden, muss die `Styles.css` Datei.</span><span class="sxs-lookup"><span data-stu-id="eda07-212">The CSS class used to format the sorting group header row `SortHeaderRowStyle` needs to be specified in the `Styles.css` file.</span></span> <span data-ttu-id="eda07-213">Verwenden Sie die formateinstellungen für Sie interessant sein können, Ich habe verwendet Folgendes:</span><span class="sxs-lookup"><span data-stu-id="eda07-213">Feel free to use whatever style settings appeal to you; I used the following:</span></span>


[!code-css[Main](creating-a-customized-sorting-user-interface-cs/samples/sample5.css)]

<span data-ttu-id="eda07-214">Durch den aktuellen Code aus, die Sortierung-Schnittstelle fügt Kopfzeilen von Gruppen sortieren, beim Sortieren nach jeder BoundField (siehe Abbildung 5, die einen Screenshot beim Sortieren von Lieferant zeigt).</span><span class="sxs-lookup"><span data-stu-id="eda07-214">With the current code, the sorting interface adds sort group headers when sorting by any BoundField (see Figure 5, which shows a screenshot when sorting by supplier).</span></span> <span data-ttu-id="eda07-215">Allerdings sind beim Sortieren von jedem anderen Feldtyp (z. B. eine CheckBoxField oder TemplateField) der Sort-Gruppenheader an keiner Stelle (siehe Abbildung 6) gefunden werden.</span><span class="sxs-lookup"><span data-stu-id="eda07-215">However, when sorting by any other field type (such as a CheckBoxField or TemplateField), the sort group headers are nowhere to be found (see Figure 6).</span></span>


<span data-ttu-id="eda07-216">[![Die Sortierung Schnittstelle enthält Header, Gruppe sortieren, beim Sortieren nach BoundFields](creating-a-customized-sorting-user-interface-cs/_static/image12.png)](creating-a-customized-sorting-user-interface-cs/_static/image11.png)</span><span class="sxs-lookup"><span data-stu-id="eda07-216">[![The Sorting Interface Includes Sort Group Headers When Sorting by BoundFields](creating-a-customized-sorting-user-interface-cs/_static/image12.png)](creating-a-customized-sorting-user-interface-cs/_static/image11.png)</span></span>

<span data-ttu-id="eda07-217">**Abbildung 5**: das Sortieren-Schnittstelle enthält sortieren Header beim Sortieren von Gruppen von BoundFields ([klicken Sie, um das Bild in voller Größe anzeigen](creating-a-customized-sorting-user-interface-cs/_static/image13.png))</span><span class="sxs-lookup"><span data-stu-id="eda07-217">**Figure 5**: The Sorting Interface Includes Sort Group Headers When Sorting by BoundFields ([Click to view full-size image](creating-a-customized-sorting-user-interface-cs/_static/image13.png))</span></span>


<span data-ttu-id="eda07-218">[![Der Sort-Gruppenheader sind fehlende beim Sortieren einer CheckBoxField](creating-a-customized-sorting-user-interface-cs/_static/image15.png)](creating-a-customized-sorting-user-interface-cs/_static/image14.png)</span><span class="sxs-lookup"><span data-stu-id="eda07-218">[![The Sort Group Headers are Missing When Sorting a CheckBoxField](creating-a-customized-sorting-user-interface-cs/_static/image15.png)](creating-a-customized-sorting-user-interface-cs/_static/image14.png)</span></span>

<span data-ttu-id="eda07-219">**Abbildung 6**: die Kopfzeilen von Gruppen sortieren sind fehlende beim Sortieren einer CheckBoxField ([klicken Sie, um das Bild in voller Größe anzeigen](creating-a-customized-sorting-user-interface-cs/_static/image16.png))</span><span class="sxs-lookup"><span data-stu-id="eda07-219">**Figure 6**: The Sort Group Headers are Missing When Sorting a CheckBoxField ([Click to view full-size image](creating-a-customized-sorting-user-interface-cs/_static/image16.png))</span></span>


<span data-ttu-id="eda07-220">Der Sort-Gruppenheader beim Sortieren nach einer CheckBoxField fehlen ist, weil der Code derzeit nur verwendet die `TableCell` s `Text` Eigenschaft zum Bestimmen des Werts, der die sortierte Spalte für jede Zeile.</span><span class="sxs-lookup"><span data-stu-id="eda07-220">The reason the sort group headers are missing when sorting by a CheckBoxField is because the code currently uses just the `TableCell` s `Text` property to determine the value of the sorted column for each row.</span></span> <span data-ttu-id="eda07-221">Für CheckBoxFields die `TableCell` s `Text` Eigenschaft ist eine leere Zeichenfolge; stattdessen des Werts über ein Kontrollkästchen-Steuerelement, das innerhalb der `TableCell` s `Controls` Auflistung.</span><span class="sxs-lookup"><span data-stu-id="eda07-221">For CheckBoxFields, the `TableCell` s `Text` property is an empty string; instead, the value is available through a CheckBox Web control that resides within the `TableCell` s `Controls` collection.</span></span>

<span data-ttu-id="eda07-222">Um Feldtypen als BoundFields behandeln zu können, müssen wir den Code zu erweitern, in denen die `currentValue` Variable zugewiesen wird überprüft, ob ein Kontrollkästchen in der `TableCell` s `Controls` Auflistung.</span><span class="sxs-lookup"><span data-stu-id="eda07-222">To handle field types other than BoundFields, we need to augment the code where the `currentValue` variable is assigned to check for the existence of a CheckBox in the `TableCell` s `Controls` collection.</span></span> <span data-ttu-id="eda07-223">Anstelle von `currentValue = gvr.Cells[sortColumnIndex].Text`, ersetzen Sie diesen Code durch Folgendes:</span><span class="sxs-lookup"><span data-stu-id="eda07-223">Instead of using `currentValue = gvr.Cells[sortColumnIndex].Text`, replace this code with the following:</span></span>


[!code-csharp[Main](creating-a-customized-sorting-user-interface-cs/samples/sample6.cs)]

<span data-ttu-id="eda07-224">Dieser Code überprüft die sortierte Spalte `TableCell` für die aktuelle Zeile zu bestimmen, ob alle Steuerelemente in der `Controls` Auflistung.</span><span class="sxs-lookup"><span data-stu-id="eda07-224">This code examines the sorted column `TableCell` for the current row to determine if there are any controls in the `Controls` collection.</span></span> <span data-ttu-id="eda07-225">Wenn Sie vorhanden sind, und das erste Steuerelement ein Kontrollkästchen, die `currentValue` Variable wird festgelegt, auf Ja oder Nein, je nach den Kontrollkästchen s `Checked` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eda07-225">If there are, and the first control is a CheckBox, the `currentValue` variable is set to Yes or No, depending on the CheckBox s `Checked` property.</span></span> <span data-ttu-id="eda07-226">Der Wert stammt, andernfalls aus der `TableCell` s `Text` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eda07-226">Otherwise, the value is taken from the `TableCell` s `Text` property.</span></span> <span data-ttu-id="eda07-227">Diese Logik kann repliziert werden, um Sortierung für alle von TemplateFields, die ggf. in der GridView vorhanden, zu verarbeiten.</span><span class="sxs-lookup"><span data-stu-id="eda07-227">This logic can be replicated to handle sorting for any TemplateFields that may exist in the GridView.</span></span>

<span data-ttu-id="eda07-228">Mit dem obigen Code außerdem der Sort-Gruppenheader vorhanden sind beim Sortieren nach CheckBoxField nicht mehr unterstützt (siehe Abbildung 7).</span><span class="sxs-lookup"><span data-stu-id="eda07-228">With the above code addition, the sort group headers are now present when sorting by the Discontinued CheckBoxField (see Figure 7).</span></span>


<span data-ttu-id="eda07-229">[![Der Sort-Gruppenheader sind jetzt vorhanden beim Sortieren einer CheckBoxField](creating-a-customized-sorting-user-interface-cs/_static/image18.png)](creating-a-customized-sorting-user-interface-cs/_static/image17.png)</span><span class="sxs-lookup"><span data-stu-id="eda07-229">[![The Sort Group Headers are Now Present When Sorting a CheckBoxField](creating-a-customized-sorting-user-interface-cs/_static/image18.png)](creating-a-customized-sorting-user-interface-cs/_static/image17.png)</span></span>

<span data-ttu-id="eda07-230">**Abbildung 7**: die Kopfzeilen von Gruppen sortieren sind jetzt vorhanden beim Sortieren einer CheckBoxField ([klicken Sie, um das Bild in voller Größe anzeigen](creating-a-customized-sorting-user-interface-cs/_static/image19.png))</span><span class="sxs-lookup"><span data-stu-id="eda07-230">**Figure 7**: The Sort Group Headers are Now Present When Sorting a CheckBoxField ([Click to view full-size image](creating-a-customized-sorting-user-interface-cs/_static/image19.png))</span></span>


> [!NOTE]
> <span data-ttu-id="eda07-231">Wenn Sie über Produkte mit `NULL` Datenbank Werte für die `CategoryID`, `SupplierID`, oder `UnitPrice` Felder, diese Werte als leere Zeichenfolgen in den GridView-Ansicht angezeigt werden, standardmäßig, d. h. die Trennzeichen Zeilentext s für diese Produkte mit `NULL`Werte gelesen werden, wie Kategorie: (d. h. es s keine Namen nach Kategorie: wie Sie mit der Kategorie: Getränke).</span><span class="sxs-lookup"><span data-stu-id="eda07-231">If you have products with `NULL` database values for the `CategoryID`, `SupplierID`, or `UnitPrice` fields, those values will appear as empty strings in the GridView by default, meaning the separator row s text for those products with `NULL` values will read like Category: (that is, there s no name after Category: like with Category: Beverages ).</span></span> <span data-ttu-id="eda07-232">Wenn ein Wert, der hier angezeigt werden soll, können Sie entweder die BoundFields festlegen [ `NullDisplayText` Eigenschaft](https://msdn.microsoft.com/library/system.web.ui.webcontrols.boundfield.nulldisplaytext.aspx) auf den Text, angezeigt werden sollen oder Sie können eine bedingte Anweisung in der Render-Methode hinzufügen, beim Zuweisen der `currentValue` auf das Trennzeichen Zeile s `Text` Eigenschaft.</span><span class="sxs-lookup"><span data-stu-id="eda07-232">If you want a value displayed here you can either set the BoundFields [`NullDisplayText` property](https://msdn.microsoft.com/library/system.web.ui.webcontrols.boundfield.nulldisplaytext.aspx) to the text you want displayed or you can add a conditional statement in the Render method when assigning the `currentValue` to the separator row s `Text` property.</span></span>


## <a name="summary"></a><span data-ttu-id="eda07-233">Zusammenfassung</span><span class="sxs-lookup"><span data-stu-id="eda07-233">Summary</span></span>

<span data-ttu-id="eda07-234">Das GridView enthält viele integrierte Optionen für das Anpassen der Benutzeroberfläche der Sortierung keine.</span><span class="sxs-lookup"><span data-stu-id="eda07-234">The GridView does not include many built-in options for customizing the sorting interface.</span></span> <span data-ttu-id="eda07-235">Allerdings mit etwas Code auf niedriger Ebene es möglich, optimieren die GridView-s-Steuerelementhierarchie um eine individuellere Benutzeroberfläche zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="eda07-235">However, with a bit of low-level code, it s possible to tweak the GridView s control hierarchy to create a more customized interface.</span></span> <span data-ttu-id="eda07-236">In diesem Tutorial wurde erläutert, wie eine Art Separator Gruppenzeile ein sortierbar GridView, hinzufügen. die unterschiedlichen Gruppen und diesen Gruppen Grenzen leichter identifiziert.</span><span class="sxs-lookup"><span data-stu-id="eda07-236">In this tutorial we saw how to add a sort group separator row for a sortable GridView, which more easily identifies the distinct groups and those groups boundaries.</span></span> <span data-ttu-id="eda07-237">Weitere Beispiele für benutzerdefinierte Sortierung Schnittstellen, sehen Sie sich [Scott Guthrie](https://weblogs.asp.net/scottgu/) s [einige ASP.NET 2.0 GridView sortieren Tipps und Tricks](https://weblogs.asp.net/scottgu/archive/2006/02/11/437995.aspx) Blogeintrag.</span><span class="sxs-lookup"><span data-stu-id="eda07-237">For additional examples of customized sorting interfaces, check out [Scott Guthrie](https://weblogs.asp.net/scottgu/) s [A Few ASP.NET 2.0 GridView Sorting Tips and Tricks](https://weblogs.asp.net/scottgu/archive/2006/02/11/437995.aspx) blog entry.</span></span>

<span data-ttu-id="eda07-238">Viel Spaß beim Programmieren!</span><span class="sxs-lookup"><span data-stu-id="eda07-238">Happy Programming!</span></span>

## <a name="about-the-author"></a><span data-ttu-id="eda07-239">Der Autor</span><span class="sxs-lookup"><span data-stu-id="eda07-239">About the Author</span></span>

<span data-ttu-id="eda07-240">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), Autor von sieben Büchern zu ASP/ASP.NET und Gründer von [4GuysFromRolla.com](http://www.4guysfromrolla.com), arbeitet mit Microsoft-Web-Technologien seit 1998.</span><span class="sxs-lookup"><span data-stu-id="eda07-240">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), author of seven ASP/ASP.NET books and founder of [4GuysFromRolla.com](http://www.4guysfromrolla.com), has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="eda07-241">Er ist als ein unabhängiger Berater, Schulungsleiter und Autor.</span><span class="sxs-lookup"><span data-stu-id="eda07-241">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="eda07-242">Sein neueste Buch wird [*Sams Schulen selbst ASP.NET 2.0 in 24 Stunden*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span><span class="sxs-lookup"><span data-stu-id="eda07-242">His latest book is [*Sams Teach Yourself ASP.NET 2.0 in 24 Hours*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span></span> <span data-ttu-id="eda07-243">Er ist unter [ mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="eda07-243">He can be reached at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span> <span data-ttu-id="eda07-244">oder über seinen Blog finden Sie unter [ http://ScottOnWriting.NET ](http://ScottOnWriting.NET).</span><span class="sxs-lookup"><span data-stu-id="eda07-244">or via his blog, which can be found at [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="eda07-245">[Zurück](sorting-custom-paged-data-cs.md)
> [Weiter](paging-and-sorting-report-data-vb.md)</span><span class="sxs-lookup"><span data-stu-id="eda07-245">[Previous](sorting-custom-paged-data-cs.md)
[Next](paging-and-sorting-report-data-vb.md)</span></span>
